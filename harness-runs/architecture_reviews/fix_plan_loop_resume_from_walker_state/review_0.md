# Architecture review — iteration 0

## Must Fix

1. **Task 5 cites a §1.7 step 4 sentence that only Task 6 writes, so the fork is wrong for one commit** — offending files: `task_5_plan.md` and `task_6_plan.md`; the story index's readiness order is affected too (`fix_plan_loop_resume_from_walker_state_story_plan.md`). Rule: `.claude/context/conventions.md` → `### The order files are created, so a half-built feature is still coherent` (*"Contract before consumer, always"*, and for a plugin contract *"… then every file that cites it"*).
   Task 5's rewritten **Resume-from-ledger (planning).** paragraph in `plugin/instructions/task_plan_writing_instructions_autonomous.md` says that within `P1`/`P2` *"the saved walk is the position the resume continues from, as canonical-doc §1.7 step 4 states"*. Today §1.7 step 4 of `plugin/instructions/autonomous_pause_and_ledger.md` says the opposite: *"the phase's **detail index** drives within-phase resume … (find the first `[ ]` item)"*. Task 6 writes the planning sentence into step 4 and ships **after** Task 5. Task 6's own plan admits this (*"This task writes that §1.7 step 4 sentence, so the citation resolves"*). So at Task 5's commit the fork cites a rule the cited document contradicts. The two edits also cite each other: the new step 4 text points at the fork's `## Override 2 — resumability` as Task 5 rewrites it. Reordering alone cannot fix that, because whichever edit lands first, the other side is left dangling for one commit.
   **Fix:** Move the `§1.7 step 4` work item, with its verification bullets (`grep -n 'saved walk'` confined to `### 1.7`; no line of §1.6 or §2.x touched), out of `task_6_plan.md` and into `task_5_plan.md`, so both halves of the citation pair land in one commit. Update Task 5's `### Targets` to add `plugin/instructions/autonomous_pause_and_ledger.md` → `### 1.7` step 4 only. Remove that target and its "Depends on Task 5 … so the citation resolves" wording from Task 6. Re-point the story index's `## Scope register` row 4 from `Task 6` to `Task 5`, and adjust both readiness entries' titles and points to match.

## Should Fix

1. **The class (i) registry in `mode_contract.md` will state something false about §1.7's citers once the new step 4 names the fork by file and heading** — offending files: `task_6_plan.md` (or `task_5_plan.md` once Must Fix 1 is applied) and the story index's `## Scope register` row 8. Rule: `.claude/context/plugin.md` → `## Registries — a name routed to an implementation` (`### Sanctioned cross-fork anchors` is one of the listed registries) and `## Cores, forks and the single-owner rule`.
   `plugin/instructions/mode_contract.md` → `### Sanctioned cross-fork anchors` → class (i) *Reason:* currently says the planner's section *"is named as `Override 2(a)`, with neither filename nor heading, by `${CLAUDE_PLUGIN_ROOT}/instructions/autonomous_pause_and_ledger.md` → `### 1.7 …`"*. The planned step 4 addition cites `${CLAUDE_PLUGIN_ROOT}/instructions/task_plan_writing_instructions_autonomous.md` → `## Override 2 — resumability` by both file and heading, which makes that sentence false. Row 8 is marked `no-change` on the grounds that only the heading and the `Override 2(a)` label matter. It misses this sentence.
   **Fix:** Add `plugin/instructions/mode_contract.md` → class (i) *Reason:* as a target of the task that writes §1.7 step 4. Amend the sentence to say §1.7 names the section by label in step 5, and by file and heading in step 4. Flip row 8 to `change`, owned by that task.

2. **Task 2's test comments cite plugin prose that Tasks 4 and 5 have not written yet** — offending file: `task_2_plan.md`. Rule: `.claude/context/conventions.md` → `### The order files are created, so a half-built feature is still coherent`.
   This is non-blocking. The configured bottom-up layer order forces it, and the plan already handles it: the story index's second top risk, byte-fixed phrases, and `grep -F` checks on the owning side in Tasks 4 and 5. Still, for three commits the suite's header and case comments cite headings (**Continuing a saved walk.**) and phrases that do not exist in the tree.
   **Fix (optional):** State in Task 2's file header that the cited phrases land in the plugin-layer tasks of the same branch. A reader of an intermediate commit then sees a pending citation, not a broken one.
