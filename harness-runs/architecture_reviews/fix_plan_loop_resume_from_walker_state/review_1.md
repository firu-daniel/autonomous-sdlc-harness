# Architecture review — iteration 1

Iteration 0's Must Fix is resolved. Task 5 now owns both halves of the §1.7 step 4 citation pair, Task 6 no longer targets `autonomous_pause_and_ledger.md`, and `## Scope register` row 4 points at Task 5. Layer placement holds for all eight tasks: `cli/templates/` and `cli/test/` → `cli`; `plugin/instructions/` and `plugin/docs/` → `plugin`; `docs/`, `schemas/` and this checkout's `scripts/` (`init` output) → `general`.

## Must Fix

1. **Task 3 renames a test-case title that `docs/flow-graph-walker.md` cites, and the citation is not repointed until Task 7** — offending file: `task_3_plan.md`, with `task_7_plan.md` affected. Rule: `.claude/context/conventions.md` → `### The order files are created, so a half-built feature is still coherent` (*"Contract before consumer, always"*, and for a contract, *"… then every file that cites it"*). The same rule is in `.claude/context/plugin.md` → `## Citation` (*"A cited heading is therefore a wire: carry its text across byte-identical … a rename strands every citer"*).
   Task 3 changes case `(6)`'s title in `cli/test/flow-walker-ui-and-reentry.test.mjs` from `(6) re-entry mid-loop restarts the counter and continues the findings folder` to `(6) start mid-loop restarts the counter and continues the findings folder`. `docs/flow-graph-walker.md` → `### Item 3a` → **Evidence.** cites the old title verbatim, and a `git grep` finds no other citer. Task 3 admits that only Task 7 updates that citation. So the decision of record cites a test case that does not exist for four commits (Tasks 3 to 6).
   This case is different from iteration 0's Should Fix 2, where the test comments cite plugin prose that is not written yet. There, the configured layer order forces the gap. Here nothing forces it. Before Tasks 4 and 5 land, the old title is still true, and the rename changes no assertion.
   **Fix:** In `task_3_plan.md`, drop the retitle. Keep case `(6)`'s title string byte-identical, change only its step comment, and remove the `start mid-loop ... this (6)` re-label from the closing-table bullet. You may re-label a comment-table line, but not the `test(...)` title string. Add a verification bullet: `git grep -n "re-entry mid-loop restarts the counter" -- cli/test docs` still prints both the test line and the `docs/flow-graph-walker.md` line. In `task_7_plan.md`, cite case `(6)` by its unchanged title in **Evidence.**, and drop the Task 3 dependency bullet that names the new title.
   Alternative: if the new title is wanted, Task 7 renames it. Task 7 is `general`, so this would be a cross-layer edit, and it is not recommended. The cleaner course is for a later branch to retitle the case together with its citer.

## Should Fix

1. **Carried over from iteration 0, still unaddressed: the class (i) *Reason:* in `plugin/instructions/mode_contract.md` becomes false once Task 5 writes §1.7 step 4** — offending files: `task_5_plan.md`, and the story index's `## Scope register` row 8. Rule: `.claude/context/plugin.md` → `## Registries — a name routed to an implementation` (`### Sanctioned cross-fork anchors`).
   The *Reason:* now says the planner's section *"is named as `Override 2(a)`, with neither filename nor heading, by `${CLAUDE_PLUGIN_ROOT}/instructions/autonomous_pause_and_ledger.md` → `### 1.7 …`"*. Task 5's new step 4 cites `${CLAUDE_PLUGIN_ROOT}/instructions/task_plan_writing_instructions_autonomous.md` → `## Override 2 — resumability` by both file and heading. That makes the registry's sentence false.
   Row 8's `no-change` reason covers only the heading and the `Override 2(a)` label, so it misses this sentence.
   **Fix:** Add `plugin/instructions/mode_contract.md` → `### Sanctioned cross-fork anchors` class (i) *Reason:* as a Task 5 target. Amend the sentence to say that §1.7 names the section by label in step 5 and by file and heading in step 4. Change row 8 to `change`, owned by Task 5. If this is rejected, record the rejection with a reason under a `## Rejected findings` section of the story index.

2. **Carried over from iteration 0 (optional): Task 2's suite header and Task 1's walker header describe plugin re-entry prose that Tasks 4 and 5 have not written yet** — offending files: `task_1_plan.md`, `task_2_plan.md`. Rule: `.claude/context/conventions.md` → `### The order files are created, so a half-built feature is still coherent`.
   The bottom-up layer order forces this, and the `grep -F` checks on the owning side in Tasks 4 and 5 mitigate it.
   **Fix (optional):** State in Task 2's file header that the quoted phrases land in the plugin-layer tasks of the same branch, so a reader of an intermediate commit sees a pending citation, not a broken one.
