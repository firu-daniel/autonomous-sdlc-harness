# Architecture review — iteration 0

## Must Fix

1. **Task 2 puts an intake-only content rule inside a paragraph the clarification digest imports** — offending file: `task_2_plan.md` (fourth Work bullet, and its `### Targets` line naming `## Where it is written` → **Write mechanics — absolute write, repo-relative commit.**). Rule source: `.claude/context/plugin.md` → `## Cores, forks and the single-owner rule` (*"A policy file is activated by pointer and never restated. … The same rule governs every shared module here"*) and `## Citation` (*"A cited heading is therefore a wire"*); `.claude/context/conventions.md` → `### Where a new responsibility goes` (*"A responsibility that already has a home does not get a second one"*).
   `plugin/instructions/clarification_digest_instructions.md` → `## Where it is written` imports this exact paragraph by pointer: *"**Write mechanics — absolute write, repo-relative commit**, the split `${CLAUDE_PLUGIN_ROOT}/instructions/improvement_observations_instructions.md` → `## Where it is written` states and this file does not restate"*. Task 2 adds a sentence to that paragraph saying the file's content *"is bound by `## The entry format` → **Every path in an entry is repo-relative.**"* Anyone who follows the digest's pointer now reads a sentence that ties the digest's content to the observations entry-format rule. The same edit sets up no machine-path check for the digest. So the observations rule reaches a second module through an import the plan never lists, and the story index then states the opposite. `## Scope register` row 16 says the digest imports *"the absolute-write split"* and does not change. The `## Context` bullet says the digest *"neither gains the check nor needs an edit"*, and the task prompt says *"Change nothing outside improvement observations on this branch."* Task 3 handles the same boundary for `## Commit mechanics` explicitly, with its own paragraph plus a scope sentence. Task 2 does nothing like that for the paragraph it edits.
   **Fix:** In `task_2_plan.md`, either (a) take the argument-versus-content sentence out of the **Write mechanics** paragraph and put it in the **Every path in an entry is repo-relative.** paragraph under `## The entry format`, which is intake-only, and drop the `## Where it is written` target (and move `## Scope register` row 2 to `no-change` in the story index, or to Task 2's `## The entry format` site); or (b) keep it in **Write mechanics** but spell it so that it binds only this intake file (for example *"what **this intake file** says is bound by …; a document that imports this split does not import that rule"*). Also add a verification step that `plugin/instructions/clarification_digest_instructions.md` is unchanged and that its `## Where it is written` pointer still resolves to a paragraph that states only the split, plus that intake-scoped sentence. Update row 16's evidence in the story index to say how the Task 2 edit is kept out of the digest's import.

## Should Fix

1. **Task 3's fenced command adds `<…>` names that are neither snake_case nor listed in the module's placeholder-resolution exceptions** — offending file: `task_3_plan.md` (the **The check paragraph.** Work bullet's fenced `grep -nF -e <home directory> -e <checkout root> … <absolute path of the intake file>`). Rule source: `.claude/context/plugin.md` → `## The placeholder vocabulary` (*"A placeholder is `<snake_case>` in angle brackets, and its spelling is its identity"*; *"Two `<…>` vocabularies exist and conflating them is silent"*). In `plugin/instructions/improvement_observations_instructions.md`, the **Placeholder resolution.** note says every `<…>` path placeholder resolves from the activating core's `## Setup`, and it lists the exceptions (`<i>`, `<fix_round>`, `<n>`, `<N>`, each *"defined at its own use site"*). An orchestrator who reads the new command by that note would look for `<home directory>` and `<checkout root>` in `## Setup` and find neither. Task 2 already deals with the same risk for `<main checkout root>`, `<other branch>` and `<name>`: it says to state inline that they are illustrative. Task 3 has no matching instruction.
   **Fix:** In `task_3_plan.md`, spell the needles as snake_case locally-defined tokens (for example `<home_dir>`, `<checkout_root>`, `<intake_file_abs_path>`), say at the use site that they are defined there from the needle commands' output, and add them to the **Placeholder resolution.** note's list of locally-defined exceptions. Or state inline, as Task 2 does, that they are illustrative text. Also use the same spelling for the `<home>` rewrite token in the acceptance record.

## Nice to Have

- None. Every task's `_(layer: …)_` tag matches its targets through `harness.config.json` → `layers` (`cli/templates/…` → `cli`; `plugin/instructions/…` and `plugin/docs/…` → `plugin`; `harness-runs/…` → `general`), and the order puts the catch-all last. The cli template and `plugin/docs/AUTONOMOUS_FLOW.md` cite the module rather than restate it. The forks' edits point at the module by heading plus bold lead and add no cross-fork citation.
