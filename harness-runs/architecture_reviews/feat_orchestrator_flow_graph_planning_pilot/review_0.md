# Architecture review — iteration 0

## Must Fix

1. **Schema-valid check fixtures placed in `schemas/negative/`, a directory whose every document must be rejected** — `task_12_plan.md` (Targets: `schemas/negative/flow-graph-check-*.json (new, six files)`; Work: "**Six check fixtures**, each schema-valid"), carried into `task_13_plan.md` (`--negatives` globs `schemas/negative/flow-graph-check-*.json`) and `task_14_plan.md`. Rule sources: `.claude/context/conventions.md` → `## What accompanies a new unit of each kind`, row *A negative schema fixture*: "Its file under `schemas/negative/` **and** its chained `ajv test … --invalid` assertion … a fixture no assertion wires in is never run"; and `schemas/negative/README.md`, opening: "Every document in this directory exists to be **rejected**."
   The six `flow-graph-check-*` documents are **schema-valid by design**. Task 12 says they carry no `ajv --invalid` assertion, and `validate:flow-graph` validates them as valid. That puts documents the checker owns inside the directory the conventions document gives to schema-negative fixtures, and it breaks the invariant that directory states for every file in it. Task 12's plan to add a README paragraph carving out an exception does not fix this. It rewrites a stated rule from inside an implementing run, and after that "a file under `schemas/negative/`" no longer tells a reader or a gate what the file is for. These fixtures belong to `scripts/check-flow-graph.sh`, not to the schema.
   **Fix:** In `task_12_plan.md`, give the six check fixtures their own sibling directory. For example, `schemas/flow-graph-check/flow-graph-check-<check-id>.json` (or another path outside `schemas/negative/`), with its own `README.md` stating that each document is schema-valid, differs from the planning graph by one change, and is proved against `scripts/check-flow-graph.sh --negatives`. Keep only the four `flow-graph-schema-*` documents in `schemas/negative/`, and drop the planned `schemas/negative/README.md` exception paragraph. It then only needs to note the second schema's negatives and their wiring. Update the fixture path in `task_12_plan.md`'s `validate:flow-graph` script, in `task_13_plan.md`'s `--negatives` glob and Depends-on paragraph, and in `task_14_plan.md`'s `docs/development.md` paragraph.

2. **The iteration cap moves to the graph but keeps a second, unchecked home in the core's retained prose, and Task 9's own verification contradicts what it keeps** — `task_9_plan.md` (Work: "Keep step 4's `.dispatch_counter` reset and `MAX_TOTAL_DISPATCHES = 40` sizing sentence unchanged", and the Safety contract step 2 halt message kept verbatim; Verification: "`grep -nE 'iteration >= 5|fall through to step|loop back to' …` finds no hit outside the new walker section"). Related: `task_1_plan.md` (`counters: { "iteration": { "cap": 5 } }`) and `task_13_plan.md` (the static-check set). Rule source: `.claude/context/conventions.md` → `### Where a new responsibility goes`: "**A responsibility that already has a home does not get a second one.**"
   After this branch, `counters.iteration.cap` in `cli/templates/scripts/flows/task_plan_writing.graph.json` is the cap's owner: Tasks 10 and 11 repoint every "canonical in the core" claim at it. But the core's `## Setup` step 4 still reads *"a runaway backstop that sits **behind** both loops' own `iteration >= 5` caps … caps at 5 revisions → 20 … → 10; 30 bounded"*, and the step 2 halt message still reads *"Dispatches ran past both loops' 5-revision caps"*. Both are kept byte-identical: step 4 is cited from outside, and the task prompt freezes the halt messages. The graph's cap therefore has a second statement in the core, and `MAX_TOTAL_DISPATCHES = 40` is sized arithmetically from it. Nothing holds the two in agreement, so a change to the graph's cap silently invalidates the core's sizing argument. The plan already accepts one such duplication, the findings-folder templates, and holds it with the `findings-folder-in-core` check. The cap gets no equivalent. As written, Task 9's grep also fails against step 4's `iteration >= 5`, which Task 9 itself keeps.
   **Fix:** In `task_9_plan.md`, name the step-4 sizing sentence and the step-2 halt message as retained **descriptions** of the graph's cap. They stay byte-identical for the stated citer and prompt reasons, and the routing grep exempts `## Setup` step 4 and `## Safety contract` step 2 explicitly. In `task_13_plan.md`, add a check (for example `cap-matches-core`) that fails when the graph's `counters.iteration.cap` disagrees with the cap value the core's `## Setup` step 4 states, extracted the way `findings-folder-in-core` extracts the step 2 table. Give it a one-change fixture in Task 12's check-fixture set, and add it to Task 13's `THE CONTRACT` list and to Task 16's decision record. Alternatively, record in Task 16 why the duplication is accepted unguarded, but then Task 9's grep exemption is still owed.

## Should Fix

1. **The outer-loop table's own header and doc comments go false when a second library and a data file join it** — `task_5_plan.md`. Rule source: `.claude/context/cli.md` → `## What "done" means here`: "A reviewer holds a change to its module's own header … the change either satisfies it or amends the header in the same edit." Task 5 amends only the `agentInvocable` doc comment. `cli/src/generators/outerLoopScripts.ts` also states:
   - that the module writes *"the shared library they all source"*;
   - *"Every one of these scripts reads `<repo_root>/harness.config.json` at run time through the one shared library"*;
   - that the table is ordered *"the shared library first, because every other row sources it"*;
   - the `mode` comment: `0o644` is *"for one that is only ever sourced"*;
   - the `subdir` comment: *"Absent means directly in `scriptsDir`, which is where every *invoked* script lives"*;
   - the `init` note: *"each one reads harness.config.json at run time"*.

   A JSON graph that is neither sourced nor run, and a second library that sources nothing, falsify several of these. The row also receives a meaningless `invocation` (`bash <scriptsDir>/flows/task_plan_writing.graph.json`) in `written`. Add these sites to Task 5's targets and amend them in the same edit so that the data row is stated rather than implied. At minimum, say that a `flows/` row is data and is never invoked.

2. **The rename sweep does not reach the walker's plugin-owned literals** — `task_3_plan.md` and `task_4_plan.md`. Rule sources: `.claude/context/conventions.md` → `### The order files are created…` ("`grep -rn` the exact string across `plugin/`") and `.claude/context/plugin.md` → `## The placeholder vocabulary` ("Changing a shape is a change to every reader of it, inside this tree and outside it"). The gate library keys on shapes owned by plugin documents:
   - the ledger's `## Run mode` block line `- skipped: ` (owned by `plugin/instructions/autonomous_pause_and_ledger.md`, per `run_mode_instructions.md` → `## The durable record`);
   - the `review_<n>.md` naming;
   - the `## Rejected findings` / `## Scope register` headings;
   - the `call stands` rebuttal marker (`plugin/agents/task-plan-writer.md`).

   The walker ships from `cli/templates/`, so the prescribed `grep -rn … plugin/` never reaches it. Task 13 guards the run-mode ids and the ledger ids but none of these. Have Task 3's header list each keyed-on literal with its owning file and anchor. Have Task 16 record the reverse-direction dependency and which of these literals no check holds.

3. **The new schema's negatives are wired outside the chain the conventions document names** — `task_12_plan.md`. `.claude/context/conventions.md` → `## What accompanies a new unit of each kind` names `package.json` → `validate:config:negative` as the chained assertion for a negative schema fixture. Task 12 wires the flow-graph negatives into a new `validate:flow-graph:negative`, which Task 14 runs. That wiring is reasonable, but it departs from the written rule. Raise it as a `stale-rule` entry for a supervised `/harness-analyze` re-run, as that document prescribes for its rules, and record it in the task's notes.

## Nice to Have

- `task_12_plan.md` / `task_13_plan.md`: the schema negatives `schemas/negative/flow-graph-schema-*.json` differ in shape from the configuration fixtures beside them. `schemas/negative/README.md` currently says every document there is a one-change copy of `examples/harness.config.json`. Task 12 already plans a README paragraph; make sure it re-scopes that opening sentence to the configuration fixtures, so the directory's rule stays literally true for both families.
