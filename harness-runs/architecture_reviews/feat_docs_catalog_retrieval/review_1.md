# Architecture review — iteration 1

The plan has fixed all three Must Fix findings from iteration 0:

- The writer-monopoly exception is now declared in `writer.ts`'s header by Task 5, and the conflict is raised in `## Corpus staleness`.
- `retrievalRuntimeState()` is now the single predicate, owned by Task 2 and read by Tasks 12 and 13.
- The launcher mirror is now declared in both owners' headers (Tasks 2 and 9).

Two new Must Fix findings follow. The three Should Fix items from iteration 0 are still open and are raised again below.

## Must Fix

1. **The new module `cli/src/retrieval/search.ts` has no header comment in the plan** — `task_7_plan.md` (Targets: `cli/src/retrieval/search.ts` (new); Work: the three `search.ts` bullets).
   Rule: `.claude/context/conventions.md` → `## What accompanies a new unit of each kind`, row *A module under `cli/src/`*: *"A header comment stating what the module owns and, where it exists to hold a rule, that rule in the words 'The rule this module exists to enforce'. Every file under `cli/src/` opens with one."* The same rule appears in `.claude/context/cli.md` → `## Naming and file layout`, third bullet.
   Every other new `cli/src/retrieval/` module in the plan specifies its header: `runtime.ts` (Task 2), `models.ts` (Task 3), `chunk.ts`/`corpus.ts` (Task 4), `store.ts`/`refresh.ts` (Task 5), `server.ts` (Task 8) and `setup.ts` (Task 12). Task 7 lists constants, types and functions for `search.ts` but no header. Downstream agents each read only their own task file, so the Task 7 implementer never sees the header requirement, and the reviewer has no stated rule to hold the module to. This module does hold a rule, which the plan states only in prose: abstention happens only in `fused-rerank`, below one provisional threshold.
   **Fix:** In `task_7_plan.md`, add a Work bullet requiring `search.ts` to open with a module header. The header states what the module owns (hybrid search, RRF fusion, rerank, abstention, result rendering) and the rule in the words *"The rule this module exists to enforce"*. For example: *"only `fused-rerank` abstains, and only below `ABSTAIN_SCORE_THRESHOLD`, a provisional constant; the other modes' scores are uncalibrated and never abstain"*. Add a Verification line checking that the header is present.

2. **The new module `cli/src/retrieval/session.ts` has no header comment in the plan** — `task_6_plan.md` (Targets: `cli/src/retrieval/session.ts` (new); Work: the `retrieval/session.ts` bullet).
   Rule: the same one, `.claude/context/conventions.md` → `## What accompanies a new unit of each kind`, row *A module under `cli/src/`*.
   Task 6 specifies `openRetrieval`'s signature and its two refusals, but no header for the module. `session.ts` also holds a rule: it is the one gate every index-reading sub-verb passes through. It refuses before loading anything when `retrievalApplies` is false or the model files are missing, and it always loads models with `allowRemote: false`.
   **Fix:** In `task_6_plan.md`, add to the `retrieval/session.ts` bullet (or as its own bullet) a module header stating what the module owns and the rule in the words *"The rule this module exists to enforce"*. For example: *"every sub-verb that reads the index opens it here, and nothing is loaded before the retrieval-on and model-files-present refusals; models load with remote loading disabled"*. Add a Verification line checking that the header is present.

## Should Fix

- **Raised again from iteration 0, still open: the new machine-state pieces are not raised against the enumeration in `.claude/context/cli.md`.** That document's `## How a module in this layer is written` → *"Machine-scoped state is not an adopter's file"* says what this layer writes outside the repository "is enumerated and each piece has one owner": the registry, the daemon unit and the push settings. Tasks 3 and 12 add two more pieces under `machineCacheDir()/retrieval/`: the runtime, owned by `cli/src/retrieval/setup.ts`, and the model cache, owned by `cli/src/retrieval/models.ts` → `fetchModels`. Task 12 records them only in `writer.ts`'s header. `## Corpus staleness` in `feat_docs_catalog_retrieval_story_plan.md` has entries for the writer monopoly and the verb list, but none for this enumeration. Add a `stale-rule` entry naming the two new pieces and their owners.
- **Raised again from iteration 0, still open: `doctor`'s header choice 3 is not amended.** `task_13_plan.md` → `RETRIEVAL_INDEX_CHECK` runs a child CLI that embeds the whole corpus, with a 600 s timeout. `cli/src/doctor/checks.ts` header choice 3 still reads *"Reachability is command resolution by default … never a launch"*. Task 13 adds a separate header paragraph and argues the case in the check's doc comment, but leaves that absolute sentence as it is. `.claude/context/cli.md` → `## What "done" means here`: *"the change either satisfies it or amends the header in the same edit"*. Have Task 13 amend choice 3 itself, for example by qualifying "never a launch" with the in-memory index probe as its one bounded exception.
- **Raised again from iteration 0, still open: the schema still lands after its readers, and the conflict is not in `## Corpus staleness`.** Task 18 adds the schema after the model and check (Task 1) and after the plugin contracts that read `docs.retrieval` (Tasks 15–16). This conflicts with `.claude/context/conventions.md` → `### The order files are created…` item 1 and with `## The layers` (*"a plugin contract reading a shape the schema does not carry is a defect"*). The story index's `## Context` bullet now argues the order, but the conflict is still missing from `## Corpus staleness`. Record it there.
