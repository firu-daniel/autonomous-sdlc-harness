# Architecture review — iteration 3

The Must Fix from iteration 2 is fixed. `task_9_plan.md` now exports `DOCS_SEARCH_SERVER_SCRIPT_NAME` from `cli/src/generators/outerLoopScripts.ts`, with a doc comment naming its second consumer, and builds the row from it. `task_10_plan.md` imports that constant into `retrievalWiring` and adds a Verification grep that proves the name is never retyped.

There is one new Must Fix. The four Should Fix items raised in iterations 0 to 2 are still open. The index has no `## Rejected findings` section that answers them, so they are raised again below.

## Must Fix

1. **Shipped plugin text would name this repository's run branch and its run artifacts** — `task_17_plan.md` (the `plugin/agents/README.txt` bullet, sub-bullet **The source**) and `task_16_plan.md` (the `skeptic-reviewer.md` bullet).
   Rule: `.claude/context/conventions.md` → `## Configuration is the source of truth, and it is read at run time`: *"No branch name, no directory name and no command name from any one adopter appears as a literal in `plugin/`."* Also `.claude/context/plugin.md` → `## What this layer is`: *"No adopter's value is ever a literal here … an asset that named one would be wrong in every repository but the one it was copied from."* And `.claude/context/conventions.md` → `## Documents of record`: this repository has adopted its own harness, so *"A rule read off `harness-runs/` describes a run, not this project."*
   Task 17 has `plugin/agents/README.txt` say the set *"was fixed by the user for branch `feat_docs_catalog_retrieval`"*. That branch name belongs to this repository in its role as an adopter, and it comes from a run whose record sits under `harness-runs/`. That record is not part of the plugin and does not ship with it. Task 16 also tells the implementer to amend `skeptic-reviewer.md`'s `**Not the documentation corpus.**` paragraph with *"When the user granted the search tool (`answer_3.md`)"*. The plan gives no instruction to keep that citation out of the agent text, so the implementer reading only `task_16_plan.md` is likely to copy it in. An adopter's run then loads a contract that points at `harness-runs/clarifications/feat_docs_catalog_retrieval/answer_3.md`, a file that does not exist in their repository. Nothing in `plugin/` does this today. A grep for `feat_` and `clarifications/` over `plugin/` outside `plugin/samples/` finds only `<branch>` placeholders and `e.g.` examples.
   **Fix:** In `task_17_plan.md`, reword **The source** so it states the rule without its provenance. For example: *"The set is every plan writer, every plan reviewer and every end-of-branch reviewer; the per-unit `layer-reviewer`, the implementers, the committer, `docs-writer` and the interactive-test agents are excluded."* Name no branch, no user answer and no `harness-runs/` path. Add a Verification line: `grep -rn -e "feat_docs_catalog_retrieval" -e "answer_[0-9]" -e "clarifications/" plugin/agents` is empty. In `task_16_plan.md`, change the `skeptic-reviewer.md` bullet so the amended paragraph says the reviewer *is granted* the search tool (no reference to a user answer or `answer_3.md`) and uses a result's `path#heading` only as a locator. State explicitly that the plan's citation of `answer_3.md` is the plan's justification and is not copied into the agent file. Add the same grep to Task 16's Verification, run over the five target files.

## Should Fix

- **Raised again (iterations 0, 1 and 2), still open: the new machine-state pieces are not raised against the enumeration in `.claude/context/cli.md`.** The rule is `## How a module in this layer is written` → *"Machine-scoped state is not an adopter's file. What this layer writes outside the repository it was aimed at is enumerated and each piece has one owner"*. That list names the registry, the daemon unit and the push settings. Tasks 3 and 12 add two more pieces under `machineCacheDir()/retrieval/`: the runtime and the model cache. `## Corpus staleness` in `feat_docs_catalog_retrieval_story_plan.md` still has no entry for them. `task_12_plan.md` → the `writer.ts` bullet still says both pieces are *"created by `cli/src/retrieval/setup.ts`"*, but `fetchModels` in `cli/src/retrieval/models.ts` is what writes the weights. Add a `stale-rule` entry that gives each piece one owner (the runtime: `setup.ts`; the model cache: `models.ts` → `fetchModels`), and make Task 12's `writer.ts` clause name the same owners.
- **Raised again (iterations 0, 1 and 2), still open: `doctor`'s header choice 3 is left absolute.** `cli/src/doctor/checks.ts` choice 3 reads *"Reachability is command resolution by default, and a read-only registry query at most — never a launch."* `task_13_plan.md` → `RETRIEVAL_INDEX_CHECK` launches a child CLI that loads both models and embeds the whole corpus, with a 600 s timeout. The task argues this in a separate header paragraph and in the check's doc comment, but it leaves choice 3's sentence unqualified. `.claude/context/cli.md` → `## What "done" means here` says *"the change either satisfies it or amends the header in the same edit"*. Have Task 13 qualify choice 3 itself: the in-memory index probe is its one bounded exception, and it starts no server and reaches no network.
- **Raised again (iterations 0, 1 and 2), still open: the schema still lands after its readers, and the conflict is not in `## Corpus staleness`.** Task 18 adds the schema after Task 1's model and check, and after the plugin contracts in Tasks 15–16 that read `docs.retrieval`. That conflicts with `.claude/context/conventions.md` → `### The order files are created…` item 1, and with `## The layers`: *"a plugin contract reading a shape the schema does not carry is a defect"*. The story index argues the order only in `## Context`. Record the conflict as a `stale-rule` entry in `## Corpus staleness`.
- **Raised again (iteration 2), still open: `writer.ts`'s own rule sentence stays absolute while the index exception is added below it.** `cli/src/core/writer.ts`'s header rule says that every write into an adopting repository is enqueued in the engine. `task_5_plan.md` → the `writer.ts` bullet extends only the *"One artifact is deliberately outside this engine"* paragraph. The new exception, `<stateDir>/docs_index/`, is inside the repository, so it contradicts the rule sentence itself. The registry exception did not, because the registry lives outside the repository. Have Task 5 also qualify the rule sentence so it points at the named exception (`.claude/context/cli.md` → `## What "done" means here`, the header clause).

## Nice to Have

- **Raised again (iteration 2): `generators/repoRoot.ts` and `generators/permissionProfile.ts` (Task 10) import `DOCS_SERVER_NAME` and `SEARCH_TOOL_PERMISSION` from `cli/src/retrieval/server.ts`.** That module's import graph reaches `session.ts`, `store.ts`, `models.ts` and `search.ts`, so every `init` loads the retrieval module graph just to read strings. No peer package is loaded. A leaf module holding only the wire constants (for example `cli/src/retrieval/names.ts`), imported by `server.ts`, would limit the generators' dependency on retrieval to the wire alone.
