# Review plan meta-review — iteration 0

## Must Fix
1. **Finding 4 is a false positive: the short heading form it calls "truncated" is the citation form the cited file mandates** — refers to review finding #4. Offending files: `harness-runs/code_reviews/chore_withdraw_second_runtime_port_code_review/finding_4.md`, plus its pointer and readiness entry in `harness-runs/code_reviews/chore_withdraw_second_runtime_port_code_review.md`.
   The finding tells the implementer to replace `` `## The sanctioned form` `` in `docs/second-runtime-port-decision.md` → `## 5. What a graph runtime cannot do that the orchestrator does` with the full heading, written in double backticks. It calls the short form inconsistent and incomplete. But the file being cited sets its own citation form for that heading. `plugin/instructions/dispatch_discipline_instructions.md` → `## Knowledge, not conclusions — the boundary`, in its **Knowledge — permitted, and shaped.** paragraph, says: "that section's full heading carries a code span, so it is cited by that backtick-clean short form throughout". `.claude/context/plugin.md` → `## Wires: dispatch in, return out` repeats this ("the section that file itself cites as the backtick-clean short form `## The sanctioned form`"). So the record's current text follows the documented convention and is correct. The task prompt spells out the full heading in italic prose, not as a code-span citation, so it does not override that convention. Applying Finding 4 would make the loop swap a sanctioned citation for a nonstandard one. Nice-to-Have severity does not excuse this: the loop still spends an item, a commit and a checkbox on it.
   **Fix:** Delete `finding_4.md` from `harness-runs/code_reviews/chore_withdraw_second_runtime_port_code_review/`. In the index, remove the `### 4. §5 cites the dispatch-discipline heading ...` pointer under `## Nice to Have` (the section then reads `_None._`). Remove readiness entry `1. [ ] **Finding 4** — ...` and renumber the remaining readiness entries 1..3. If you want to keep the observation, move it to the call-out section as a verified-OK note ("the short form `## The sanctioned form` is the form `plugin/instructions/dispatch_discipline_instructions.md` mandates for citing that heading").
