# Review plan meta-review — iteration 0

## Must Fix
1. **No lessons-ledger pass for the three ledger categories the diff exhibits** — refers to "Structure" (missed check). Offending file: the index, `harness-runs/code_reviews/feat_orchestrator_flow_graph_planning_pilot_code_review.md` (`## Context`).
   `harness-runs/lessons.md` holds rules that carry conventions-document force, and this diff touches three of its categories. The story index's own Scope register lists them as rows 26–29, bound to Tasks 8, 14 and 16:
   - **Adopter-facing command text.** Rule: "Every command an adopter is meant to run sits in a fenced block…". The diff adds four commands to `docs/development.md` → `**Gate 3 — configuration and flow-graph schemas.**` and rewrites adopter-facing prose in `cli/templates/scripts/README.md` (*"**One of them is run by the orchestrating session:**"*), `cli/README.md` and `docs/cli.md` (*"**Two families land in `scriptsDir`…**"*).
   - **Adopter-facing naming.** Rule: "Name an adopter-facing surface with the term adopters already arrive with…". The diff introduces the adopter-facing terms *flow walker* and *flow graph*.
   - **Evidence and measurement.** Rules: "A figure measured under a test stub or a fixture-sized corpus never justifies a design decision…" and "A decision rule's outcome is a proposal to the maintainer…". `docs/flow-graph-walker.md` → `## 1. The core's size, measured` records measured figures, and `## 4. What a second flow would need before it adopts the walker` records a decision outcome.

   The index gives none of these a finding or a clean-pass rationale. `## Context` covers Pass 0's grep and caller checks, Pass 2, the test and checker runs, the mirror byte-identity check and the walker-versus-pre-change-core reading, but it never mentions the ledger. Silence about a ledger category the diff exhibits is the missed-check failure. It is not a clean pass, even if the code complies. A spot read suggests the code does comply: the Gate 3 commands sit in a fenced block, §1 measures the real core with `wc -c`, and §4 opens "A proposal to the maintainer, not scheduled work". That compliance is exactly what the review should have stated and verified.
   **Fix:** In the index's `## Context`, add one clean-pass paragraph for the lessons ledger. For each of the categories above, name the rule, the diff site it governs (the site anchors above), and what the reviewer checked there. If that check turns up a real violation, write it as a new `finding_6.md` in `harness-runs/code_reviews/feat_orchestrator_flow_graph_planning_pilot_code_review/`, with a matching `### 6.` pointer and a `**Finding 6**` readiness entry. No existing finding file changes.

## Should Fix
1. **The Context's reviewed-files enumeration omits four changed files** — refers to "Structure". Offending file: the index (`## Context` → `**Reviewed:**`).
   `git diff dev...HEAD --name-only` also lists `cli/src/generators/repoRoot.ts` (`RUN_CONTROL_ARTIFACTS` gains `.flow_walker_state`, and its doc comment is amended), `package.json` (`validate:flow-graph`, `validate:flow-graph:negative`), `cli/test/helpers/walker.mjs` and `schemas/README.md`. The **Reviewed:** paragraph names none of them explicitly. "The run-control ignore entry" arguably covers `repoRoot.ts`, but a reader cannot tell whether the other three were read.
   **Fix:** Name the four files in the relevant layer's clause of the **Reviewed:** paragraph (`cli` for the first and third, `general` for the other two).

2. **The outer-loop accompanying set is verified only for the walker's row** — refers to "Structure". Offending file: the index (`## Context`).
   `.claude/context/conventions.md` → `## What accompanies a new unit of each kind` → *An outer-loop script* requires three things of a new row: `agentInvocable` set deliberately, the profile entries that flag implies, and a `DENY_SCRIPT_BASENAMES` entry where the file must not be agent-runnable. The Context confirms this only for `flow-walker.sh` ("the new `agentInvocable: true` row grants it"). The two `agentInvocable: false` rows, `lib/flow-walker-gates.sh` and `flows/task_plan_writing.graph.json`, get no statement of why they need no deny entry. They are non-destructive, like the unlisted `harness-run-lib.sh`, so no finding is expected.
   **Fix:** Add one sentence to `## Context` stating the deny-list disposition for those two rows.
