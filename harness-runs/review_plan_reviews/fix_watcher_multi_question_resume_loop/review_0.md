# Review plan meta-review — iteration 0

## Must Fix
1. **Finding 1 is rated Must Fix, but it is a comment-accuracy defect, and the review rates the same kind of defect Should Fix in Finding 4** — refers to review finding #1. The file to change is the index, `harness-runs/code_reviews/fix_watcher_multi_question_resume_loop_code_review.md`. `finding_1.md` needs no change.
   Must Fix is only for three things: a business-logic divergence from the reference implementation (not applicable, because `phases.parity` is `false`); broken architecture, meaning a responsibility in the wrong layer, a boundary those documents draw being crossed, or a missing required accompanying item; and a hard violation of a conventions document. Finding 1 is none of these. The defect is one sentence in a shell comment, "The clause's wording is quoted by the engine's Override 2(a); change both together." It sits above `local resume_clause=""` in `spawn_engine`, in `cli/templates/scripts/autonomous-watcher.sh` and its mirror. The facts in the finding check out:
   - no plugin file quotes the clause text (`This is a RESUME` / `consume ALL` have no hits under `plugin`, `cli/src` or `docs`);
   - the label is new on this branch;
   - `plugin/instructions/mode_contract.md` → `### Sanctioned cross-fork anchors` → Class (i) *Reason* says the watcher "never uses the label at all".

   Still, the sentence changes no behaviour. It does not cross the "neither shipped half may reach into the other at run time" boundary (`.claude/context/conventions.md` → `## The layers`), because a comment is not a run-time edge. It also bypasses no mandated identifier.

   The review's own Context says of all four findings: "all prose or comment accuracy defects the branch introduced; none is a behaviour defect". Its second argument, that the comment makes a stated `mode_contract.md` invariant false, is the same harm Finding 4 describes for Class (i-b). Finding 4 is rated `## Should Fix`. Rating one of these Must Fix and the other Should Fix is inconsistent, and it spends the fix loop's highest severity on an item that did not earn it.
   **Fix:** In the index, move the `### 1. The watcher's resume-clause comment claims Override 2(a) quotes the clause, …` heading and its `→ [finding_1.md](…)` pointer from `## Must Fix` to `## Should Fix`. Keep the finding number, and place it by impact among Findings 2, 3 and 4. `## Must Fix` then reads `_None._`, matching the existing `## Nice to Have` form. Leave the readiness entry `1. [ ] **Finding 1** — …` where it is, because the readiness list is the ship order, not the severity order. Leave `finding_1.md` as it is. Alternatively, if the author holds that Finding 1 is a hard conventions violation, name the conventions-document rule it breaks in `finding_1.md` and keep the rating. In that case, apply the same test to Finding 4 and make the two ratings consistent.
