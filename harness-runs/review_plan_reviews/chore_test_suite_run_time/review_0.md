# Review plan meta-review — iteration 0

## Must Fix

1. **Finding 2's fix, applied after Finding 1's, leaves the gate-4 record stating a measurement provenance that is no longer true** — refers to review finding #2 (and its interaction with #1). Offending file: `harness-runs/code_reviews/chore_test_suite_run_time_code_review/finding_2.md`.

   The readiness list orders Finding 1 before Finding 2, and Finding 2 says "Apply Finding 1 first". Finding 1 replaces the sentence in `docs/development.md` → Gate 4 → **Run time, measured.** that begins "`a885d631d85b` is the commit before the template-copied fixtures…" with a new sentence saying the before column "was measured at `a885d631d85b`", that "The after columns were measured at that branch's `4c36164a9759`", and that "the after command runs at the checkout's own `HEAD`". Finding 1's own **Verification** then requires `git grep` for each of those two SHAs to print that sentence.

   Finding 2 then re-measures both sides: before at `--ref fe17b4e2293f --runs 3`, after at `HEAD` with `--runs 3`. It replaces the fenced commands and every cell of the `| host, 10 cores |` row. Its last rewrite sub-step touches only "the paragraph's first two sentences ('Read the host row as a range, not a point. The host was shared…')" and says "Leave the rest of the paragraph as it is". It never touches the explanatory sentence Finding 1 wrote above the table. So once both fixes land, the document says every figure was measured at `a885d631d85b` / `4c36164a9759`, when in fact every figure was measured at `fe17b4e2293f` / `HEAD`. That is a false statement in a document of record. `.claude/context/conventions.md` → `## Documents of record` requires a measured fact to state "what was measured, the command". The review's own fix sequence would create the kind of defect Finding 1 exists to remove.

   Finding 2 also leaves "Its column was measured at `a885d631d85b`" in place. Finding 1's sentence uses the singular "Its column", but there are two before columns (`npm test` and `run-gates.sh`). Finding 2 is the right place to fix that too, because it rewrites that provenance anyway.

   **Fix:** In `finding_2.md`, add a sub-step after the table-row replacement: replace the explanatory sentence Finding 1 introduced (the one beginning "`fe17b4e2293f` is the `dev` commit before the template-copied fixtures…" and ending "…rounded to whole seconds:") with one that says both before columns were measured at `fe17b4e2293f` and both after columns at the checkout's `HEAD`, on the date of the re-measurement. Drop every mention of `a885d631d85b` and `4c36164a9759`, because neither commit is a measurement point any more. Add to `finding_2.md`'s **Verification** that `git grep -n "a885d631d85b\|4c36164a9759" -- docs` prints nothing once Finding 2 has landed. Also update the "Measured on 2026-09-25…" opening clause if the re-measurement date or host details differ. The index's readiness entry and the heading for Finding 2 do not need to change.
