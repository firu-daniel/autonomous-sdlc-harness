# Review plan meta-review — iteration 0

## Must Fix
1. **Finding 1 is filed as Must Fix, but a superfluous `export` on a constant that is used is hygiene, not a defect that earns Must Fix** — refers to review finding #1. Offending files: the index `harness-runs/code_reviews/feat_harness_init_notification_topic_prompt_code_review.md` (the `## Must Fix` section) and `harness-runs/code_reviews/feat_harness_init_notification_topic_prompt_code_review/finding_1.md`.
   Must Fix covers three things: broken architecture (a responsibility placed in the wrong layer, a crossed boundary, or a missing required accompanying item), a hard violation of a conventions document, or a parity divergence. Parity is off here (`phases.parity: false`). Finding 1 meets none of the others:
   - The finding says itself that `NTFY_TOPIC_PATTERN` is read by `resolvePushDestination` in the same module ("if (NTFY_TOPIC_PATTERN.test(topic))"). The grammar is wired through the one resolver and is tested there. This is not the "un-wired / dead feature" that the Pass 0 caller check exists to catch. Only the `export` keyword has no importer.
   - An exported regex is not a second copy of the grammar. `.claude/context/conventions.md` and `.claude/context/cli.md` contain no rule that bans an export with no importer. `noUnusedLocals` does not reach exports either, so no gate treats it as an error. The harm the finding describes ("a later caller *could* test a topic against the exported pattern directly") is hypothetical, and no code on the branch does it.
   - The export is plan-correct work. `harness-runs/task_plans/feat_harness_init_notification_topic_prompt/task_1_plan.md` → **Work**, first bullet, says the constants are "exported with these exact names and shapes" and lists `export const NTFY_TOPIC_PATTERN = /^[-_A-Za-z0-9]{1,64}$/;`.
   - The finding cites a precedent that undercuts its own severity. The sibling correction on `GUIDED_ENDPOINT_EXAMPLE` was filed as **Nice to Have** (Finding 2 of `harness-runs/architecture_branch_reviews/feat_harness_init_notification_topic_prompt_arch_review.md`), and that one also had a doc comment falsely claiming an importer. This case has no false comment.
   Filing it as Must Fix spends the fix loop's top-severity slot on an item that did not earn it.
   **Fix:** In the index, move the `### 1. \`NTFY_TOPIC_PATTERN\` is exported with no caller outside its defining file` pointer out of `## Must Fix` and into `## Should Fix` or `## Nice to Have`. Keep its number and its `finding_1.md` link. If that leaves `## Must Fix` empty, say so in that section in one line. In the Context paragraph, reword "Its caller-check half produced Finding 1" so it no longer implies an un-wired feature. For example: "its caller-check half found no un-wired feature; the one export with no importer is Finding 1, a hygiene item." In `finding_1.md`, keep the site anchor and the fix snippet. Soften the second paragraph so it describes a narrowing of the public surface and not a present risk to the grammar. Also note that the plan's Task 1 prescribed the export. The readiness entry and its position may stay as they are, since small-and-safe-first ordering still puts it first.
