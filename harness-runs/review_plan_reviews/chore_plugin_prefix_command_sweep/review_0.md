# Review plan meta-review — iteration 0

## Must Fix
1. **The one lesson in the ledger goes unchecked, though the diff falls squarely in its category** — refers to "Structure" (a missed check). Offending file: the index, `harness-runs/code_reviews/chore_plugin_prefix_command_sweep_code_review.md` (its `## Context` → `**What else was checked:**` list), plus a possible new `finding_5.md`.
   `harness-runs/lessons.md` → `## Adopter-facing documentation` has one real lesson: *"Every command an adopter is meant to run sits in a fenced block, one command per line; never inline it, join two with prose, or cut its block when compacting a document."* This branch rewrites command spellings across the adopter-facing documents: `README.md`, `llms.txt`, `docs/cli.md`, `docs/analyze.md`, `examples/notes-app/README.md`, `cli/templates/claude/README.md` and `plugin/README.md`. The story index also says the lesson binds Task 12 (scope-register row 89). The review has no finding drawn from this lesson, and no clean-pass line for it. Its `**What else was checked:**` list covers code literals, callers, command names, parsers and parity, and nothing else.
   This is not only a paperwork gap. `README.md` still carries a command inline, in prose, in a sentence the branch rewrote: "Two direct routes reach the same drop: `/autonomous-sdlc-harness:branch-prompt` itself, or a file named `<branch>_task_prompt.md` written into `<state_dir>/autonomous_inbox/`". Task 12 did move the `harness-analyze <target>` line into a fenced block, so the review could have noted that as the lesson's clean pass. It did neither.
   **Fix:** run the lesson over every adopter-facing document the diff touched.
   - For each place that inlines a command an adopter is meant to run, add a new `finding_<N>.md` in `harness-runs/code_reviews/chore_plugin_prefix_command_sweep_code_review/`. Give it a site anchor (path plus quoted substring) and the exact fenced-block rewrite. Then add its `### N. Title` pointer under the right severity section of the index, and its `N. [ ] **Finding N** — … _(layer: general)_` entry in `## Phase 2 Readiness — Ordered Fix List`. The `README.md` sentence quoted above is the first candidate to judge.
   - Where a mention is a referent rather than a command to run (for example "on a yes it invokes `/autonomous-sdlc-harness:branch-prompt` with the request"), or where nothing is left to flag, add a `**Lessons.**` bullet to the index's `**What else was checked:**` list. It must name the lesson, the documents checked and why each remaining inline mention passes.
