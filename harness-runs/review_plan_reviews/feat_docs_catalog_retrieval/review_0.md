# Review plan meta-review — iteration 0

The split structure is sound: the index carries Context → `## Phase 2 Readiness — Ordered Fix List` (exact heading, correctly positioned before `## Must Fix`) → the three severity sections → the divergence call-out; eight readiness entries map 1-to-1 onto eight `### N.` pointers and eight `finding_<N>.md` files, with no orphan and no dangling pointer; the round suffix on index and folder agree. Every site anchor, symbol and quoted substring in findings 1–8 was re-opened in the tree and resolves, and none of the eight findings is a false positive: the claims about `ABSTAIN_MESSAGE`, the three `--docs-retrieval` literals against `QA_DRIVER_FLAG`, `retrievalWired` / `mcpWritten`, the five unread `export`s, the `rendered !== ''` guard, the inline `git -C … rev-parse` in the launcher and the bare `exec node` were each confirmed at source. `phases.parity` is `false`, so the parity spot-check is inert and the empty call-out section is correct. No finding carries a phase gate. What follows is what the review missed or mis-filed.

## Must Fix

1. **Missed checks — the `plugin` layer is touched and the review shows no check drawn from `.claude/context/plugin.md`** — Structure. Offending file: the index, `harness-runs/code_reviews/feat_docs_catalog_retrieval_code_review.md` (the Context paragraph).
   The diff changes fourteen files under `plugin/` — ten agent `tools:` allowlists plus their `<docs_retrieval>` rows, the new docs-retrieval roster block in `plugin/agents/README.txt`, `plugin/hooks/README.md` and the guard's LEFT-ALLOWED comment. All eight findings are `_(layer: cli)_`, and the Context's clean-pass enumeration draws only on `.claude/context/conventions.md` rows (`SUBCOMMANDS`, the configuration key in four places, the negative fixture's `ajv` assertion, the outer-loop row, the module headers, the idempotence assertions). Nothing in the review is drawn from the `plugin` layer's own conventions document — specifically `## Verifying a change in this layer` (*"A change to a wire is verified by the grep that re-derives its readers"*), which is exactly the case here: `mcp__harness-docs__search_docs` and the `harness-docs` server name are a new wire spanning ten agent files, `cli/src/retrieval/server.ts` (`DOCS_SERVER_NAME`, `SEARCH_TOOL_NAME`) and two CLI templates. Silence about a touched layer is the failure this check exists for.
   I ran the sweep the roster itself prescribes — `grep -rln --include='*.md' "mcp__harness-docs__search_docs" plugin/agents` — and it returns exactly the ten files `plugin/agents/README.txt` lists, and the server name agrees across `server.ts`, `cli/templates/repo/mcp.retrieval.json` and `cli/templates/claude/settings.autonomous.retrieval.json`. So the expected outcome is a clean pass, not a new finding.
   **Fix:** in the index Context, add an explicit `plugin`-layer clean-pass rationale naming the check and its result — that the wire sweep prescribed by `.claude/context/plugin.md` → `## Verifying a change in this layer` and by `plugin/agents/README.txt` was re-derived and returns exactly the ten granted agents, and that the guard edit is comment-only so `.claude/context/plugin.md` → `## Guards, the shared library and the helper scripts`'s re-measurement obligation in `docs/guard-verification.md` is not engaged. No new `finding_<N>.md` and no readiness entry.

2. **Missed check — the lessons ledger's adopter-documentation category is exercised by the diff and the review never addresses it** — Structure. Offending file: the index, `harness-runs/code_reviews/feat_docs_catalog_retrieval_code_review.md` (the Context paragraph).
   `harness-runs/lessons.md` → `## Adopter-facing documentation` carries *"Every command an adopter is meant to run sits in a fenced block, one command per line; never inline it, join two with prose, or cut its block when compacting a document."* This branch adds adopter-run commands to `README.md`, `cli/README.md`, `docs/cli.md`, `docs/development.md`, `docs/retrieval.md` and `llms.txt` — precisely that category. The review mentions neither the ledger nor the rule, so there is no finding and no clean-pass rationale covering it.
   I checked the added command lines; they sit in fenced blocks, one per line, so again the expected outcome is a clean pass rather than a finding.
   **Fix:** add one sentence to the index Context stating that the `## Adopter-facing documentation` lesson was checked against the commands this branch adds to the adopter-facing documents and that each sits in a fenced block, one per line. No new finding file.

3. **Finding 8's readiness tag routes the fix to `cli` alone, but its fix edits `docs/cli.md`, which is the `general` layer** — refers to review finding #8. Offending file: the index, `harness-runs/code_reviews/feat_docs_catalog_retrieval_code_review.md`, readiness entry 8.
   Entry 8 reads `8. [ ] **Finding 8** — Make `docs search` answer when a non-abstaining mode returns zero hits _(layer: cli)_`. `finding_8.md`'s fix reaches four places: `cli/src/retrieval/search.ts`, `cli/src/commands/docs.ts`, `cli/test/docs-retrieval.test.mjs` — all `cli` — **and** *"add one sentence to `docs/cli.md` → `### docs search`"*, which sits under the catch-all layer whose `path` is `.` (`general`). The fix loop routes off this tag without opening the finding body, so as written it dispatches a `cli` implementer against an edit that lands outside `cli/`. A comma-joined tag is the sanctioned form for exactly this case, catch-all last.
   **Fix:** change the tag on readiness entry 8 to `_(layer: cli, general)_`. Nothing in `finding_8.md` changes.

4. **Finding 3 is graded Should Fix, but it bypasses a stated monopoly — and the index asserts in bold that nothing on this branch is a Must Fix** — refers to review finding #3. Offending file: the index, `harness-runs/code_reviews/feat_docs_catalog_retrieval_code_review.md` (Context, `## Must Fix`, `## Should Fix`, readiness order).
   `finding_3.md` is a mandated shared accessor bypassed: `cli/templates/scripts/docs-search-server.sh` re-derives the repository root with its own `git -C "$script_dir" rev-parse --show-toplevel` on the line after it sources the library whose header declares itself *"the one place every generated outer-loop script resolves the repository it is operating on"*, and which every one of the nine other outer-loop scripts calls as `hr_repo_root "$script_dir"`. `.claude/context/conventions.md` → `### Where a new responsibility goes` states the monopoly rule and *"Before adding a copy of anything, grep for it"*. The finding also documents a behavioural divergence — under `set -euo pipefail` the inline form exits with git's 128 and git's own message, where the library form returns 1 for the caller to name — so the script's own stated exit contract is wrong on that arm today. That is the Must Fix class (a hard conventions violation, a mandated accessor bypassed), not the Should Fix class.
   The consequence of leaving it as it stands is not only ordering: the Context asserts **"Nothing in this branch is a Must Fix."**, which is the sentence a human reads to decide the branch ships, and `## Must Fix` reads `None.`
   **Fix:** move the `### 3.` pointer from `## Should Fix` to `## Must Fix` in the index (renumbering the remaining pointers is not required — the `K` in `**Finding K**` is stable identity, and the `### N.` headings should keep naming the same findings), delete the `None.` placeholder under `## Must Fix`, and re-word the Context sentence so it states the one Must Fix rather than claiming there is none. Re-order the readiness list so Finding 3 leads it, or state in the section's preamble why the blast-radius order keeps it where it is. `finding_3.md` itself needs no change.

## Should Fix

- **Finding 5's second site anchor is an elided quote that does not resolve verbatim** — `finding_5.md`. The anchor reads ``(`parseInitFlags`) — `'init: --docs-retrieval needs --docs: …'` ``; the `…` means a literal search for that string fails. The symbol `parseInitFlags` does resolve and the fix block quotes the full message, so the site is locatable, but the substring as cited is not one a consumer can grep. **Fix:** in `finding_5.md`, cut the quoted substring to a resolvable prefix — `'init: --docs-retrieval needs --docs'` — or drop the quote and keep the symbol alone.
- **Finding 2 is arguably the same class as Finding 3.** `.claude/context/conventions.md` → `## Configuration is the source of truth, and it is read at run time` states *"The shared constants have owners, and a value is imported from its owner rather than retyped"* as a general rule, and `ABSTAIN_MESSAGE` declares itself the owner of that string in `cli/src/retrieval/search.ts`. Left at Should Fix here only because the rendered text is byte-identical today and the enumerated owner list does not name this constant; raise it with Finding 3 if the author prefers one consistent grading.

## Nice to Have

None. The Context's `72 files, +3993/-120` count was re-derived with the same exclusion pathspecs and matches exactly.
