# Review plan meta-review — iteration 0

## Must Fix
1. **Finding 2 is a latent-drift guard dressed up as Must Fix** — refers to review finding #2. Offending files: the index `harness-runs/code_reviews/feat_remote_execution_github_actions_code_review.md` (the `## Must Fix` placement of `### 2.` and the Context sentence "Two cross-unit gaps are Must Fix"), and `harness-runs/code_reviews/feat_remote_execution_github_actions_code_review/finding_2.md` (no change needed to its body).
   Must Fix covers three things: broken architecture (a missing required accompanying item included), a hard violation of a conventions document, and a parity divergence. `phases.parity` is `false`, so the third does not apply. Finding 2 meets neither of the other two:
   - **Nothing is broken today.** All three spellings agree on `harness-state`: `cli/src/remote/githubActions.ts` → `STATE_ARTIFACT_NAME`, `cli/templates/scripts/remote-run.sh` → `STATE_ARTIFACT_NAME='harness-state'` and `cli/templates/github/workflows/harness-run.yml` → the `Upload the state bundle` step's `name: harness-state`. The finding describes what *would* happen if they drifted. It does not describe a defect in the diff.
   - **The mirror rule is met.** `.claude/context/conventions.md` → `## Configuration is the source of truth…` → the persisted-key bullet, and `## What accompanies a new unit of each kind` → *A persisted machine-state key*, require only that the owning module's header declares every mirror. `cli/src/remote/githubActions.ts`'s header declares `remote-run.sh`, `harness-run.yml` and `harness-resume.yml`. `remote-run.sh` → "STATE_ARTIFACT_NAME  mirrors  STATE_ARTIFACT_NAME" and `harness-run.yml`'s `DECLARED MIRRORS` block ("the artifact harness-state") declare the other direction.
   - **No conventions document requires a mirror-agreement test.**
   - **The module's own rule is also met.** It reads "a copy anywhere else in `cli/src` imports it", and no copy exists in `cli/src`.
   - **An export no module consumes breaks no gate.** `noUnusedLocals` does not flag exports, and the "either a consumer or a written statement" requirement applies to a *configuration key*, not a constant.

   Adding a test that pins the mirrors is a worthwhile hardening, and that is Should Fix. As Must Fix, it spends the fix loop's highest severity on a risk that has not happened.
   **Fix:** In the index, move the `### 2. \`STATE_ARTIFACT_NAME\` is exported and nothing consumes it…` heading and its `→ [finding_2.md](…)` pointer from `## Must Fix` to `## Should Fix`. Place it by impact among Findings 3, 4 and 5: after Finding 3, whose defect happens today, drops a relayed answer and sends no notification, and no higher than that. Rewrite the Context paragraph's "**The findings.** Two cross-unit gaps are Must Fix" bullet list so that only Finding 1 is called Must Fix, and adjust "Three runtime and report defects follow" to match. Keep the finding number and the `finding_2.md` file as they are. The readiness entry `2. [ ] **Finding 2** — …` can keep its place, because the readiness list is ordered by ship sequence, not by severity.
