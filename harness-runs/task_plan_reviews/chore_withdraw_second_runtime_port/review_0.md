# Task plan review — iteration 0

## Must Fix

1. **Scope register: derivation entry B is under-inclusive and misses every "roadmap item N" citation** — story index (`chore_withdraw_second_runtime_port_story_plan.md`), `## Scope register`. The same fix also changes a claim in `task_1_plan.md` and a check in `task_2_plan.md`.
   The task prompt's `## Establish, do not assume` asks *"Whether anything cites a roadmap priority by number, which renumbering the index would break."* Entry B (`grep -rn -E "ROADMAP\.md|[Pp]riority [0-9]+|this file's priorities" …`) only reaches the spellings `ROADMAP.md`, `priority N` and `this file's priorities`. This tree cites roadmap numbers as **"roadmap item N"** and as the constant `ROADMAP_ITEM = N`, and entry B matches neither. I re-ran it and it reaches only register rows 4–6. None of the following sites is a row:
   - `evals/README.md` → the `plan-shape/` bullet: *"the native eval runner, which is **roadmap item 8**"*. This one is not safe. `docs/development.md` → `## 6. The roadmap this tree defers to` item 8 is the example project `examples/notes-app/`, not an eval runner. `ROADMAP.md` priority 8 is the item being withdrawn, and after the renumber it becomes **Engine / provider abstraction**. So neither numbering currently resolves this citation to an eval runner. The register has to state which numbering it cites and give it a disposition with a reason, and it cannot assume the reading that `README.md` → *"Roadmap item numbers cited throughout this tree are listed in `docs/development.md`"* suggests.
   - `cli/src/commands/doctor.ts`, `cli/src/commands/daemon.ts`, `cli/src/commands/config.ts`, `cli/src/commands/init.ts`: `ROADMAP_ITEM = 13`. Per `cli/src/commands/registry.ts` → `roadmapItem`, this is numbered by `docs/development.md`'s legend.
   - `cli/src/core/report.ts` and `cli/src/core/writer.ts`: "roadmap item 13".
   - `cli/templates/README.md` ("Roadmap items 6 …"), `cli/templates/claude/README.md` ("roadmap item 14"), `cli/templates/githooks/README.md` and `cli/templates/repo/README.md` ("Roadmap item 13").
   - `docs/analyze.md` ("roadmap item 15"), `docs/cli.md` ("roadmap item 18"), `docs/development.md` ("roadmap item 11", "roadmap item 12"), `docs/retrieval.md` ("roadmap item 18", "Roadmap item 17"), `docs/retrieval-eval-results.md` ("roadmap item 17", "roadmap item 18", "Roadmap item 18").
   - `plugin/README.md` ("Roadmap items 3 …"), `plugin/agents/README.txt` ("Roadmap item 4"), `plugin/commands/README.txt` ("Roadmap item 5"), `plugin/hooks/README.md` ("Roadmap item 5", "roadmap item 6"), `plugin/instructions/README.md` ("Roadmap item 3", "roadmap item 4"), `plugin/scripts/README.md` ("roadmap item 5").
   - `README.md` → the closing line of `## Where to read more`, *"Roadmap item numbers cited throughout this tree are listed in `docs/development.md`"*. This is the evidence for every `no-change` disposition above, so it belongs in the register too.
   Because of this gap, register row 14's reason (*"Entry B shows that nothing cites a `ROADMAP.md` priority"*) and `task_1_plan.md`'s instruction to write *"No citation elsewhere in the tree uses a `ROADMAP.md` priority number"* into the published record both rest on a derivation that never looked at the citation form this tree actually uses.
   **Fix:** In the story index, replace entry B with this wider command, run from the checkout root:
   `grep -rn -E "ROADMAP\.md|[Pp]riority [0-9]+|this file's priorities|[Rr]oadmap (item|items|row|priority|entry) [0-9]+|ROADMAP_ITEM = [0-9]+" --exclude-dir=node_modules --exclude-dir=harness-runs --exclude-dir=.git --exclude-dir=dist .`
   Add one row for each site it reaches. Most take `no-change`, with the reason that the number resolves against `docs/development.md` → `## 6` and cite the `README.md` closing line as evidence. Give `evals/README.md` → *"roadmap item 8"* its own row. Either establish which numbering it cites and why the renumber does not break it, or record that it was already broken before this branch and does not resolve against either numbering, with the reason it is `no-change` here. Then update row 14's reason. In `task_2_plan.md`, point the `**Verification:**` priority-citation bullet at the corrected command and at the new rows instead of "rows 4–6". In `task_1_plan.md`, make sure the record's "No citation elsewhere …" sentence matches what the corrected derivation actually established.

## Should Fix

1. **`task_1_plan.md`: files removed from the published tree are cited as code spans only for `harness-runs/`.** The record will also cite `scripts/publish-main.sh`, and `scripts` is itself an entry of that script's `removed_paths` (as are `.claude`, `harness.config.json` and `githooks`). Nothing in the plan stops the implementer from linking `scripts/publish-main.sh`, and that link would be dead on the published `main`. **Fix:** widen the Work bullet and the `grep -n "harness-runs/"` Verification bullet to cover every `removed_paths` entry (`scripts/`, `.claude/`, `harness.config.json`, `githooks/`, `harness-runs/`): code span, never a `](` link target.

2. **`task_1_plan.md`: the stated read command does not reproduce the selection.** `harness-runs/autonomous_logs/<run>.stream.jsonl` is one file per run with several sessions in it. The given `jq` prints every session's `total_cost_usd`. For `feat_arm_a_real_catalog_measurement` that is five values, and for `feat_docs_catalog_retrieval` seven. The "planning-only" selection (sessions that dispatched no `layer-implementer`) is not something the command does. `.claude/context/conventions.md` → `## Documents of record` requires a measured fact to state *"what was measured, the command and the exact message"*. **Fix:** have the record either give the command, or the procedure, that picks out the planning-only sessions (for example, which dispatch descriptor marks a session as containing a `layer-implementer`), or say plainly that the selection was made by reading each session's dispatches by hand.

3. **`task_1_plan.md` has no `**Source:**` line.** The task has one primary source, the task prompt, and names it only in a bold prose paragraph (*"The task prompt is the content source."*). **Fix:** add `**Source:** harness-runs/task_prompts/chore_withdraw_second_runtime_port_task_prompt.md → ## What the research found (2026-09-24)`, in the form the per-task template uses.

## Nice to Have

1. **`task_3_plan.md` → Goal miscounts the frameworks.** It says the record names *"LangGraph and six other agent frameworks"*, but the `**Depends on:**` line lists seven besides LangGraph (Claude Agent SDK, Pydantic AI, OpenAI Agents SDK, CrewAI, Google ADK, Microsoft Agent Framework, Mastra). Change it to "seven", or drop the count.
