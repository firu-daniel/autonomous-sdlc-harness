# Task plan review — iteration 1

Iteration 0's Must Fix is resolved. Entry B is now the wider command. I re-ran it, and every hit it reaches is a row (rows 4–6 and 24–50). Row 24 records the `evals/README.md` → *"roadmap item 8"* case, and I checked its claims: `docs/development.md` → `## 6` item 8 is `examples/notes-app/`, `2c01123` is the first commit, and `git log --diff-filter=A -- ROADMAP.md` names `cd2f076`. Row 14's reason, `task_2_plan.md`'s priority-citation Verification bullet and `task_1_plan.md`'s citation sentence all now match the corrected derivation. I also re-ran entries A, C and F verbatim, and every site they reach is a row. For entry E, I re-walked the procedure and it reaches rows 18–22 exactly.

## Must Fix

1. **Scope register: derivation entry D reaches sentences in `### Why no candidate is named here` that are not rows.** This is in the story index (`chore_withdraw_second_runtime_port_story_plan.md`), `## Scope register`. The same fix also touches `task_3_plan.md`.
   Entry D's traversal says to read *"every sentence of §9 → `### Why no candidate is named here`"*. Its decision rule reaches a sentence *"when it states what the tree names or builds as a second runtime, or what a first adapter would prove"*. I re-walked it against `ARCHITECTURE.md`, and three sentences meet that rule besides the opening one, which is row 16. None of the three has a row:
   - The first bullet's shipped sentence: *"**[shipped]** Nothing of that note is in this tree, which is why the claim carries the `**[external]**` marker"*. It states what the tree holds about the leading candidate.
   - The fourth bullet: *"**[designed]** The place where a candidate is named is a funding application's milestone wording — a document outside this repository and outside this release's scope."* It states that the tree is not where a candidate gets named. After Task 1, the tree does name agent frameworks.
   - The closing paragraph: *"**[designed]** What the naming will require when it happens is the source audit itself"*. It states that the naming has not happened yet.

   `task_3_plan.md` already argues that these sentences stay true: *"The four reasons that follow the sentence are about **this** document naming a backend candidate, and they still hold"*, and its Work bullet leaves *"the four bullets below the amended paragraph, the closing 'What the naming will require' paragraph"* unchanged. The register never records that disposition, though. The closure invariant, *every site that any entry above reaches appears as a row*, therefore fails on entry D.
   **Fix:** In the story index's `## Scope register`, add one row for each of the three sentences, cited by the quoted anchors above, each with evidence `entry D, bullet 2`. Each row is `no-change` with a stated reason. The record names frameworks only as the ones considered for a withdrawn port and selects none of them, so no candidate has been named in §9's sense, and the naming and its source audit are still in the future. Alternatively, if you judge any of the three falsified, make it `change` and give it to Task 3. In `task_3_plan.md`, point the "Leave these unchanged" bullet at the new row numbers so both files carry the same disposition.

## Should Fix

1. **`task_1_plan.md`: only `harness-runs/` is guarded from being linked, and it is not the only published-tree removal.** This is carried over from iteration 0 and is still not applied. The record will cite `.gitignore` and `scripts/publish-main.sh`. `scripts/publish-main.sh` → `removed_paths` also strips `scripts`, `.claude`, `harness.config.json`, `githooks` and `.gitattributes`, so a `](../scripts/publish-main.sh)` link would be dead on the published `main`. **Fix:** widen the Work bullet and the `grep -n "harness-runs/"` Verification bullet to cover every `removed_paths` entry: code span only, never a `](` link target.
2. **`task_1_plan.md`: the stated read command does not do the planning-only selection.** This is carried over from iteration 0. `jq -c 'select(.type=="result") | .total_cost_usd'` prints every session's cost in a run log. The selection of sessions with no `layer-implementer` dispatch is stated only as a criterion in prose. `.claude/context/conventions.md` → `## Documents of record` requires *"what was measured, the command and the exact message"*. **Fix:** have the record either give the command that picks out planning-only sessions, or say plainly that the selection was made by reading each session's dispatches by hand.
3. **`task_1_plan.md` has no `**Source:**` line.** This is carried over from iteration 0. The task has one primary source, the task prompt, but names it only in a bold prose paragraph. **Fix:** add `**Source:** harness-runs/task_prompts/chore_withdraw_second_runtime_port_task_prompt.md → ## What the research found (2026-09-24)`.

## Nice to Have

1. **`task_3_plan.md` → Goal still says "LangGraph and six other agent frameworks".** The `**Depends on:**` line lists seven besides LangGraph. Change it to "seven", or drop the count.
2. **`task_2_plan.md` → the example row sentence says *"code-enforced routing no run has needed"*.** The prompt words this as the maintainer's observation: *"The maintainer has not observed an orchestrator skip or misroute a flow step"*. `task_1_plan.md` requires that framing too: *"never as a count or a measurement"*. Rewording the example in the same terms keeps the row and the record consistent.
