# Task plan review — iteration 4

Structure, correspondence, granularity and layer tags pass. The story index carries `# Story:`, a `## Context` ending in `Top risks:` and `Manual setup required:`, `## Phase 2 Readiness — Ordered Fix List` immediately after Context with the exact heading text, no `## Tasks` section, no index-level `### Targets` / `### Sources of truth`, `## Scope register` in position and `## Rejected findings` last. Fourteen readiness entries map 1:1 onto fourteen `task_<N>_plan.md` files; every `_(layer: …)_` carries exactly one configured name; every `_(points: …)_` is ≤ 20; no per-task file exceeds five `**Work:**` bullets; no `**Depends on:**` link points at a later readiness entry; no task targets a conventions document. `phases.parity` and `phases.docs` are `false`, so the parity check group and the docs-retrieval tool are out of play.

Iteration 3's Must Fix is applied: `task_5_plan.md`'s second `**Verification:**` bullet now drives the re-run through `harness-runs/scratch/recheck-arm-e.mjs` with **no `--out`**, names `run-eval.mjs` as the launcher not to use, and states the reason (the pre-move fence must survive until Task 14 has quoted it; Task 9 is the only regenerator).

I re-ran both derivation commands verbatim and re-walked all three procedure entries (artifact → traversal → per-candidate decision rule). The register **closes**: entry 1's fourteen matches and entry 2's twelve matches each appear as a row; entry 3's reached directory contracts (`cli/README.md`, `evals/README.md`, the root `README.md`, the new `evals/docs-retrieval/queries/README.md`) are rows — `docs/` and `scripts/` carry no `README.md`, so no contract is reached there; entry 4's two index lists are rows; entry 5's four reached ledger rules (the two under `## Evidence and measurement`, the two under `## Adopter-facing documentation`) are rows, and the `## Layer ownership` rule is not reached by the stated decision rule. No row cites a line coordinate; no derivation command bakes in a count. A wider search for other spellings of the claim (`uncalibrated|unmeasured|not measured|ABSTAIN_SCORE_THRESHOLD`) reached no site the register misses. Every pointer I dereferenced resolves: `SEARCH_MODES` and `RERANK_CANDIDATES = 20` in `cli/src/retrieval/search.ts`, `modelFilesPresent`'s `{ present, missing }` signature and `RETRIEVAL_STUB_ENV` in `models.ts`, `RETRIEVAL_LOG_ENV` and `QueryLogRecord`'s nine keys in `queryLog.ts`, `retrievalModelCacheDir` / `retrievalRuntimeState` in `runtime.ts`, `serve (h)`–`(j)` in `cli/test/docs-retrieval.test.mjs`, `run-gates.sh`'s `failed` array tail, its hand-written `2e` arm, its `== gates this script cannot run` block and its `6a` / `6c` labels, `docs/development.md` §5's *"Ten gates"* / *"Five of the ten"* and gate 10's legs (i)–(v), `evals/README.md`'s `evals/**/prompt.md` discovery glob, and `.gitignore`'s `harness-runs/scratch/*` pair. The `rejected round 0/1/2/3/4` entries were re-read: each records a revision-scope reason that still holds and none is a finding I graded Must Fix, so none is re-raised.

## Must Fix

1. **The `self-docs` corpus is not stable across the branch — it swallows the branch's own `docs/` output — and the plan pairs figures taken over different corpus snapshots as though the only variable were the threshold** — the story index (`feat_docs_retrieval_eval_story_plan.md`, `## Context`), with the consequences landing in `task_5_plan.md`, `task_9_plan.md` and `task_10_plan.md`.

   `corpusConfig('self-docs')` sets `docs.root: 'docs'` (`task_3_plan.md`, first `**Work:**` bullet), so the corpus is **every** `*.md` under `docs/` plus the three conventions documents — including the files this branch itself adds there. The order the readiness list ships makes the corpus move under the measurements three times:
   - entry 3 (Task 3): 12 files, ~166 chunks — the count `task_3_plan.md`'s clean-run verification asserts and the one the prompt's *"roughly 165"* confirms;
   - entry 4 (Task 4): `docs/retrieval-eval-results.md` is created **and then indexed by every later run**, so from here on the corpus is 13 files and the eval's own file of record is inside the corpus it measures;
   - entries 11–13 (Tasks 10, 12): `docs/retrieval-eval.md` is added and `docs/retrieval.md` / `docs/cli.md` are rewritten, taking the corpus to 14 files with changed chunk text.

   Three concrete breakages follow, and no task states any of them:
   (a) **`task_5_plan.md`'s decision input is confounded.** Its second `**Verification:**` bullet takes the **after** recall from its own re-run and its **before** recall from Task 4's fence (`**Depends on:**`, and the hand-off paragraph: *"the arm E recall figures before and after the move, from the verification below"*), then judges the choice on whether *"recall@5 has not dropped by more than the positives the choice was known to cost"*. Those two runs are over different corpora — Task 4's generation and the re-run differ by a filled `docs/retrieval-eval-results.md` — so a drop cannot be attributed to the threshold, which is the one thing that comparison exists to decide.
   (b) **What ships in the file of record is a before/after pair over two corpora.** `task_14_plan.md` quotes the pre-move per-query distributions taken at one snapshot; `task_9_plan.md` regenerates the region from a post-move run at another, and its provenance line states *"which threshold value was in force for this run"* plus *"that arm E's figures here are the post-calibration ones and that the pre-calibration distributions the value was chosen from are in `## Threshold calibration`"* — naming the threshold as the difference and not the corpus. This is the plan's own stated first risk (*"a number that looks measured and is not"*) and the ledger rule it cites (*"A figure measured under a test stub or a fixture-sized corpus never justifies a design decision…"*), arriving from a direction no task guards.
   (c) **The shipped numbers are not reproducible by the procedure Task 10 documents.** The last self-docs generation happens at entry 10, before `docs/retrieval-eval.md` exists; an operator following `docs/retrieval-eval.md` after the branch merges runs over a 14-file corpus and gets different numbers, and `task_10_plan.md` — which documents *"point `--repo` at your checkout and the conventions documents your own `layers[]` names are in the corpus automatically"* — gives no warning that writing the results file into a document root makes it part of the next run's corpus. The prompt treats exactly this hazard as settled in the other direction: *"A catalog run would also change the corpus mid-branch, so the roughly 165 chunks deliverable 10 rests on, and any floor deliverable 6 records, would drift the next time it regenerated."*

   Nothing here touches the gate: `task_9_plan.md`'s floor is `fixture-catalog` only, and that corpus is stable.

   **Fix:** settle the corpus-snapshot rule once in the story index's `## Context`, in the paragraph that defines `self-docs` — state that this branch's own additions under `docs/` enter that corpus, which snapshot each recorded figure was taken over, and that figures from two snapshots are not a before/after pair — and carry it into the three tasks it binds:
   - `task_3_plan.md`, first `**Work:**` bullet: state the rule the `self-docs` corpus is pinned by. Either exclude the eval's own two documents from the resolved file set (a filter applied by the runner over `corpusFiles`' result, stated as the runner's and not as an edit to `corpus.ts`), or keep them and require every self-docs figure to be stamped with the file and chunk count it was taken at. Say which, once, here.
   - `task_5_plan.md`, second `**Verification:**` bullet: take **both** recall figures in this task's own pass — run arm E through `recheck-arm-e.mjs` once **before** editing the constant and once after, over the same tree — rather than reading the **before** figure out of Task 4's fence, so the only difference between the two numbers is the constant. Keep the fence as the source of the per-query distributions only, which is what the iteration-3 fix protects.
   - `task_9_plan.md`, last `**Work:**` bullet: add the corpus snapshot to the provenance the regeneration writes — the corpus file and chunk count of this run, and one clause saying the corpus includes this branch's own `docs/retrieval-eval-results.md`, so the post-move figures are not read against the pre-move quotation as if the corpus had held still.
   - `task_10_plan.md`, second `**Work:**` bullet: one sentence in the how-to-run section — a results file written with `--out` into a path under `docs.root` joins the corpus the next run measures, so an adopter either keeps it outside that root or reads the corpus counts in the provenance before comparing two runs.

## Should Fix

- **`task_10_plan.md` documents columns `task_4_plan.md`'s results file does not render** — `task_10_plan.md`, fourth `**Work:**` bullet: *"the abstention columns over negative queries"*. `task_4_plan.md`'s `results.mjs` bullet declares the table's columns exhaustively — `Arm`, `Mode`, `recall@1`, `recall@3`, `recall@5`, `MRR`, `strict recall@5`, `strict MRR`, `p50 ms`, `p95 ms`, `cost` — and carries no abstention column; `abstainedOnNegative` and `bestScoreOnNegative` are computed in `metrics.mjs` and reach the file only inside the machine half's per-query records. Task 10 ships at entry 11 against a file Task 4 committed at entry 4, so its implementer will read the artifact and find nothing to describe. Close it on one side: either add the abstention count to `results.mjs`'s table walk in `task_4_plan.md`'s column list, or reword Task 10's clause to name the per-query `abstained` field in the trailing `json` block. The same clause is what `task_5_plan.md`'s *"confirm the arm abstains on every negative query"* reads, and with no `--out` only the returned result object carries it — worth one clause there too.

- **`task_12_plan.md`'s list of gate 10 legs that remain unrun omits leg (iv)** — `task_12_plan.md`, first `**Work:**` bullet: *"the legs of gate 10 that remain unrun — the adopter-facing provisioning path (leg (i)), the `.mcp.json` unattended-session leg, and any Linux host."* `docs/development.md` §5 gate 10 has five legs, and leg (iv) **Search** runs `npx autonomous-sdlc-harness docs search`, which this branch cannot reach — `retrievalApplies` is false here, which is the whole reason the runner owns its own config. Leg (ii) is likewise unrun. Item (d) becoming a partial result is acceptance 11, so the enumeration is the part a reader will trust: name every leg that stays unrun, or state the list as *the legs that ran* and let the rest follow. Note `task_9_plan.md` rewrites leg (iv)'s closing sentence without running it, which is exactly the pairing that makes an incomplete list read as a claim.

## Nice to Have

- `task_9_plan.md` remains at both ceilings (five `**Work:**` bullets, 20 points). Nothing breaches the budget. Should the Must Fix above add anything to it beyond the one provenance clause, the regeneration bullet is still the clean split point into its own `general`-layer task shipping immediately after it.
