# Task plan review — iteration 0

## Must Fix

1. **Scope register: derivation entry 1 misses README citations that aren't heading or bullet names** — story index (`feat_readme_summary_compact_llms_txt_story_plan.md`), `## Scope register`; also `task_5_plan.md`.
   Derivation entry 1 finds a citation only if it uses a README heading, an anchor or a bullet's lead words. A file that points at the root README some other way ("root README", "(`README.md`)", a bare `[README.md](README.md)` link) is never found. The story's own `Top risks:` paragraph says the register lists *every* citation that "still resolves but now lies". I ran a wider command:
   `git grep -n -i -F -e "root README" -e "root \`README.md\`" -e "(README.md)" -e "(\`README.md\`" -e "blob/main/README.md" -e "README.md's" -- . ":!harness-runs" ":!README.md" ":!examples/notes-app/sdlc-harness"`
   It finds these sites, none of which is a row in the register:
   - `plugin/docs/README.md` → "the repository's root README frames the flow documents that way, alongside the agents, slash commands, hooks, instructions, samples and helper scripts". **This one can go stale.** `task_5_plan.md` lets the implementer drop any sentence of the three paragraphs under the diagram whose fact is already at a linked file. The plugin paragraph's "the two flow documents that describe the loop they run" is such a sentence, because `plugin/README.md` has that inventory. If it is dropped, this sentence in `plugin/docs/README.md` becomes false, and no task re-points it.
   - `.claude/CLAUDE.md` → "ships an autonomous software-delivery harness as two halves** (`README.md`)". Probably `no-change`, since Task 5 keeps the two halves in the opening lines.
   - `.claude/context/conventions.md` → the `_Written by /harness-analyze …_` provenance footer. A conventions document, so `no-change` with no exception.
   - `ROADMAP.md` → "For what ships today, see [`README.md`](README.md)." A plain file link, so `no-change`.
   - `docs/development.md` §6 → the item 7 row, "the tree's own root README". This means the run-artifact tree's README, so `no-change`.
   - `plugin/samples/sample_code_review.md` → "Name the surface in the root `README.md` after the label…" and "The root `README.md` calls the surface \"Search history\"…", plus `plugin/samples/sample_code_review/finding_3.md` → the same heading. These describe the sample's imaginary project, so `no-change`.
   **Fix:** In the story index, replace derivation entry 1 with a command at least as wide as the one above: keep entry 1's current terms and add these. Add one row per site listed, with its disposition and reason. For the `plugin/docs/README.md` site, add an invariant to `task_5_plan.md` → **Invariants this task must keep**: the plugin paragraph must still say the plugin carries the flow documents with the other process assets. Also add the site to Task 7's list of citations that are deliberately left alone and checked against the finished README. Fixing it in `task_5_plan.md` avoids a `plugin`-layer task, which would have to ship before the `general` README rewrite it depends on.

## Should Fix

- **The story index says three `cli/` comments, Task 1 says four** — story index `## Context` says "the three `cli/` comments that send a reader to the README". `task_1_plan.md` → **Goal** and **Targets** name four: `init.ts`, `checks.ts`, and two in `doctor.test.mjs`. Make the index match.
- **Task 1 says its new pointers are already true, but part of one isn't until Task 2** — `task_1_plan.md` → *Where this task stops* says each new pointer names a section that already states the fact. The `doctor.test.mjs` fourth-case header, and the `JJ_DIR` comment in `checks.ts`, say this fact is "recorded" at the destination: `.jj/repo/store/git_target` holds `../../../.git`, and `.jj/repo/store/git` does not exist. That fact is in `docs/cli.md` only after Task 2 merges it into the `jj-repository` bullet. Say so in Task 1. Or word the new pointer so it cites only the 0.44.0 measurement that is already there.
- **Task 6 doesn't protect the parts of two bullets that `ARCHITECTURE.md` quotes** — `task_6_plan.md` → *Invariants* keeps the *Forge-agnostic* and *Design→code generation is out of scope* lead words, but not what `ARCHITECTURE.md` quotes from them. For `forge`, "states the same for an adopter in its *Forge-agnostic* bullet" means: the key is declared with no default, nothing reads it, and its reader is named as the forge coupling. For `design.source`, it quotes "nothing in this release turns a design file into code". Squeezing each bullet to two to four sentences could drop those facts. Task 7 would only catch that afterwards, as a blocker in a file it cannot edit. Add both facts to Task 6's invariants.
- **The record of moved paragraphs disappears after the run** — `task_5_plan.md` and `task_6_plan.md` → **Verification** put the moved-facts and moved-paragraph tables in "the task's final report". The prompt's §2 item 2 says "The plan lists each moved paragraph and where it ended up". A final report is not committed. The five caveat moves are recorded in `task_6_plan.md`'s table. Record the other expected homes (How it is measured ¶1–4, Two ledgers ¶1–4) in the plan too, as Task 5 already does for its region.
