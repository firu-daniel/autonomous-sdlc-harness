# Task plan review — iteration 0

## Must Fix

1. **Scope register: derivation entry C misses sites (disposition ii)** — story index (`feat_orchestrator_flow_graph_planning_pilot_story_plan.md`), `## Scope register`.
   Entry C's own site class is *"texts that enumerate the outer-loop script set"*. Its command, `git grep -ln "docs-retrieval server launcher" -- . ':!harness-runs' ':!examples'`, matches one phrase only, so it misses enumerations that list the set without that phrase. I verified these durable-corpus sites. Each goes stale once Task 5 adds three rows, one of them `agentInvocable: true`, and none of them is a row:
   - `docs/watcher.md` → `## 2. The scripts`: the `| File | What it does | Who runs it | Profile allow entry |` table lists every outer-loop file and would lack `flow-walker.sh`, `lib/flow-walker-gates.sh` and `flows/task_plan_writing.graph.json`. The paragraph under it, *"**Why only four carry a profile entry, and why that is not the reachability answer.**"*, becomes false, because five rows will carry one. `plugin/docs/AUTONOMOUS_FLOW.md` names this document as the watcher's format of record.
   - `plugin/docs/AUTONOMOUS_FLOW.md` → the `| Git wrappers — the outer-loop scripts an agent may invoke |` row, whose cell says the three git wrappers are *"the rows the shipped table marks agent-invocable"*. Task 11 adds a walker row to this same table and leaves this one contradicting it. Register row 6 anchors only the `Planning **core**` row, so this row is not covered.
   - `docs/outer-loop-verification.md` → *"**Which outer-loop scripts carry a generated permission entry, and which an agent can actually run.** **Four** rows have `agentInvocable: true`…"* and *"**Eleven of the twelve outer-loop files have their written mode and byte-identity checked only by hand…**"*. This is a dated verification record, so `no-change` with a stated reason may well be the right disposition, but it needs a row.
   - `docs/development.md` → roadmap item 6's row, which lists the outer-loop shell assets. `cli/scripts/README.md` → *"**Coverage is a row, not a mechanism.** An agent-invocable row…"*.

   A wider command that reaches all of these and still includes entry C's own pattern: `git grep -lE "docs-retrieval server launcher|agent-invocable|Profile allow entry" -- . ':!harness-runs' ':!examples' ':!evals'`. Besides the sites above, it also reaches `cli/src/commands/init.ts`, `cli/src/generators/permissionProfile.ts`, `cli/src/generators/scripts.ts`, `cli/templates/scripts/setup-worktree.sh`, `scripts/setup-worktree.sh` and `cli/test/profile.test.mjs`. Under the closure invariant, each of those needs a row too, with `no-change` where the text only names the flag.
   **Fix:** In the story index, replace entry C with the wider command above, or with an equivalent that is strictly wider. Add a row for every site it reaches. Assign a `change` disposition, and an owning task, to at least these: `docs/watcher.md` §2 (add the three rows and correct *"only four"*, in a `general` task such as Task 16) and the `AUTONOMOUS_FLOW.md` git-wrappers row (Task 11). Then extend `task_16_plan.md` and `task_11_plan.md`: add these sites to each file's `### Targets` and `**Work:**`, and stay within the 5-bullet and 20-point ceilings (split a task if either would be exceeded).

2. **Task 10 leaves two stale fork statements that Task 9's rewrite creates** — `task_10_plan.md`.
   - Task 9 appends `## Setup` step **7**. Both forks say the core's Setup has six steps, and Task 10's Work touches neither sentence: `plugin/instructions/task_plan_writing_instructions_autonomous.md` → `## What this file does NOT redefine` (*"`## Setup (once per session)` — its six steps and the `Artifact | Path` table…"*), and `plugin/instructions/task_plan_writing_instructions_semi_autonomous.md` → `## What this fork does NOT redefine` (the same sentence). After this branch both statements are false.
   - Task 10 says the semi-autonomous binding table is untouched because *"none of its values quotes a moved anchor"*. That is not true. Its `<escalate>` value reads *"(For an `iteration >= 5` stop — the common case — that is the latest findings path plus the one-paragraph non-convergence summary **the core's site words**.)"*. Task 9 deletes every *"increment `iteration`. If `>= 5`"* sentence, and those sentences hold the summaries. Task 1 moves the summaries into the graph's `onCap.summary`, which the walker prints as `summary:`. The core's site then no longer words the summary.
   **Fix:** In `task_10_plan.md`, add Work that changes *"its six steps"* to seven, or to a count-free wording, in both forks' NOT-redefine lists. Reword the semi-autonomous fork's `<escalate>` value so the summary is the one the walker's `summary:` line carries, not one *"the core's site words"*. Remove the claim that the semi-autonomous binding table is untouched, and correct the Verification to match. The `git diff --stat` line can still check for exactly two files.

3. **Collapsing `## Stop conditions` strands the *"entries 4–7"* citations** — `task_9_plan.md` (and `task_10_plan.md`).
   The core's `## Stop conditions` has seven entries today: STOP, the dispatch cap, `## Questions`, three cap entries (4, 5, 6) and `error:`/`blocker:` (7). Task 9 says to *"Collapse the three cap entries into *the walker printed `binding: <escalate>`*"*, which leaves five entries, with the `<escalate>` entries at 4–5. Two places cite the old numbers: the core's own `## Mode contract — bindings this file uses` → `<escalate>` Used-at cell (*"`## Stop conditions` entries 4–7"*), and `plugin/instructions/task_plan_writing_instructions_autonomous.md` → `## Mode contract — bindings` → the `<escalate>` row (*"and `## Stop conditions` entries 4–7"*). Task 9's Mode-contract bullet keeps only the node sections as sites. Task 10 edits the autonomous row only *"if Task 9 reworded the core's `Used at` cell in a way this row quotes"*. So neither task commits to fixing the entry range, and both cells will cite entries that no longer exist.
   **Fix:** In `task_9_plan.md`, state the new entry numbers of `## Stop conditions` after the collapse, and add Work that updates the core's `<escalate>` Used-at cell to match. Add a Verification line that checks the cell against the section. In `task_10_plan.md`, make the autonomous fork's `<escalate>` row edit unconditional for the entry range, and restate the new range in the `**Depends on:**` block. Alternatively, keep the entry count unchanged in Task 9 and say so in both files.

## Should Fix

1. **The flow-graph negatives are wired outside the chain the conventions document names** — `task_12_plan.md`. `.claude/context/conventions.md` → `## What accompanies a new unit of each kind` requires a negative schema fixture's `ajv test … --invalid` assertion to be in `package.json` → `validate:config:negative`. The plan wires the four new negatives into a new `validate:flow-graph:negative` instead (gate 3d, Task 14). That still honours the purpose of the rule, since every fixture is run. However, the conventions row becomes stale, and the writer may not edit a conventions document. Record the deviation in the task file, and raise a `stale-rule` entry in the writer's `## Corpus staleness` return.
2. **`fw_id_in_list` may miss ids that follow a space in the ledger's list** — `task_3_plan.md`. The ledger's `- skipped:` line is *"comma-separated"*, and Task 3's own verification uses `- skipped: parity, qa`. Nothing requires `fw_id_in_list` to trim whitespace around each id, and no case tests the second id: the `" qa"` in `parity, qa`. State the trimming rule, and add a REPRO or verification line showing that `fw_id_in_list qa "parity, qa"` exits 0.
3. **The fenced-command rule is not stated where it applies** — `task_16_plan.md`. Register row 26 says Task 16 honours the ledger rule *"Every command an adopter is meant to run sits in a fenced block"*. Task 16's own Work and Verification never say so, even though `docs/cli.md` is adopter-facing and `docs/flow-graph-walker.md` records commands (`git cat-file -s …`, `wc -c …`). State it in Task 16's file, as Tasks 8 and 14 do.

## Nice to Have

1. `task_15_plan.md`'s second Verification bullet runs `flow-walker.sh start` against this checkout, and that overwrites `harness-runs/.flow_walker_state`. It is harmless now, but the step could run in a scratch worktree, as Task 14 does for its merge-base comparison.
