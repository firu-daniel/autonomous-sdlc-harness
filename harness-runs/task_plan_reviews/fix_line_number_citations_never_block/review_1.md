# Task plan review — iteration 1

## Must Fix
1. **Task 6 puts C3 inside `task-plan-reviewer`'s Must Fix completeness group, where its pointer-resolution half reads as a new trigger that every plan trips** — `task_6_plan.md`, and the story index (`fix_line_number_citations_never_block_story_plan.md`) → `## Context` → `**How each grading reviewer's route closes.**`.
   Iteration 0 raised this defect for Task 4 (`review-plan-reviewer.md`), and Task 4 now carries a route analysis for it. Task 6 has the same defect and got no such fix. Here it costs more.
   - `plugin/agents/task-plan-reviewer.md` has no `**Your own checklist is not downgradable.**` guard. So *"Pointer resolution binds …"* has no guard for "binds" to modify. Task 6's Work bullet adds C3 as a paragraph at the end of `### Completeness (Must Fix)`. There, the natural reading of *"Pointer resolution binds a cited path, symbol, heading or quoted substring that does not resolve"* is that such a citation is a Must Fix.
   - A task plan cites things that do not resolve yet, by design. A `### Targets` entry names a file marked `(new)` (`plugin/samples/sample_story/task_1_plan.md` → `recentSearchRecord.ts` (new)). A per-task file quotes text the task is about to write (this plan's own C1–C5 fences). A `## Scope register` row anchors on a substring a `change` row will replace.
   - So after the next run loads the edited reviewer, it can fail every plan that creates a file or quotes new text. That is a new Must Fix route, not a closed coordinate route. It breaks the prompt's `## Out of scope` (*"Close the coordinate routes and nothing else"*) and §2 (*"Do not add a clause it does not need"*).
   - Task 6's `**The route analysis this task acts on.**` paragraph says only that the closing clauses are the new step 3 and C3. It says nothing about C3's first half. The index's route paragraph says *"plus the carve-out (Task 6)"* with no caveat either. An implementer has nothing telling them not to read it as a trigger.
   **Fix:** In `task_6_plan.md`, do one of these:
   - (a) Recommended. Close `task-plan-reviewer`'s `lessons.md` route with a clause that has no pointer-resolution half, carried only by this file. Then update the C3 carrier set everywhere it is stated: the index `**Top risks:**`, Task 2's *"C3 by Tasks 4 and 6"* note, Task 6's `**Depends on:**` and C3 fence, and Task 10's `**Byte-identity.**` bullet (C3 → four files).
   - (b) Keep C3 byte-identical, but move it out of the `(Must Fix)` group. For example, put it directly after the `## Read first` ledger bullet. Then add a route-analysis sentence matching Task 4's: C3's first half adds no trigger in this file, and a plan target or quoted text that does not exist yet is never a finding under it. Add a Verification bullet that grades a plan with a `(new)` target against the edited file and confirms no Must Fix. Make the matching edit to the index's route paragraph.

## Should Fix
1. **Iteration 0's Should Fix on the dispatch bullet is still open** — `task_2_plan.md`. `plugin/instructions/plan_orchestration_instructions.md` → the `**Single-layer:**` bullet still carries *"The reference-implementation line(s) apply only when `phases.parity` is `true`"*. Task 2 rewrites that bullet's "source line(s)" to "source anchors" and leaves this sentence still talking about lines. Neither Task 2's verification grep nor Task 10's `source line` grep catches it. **Fix:** add *"The reference-implementation anchors apply only when …"* to Task 2's `plan_orchestration_instructions.md` Work bullet, and add `line(s)` to its verification grep.
2. **`<quoted substring>` still breaks the placeholder spelling rule** — `task_2_plan.md` (the `## Resolved values` intro edit and the template line), plus the written form repeated in the story index decision (2), `task_8_plan.md` and `task_9_plan.md`. `.claude/context/plugin.md` → `## The placeholder vocabulary`: *"A placeholder is `<snake_case>` in angle brackets"*. **Fix:** spell it `<quoted_substring>` everywhere. The C5 fence has no placeholder, so byte-identity is unaffected.
3. **P1's traversal is still a closed list** — story index → `## Scope register` → `Derivation entry P1`. Re-walking it, then probing wider with `grep -rl 'verdict: FAIL\|Must Fix' plugin/agents plugin/commands`, finds no missing site. `qa-tester.md` fails on test outcomes, and its fix target needs no line. But the entry cannot be re-run to show that. **Fix:** start P1 with that runnable grep as its first step, and record `qa-tester.md` as a `no-change` row with that reason.

## Nice to Have
1. **Task 6's ledger rewording is broader than a coordinate route needs** — `task_6_plan.md`. *"cite the ledger line"* means the one-line ledger entry, not a coordinate. Replacing it with *"quote the ledger entry"* is harmless, but one clause in the route analysis saying it is a clarity edit and not a route closure would keep the §2 accounting exact.
