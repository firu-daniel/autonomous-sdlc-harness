# Task plan review — iteration 0

## Must Fix
1. **Task 4 turns C3 into a new Must Fix trigger in `review-plan-reviewer`, and its verification asserts grades the edited text does not produce** — `task_4_plan.md`.
   In `branch-reviewer`, `skeptic-reviewer` and `layer-reviewer`, C3's first half (*"Pointer resolution binds a cited path, symbol, heading or quoted substring that does not resolve"*) restates a trigger each file already has: the Must Fix bullet *"a pointer's target does not exist"*. `plugin/agents/review-plan-reviewer.md` has no such trigger. It has no pointer-resolution rule for finding anchors and no not-downgradable guard, and its `### False positives (Must Fix)` bullet reserves Must Fix for business-logic divergence, broken architecture and hard convention violations. Task 4 puts C3 under `### Missed checks`, where its first half has nothing to modify. Its `**Verification:**` then treats that half as binding. It says a finding anchored on the absent symbol `fetchRecentSearchs` *"is bound by C3"*, and that a finding anchored on the absent path `searchServce.ts` *"carries no resolvable site anchor, so it fails the per-finding check"*. The rewritten per-finding check fires only when a file *"omits the site anchor"*, and an anchor that is present but wrong has not been omitted.
   So Task 4 has two possible outcomes, and both are wrong:
   - The implementer cannot honestly pass the bullet.
   - Or the implementer reads C3 as creating a new anchor-resolution Must Fix in the meta-reviewer. That breaks the prompt's `## Out of scope` (*"Close the coordinate routes and nothing else"*) and §2's *"Do not add a clause it does not need"*.

   The binding constraint only needs the dead-pointer examples to *stay* Must Fix where they already were. The story index's `## Context` and Task 10 show that in `branch-reviewer`, `skeptic-reviewer` and `layer-reviewer`, with `review-plan-reviewer` covering only the missing-`finding_<K>.md` case.
   **Fix:** In `task_4_plan.md`, remove the two verification claims that a finding with an absent path or an absent symbol grades Must Fix in `review-plan-reviewer.md`. Keep the missing-`finding_4.md` example and the stale-hint example. Then settle what C3 does in this file, in one of two ways:
   - Add a sentence to Task 4's route analysis (and to the index's `**How each grading reviewer's route closes.**` paragraph) saying that C3's pointer-resolution half adds no trigger here, and that only its *"a line coordinate — stale, missing or present — never binds it … `lessons.md` entry"* half closes the missed-check route.
   - Or close the `lessons.md` route with a clause that has no pointer-resolution half, and update the carrier set in the index Top risks, Task 2's C3 carrier note, Task 6's note and Task 10's byte-identity bullet to match.

## Should Fix
1. **Task 2 leaves a coordinate-shaped phrase in the dispatch bullet it rewrites** — `task_2_plan.md`. `plugin/instructions/plan_orchestration_instructions.md` → the `**Single-layer:**` bullet also carries *"*The reference-implementation line(s) apply only when `phases.parity` is `true`…*"*. Task 2 replaces "relevant source line(s)" in that same bullet with "source anchors" but leaves this sentence, which will still talk about "line(s)". Task 2's verification grep (`source line`) does not catch it. **Fix:** add a replacement to the Work bullet, e.g. *"The reference-implementation anchors apply only when …"*.
2. **New placeholder spelling breaks the placeholder rule** — `task_2_plan.md` (and the written form restated in the story index, `task_8_plan.md` and `task_9_plan.md`). `<quoted substring>` goes into `layer-reviewer.md`'s `## Resolved values` placeholder list, but `.claude/context/plugin.md` → `## The placeholder vocabulary` says *"A placeholder is `<snake_case>` in angle brackets"*. The corpus already has some spaced template placeholders, but this is a new one. **Fix:** spell it `<quoted_substring>` everywhere the written form appears.
3. **P1's traversal is a closed list, not a derivation** — story index (`fix_line_number_citations_never_block_story_plan.md`) → `## Scope register` → `Derivation entry P1`. The prompt's §2 says to check *"every agent that grades or returns `verdict: FAIL`"*. `plugin/agents/qa-tester.md` returns `verdict: FAIL` and writes self-contained per-finding files with a **fix target**, but it is neither traversed nor a row. It has no coordinate route (it fails on test outcomes, and its fix target requires no line), so the answer is likely `no-change`. **Fix:** start P1 with a runnable step such as `grep -rl 'verdict: FAIL' plugin/agents`, and add a `no-change` row for `qa-tester.md` with that reason.

## Nice to Have
1. **Remaining "line" wording in parity prose** — `task_6_plan.md`. `task-plan-writer.md` step 4's list keeps *"no single reference line names them"*, and prompt-exclusion wording (*"citing the exact prompt line"*, *"a cited task-prompt line"*, and the parity reviewer's *"cited prompt line"*) stays. Each reads as a quote rather than a coordinate. A one-line note in the register recording that call would close the question.
