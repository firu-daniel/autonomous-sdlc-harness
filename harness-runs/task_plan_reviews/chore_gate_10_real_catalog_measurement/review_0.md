# Task plan review — iteration 0

The structural checks pass: the index is thin (Context + `Top risks:` + `## Phase 2 Readiness — Ordered Fix List` immediately after Context + `## Scope register`, no `## Tasks`, no index-level `### Sources of truth` / `### Targets`), all four readiness entries are shaped with single-layer `_(layer: general)_` and `_(points: …)_` tags within the ≤20 ceiling, index↔file correspondence is 1:1, every per-task file is self-contained with Goal / Targets / Source / Work / Verification, no task targets a conventions document, and no task exceeds five `**Work:**` bullets. The parity check group is skipped: `phases.parity` is `false`.

I re-ran derivation entries A–D verbatim and re-walked entry E. Every path A–D return is a register row, and the rows' section-level anchors hold when checked line by line against the files (`docs/retrieval-eval-results.md`'s hits split exactly between `## Cold build and index size` and `## The shipped default against fusion alone`; `docs/retrieval.md`'s between the **Why `setup-worktree.sh` does not warm the index.** subsection, item (d), `## Still open` and `## What it costs`). A wider probe (`60-second`, `sixty second`, `43.2 MB`, `12.2 s`, `1,500 chunk`, `model load per worktree`, `crossover`, `MCP_TOOL_TIMEOUT`, `cold build`) turned up no site the register misses: the `crossover` hits in `cli/test/docs-retrieval-store.test.mjs`, `docs/guard-verification.md` and `plugin/hooks/lib/harness-config-lib.sh` are index-scan and parser crossovers, the `SIXTY SECONDS` hits in both copies of `cleanup-merged-worktrees.sh` are a fetch timeout, and `README.md` / `llms.txt` / `evals/docs-retrieval/README.md` only link the results file without stating a figure or a rule. So there is **no under-inclusive-derivation finding** this round.

## Must Fix

1. **Cited heading does not resolve — `## Metrics` in `docs/retrieval-eval.md`** (`task_4_plan.md`, `**Work:**` bullet 1)
   The bullet says *"Check the sibling reference at `## Metrics` (the **cost** bullet…) and the **latency** bullet's host-stamp sentence"*. `docs/retrieval-eval.md` has no `## Metrics` heading; its headings are `## What the runner owns, and what it does not`, `## Preconditions`, `## How to run it`, `## The query-set format`, `## What each metric means`, `## The decision rule`, `## The regression floor`, `## Running arm A by hand`. The quoted cost-bullet text (*"The one-time index build those local arms depend on is charged where…"*) does resolve — it sits under `## What each metric means` — so only the heading is wrong, but an implementer instructed to confirm a section that does not exist has no way to close the bullet.
   **Fix:** in `task_4_plan.md`, replace `## Metrics` with `## What each metric means` in that bullet.

2. **Mis-located anchor — the *"This step does not close the roadmap row"* paragraph is not in `## The regression floor`** (`task_4_plan.md` `### Targets` and `**Work:**` bullet 2, and the story index's `## Scope register` row 8)
   Both the task file and register row 8 place that closing paragraph, and the sentence *"This hand run plus the private-catalog numbers from `docs/development.md` §5 → gate 10 are together what closes it"*, in `docs/retrieval-eval.md` → `## The regression floor`. It is actually the closing paragraph of `## Running arm A by hand` — `## The regression floor` ends before it, and its own closing text is the `BLOCKED` / shortfall paragraph about the empty model cache, which this branch must **not** touch. As written the plan points an editing task at the wrong section, and the wrong section is the floor-policy record the branch is forbidden to move.
   **Fix:** in `task_4_plan.md` (both the `### Targets` entry and the `**Work:**` bullet) and in the story index's `## Scope register` row 8, cite `docs/retrieval-eval.md` → `## Running arm A by hand`, closing paragraph *"This step does not close the roadmap row, and neither did this branch."* Keep `## The decision rule`'s *"The decision is taken on a real catalog…"* paragraph as the separate, correctly located site it already is.

3. **Unmapped prompt requirement — mid-leg recoverability of the shared model cache** (no per-task file; assign it to `task_3_plan.md`)
   The prompt's `## Establish, do not assume` → *"**What the run does to the shared model cache — STILL THE BRANCH'S TO CHECK.** … Establish that nothing in **this branch's** work leaves it moved, truncated or pointed somewhere else, **and that a failure mid-leg is recoverable.**"* The first half is covered (`task_4_plan.md`'s last verification bullet, and `task_2_plan.md`'s item (d) sentence recording the cache restored and verified at 57M). The second half — that a failure part-way through leg (ii), which moves the machine-wide cache aside, is recoverable — maps to no task in the set.
   **Fix:** add a `**Work:**` bullet to `task_3_plan.md` (gate 10's own text is where the recovery instruction belongs, beside leg (ii)'s move-aside command) requiring the gate to state how the operator restores the machine-wide cache if a leg fails between the move and the restore, and add a matching `**Verification:**` bullet that the restore command is in a fenced block, one command per line. If the writer prefers `task_2_plan.md` (item (d)'s cache sentence) as the home, state it there instead — but one file must own it. Either way the bullet count stays within the five-bullet ceiling (task 3 currently has five, so fold it into the existing leg-(ii)/defect bullet rather than adding a sixth, or split the task).

## Should Fix

- **Baked-in counts in verification commands** (`task_4_plan.md`). *"the six `plugin/instructions/*.md` hits are the API-overload retry backoff"* and *"Re-run the four derivation commands"* both state a cardinal. The register's own closure invariant is correctly stated as a property; the verification bullets should be too — *"every `plugin/instructions/*.md` hit is the API-overload retry backoff"*, *"re-run every derivation command in the register"*.
- **The sweep omits derivation entry E** (`task_4_plan.md`, `**Work:**` bullet 3 and the verification list). The register carries five entries, A–D (commands) and E (the standing-artifact procedure over `harness-runs/lessons.md`); the closure task re-runs only A–D. Add the re-walk of entry E — first step `grep -nE '^## |^- \*\*' harness-runs/lessons.md`, then its topic headings in file order and the per-rule decision rule — so the row class no path-emitting command reaches is closed too.
- **Derivation entry C's pathspec excludes `*.sh`** (story index, `## Scope register`). `scripts/run-gates.sh:124` mentions gate 10 (*"which is why §5 sorts real-model retrieval into gate 10"*) and has no row. I read it: it states where the gate sorts real-model retrieval and is unaffected by the run, so this is not a verified missing site and not a Must Fix — but widening entry C to `'*.sh'` and carrying that hit as a `no-change` row would make the sweep's closure visible rather than dependent on the pathspec happening to exclude it.

## Nice to Have

- `task_1_plan.md`'s five `**Work:**` bullets are each a dense paragraph carrying the whole of the section's replacement text. They are within the five-bullet ceiling and the 20-point ceiling, but a reviewer of the resulting commit will have to read each bullet as a spec rather than a step; consider sub-bullets inside them (permitted, informational only) so the per-bullet obligations can be ticked individually.
