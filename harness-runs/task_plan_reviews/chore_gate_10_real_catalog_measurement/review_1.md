# Task plan review — iteration 1

Structure passes again: the index is thin (`# Story:` → `## Context` with `Top risks:` → `## Phase 2 Readiness — Ordered Fix List` → `## Scope register`, no `## Tasks`, no index-level `### Sources of truth` / `### Targets`), all four readiness entries carry a single `_(layer: general)_` tag and a `_(points: …)_` value inside the ≤20 ceiling (20/20/20/10), index↔file correspondence is 1:1, each per-task file is self-contained with Goal / Targets / Source / Work / Verification, no task's `### Targets` names a conventions document, and no task exceeds five `**Work:**` bullets (5/5/5/3). The parity group is skipped: `phases.parity` is `false`.

The three iteration-0 Must Fix items are all applied: `task_4_plan.md` now cites `## What each metric means` (which resolves — the quoted cost bullet is at `docs/retrieval-eval.md:279`); the *"This step does not close the roadmap row"* paragraph is now correctly placed in `## Running arm A by hand` in both `task_4_plan.md` and register row 8 (verified: `## The regression floor` spans 341–390 and closes on the `BLOCKED` paragraph at 385, the cited paragraph is at 522–524); and mid-leg recoverability of the machine-wide model cache is now owned by `task_3_plan.md`'s leg (ii) work bullet with a matching verification bullet, without adding a sixth bullet. Two of the three Should Fix items are applied (the baked cardinals are now properties; derivation entry E is re-walked in `task_4_plan.md`). Every cited path, heading and quoted substring in the four task files resolves — I checked each against the target files, including the hard-wrapped quotes in `docs/retrieval-eval.md` and the row-17 / leg-(iii) quotes in `docs/development.md`, and confirmed `git grep -cE '/(Users|home)/[a-z]' d31e6c8 -- docs/development.md` really reports `1` and nothing for the other three files.

I re-ran derivations A–D verbatim and re-walked E. Every path A–D return is a register row. One Must Fix follows: the derivations are under-inclusive at the *section* level inside `docs/retrieval-eval-results.md`, and the site they miss is one this branch must change.

## Must Fix

1. **Under-inclusive derivation — a real-model leg is still recorded as unperformed in `docs/retrieval-eval-results.md` → `### The limit on this calibration`, and no row or task owns it** (story index `## Scope register`, derivation entry C and rows 1/3; and `task_1_plan.md` `### Targets`)
   Derivation C (`git grep -lE 'ate 10' …`) returns `docs/retrieval-eval-results.md`, and the register accounts for that file with rows 1 (`## Cold build and index size`) and 3 (`## The shipped default against fusion alone`, its *"which this branch does not perform"* sentence). But the same file carries a second, near-identical false-after-the-run sentence 60 lines earlier, outside both rows' anchors and outside `task_1_plan.md`'s two `### Targets`:
   - **`docs/retrieval-eval-results.md` → `## Threshold calibration` → `### The limit on this calibration`** — *"its standing reproduction is gate 11 of `scripts/run-gates.sh` … rather than a remembered run; its confirmation is a hand run against a private real catalog, **which this branch does not perform**."* That hand run has now been performed: leg (iv) ran the default `fused-rerank` mode over the 1,960-chunk real catalog and the calibration held at both ends (`#serverproxy` at 1.000 on the positive, `no confident match` on the negative), which the task prompt states as *"The calibration in `docs/retrieval-eval-results.md` → `## Threshold calibration` holds on a real catalog at both ends."* The section sits **below** the `<!-- eval:generated:end -->` marker (line 4466), so it is hand-written territory and editable without touching the generated region. Correcting it is not re-calibration and not a re-run, so `## Out of scope` does not bar it. Leaving it is a direct contradiction with the branch's own work: `task_2_plan.md` retires `docs/retrieval.md` → `## Still open`'s *"The abstain threshold on a real catalog"* entry on the strength of leg (iv) while pointing the reader at this very section as the record — and that record would still say the confirming run was never made.
   - The dependent cross-reference in the same file, **`## The query-log pass`**'s closing *"Repeatability, and why the gap above is a ceiling…"* paragraph — *"that needs repetitions on an idle host, which is the same real-catalog hand run `## The limit on this calibration` above already names as out of scope here"* — cites the sentence above and has to stay true after it is rewritten. Check it and, if it no longer reads correctly, correct it in the same task.
   **Fix:** (a) add a `### Targets` entry and a `**Work:**` obligation to `task_1_plan.md` (it owns `docs/retrieval-eval-results.md`; fold the obligation into an existing bullet rather than adding a sixth) requiring `### The limit on this calibration` to record that the real-catalog confirmation has now been taken — dated, with the corpus stamp by commit / size / character and never by path, citing leg (iv)'s two queries and their outcomes — **without** re-calibrating, moving `ABSTAIN_SCORE_THRESHOLD` or touching `floor.json`, and requiring the `## The query-log pass` cross-reference above to be read and kept true. (b) Add both sites as `change` rows to the story index's `## Scope register`, owned by Task 1. (c) State the corrected, strictly wider derivation entry C in the register — the site is reached at file level but its own prose is not, so widen it to reach the *sentence class* and to close the `*.sh` gap iteration 0 raised as a Should Fix:
   ```
   git grep -nE 'ate 10|private real catalog|real-catalog hand run|this branch does not perform|hand run .*(not|never) (made|performed|run)' -- '*.md' '*.mjs' '*.ts' '*.sh' ':!harness-runs' ':!examples'
   ```
   (`-n` rather than `-l`, because this class is only decidable by reading the matched line). Carry every hit it adds as a row: `scripts/run-gates.sh` (*"which is why §5 sorts real-model retrieval into gate 10"*) as `no-change` with its reason, `docs/retrieval-eval.md:397`'s citation of `## Arm A — awaiting a hand run` as `no-change` (that heading stays accurate), and the two sites above as `change` under Task 1. (d) Add a matching `**Verification:**` bullet to `task_4_plan.md`'s sweep so the widened entry C is one of the commands it re-runs.

## Should Fix

- **Derivation entry C still excludes `*.sh`** (story index, `## Scope register`). Carried over from iteration 0 and neither applied nor recorded in a `## Rejected findings` section, so I am re-raising it once. `scripts/run-gates.sh:124` mentions gate 10 and has no row; I read it and it is unaffected by the run, so this is not a missing site in its own right — but the pathspec in the Must Fix above already fixes it, and carrying that hit as a `no-change` row makes the sweep's closure visible rather than dependent on the pathspec happening to exclude it.

## Nice to Have

- `task_3_plan.md`'s machine-path verification bullet states the expected branch-point count (`reports 1`) as a cardinal. It is true — I confirmed `git grep -cE '/(Users|home)/[a-z]' d31e6c8 -- docs/development.md` reports `docs/development.md:1` — and it is an unchanged-count invariant rather than a derivation's expected yield, so it is not the baked-count defect; but phrasing it as *"the count is unchanged from branch point"* with the command beside it would keep it a property.
- `task_1_plan.md`'s five `**Work:**` bullets remain dense paragraph-specs (carried over from iteration 0). Sub-bullets inside them are permitted and informational; they would let the per-bullet obligations be ticked individually.
