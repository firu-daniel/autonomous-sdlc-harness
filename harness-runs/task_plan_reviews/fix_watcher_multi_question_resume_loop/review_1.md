# Task plan review — iteration 1

Both Must Fix findings from round 0 are resolved in the artifact. `task_5_plan.md`'s legacy case now runs under the shipped defaults and asserts `park_loop_cycles` `"1"`, with the reason stated. `task_2_plan.md` now carries the `process_inbox_file` guard and the REPRO `a guard` extension. I re-ran derivation entries C1–C4 verbatim, and every file they reach appears as a register row.

## Must Fix

1. **The `doctor --help` text keeps saying the watcher has "six" lifecycle words, and Task 3 forbids the edit that would fix it** — `task_3_plan.md` (and row 21 of the story index's `## Scope register`, `fix_watcher_multi_question_resume_loop_story_plan.md`)
   Row 21 names only the `TEST_NOTIFICATION_EVENT` doc comment in `cli/src/commands/doctor.ts`. Task 3's fourth Work bullet says to edit that comment and adds "No code changes — `doctor-test` is still none of them". Its Verification also says "the `doctor.ts` edit is a comment". But the same file has a second site that states the count, and it is shipped runtime output rather than a comment. `DOCTOR_USAGE` is the text `doctor --help` prints, and it contains `` `test send carries the event word ${TEST_NOTIFICATION_EVENT}, which is none of the watcher's six` `` followed by the line `"lifecycle words, so it cannot be read as a real run's outcome. …"`.
   Task 3 raises the notifier's vocabulary to seven words, and Task 12 changes `docs/cli.md`'s matching sentence to "seven". If Task 3 is implemented as written, `doctor --help` will be the one place that still says six, which is false for the notifier this branch ships. C3 reached `doctor.ts` only through the doc comment: the usage string splits "six" and "lifecycle words" across two array elements, so the pattern `six lifecycle words` never matches it. This is disposition (iii): the file already has a row, but the row's site and the task's work leave a stale site in it. The same stale count sits in a comment in `cli/test/doctor.test.mjs` (`/** The event word the test send carries — none of the watcher's six, so it reads as no run's outcome. */`, above `TEST_EVENT`). No derivation entry reaches that file, because C3 does not search `cli/test`.
   **Fix:** In `task_3_plan.md`:
   - Widen the `cli/src/commands/doctor.ts` target to cover the `DOCTOR_USAGE` array too, and add `cli/test/doctor.test.mjs` (the `TEST_EVENT` doc comment) to `### Targets`.
   - Rewrite the fourth Work bullet so both the doc comment and the `DOCTOR_USAGE` line say "seven" (the comment also lists `park_loop`), and the `doctor.test.mjs` comment says "seven". Drop "No code changes". Changing the string literal changes no behaviour, and `doctor-test` is still none of the words.
   - Change the Verification bullet from "the `doctor.ts` edit is a comment" to a check that `node cli/dist/cli.js doctor --help` prints "seven" after the build, and that `grep -rn "watcher's six" cli/src cli/test docs` prints nothing.
   
   In the story index, extend row 21's Site cell to name `DOCTOR_USAGE` as well. Either widen C3 so it reaches a count split across lines, for example by adding `watcher's six` to its patterns and `cli/test` to its roots, or add a row for `cli/test/doctor.test.mjs` owned by Task 3.

## Should Fix

- `task_2_plan.md`: round 0's point about the stall-watchdog restart is still unaddressed. `check_stalled_runs` restarts a run through `spawn_engine "$b" "$worktree" "$log_path" "" 1`, which launches it with the pause-resume clause. It does not touch `resume_kind` or `resumed_at_epoch`. So if a park-resumed session is restarted by the watchdog and then parks, `classify_run_exit` will count it as an `answer` cycle measured from the original resume time. That contradicts the plan's own rule that a pause-style relaunch is never counted. Say which behaviour is intended, and set or clear `resume_kind` in `check_stalled_runs` to match (for example, set it to `pause` beside that function's `registry_set "$b" status running`). This adds `check_stalled_runs` to Targets.
- `task_11_plan.md`: `plugin/docs/AUTONOMOUS_FLOW_WHITEBOARD.md` has a sentence after the stop table that reads "The protocol behind the first four is `…/autonomous_pause_and_ledger.md`, the two brakes are …". The task adds a **Park-loop guard** row but does not say where. Inserted among the first four rows, it would make that sentence claim the pause-and-ledger protocol is behind the guard. State the row's position, for example after the two brakes, or update that sentence.
- `task_9_plan.md`: the opening paragraph of `plugin/instructions/clarification_digest_instructions.md` still says "one committed block per clarification the branch parked on". The task's Verification forbids any change outside `## What is digested` and `## The entry format`, so this sentence cannot be aligned with "one block per park". Task 11 changes the same wording in `AUTONOMOUS_FLOW.md` ("block per clarification"). Either add the sentence to Targets or state why it stays true.
- `task_2_plan.md`, `task_5_plan.md`: both still point at `docs/watcher.md` §4 **A resume that makes no progress is bounded.**, a paragraph that only lands with Task 12. Both files now say so ("Task 12" / "written by Task 12"), which addresses round 0's point about the dangling pointer. Also cite §4 by its existing heading `## 4. Pausing, parking and the usage gate`, so the pointer still resolves if the paragraph's bold title changes in Task 12.

## Nice to Have

- `task_4_plan.md`: the old-layout observed-sequence case and the partly-archived case run under Task 2's default window. Each resume there re-parks instantly and counts one no-progress cycle. Neither case reaches the bound, so nothing fails, but a sentence in the suite header would stop a future reader from being surprised by `park_loop_cycles` being `"1"` in a record dump.
