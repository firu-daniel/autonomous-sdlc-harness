# Task plan review — iteration 2

The Must Fix from round 1 is resolved. `task_3_plan.md` now targets the `DOCTOR_USAGE` array and the `cli/test/doctor.test.mjs` comment, and checks `doctor --help` after the build. C3 now matches `watcher's six` and searches `cli/test`, and row 34 was added. I re-ran C1–C4 verbatim. Every file they print is a register row. I also ran Task 13's retired-form sweep on the current tree: every hit is in a file some task targets.

## Must Fix

1. **`docs/watcher.md` §1 step 3 will still list only `parked` and `paused` as the records that turn a drop away, but Task 2 adds `park_loop` to that guard** — `task_12_plan.md` (and row 16 of the story index's `## Scope register`, `fix_watcher_multi_question_resume_loop_story_plan.md`)
   Task 2's fourth Work bullet extends `process_inbox_file` so a drop for a branch whose record is `park_loop` is also archived as `rejected_<ts>_…`, and the record stays byte-identical. `docs/watcher.md` → `## 1. The loop in one page`, step 3 (**The guards, in one order.**), is the developer doc's ordered, closed list of the inbox guards. It names only "a `parked` or `paused` record that already owns this branch's working copy", and then says "the branch-owning record are **archived** instead … and the owning record is left byte-identical".
   Row 16 limits the site to "§1 status table and steps 7–8, §4". Task 12's Targets name §1 as "(steps 7 and 8, the status vocabulary sentence and table)", and none of its Work bullets touches step 3. If Task 12 is implemented as written, the doc's guard list will be wrong about a behaviour this branch adds: by that list, a drop for a `park_loop` branch would pass every guard and launch. This is disposition (iii): the file already has a row, but the row's site and the task's work leave a stale sentence in it. It is the same class of gap as round 1's `DOCTOR_USAGE` finding.
   **Fix:** In `task_12_plan.md`, add §1 step 3 to the `docs/watcher.md` target. Extend the first Work bullet so step 3's branch-owning guard reads "a `parked`, `paused` or `park_loop` record that already owns this branch's working copy". The rejection for `park_loop` names the `PARK_LOOP_CLEAR` action rather than an answer. Add `park loop` to that Verification grep, or add a check that step 3 names `park_loop`. In the story index, extend row 16's Site cell to "§1 steps 3, 7–8, the status table, §4".

## Should Fix

- `task_2_plan.md`: the `reject_preserving_status` doc comment in `cli/templates/scripts/autonomous-watcher.sh` still reads "Used when the record already there (`parked`, `paused`) has to survive untouched". After this task's inbox guard it also serves `park_loop`. Add that comment to Targets, in the same bullet as the `process_inbox_file` change.
- `task_2_plan.md`: this point was raised in rounds 0 and 1 and is still neither addressed nor recorded under `## Rejected findings`. `check_stalled_runs` restarts a run through `spawn_engine "$b" "$worktree" "$log_path" "" 1` and leaves `resume_kind` and `resumed_at_epoch` untouched. A watchdog-restarted park-resume that then parks is counted as an `answer` cycle, timed from the original resume. That may be the intended behaviour, since the restart continues the same park-resume. If so, say so in the task. If not, set `resume_kind` there. Either way, record the decision, or add the finding to `## Rejected findings` with a reason.
- `task_2_plan.md` / `task_7_plan.md` / `task_10_plan.md`: the window detector is OR'ed with the index detector. So a resumed session that raises a new, higher-indexed question in under `PARK_LOOP_WINDOW_SECS` still counts as no progress, and Task 5's window case asserts exactly that. Tasks 7 and 10 now document a partial answer: answer `Q2`, and the resumed run re-parks the unaddressed questions "in a **new** question file". That re-park can be quick. An operator who partly answers two parks in a row can therefore trip `park_loop` while the run is making real progress, which is the case prompt item 3 says must never be caught. Either argue in Task 2 (and later in Task 12's §4 paragraph) why a partial-answer re-park takes longer than the window, or narrow the window detector. For example, count a quick re-park only when no question file above `resume_max_question_index` appeared.
- `task_6_plan.md`: the Verification bullet "in words that do not contradict Task 7's section" cannot be checked when Task 6 lands, because Task 7 (plugin layer) ships later. The restated contract under `**Depends on:**` is the only thing Task 6 can be checked against. Say that, and leave the cross-check to Task 7's own verification.
- `task_9_plan.md`: round 1's point is still open. The opening paragraph of `plugin/instructions/clarification_digest_instructions.md` still says "one committed block per clarification the branch parked on". The task's Verification forbids any change outside the two named sections, so this sentence cannot be aligned with "one block per park". Add it to Targets or state why it stays true.
- `task_11_plan.md`: round 1's point is still open. The whiteboard sentence under the stop table says "The protocol behind the first four is `…/autonomous_pause_and_ledger.md`". State where the **Park-loop guard** row goes (for example, after **Stall watchdog**), so that sentence stays true.
- `task_2_plan.md`, `task_5_plan.md`: both still cite §4 by the bold paragraph title Task 12 has yet to write. Also cite the existing heading `## 4. Pausing, parking and the usage gate`.
