# Task plan review — iteration 0

## Must Fix

1. **`init`'s printed paste-line sentence and its doc comments still argue a bare-versus-prefixed contrast that this task removes** — `task_2_plan.md`.
   Task 2 makes step 2 of `init`'s closing report print `/autonomous-sdlc-harness:harness-analyze`, the same spelling as the paste line under it. It also says *"This task changes no output except the spelling"*. But `cli/src/commands/init.ts` prints a sentence that only makes sense while the two spellings differ. The `pasteReason` constant reads *" It is spelled with the plugin prefix because a first message is matched exactly, where a session's command picker matches the name above."* It is joined into `pasteIntro` on both marketplace arms. Once step 2 is qualified too, "the name above" is the same prefixed name, so the sentence gives a reason for a difference that no longer exists. Every accepted-arm run would print that false explanation to an adopter.
   The doc comment above the closing-pointer function makes the same argument: *"It names {@link ANALYZE_COMMAND_QUALIFIED} where step 2 above it names {@link ANALYZE_COMMAND}, and the difference is the two paths rather than an inconsistency: step 2 is a name to type into a session, whose picker resolves the bare form …"*. Task 2 only says to "repoint every `{@link ANALYZE_COMMAND_QUALIFIED}`, including the doc comments on the closing pointer". Repointing the link leaves that paragraph false. The `pasteUnconfirmed` comment (*"the measured route is pointed at ({@link ANALYZE_COMMAND_QUALIFIED}, …)"*) has the same problem.
   **Fix:** In `task_2_plan.md`'s `cli/src/commands/init.ts` Work bullet, add three steps:
   - Rewrite or remove `pasteReason` so the printed paste intro no longer contrasts the paste line with step 2's spelling. Keep `pasteUnconfirmed`'s measured/unmeasured statement, which is still true.
   - Rewrite the closing-pointer doc comment's two-spelling paragraph ("the difference is the two paths rather than an inconsistency …") so it says both lines now use one spelling.
   - Rewrite the inline comment beside `pasteUnconfirmed` in the same way.

   Replace "This task changes no output except the spelling" with a sentence that names this one intended wording change. Add a Verification bullet: `grep -n "matches the name above" cli/src/commands/init.ts` returns nothing. If any `init.test.mjs` case pins the paste intro, update it in this same task.

2. **The `docs/cli.md` sentence explaining the two spellings is not scheduled, and the anchor Task 10 cites for it does not resolve** — `task_10_plan.md`.
   Task 10's `docs/cli.md` bullet points at *"The paragraph that names the one pasteable line (`ANALYZE_INVOCATION`) as one of 'the named few' qualified sites"*. The phrase "named few" does not appear anywhere in `docs/cli.md`. `grep -rn 'named few'` finds it only in `docs/development.md` → `## 6. The roadmap this tree defers to`, which is Task 11's. The paragraph in `docs/cli.md` that actually describes the paste line says the opposite of the swept tree: *"It is spelled with the plugin prefix where the step above it is not, and the introducing sentence gives the reason: **the bare name is what a session's picker takes**, while a first message meets no picker and is matched exactly, so the bare form fails there with `Unknown command`."* A spelling-only sweep that qualifies `/harness-analyze` in that paragraph leaves it claiming the step above is bare, and quoting an introducing sentence that finding 1 removes from `init`.
   **Fix:** In `task_10_plan.md`, replace the "named few" pointer with the paragraph's real anchor: the paragraph opening *"The second step is the analyze command, in the vocabulary that command settles"*, at the sentence *"It is spelled with the plugin prefix where the step above it is not"*. Tell the implementer to rewrite that sentence and its "introducing sentence gives the reason" clause so they match the paste intro Task 2 leaves in `init.ts`. The new text should say both lines use the qualified spelling and cite `docs/development.md` → `## 5. Verifying a change` → gate 6 for the rule. Add a Verification grep, for example `grep -n 'where the step above it is not' docs/cli.md` returns nothing.

3. **The gate-6d exemption contract lets one gate-8 entry hide every bare `/harness-analyze` in `docs/development.md`** — `task_11_plan.md`.
   The contract Task 11 writes (item **(e)**) skips *"a line that contains an entry of its in-script exemption table of `<path>|<exact substring>` pairs"*. One line of the gate-8 fenced block consists of nothing but `/harness-analyze`, so the only substring that can exempt it is `/harness-analyze` itself. The entry `docs/development.md|/harness-analyze` then exempts **every** line of `docs/development.md` that contains a bare `/harness-analyze`. That includes any prose site added or reverted later, and it is the very file that states the rule. The `--dry-run` line has the same weakness: an entry such as `--dry-run` exempts any line holding that flag. The check then fails deliverable 5 (*"It fails on a bare `/branch-*` or `/harness-analyze` spelling at a human-facing site"*) in its own home file. The planted-failure proof in `docs/cli.md` cannot catch this, because it plants in a different file.
   **Fix:** In `task_11_plan.md`'s item **(e)**, change the exemption matching rule so an entry cannot reach beyond the lines it was written for. Either:
   - support a whole-line form, where an entry matches only a line whose entire content equals the given text, and require it for the gate-8 lines; or
   - require every entry to match exactly the number of lines it declares, reporting a mismatch as a finding, in the same way a stale entry is reported now.

   State the chosen rule in the contract paragraph.

4. **The gate-6d script implements the over-broad substring exemption, and its table-filling step picks entries that make it worse** — `task_13_plan.md`.
   Task 13 restates the same contract (*"A matching line is exempt when its path equals the entry's path and the line contains the entry's substring"*). Its table-filling bullet then says to use *"the shortest substring that is unique on that line"*. For class (a), that step produces `docs/development.md|/harness-analyze` and a short `--dry-run`-style entry, which is exactly the file-wide blind spot described in finding 3. None of the Verification bullets would detect it: the planted line goes into `docs/cli.md`, and the stale-exemption test checks only the opposite failure.
   **Fix:** In `task_13_plan.md`, restate whatever matching rule Task 11's revised contract adopts (whole-line entries, or entries that must match exactly their declared number of lines). Replace "the shortest substring that is unique on that line" with a rule that matches only the intended lines within the file. Add a Verification bullet: temporarily append a line holding `` Run `/harness-analyze` here. `` to `docs/development.md` **outside** the gate-8 block, confirm the script exits 1 with a `docs/development.md` finding, then revert. Record that run in the `REPRO` block next to the `docs/cli.md` plant.

## Should Fix

1. **Scope register row 89 says the lessons-ledger rule binds `README.md` only, and does not say why.** Story index (`chore_plugin_prefix_command_sweep_story_plan.md`), `## Scope register` row 89. The ledger rule is *"Every command an adopter is meant to run sits in a fenced block, one command per line; never inline it"*. The row restricts it to `README.md`'s `/harness-analyze <target>`. But Tasks 6, 9 and 10 rewrite other adopter-facing lines that keep a command inline: the `**Usage:**` lines in `plugin/commands/` (*"type `/autonomous-sdlc-harness:branch-prompt` and then keep typing …"*), `docs/cli.md`'s `/harness-analyze <target>`, and `examples/notes-app/README.md`. The reason cell should say why those sites describe syntax rather than tell a reader what to run, so they fall outside the rule. Otherwise the owning tasks should fence the lines the rule does cover.

## Nice to Have

- Task 13's `REPRO` block will record a `docs/cli.md:<line>` coordinate taken from the planted run. That is verbatim tool output, so it is acceptable. Still, a sentence noting that the line number belongs to that one run, not to the current file, would stop a later reader from treating it as an anchor.
