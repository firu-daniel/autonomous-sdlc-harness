# Task plan review — iteration 0

## Must Fix
1. **Existing-suite citations of `**Resume-from-ledger (planning).**` are left to break** — `task_3_plan.md` and `task_5_plan.md`.
   `cli/test/flow-walker-ui-and-reentry.test.mjs` quotes two substrings from the autonomous fork's `## Override 5 — pause/resume + flow-progress ledger (planning half)` → **Resume-from-ledger (planning).** paragraph:
   - case `(7) re-entry past a converged task-plan loop dispatches the UI-test writer first`, in its comment: *"Override 5, resume-from-ledger: "on re-entry, if `P1` is `[x]` skip the task-plan loop"."*
   - case `(8) re-entry with planning resolved prints the hand-off immediately`, in its comment: *"Override 5: "if all three `## Planning` entries are **resolved** … fall straight through to implementation (`<terminal_handoff>`)"."*

   Task 5 rewrites that paragraph. Its opening sentence goes from *"Override 2's resumability is unchanged and composes: on re-entry, if `P1` is `[x]` skip the task-plan loop; …"* to "Override 2 now reads the `## Planning` entries directly", and the convergence case is reworded as "With all three entries resolved, or with `P1`/`P2` resolved and `P3` `[ ]`, the result is `--entry convergence`". Task 5 keeps only the `[-]`-is-resolved sentences verbatim. It does not list either quoted phrase among the phrases it must keep, and its `**Verification:**` does not check them.

   Task 3 edits only this suite's header, case `(6)`, case `(9)` and the closing table. It leaves the case `(7)` and `(8)` comments alone. Its new header also says the suite's re-entry citations follow those two sections **"as rewritten on `fix_plan_loop_resume_from_walker_state`"**, so after Task 5 lands, two quoted substrings in the suite point at text that no longer exists.

   That is the same kind of cross-task drift the plan already guards against for Task 2's phrases (story index `Top risks:`, second risk), but here nothing guards it. A quoted substring is a wire (`.claude/context/plugin.md` → `## Citation`: "a cited heading is therefore a wire"; "A symbol anchor, a heading or a short quoted substring"). Task 3 ships before Task 5, so it cannot quote Task 5's new wording unless both files fix that wording.
   **Fix:** Pick one of these two and write it into both files:
   - (a) In `task_5_plan.md`, add both substrings to **Phrases this task must write verbatim** — "on re-entry, if `P1` is `[x]` skip the task-plan loop" and "if all three `## Planning` entries are **resolved**" — and add a `grep -F` for each backtick-free tail to its `**Verification:**`. In `task_3_plan.md`, add a `**Verification:**` bullet noting that the case `(7)` and `(8)` comments cite those two phrases, which Task 5 keeps.
   - (b) Fix new replacement phrases in both files. `task_3_plan.md` rewrites the case `(7)` and `(8)` comments to quote them, and adds case `(7)`/`(8)` comments to its Targets line and Work list. `task_5_plan.md` lists them as verbatim phrases, each with a `grep -F` check.

   Either way, add a `**Verification:**` step to `task_5_plan.md` that greps `cli/test/flow-walker-ui-and-reentry.test.mjs` for every `Override 5` / `Override 2` quotation and confirms each still resolves in the rewritten fork.

## Should Fix
1. **`task_4_plan.md` — the `## Setup` step 5 gate still reads "If a story index already exists".** Task 4 adds one sentence saying a fork may select **continue** whether or not a story index exists. It also widens the `<existing_artifact_decision>` row to "a story index or a saved walk". But step 5 still applies `<existing_artifact_decision>` only under its opening condition, *"**If a story index already exists** at …"*. Rewrite that opening condition, for example "If a story index or a saved walk of this flow already exists …". Otherwise the core does not say where the decision runs when a writer parked before writing the index, which is the exact case the added sentence is there for.
2. **`task_5_plan.md` — the fork's `## Mode contract — bindings` "Values only." paragraph** says that section's heading "and its `(a)` / `(b)` / `(c)` case labels are resolved **by name**". Task 5 adds case (d) but does not name this paragraph as a target, and Scope register row 2 does not list it either. Say whether (d) joins that list or is deliberately left out, because the watcher does not resolve (d) by name. Add the paragraph to row 2 if it changes.
3. **`task_7_plan.md` (or `task_4_plan.md`) — upgrade lag for adopters.** `docs/flow-graph-walker.md` → `### Item 6` → **Cost.** records that an adopter's walker and graph are `create-if-absent` and lag a plugin upgrade until `init` is re-run. An adopter on an upgraded plugin with an old graph gets exit 1 when the core's new **review** mapping issues `start --entry business_parity_review` (or `ui_review`): *"'…' is not an entry of flow 'task_plan_writing'"*. The core routes that to `<escalate>`. The failure is loud, not silent, but no task records it. Add it to Item 3a's rewritten **Cost.**, next to the existing Item 6 lag sentence.

## Nice to Have
1. **`task_7_plan.md` / story index** — the rewritten Cost records only one lost-flip cost: a lost `P2` flip costs one UI-review round. A lost `P1` flip costs more when the UI-test loop does not run (`phases.qa` false). The `ledger: P1` line then rides on a finished `<terminal_handoff>`, which is never acted on again, so case (d) re-runs a whole task-plan review round. Record that cost next to the `P2` one.
2. **`task_1_plan.md`** — "**Only `start`** overwrites the state" is loose: `next` also rewrites `.flow_walker_state` on every call. Consider "only `start` discards the saved walk and resets every counter to 0".
