# Task plan review — iteration 1

Both Must Fix items from iteration 0 are fixed. `task_9_plan.md` now targets `cli/test/init.test.mjs` → `OUTER_LOOP_SCRIPT_FILES`. The story index has derivation entry 6 and rows 41–56. I re-ran entries 2–6 exactly as written, and every site they reach has a row. Every index entry has a matching file (24 entries, 24 files, numbers match). Each task has exactly one layer tag. Every task is within 20 points and 5 Work bullets. The order is `cli`, then `plugin`, then `general`, and no `**Depends on:**` points forward. No task targets a conventions document.

## Must Fix

1. **Index structure and per-task files cite clarification answers at paths that do not exist** — the story index (`feat_docs_catalog_retrieval_story_plan.md`), `task_15_plan.md` and `task_16_plan.md`.
   The Context bullet *"The user's answers override the task prompt in three places"* cites `harness-runs/clarifications/feat_docs_catalog_retrieval/answer_1.md` to `answer_3.md`. `## Scope register` → **Derivation entry 1** names its **Artifact** as `harness-runs/clarifications/feat_docs_catalog_retrieval/answer_3.md`. `task_15_plan.md` (*"Settled by the user, so not reopened here"*) and `task_16_plan.md` (*"Settled by the user"*) cite `answer_2.md` and `answer_3.md` at the same top-level path. None of these files exists there. The directory's top level now holds only `answer_4.md` and `question_4.md`. The first three answers were archived to `harness-runs/clarifications/feat_docs_catalog_retrieval/answered/` (`answer_1.md`, `answer_2.md`, `answer_3.md`), as `plugin/instructions/task_plan_writing_instructions_autonomous.md` → the consume-then-archive contract requires. So derivation entry 1 cannot be re-walked as written. Its artifact does not resolve, and I had to find the archive myself. The implementer of Tasks 15 and 16 would hit the same dead pointer.
   **Fix:** In the story index, change the Context citation and derivation entry 1's **Artifact** to `harness-runs/clarifications/feat_docs_catalog_retrieval/answered/answer_1.md` … `answered/answer_3.md`. In `task_15_plan.md` and `task_16_plan.md`, point the `answer_2.md` / `answer_3.md` citations at the same `answered/` paths. Plan text only: the no-provenance rule in Tasks 16 and 17 for agent files stays as it is.

2. **The Scope register's derivation misses durable sites this branch makes false** — the story index (`feat_docs_catalog_retrieval_story_plan.md`, `## Scope register`), with the owning task to be assigned (`task_23_plan.md`, or a new `general` task if Task 23's 5-bullet ceiling binds).
   I re-ran every derivation entry. Closure holds for the entries as written, but a wider probe reaches candidate sites that no entry reaches and no row lists (disposition (ii)):
   - `ARCHITECTURE.md` → `## 7. The seam: what an adapter would have to carry`, the bullet *"**No MCP server.** **[shipped]** This tree consumes servers an adopter enables and publishes none"*. Task 8 ships `docs serve`, an MCP server this tree authors and publishes, so this statement becomes false.
   - `ARCHITECTURE.md` → `## 10. MCP as the open-standard surface` → `### What MCP is here today`. These statements become false:
     - *"This repository **authors no MCP server**"*.
     - *"declared in one template — `cli/templates/repo/mcp.json`"*. Task 10 adds `mcp.retrieval.json`.
     - The three re-derive greps and the claim that *"The first emits the whole of what this tree declares and the second the whole of what it allows to be called"*. Task 10 adds `settings.autonomous.retrieval.json`.
     - *"**Reachable by one agent, and by nothing else in the tree.** `plugin/agents/qa-tester.md` is the only definition under `plugin/agents/` whose front matter carries an `mcp__` name at all"*. Tasks 15–16 add it to ten more.
     - The closing *"MCP is a consumption surface for one optional phase — not a capability this project publishes."*
     
     Every one of these is marked **[shipped]**. `task_23_plan.md` edits `ARCHITECTURE.md` with *"no other edit"*, so the branch would ship them false.
   - `docs/development.md` → `## 6`, the legend row whose text *"places MCP as a consumption surface for one optional phase rather than a capability this project publishes"* and *"no MCP server ships with the document"*. This is probably `no-change`, as a record of what that item shipped, like rows 20 and 44, but it needs a row.
   - `docs/cli.md` → `## 5. The wrapper-script contract`, the paragraph *"**Two families land in `scriptsDir`, and only one of them is generated.**"*. It lists the outer-loop family (*"the run watcher, the git wrappers, the worktree tooling, the scratch runner and the shared library they all source"*) and says the `agentInvocable` flag is *"false of everything the watcher process or a person starts"*. The launcher is `false` but is started by the agent runner. This is the same kind of enumeration as row 56, and entry 6 does not reach it.
   - `cli/templates/claude/settings.autonomous.json` → the `_README` entry *"THE OUTER-LOOP SCRIPTS AN AGENT RUNS ARE LISTED THE SAME THREE WAYS, AND THE REST GET NOTHING"*. It lists the no-entry scripts as *"The run watcher, the worktree tooling and the cleanup sweep are started by the watcher process or by you"*. The launcher gets no entry and is started by neither. This is an adopter-facing template, so it is corpus.
   
   **Fix:** In `## Scope register`, widen derivation entry 6 by adding the alternatives `the worktree tooling|watcher process or (by )?(you|a person)` to its regex, with its exclusions unchanged. Add a derivation entry 7 for sites that describe the tree's MCP surface (command): `git grep -n -i -E "no MCP server|authors? no MCP|publishes none|consumption surface|carries an .mcp__. name|declared in one template" -- . ":!harness-runs" ":!examples/notes-app" ":!cli/src" ":!cli/test"`. Add one row per site above, each with a disposition. The `ARCHITECTURE.md` §7 and §10 sites are `change` sites: give them to an owning `general` task that restates §10 on the evidence after this branch (this repository now authors one opt-in, local, read-only MCP server and grants its tool to ten agents; the browser servers stay the consumed surface; the greps name both templates) and adjusts the §7 bullet. Put `ARCHITECTURE.md` §7/§10 in that task's `### Targets` and Work bullets, and remove Task 23's *"with no other edit"* if Task 23 owns them. Give the `docs/cli.md` §5 and `settings.autonomous.json` sites a disposition and, if `change`, an owning task in the matching layer (`general` for `docs/cli.md`, `cli` for the template).

3. **Task 19 plans two adopter commands inline in a table cell, joined with prose, which repeats a lessons-ledger mistake** — `task_19_plan.md`.
   The `docs/config.md` §5 Work bullet has the `docs.retrieval` Meaning cell say *"that turning it on after `init` is `config set docs.retrieval true`, then `init --force`"*. That is two commands an adopter must run, written inline and joined with prose. The ledger entry (`harness-runs/lessons.md` → `## Adopter-facing documentation`): *"**Every command an adopter is meant to run sits in a fenced block, one command per line; never inline it, join two with prose, or cut its block when compacting a document.**"* Task 19's fenced-block instruction covers only the `docs/cli.md` §2 bullet, not this `docs/config.md` §5 bullet. A table cell cannot hold a fenced block.
   **Fix:** In `task_19_plan.md`, have the §5 cell state only the rule (turning retrieval on after adoption needs a config change and a forced re-run, because the profile is create-if-absent) and point to the fenced block that holds the two commands: either one in `docs/cli.md` §2 that this task writes, or one directly after the §5 table. Write that block as `npx autonomous-sdlc-harness config set docs.retrieval true` and `npx autonomous-sdlc-harness init --force`, one per line. Add a Verification line checking that no `config set` or `init --force` appears inline in the new `docs/config.md` text.

## Should Fix

- **Task 22 leaves two gate counts stale** — `task_22_plan.md`. Raised in iteration 0 and still open, with one more site. `docs/development.md` §5's *"Five of the nine run unattended"* paragraph also says the script *"prints the remaining four rather than passing over them"*. `scripts/run-gates.sh`'s header also says *"reading four gates' worth of silence as a pass"*. Both become five with Gate 10, and the Verification grep (`Nine gates\|nine gates\|of the nine`) catches neither. Add both to the Work bullets and to the grep.
- **Concurrent opens of the persisted PGlite directory are still unaddressed** — `task_5_plan.md`, `task_8_plan.md`, `task_21_plan.md`. Raised in iteration 0. `docs serve` keeps `<stateDir>/docs_index/` open for its whole life, and a second `docs serve` (a second session in the same checkout) or a `docs index` / `docs search` opens the same directory from another process. State the behaviour: a lock with a named refusal, or a documented limitation in `docs/retrieval.md` → `## Still open`.
- **Two enumerations the new units make stale** — `task_14_plan.md`, `task_9_plan.md`. Raised in iteration 0. `cli/README.md` → *"What the four subcommands do."* lists what `doctor` re-checks and does not gain the three retrieval checks. `cli/src/generators/outerLoopScripts.ts` → the `agentInvocable` doc comment lists which `false` rows are started by the watcher or a person, and the launcher is started by neither.
- **`## Corpus staleness` still omits rules this branch conflicts with** — the story index. These were raised in iteration 0 and by the architecture reviewer:
  - `.claude/context/cli.md` → `## How a module in this layer is written`: the enumerated machine-scoped state and its owners (the runtime owned by `setup.ts`, the model cache by `models.ts` → `fetchModels`; `task_12_plan.md`'s `writer.ts` clause names only `setup.ts`).
  - `.claude/context/conventions.md` → `## The testing bar`: the gate list lacks Gate 10.
  - `## Documents of record`: *"A deferral cites a numbered roadmap item"*, while this plan cites by title.
  - `### The order files are created…` item 1: the schema lands after its readers.
- **Verification commands with baked-in counts** — `task_15_plan.md` and `task_16_plan.md` (*"reports at least 3 per file"*), `task_17_plan.md` (*"`DENY_SCRIPT_BASENAMES` still holds exactly its four entries"*). Replace each count with a named-content check: name the three expected lines, and compare `DENY_SCRIPT_BASENAMES` against `git diff` showing no change to it.
- **`plugin/agents/README.txt`'s re-derive command** — `task_17_plan.md`. The file already sets each command on its own indented line (the `Sample fixture pointers` section's `grep -rno …` / `find …` lines). Have the new section put `grep -rln "mcp__harness-docs__search_docs" plugin/agents` on its own indented line in that style rather than inline.

## Nice to Have

- The `## Corpus staleness` section sits in the story index. The writer contract (`task-plan-writer.md` → `## Output contract`) routes it to the writer's return, which the caller writes to the improvement-observations intake. Leaving it out of the index would keep the index thin. This was raised in iteration 0.
