# Task plan review — iteration 0

## Must Fix

1. **Task 9 breaks the existing wrapper-family assertions in `cli/test/init.test.mjs`** — `task_9_plan.md`.
   Task 9 adds `docs-search-server.sh` to `OUTER_LOOP_SCRIPTS` as an always-written row ("The script is always written"). `cli/test/init.test.mjs` keeps its own spelled-out subtraction list, `const OUTER_LOOP_SCRIPT_FILES = [ 'commit-on-branch.sh', … 'restart-watcher.sh' ]`, and `writtenWrappers(dir)` subtracts only those names from every `.sh` it finds under `scriptsDir`. So once the new script is written, `assert.deepEqual(await writtenWrappers(dir), [])` and the wrapper-to-profile pairing in `assertWrapperPairing` would count `docs-search-server.sh` as a project-command wrapper, and those cases fail. The file's header says the list is "Spelled out rather than imported from the shipped table", so it will not update itself. `cli/test/init.test.mjs` is not in Task 9's `### Targets`, and neither the Work bullets nor the Verification bullets mention it. Task 10's Verification also says the existing `init.test.mjs` cases pass "without an edit to their expected values", which cannot hold unless Task 9 has already changed this list.
   **Fix:** In `task_9_plan.md`, add `cli/test/init.test.mjs` → `OUTER_LOOP_SCRIPT_FILES` to `### Targets`. Add a Work bullet that appends `'docs-search-server.sh'` to that list, and a Verification bullet saying the existing `writtenWrappers` cases stay green. Stay within the 5-bullet ceiling, for example by folding this into the `outer-loop-scripts.test.mjs` bullet.

2. **The Scope register has no derivation entry for sites that list the outer-loop scripts, so verified sites have no row** — `feat_docs_catalog_retrieval_story_plan.md` (`## Scope register`).
   Task 9 adds a new row to `OUTER_LOOP_SCRIPTS`. Durable corpus text outside this plan lists or counts that set. None of derivation entries 1–5 reaches it, and no row covers it (disposition (ii): the derivation misses sites). I found these sites by re-running a wider command:
   - `docs/watcher.md` → `## 2. The scripts`: the table introduced as *"Outer-loop scripts — this table"*, one row per shipped outer-loop file with a "Profile allow entry" column. It gets no `docs-search-server.sh` row.
   - `docs/outer-loop-verification.md` → *"the eleven outer-loop files as `init` writes them"* and *"Eleven of the twelve outer-loop files have their written mode and byte-identity checked only by hand"*. Both are counts of the set. They may deserve `no-change` because they record a measurement, but they need a row that says so.
   - `plugin/hooks/autonomous-script-allowlist-guard.sh` header → *"LEFT ALLOWED, DELIBERATELY: `create-worktree.sh`, `setup-worktree.sh`, `autonomous-notify.sh`, `autonomous-format-stream.sh` and `scratch-run.sh`."* Task 9 deliberately leaves the new script auto-allowed ("No `DENY_SCRIPT_BASENAMES` entry is added"), so this list of deliberately allowed scripts goes stale.
   - `plugin/docs/AUTONOMOUS_FLOW.md`: the table rows naming outer-loop scripts in `<scripts_dir>` (*"Working-copy lifecycle"*, *"Lifecycle notifier, and the stream formatter…"*).
   - `cli/templates/scripts/README.md` → *"The other family in this directory is not generated."*, which lists the family member by member. Row 40 names this file only as a target. It should name this paragraph as the site.
   **Fix:** In the story index's `## Scope register`, add a derivation entry that covers every place the outer-loop script set is listed or counted. For example (command): `git grep -n -E "setup-worktree\.sh|restart-watcher\.sh|autonomous-format-stream\.sh|(eleven|twelve) outer-loop" -- . ":!harness-runs" ":!examples/notes-app" ":!scripts" ":!cli/src" ":!cli/test" ":!cli/templates/scripts/*.sh"`. Add a row for each site above, each with a disposition. A `change` site needs an owning task: add `docs/watcher.md` to a `general` task, and give the guard header and `AUTONOMOUS_FLOW.md` to a `plugin` task, or mark them `no-change` with a stated reason. Put each new target in that task's `### Targets` list and Work bullets.

## Should Fix

- **Task 22 leaves one count in `docs/development.md` §5 stale** — `task_22_plan.md`. The same paragraph whose *"Five of the nine run unattended"* Task 22 edits also says the script *"prints the remaining four rather than passing over them"*. With Gate 10 that becomes five. The Verification grep (`Nine gates\|nine gates\|of the nine`) does not catch it. Add it to the Work bullet and to the grep.
- **`## Corpus staleness` misses four rules this branch falsifies or conflicts with** — `feat_docs_catalog_retrieval_story_plan.md`:
  - `.claude/context/conventions.md` → `## The testing bar` lists §5's gates one by one ("plugin and marketplace manifests …, and the example project"). Gate 10 (Task 22) is not in that list.
  - `.claude/context/cli.md` → `## How a module in this layer is written` says of machine-scoped state: "What this layer writes outside the repository … is enumerated and each piece has one owner: the registry …, the daemon unit …, the push settings". Task 12 adds the retrieval runtime and model cache, owned by `cli/src/retrieval/setup.ts`.
  - `.claude/context/conventions.md` → `## Documents of record`: "A deferral cites a numbered roadmap item, and the number owes a row". Following the task prompt, this plan cites the deferral by title only. Record that conflict rather than leaving it silent. Task 19's Verification also cites `docs/development.md` → `## 6` as if it supported title-only citations.
  - `.claude/context/conventions.md` → `### The order files are created…`, item 1, puts the schema first. The layer ordering here puts it last (Task 18). The Context explains why, but the conventions rule itself stays unamended.
- **Concurrent opens of the persisted PGlite data directory are not addressed** — `task_5_plan.md`, and `task_7_plan.md` / `task_8_plan.md`. `docs serve` holds `<stateDir>/docs_index/` open for its whole life (Task 8, "Open once"). `docs index` and `docs search` (Tasks 6 and 7) open the same directory from a second process in the same checkout. PGlite is a single-process embedded engine. State what happens when a second process opens a directory that is already open: a lock with a named refusal, or a documented limitation in `docs/retrieval.md`. Don't leave it implicit.
- **Two more enumerations the new units make stale** — `task_14_plan.md`, `task_9_plan.md`. `cli/README.md`'s *"What the four subcommands do."* paragraph lists what `doctor` re-checks ("… the plugin wiring and the permission profile"), and Task 14 does not add the three retrieval checks to it. `cli/src/generators/outerLoopScripts.ts` → the `agentInvocable` doc comment lists which `false` rows are auto-allowed by convention ("For the worktree scripts, the notifier and the stream formatter …"), and Task 9 does not add the launcher to that list.

## Nice to Have

- The `## Corpus staleness` section sits in the story index. The writer contract routes it to the writer's return. Keeping it out of the index would keep the index thin. The Context's long "Settled while planning" list could also be shortened, with the detail moving into the per-task files that already restate it.
