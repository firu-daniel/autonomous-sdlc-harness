# Clarification digest — feat_docs_retrieval_eval

## question_1 — how the plan loop proceeds from its first `iteration >= 5` cap, with the architecture gate still open
- **raised by:** the planning orchestrator, at the task-plan write loop's `iteration >= 5` cap. Gate open when the cap fired: the architecture gate (`architecture-reviewer`, plan-review mode). Latest findings: `harness-runs/architecture_reviews/feat_docs_retrieval_eval/review_4.md`. Round counts at the park: `architecture_reviews/` 5 rounds (`review_0.md` … `review_4.md`); `task_plan_reviews/` 0 rounds — the structural reviewer was never reached, because a revision re-runs the architecture gate first and it never passed; `business_parity_reviews/` not applicable, `phases.parity` is `false`. Must Fix counts across the five rounds: **2 → 4 → 2 → 2 → 1**, every round's Must Fixes applied and confirmed resolved by the next, so the FAILs were net-new each time rather than one unfixed item recurring. The recurring *shape*, in the reviewer's words across rounds, was single-source-of-truth placement — a fact (the `layers[]` list, the arm set, the retrieval env-var names, the floor margin, the query-set format, the calibrated threshold's figures, arm A's results row) written into more than one file, the fix each time being to name one owner and leave pointers elsewhere.
- **asked:**
  - **Q1 — How should the planning loop proceed from the cap?** Four options: (1) grant more revisions, on the reading that 2 → 4 → 2 → 2 → 1 with every round resolved is convergence in progress; (2) accept the plan with round 4's remaining Must Fix applied, then go straight to the `task-plan-reviewer`, treating the architecture gate as satisfied; (3) accept the plan as it stands without applying `review_4.md`, recording the remaining finding in the story index's `## Rejected findings`; (4) stop the branch for a hands-on look at the plan before any implementation.
  - **Q2 — Three Should Fix findings the writer deferred**, none blocking, each a wording or context edit: **Should Fix 3**, a third `${HARNESS_AGENT_CLI:-claude}` read site outside `ARCHITECTURE.md` §5's inventory (narrowed by the reviewer's own round-3 verification to a re-derivation blind spot, §4 staying literally true); **Should Fix 4**, Task 6's *"no agent can invoke this file, by construction"* overstating the allow-only script guard, which buys no *automatic permit* rather than a refusal; **Should Fix 5**, six tasks reasoning from `.claude/settings.autonomous.json`, which is generated by `init`, gitignored, and exists only at the main checkout's root, so an implementer in this worktree cannot open it. A fourth entry, **Nice to Have 6** (module headers stated for 4 of 13 new `evals/` modules), was recorded as consistency rather than a rule, no conventions document binding a `general`-layer module to a header.
  - **Q3 — Corpus staleness the writer reported at its first dispatch**, three items, none blocking and none scheduled as tasks: `.claude/context/conventions.md` → `## The testing bar`'s *"No fixture is ever created inside this checkout, and no test is ever aimed at this repository."*, which this branch falsifies on both halves (it commits `evals/docs-retrieval/corpora/fixture-catalog/` inside the checkout, and wires a gate whose second corpus is this repository's own `docs/` plus its conventions documents); `.claude/context/conventions.md` → `### Where a new responsibility goes`'s row *"a measured fact or a decision of record | `docs/`, and nowhere else"*, against `evals/docs-retrieval/floor.json` being a recorded measurement that lives outside `docs/` because a gate must read it as data; and `evals/docs-retrieval/` as an **undescribed layer** created by this branch, holding executable ES modules plus a shell script and committed corpora, covered only by the catch-all `layers[]` row (`path: "."`) whose conventions document has no rule for root-level executable JavaScript — the same directory `doctor`'s `layer-drift` check reports from the configuration side.
- **answered:** `feat_docs_retrieval_eval — Q1: option 2, apply review_4.md then jump to the structural gate. Q2: implement none. Q3: implement none.`
- **carries beyond this branch:** the three Q3 corpus-staleness items are **left alone for this branch and are not disposed of** — each remains a live question about the project's own conventions corpus after this branch merges: whether `## The testing bar`'s no-fixture-in-this-checkout rule means what it says or means only `cli/test/`'s `os.tmpdir()` fixtures; whether `### Where a new responsibility goes` admits a machine-read measurement outside `docs/`; and whether `evals/` earns a `layers[]` row of its own with rules for root-level executable JavaScript. The Q2 rulings are branch-scoped. Q1's ruling establishes the precedent park 2 then names: a gate may be treated as satisfied by operator ruling without returning PASS.

## question_2 — how the plan loop proceeds from a second cap, this time with the structural gate open
- **raised by:** the planning orchestrator, at the task-plan write loop's `iteration >= 5` cap. Gate open when the cap fired: the structural gate (`task-plan-reviewer`, `## Loop` step 5). Latest findings: `harness-runs/task_plan_reviews/feat_docs_retrieval_eval/review_4.md`. Round counts at the park: `task_plan_reviews/` 5 rounds, all that session; `architecture_reviews/` 5 rounds, all from the pre-park session and none that one, park 1's ruling having taken the gate as satisfied; `business_parity_reviews/` not applicable. Must Fix counts **4 → 3 → 1 → 1 → 1**, every round's applied, none re-raised. What the rounds were about, from the writer's returns: **round 0 (4)** a wrong module attribution for `retrievalModelCacheDir`, two plan anchors described as `##` sections when they are bold lead-in paragraphs, a missing `queries/README.md` target, and scope-register closure rows; **round 1 (3)** `modelFilesPresent(...)` used as a bare boolean in six places when it returns `{ present, missing }`, gate 11's models-absent path undefined between BLOCKED and failure, and a hardcoded `docs/` file count; **rounds 2, 3 and 4 (1 each)** all in one subject — which run the generated region of `docs/retrieval-eval-results.md` ends up holding, and how the pre-move per-query score distributions survive regeneration. The park's own stated signal: the first two rounds cleared mechanical defects and converged fast, while the last three each produced exactly one Must Fix inside the same evidence-preservation chain with each fix drawing the next, which is either genuine convergence on an intricate ordering constraint or a sign the chain is over-constrained for a plan.
- **asked:**
  - **Q1 — How should the planning loop proceed from this second cap?** Five options: (1) grant more revisions at the structural gate, with the note that on the current trend each round costs two dispatches and yields one finding; (2) accept the plan with round 4's Must Fix applied and treat the structural gate as satisfied — park 1's ruling applied to the second gate, which would mean the plan reaching implementation with **no gate having returned PASS on this branch**; (3) accept as it stands without applying `review_4.md`; (4) **simplify** the Task 5 / 9 / 14 chain rather than specify it further — for instance by having Task 14 own the pre-move distributions as hand-written evidence from the start and dropping the post-move regeneration from the branch, leaving it to the follow-up run against a real catalog — the only option addressing over-constraint and the only one changing the plan's shape; (5) stop the branch for a hands-on look, nothing of that session's work being committed yet.
  - **Q2 — Four rounds' worth of deferred non-Must-Fix findings**, every revision having been dispatched Must-Fix-only: **round 0** register row 24's reason stale after the Task 5 / Task 14 split, and a `BCDE` grep narrower than the property it checks; **round 1** register row 33 / Task 8's fenced block, and the `## Still open` entry count; **round 2** `runArm({ session, … })` needing to be `index`, and `modelFilesPresent(...).present` missing from Task 10's operator prose; **round 3** Task 14's set-relation wording, and Task 9 sitting at both its `Work:`-bullet and story-point ceilings.
  - **Q3 — Nothing new.** Park 1's Q3 items recorded as left alone; no new corpus staleness reported that session.
- **answered:** `Q1: option 1 — grant 5 further rounds at the structural gate. Run them the same way the first five ran: writer revision scoped to Must Fix, then re-dispatch `task-plan-reviewer`, up to 5 more revisions. The trend (4 → 3 → 1 → 1 → 1, no re-raises) reads as convergence, and round 4's finding is a real defect rather than further specification of an already-settled point, so the gate is earning its dispatches. Do not treat the gate as satisfied without a PASS this time; if it returns PASS earlier than 5, fall through to implementation at that point. If it hits the new cap still open, park again rather than overriding it — and if the next two rounds are again one finding each inside the Task 5 / 9 / 14 chain, say so explicitly in that park, because option 4 (simplify the chain) becomes the likely call.` / `Q2: implement none. Keep them recorded in the story index's `## Rejected findings`, grouped by round, as closed by the user for this branch.` / `Q3: nothing to dispose of.`
- **carries beyond this branch:** a standing decision about how a cap is answered — *"Do not treat the gate as satisfied without a PASS this time"* explicitly declines to extend park 1's precedent, and the ruling pairs granted rounds with a re-park requirement rather than an override, plus a named condition (two further single-finding rounds in the same chain) under which simplification becomes the call. Q2's disposition is branch-scoped but sets the recording convention the story index now carries: non-Must-Fix findings stay in `## Rejected findings`, grouped by round, marked closed by the user rather than open.

## question_3 — how Task 14's missing input is remedied, Task 5's commit-body hand-off having been impossible
- **raised by:** Phase A (implementation), readiness entry 6 — `Task 14` (`general` layer), `layer-implementer` dispatch `[A · Task 14 · general · iter 0]` (#13). The implementer returned a `blocker:` and changed no file; the tracked tree was clean. Detail file `harness-runs/task_plans/feat_docs_retrieval_eval/task_14_plan.md`; upstream unit `task_5_plan.md`, committed as `51ba982`. State at the park: ledger `A. Tasks implemented` still `[ ]`; story index entries 1–5 `[x]` with entry 6 the resume point and 8 entries after it; dispatch counter 13. Background: `task_14_plan.md` said the arm-E **recall@5 before/after pair** and its shared corpus snapshot stamp come *"from Task 5's commit body and from nowhere else"*, and its line 9 forbade the Task 14 implementer from deriving any of them a second time or re-running an arm — but `git log -1 --format='%b' 51ba982` returns a single newline. Task 5's own plan (line 29) had required the hand-off and line 33 asserted it complete. Task 5's implementer *did* take the measurement (building `harness-runs/scratch/recheck-arm-e.mjs`, gitignored, stdout only) and recorded the figures in no durable artifact. The *before* half was not recoverable by reading anything: it requires `ABSTAIN_SCORE_THRESHOLD` temporarily back at `0.3`, a `cli`-layer mutation. A second fact changed what the fix was worth: Task 5's deviation note records the committed `json` fence as generated at threshold `0.3`, so every abstaining query's best score is `null` — all negatives and 5 positives **censored** — and the interval actually chosen from was `[0.30, 0.339]` (lowest observed positive `0.33899`, `q-sd-analyze-writes`), midpoint `0.3195`, rounded to `0.32`, while `task_14_plan.md`'s second verification criterion assumed an **uncensored** quotation.
- **asked:**
  - **Q1 — how should Task 14's missing input be remedied?** Parked rather than assumed because both remedies required the orchestrator to act outside the flow's defined shape and they were not equivalent in what the branch ships: remedy **A** re-opens a unit already committed and flipped `[x]`, and the implement → commit loop walks `[ ]` entries only, so taking it means improvising flow structure; remedy **B** edits a plan file, which the orchestrator is barred from doing and which no implementation-flow agent is dispatched to do. Nor was it low-stakes: B permanently drops a measured before/after pair from the shipped record of a calibration branch whose whole purpose is the measurement, while A spends a dispatch pair to recover it. Three options: **A** re-take Task 5's pass (run `bash scripts/scratch-run.sh harness-runs/scratch/recheck-arm-e.mjs` twice over one tree, once at `0.3` and once at `0.32`, confirm the same `snapshot` stamp, record the pair durably) then re-dispatch Task 14 unchanged, with the open sub-question of whether the pair lives in `task_5_plan.md` or whether line 9's *"commit body and nowhere else"* is read as satisfied by any durable Task-5 artifact; **B** re-scope Task 14 so the "Record the effect" bullet cites Task 9's post-move regeneration alone and the section states plainly that no before/after pair was taken; **C** A plus an amendment to `task_14_plan.md`'s second verification criterion so it grades the quotation as **censored** (negatives stated `< 0.30`, not as measured values), without which the section would be written correctly and then fail its own plan's check.
- **answered:** `Q1: Option C, with one generalisation that applies to the rest of the branch.` / `**The root cause, so the fix is aimed at the right thing.** The recall pair did not reach Task 5's commit body because an implementer cannot write a commit body at all — only the `committer` writes commits, and its commit-message contract composes the subject from the unit title and the staged-path set and emits exactly one body line ever, the `record disposition of <unit token> — <cause>` line. There is no dispatch argument that carries an implementer's measured figures into a commit message. So this was not a lapse by Task 5's implementer and it is not fixed by making commit bodies non-empty: the committer would still be writing a message about facts it never sees. **The commit body is not a viable inter-task hand-off channel for implementer-produced evidence, and no task on this branch may treat it as one.**` / `**The standing rule, from here to the end of the branch.** Any output, measurement or result that one task produces and a later task must read is recorded in the **`**Deviations from plan:**` block of the producing task's own plan file** — appended to the existing block, or written as a new one where the file has none. That file is staged and committed by the unit's own commit (Task 5's block is already on the branch, in `51ba982`), so the evidence is durable, on the branch, and readable by any later dispatch. Wherever a remaining task plan says a figure, a line of output or a heading is to be carried "in the commit body" — `task_9_plan.md` and `task_10_plan.md` both still do, and `task_1_plan.md`, `task_2_plan.md` and `task_3_plan.md` mention it — read that instruction as naming the producing task's `**Deviations from plan:**` block instead. Where a plan names the commit body **and** a second home (Task 9's planted-drop line is written "into the commit body and into …"), the second home stands and the commit-body half is satisfied by the Deviations block. This is a reading rule for the implementation half; it is not licence to rewrite the plan files' prose, beyond the one amendment Option C authorises below.` / `**Option C, specifically:** 1. **Re-take Task 5's pass.** Dispatch a `cli`-layer unit that runs `bash scripts/scratch-run.sh harness-runs/scratch/recheck-arm-e.mjs` twice over one tree — once with `ABSTAIN_SCORE_THRESHOLD` temporarily back at `0.3`, once at the committed `0.32` — confirms both runs report the same `snapshot` stamp (if they do not, the tree moved and the pass is re-taken), and restores the constant to `0.32` before it finishes. The launcher exists and needs no edit. 2. **Record the pair in `task_5_plan.md`'s existing `**Deviations from plan:**` block** — the before figure, the after figure and the shared snapshot stamp, appended to the block already committed in `51ba982`. `task_14_plan.md` line 9's "from Task 5's commit body and from nowhere else" is **satisfied by any durable committed Task-5 artifact**: that line's intent is that Task 14 derives nothing a second time and re-runs no arm, not that the bytes sit in a commit message. Task 14 reads the pair from the Deviations block and still derives nothing itself. 3. **Amend `task_14_plan.md`'s second verification criterion** so it grades the quotation as **censored** — negatives stated as `< 0.30` rather than as measured values — matching the evidence Task 5's own committed deviation note describes ("no negative score is observable at all"). Without this the section is written correctly and then fails its own plan's check. Change that criterion and nothing else in the file. 4. **Re-dispatch Task 14** unchanged apart from the above.` / `**On re-entering a closed unit.** Step 1 re-opens work whose readiness entry is already `[x]`, and the implement → commit loop has no shape for that — this is the orchestrator improvising flow structure, and it is authorised here for this one case. Do not flip any readiness marker backwards, do not re-run Task 5 as a unit, and leave entries 1-5 `[x]`. Treat step 1 as a recovery dispatch that lands one commit carrying the `task_5_plan.md` Deviations amendment (and no change to `search.ts`, which must end at `0.32` exactly as `51ba982` left it), then continue the readiness walk at entry 6.` / `Record the whole of this — the impossible commit-body hand-off, the Deviations-block rule, and the re-entry — in this run's improvement-observations intake, so the harness-level defect is not lost with the session.`
- **carries beyond this branch:** two things outlive it. **(i) A harness-level defect, and its rule:** the commit body is not a viable inter-task hand-off channel for implementer-produced evidence, because no dispatch argument carries an implementer's figures into a commit message — so a plan that routes evidence through it routes it nowhere. The ruling's replacement rule, *record it in the producing task's `**Deviations from plan:**` block*, is stated as a reading rule for this branch but is a candidate plan-format rule for every branch; the defect is recorded at the ruling's own instruction in `harness-runs/improvement_observations/feat_docs_retrieval_eval.md`. **(ii) A one-case authorisation whose shape the flow lacks:** re-entering a unit whose readiness entry is already `[x]`, as a recovery dispatch that flips no marker backwards and lands one commit. The ruling authorises it *here for this one case* and does not generalise it — a later run needs its own authorisation.
