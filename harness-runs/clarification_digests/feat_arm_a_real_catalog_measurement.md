## question_1 — The task-plan loop hit its 5-revision cap: continue planning, accept the plan, or stop?
- **raised by:** the planning loop (`task-plan-writer` / `architecture-reviewer` / `task-plan-reviewer`), at the `## Loop` step 5 five-revision cap (`<escalate>`); latest findings `harness-runs/task_plan_reviews/feat_arm_a_real_catalog_measurement/review_2.md` (2 Must Fix)
- **asked:** Q1 — continue planning, or stop here? The plan had 25 tasks and 395 points. Rounds per gate, as the park recorded them:

  | Gate | Rounds | Verdicts |
  |---|---|---|
  | `architecture-reviewer` (`harness-runs/architecture_reviews/feat_arm_a_real_catalog_measurement/`) | 5 dispatches, 2 findings files | FAIL (2 Must Fix) → PASS → PASS → FAIL (1 Must Fix) → PASS |
  | `task-plan-reviewer` (`harness-runs/task_plan_reviews/feat_arm_a_real_catalog_measurement/`) | 3 | FAIL (2) → FAIL (1) → FAIL (2) |

  A repeated architecture Should Fix, neither applied nor rejected, was also raised: move `partitionOf` / `breakdown` out of `evals/docs-retrieval/arm-a/spread.mjs` (Task 11). Options: (a) continue with a fresh set of up to 5 revisions, optionally saying whether to apply or reject the `spread.mjs` Should Fix; (b) accept the plan as it is and implement; (c) stop the run.
- **answered:** Q1: (a) Continue — allow 3 more revisions (not 5). Pass review_2.md to the writer and fix both Must Fix items: (1) replace "exits 0" for scripts/test.sh in task_1, task_6 and task_22 with "prints no failure that was not already failing before this task", naming 6a as the known pre-existing failure (docs/development.md §5), and in task_22's Route 1 branch change "no other gate" to "no gate that was not already failing before this task"; (2) make task_16's --help probe harness-runs/scratch/agent-cli-help.mjs (execFileSync with fixed argv) and state task_9's driver is a .mjs that spawns run-arm-a.sh with HARNESS_AGENT_CLI set.
- **carries beyond this branch:** a verification rule for plans in this checkout: `bash scripts/test.sh` passes when it "prints no failure that was not already failing before this task", with gate 6a named as the known pre-existing failure (`docs/development.md` §5), rather than when it "exits 0"

## question_2 — Does the operator approve the `gate10-catalog` query set as committed?
- **raised by:** Phase A, `layer-implementer` on Task 15 ("Park for the operator's approval of the query set, and record it before anything is scored"), via `<escalate>`; a planned park (task prompt `## Operator checklist` row 6)
- **asked:** Q1 — do you approve `evals/docs-retrieval/queries/gate10-catalog.jsonl` as committed? The set as parked: 71 records; 42 positives (23 under `docs/expause-web/`, 19 under `docs/vite/`); 29 negatives (19 `near` — 13 Expause, 6 Vite — and 10 `far`); intents 33 `surroundings`, 19 `contract`, 19 `convention`; every record `origin: written`; label pre-flight `labels resolve` at `{ files: 156, chunks: 1960 }`. The negatives' author flagged three `near` negatives as closest to documented material: `q-g10-neg-coin-promo-code`, `q-g10-neg-sendgift-rate-limit`, `q-g10-neg-storybook-stories`. Options: (a) approved; (b) approved with corrections listed by query id; (c) not approved.
- **answered:** Q1: (b) Approved with corrections. Positives: q-g10-ew-mark-notification-read: add docs/expause-web/features/inbox.md#data grade 3. q-g10-ew-unlock-payload: relabel docs/expause-web/features/unlock-premium-content.md#cloud-functions to grade 3. q-g10-vite-mock-updated-event: add docs/vite/guide/api-plugin.md#handlehotupdate grade 3. q-g10-ew-watch-later-feed-signal: add docs/expause-web/features/saved-content.md#domain grade 2. q-g10-ew-new-notification-tap: add docs/expause-web/features/inbox.md#presentation grade 2. q-g10-ew-group-room-agora-token: add docs/expause-web/features/community-live-streaming.md#cloud-functions grade 2. q-g10-vite-wsl-file-save: relabel docs/vite/guide/troubleshooting.md#vite-does-not-detect-a-file-change to grade 2. q-g10-vite-dev-api-forward: drop docs/vite/config/server-options.md#servercors. Negatives: q-g10-neg-vite-server-mock: convert to positive with docs/vite/guide/api-plugin.md#configureserver grade 3 and docs/vite/config/server-options.md#serverproxy grade 1 (remove negativeKind). q-g10-neg-coin-promo-code: convert to positive with docs/expause-web/product/product-decision-rate-card.md#34-accompanying-mechanics-relabeled-honestly grade 3 and docs/expause-web/features/wallet-coins.md#business-behaviour grade 1 (remove negativeKind). Drop q-g10-neg-sendgift-rate-limit and q-g10-neg-vite-dev-basic-auth. q-g10-neg-storybook-stories: reword situation to "Plan step: add Storybook stories for the new shared `PayoutBadge` component's variants." q-g10-neg-group-chat: reword situation to "Task: let users start an ad-hoc group DM with three or more other users and add participants later." Update the counts in docs/retrieval-eval-results.md → ## The real-catalog query set accordingly.
- **carries beyond this branch:** nothing beyond this branch

## question_3 — Run arm A by hand and report it; accept or remove the catalog's plugin and `CLAUDE.md` in arm A sessions
- **raised by:** Phase A, `layer-implementer` on Task 16 ("Park for the arm A hand run: verify the CLI flags, take the free pre-checks, hand over the commands, and record the run"), via `<escalate>`; a planned park (task prompt `## Operator checklist` row 7)
- **asked:** Q1 — run both arm A variants (A-index and A-search), five repetitions each, repetition 1 of each first as the cost checkpoint, with outputs under `harness-runs/scratch/arm-a/gate10-catalog/`. Report the 5-hour and 7-day usage window before and after each pass and the operator time, and once the CLI version, model and host. Options: (a) ran it; (b) stopped at the cost checkpoint; (c) not running it. Q2 — the catalog's `.claude/settings.json` enables the `autonomous-sdlc-harness` plugin and every session loads `.claude/CLAUDE.md`; `--strict-mcp-config` keeps out only the root `.mcp.json` server. Options: (a) accept it and record it; (b) turn the plugin off for the run and say how.
- **answered:** Q1: (a) Ran it. Both variants, five repetitions each, all ten passes done, none stopped; outputs under harness-runs/scratch/arm-a/gate10-catalog/ (index-rep1…5.jsonl, search-rep1…5.jsonl), each with 69 records, one per query id of the set, same id order; 690 agent sessions in total. Each repetition ran as one pair, index then search back to back, so the usage window was read before and after each pair, not each variant.

  | Rep | index (local time) | search (local time) | 5h window before → after | 7-day window before → after | Operator time |
  |---|---|---|---|---|---|
  | 1 | 18:24:30–18:36:15 | 18:36:27–18:49:15 | 4% → 19% | 8% → 9% | 2 min |
  | 2 | 18:58:07–19:10:08 | 19:10:22–19:22:48 | 19% → 25% | 9% → 10% | 2 min |
  | 3 | 19:32:05–19:43:52 | 19:44:03–19:56:17 | 25% → 31% | 10% → 11% | 1–2 min |
  | 4 | 20:13:39–20:25:31 | 20:25:45–20:37:57 | 31% → 37% | 11% → 12% | 2 min |
  | 5 | 20:40:41–20:52:33 | 20:52:46–21:04:44 | 37% → 43% | 12% → 12% | 2 min |

  All on 2026-09-23. The cost checkpoint after rep 1 passed: +15 points on the 5-hour window for the pair, estimated from token usage (weighted by relative price) as ~63% index / ~37% search; ×5 stays under 90% for each variant. Reps 2–5 cost +6 each (prompt cache warm between passes; per-session cache writes fell from 3.20 M / 1.70 M tokens in rep 1 to ~1.1–1.4 M / ~0.35 M). The runs were strictly sequential, one session at a time, so durationMs is uncontended wall time.

  Once: agent CLI 2.1.280 (Claude Code); model claude-opus-5-5 (--model opus; every assistant turn of all 690 sessions reports claude-opus-5-5); host darwin 24.6.0, Mac16,12 (arm64). Catalog at 57a6c25 with docs/ unchanged, checked after every pass. The first record of every file has bare path#anchor refs or the bare none token, and no file has a code-fence entry. Tool calls: only Read, Grep and Glob in every file.

  Three records carry a prose sentence beside none in refs, left verbatim, not edited: index-rep2 q-g10-vite-health-middleware, index-rep5 q-g10-vite-mock-updated-event, search-rep5 q-g10-neg-android-keystore. Per score-transcript.mjs a none among other refs scores as an answered miss, not an abstention; the last is a far negative, so it costs A-search one abstention on that repetition. Record this in the write-up.

  Q2: (a) Accept and record it: arm A sessions ran with the catalog's autonomous-sdlc-harness plugin enabled and its .claude/CLAUDE.md loaded, which is the realistic setup for an agent navigating a harness-adopted repository. Correct the park's description when recording it: the plugin's only hook is a PreToolUse hook matching Bash, which the runner disallows, so no hook fired; the plugin declares no MCP server; the catalog's own harness-docs server in its .mcp.json was kept out by --strict-mcp-config. What the plugin added is its agent, command and skill listings in the system prompt. The plugin is also enabled in the operator's user-level settings, so disabling it in the catalog alone would not have removed it.
- **carries beyond this branch:** a standing decision for any later arm A run on a harness-adopted catalog: sessions run with the catalog's plugin and `.claude/CLAUDE.md` loaded, as the realistic setup. The plugin's only hook matches `Bash`, which the runner disallows, so no hook fires. The plugin declares no MCP server. It is also enabled in the operator's user-level settings, so disabling it in the catalog alone does not remove it.
