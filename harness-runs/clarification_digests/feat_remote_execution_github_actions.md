## question_1 — How should planning continue past the task-plan loop's 5-revision cap, with the architecture gate open?
- **raised by:** the planning orchestrator (`/autonomous-sdlc-harness:branch-start-plan-autonomous`, planning half), after the walker (`scripts/flow-walker.sh`, flow `task_plan_writing`) returned `binding: <escalate>`, `reason: cap`, at node `architecture_review` (`architecture-reviewer`). Latest findings: `harness-runs/architecture_reviews/feat_remote_execution_github_actions/review_2.md` (verdict FAIL, `must_fix_count: 1`), against a 33-task, 476-point plan. Rounds walked that session: architecture gate FAIL (4 Must Fix), FAIL (2), PASS, PASS, FAIL (1); structural `task-plan-reviewer` FAIL (5 Must Fix), FAIL (2); `rounds: architecture_review 3`, `rounds: plan_review 2`.
- **asked:** Q1 — how planning continues past the cap, since the run can neither declare convergence over an open Must Fix nor grant itself extra rounds. Options:
  - **(a) Revise once more.** On resume, the writer gets `review_2.md` together with your answer and revises. The plan loop then runs every gate again, starting a fresh walk. Add any ruling on the Must Fix itself (accept it, reject it, or how to resolve it) and the writer will apply it.
  - **(b) Rule the finding out.** Tell the writer to record the Must Fix under `## Rejected findings`, and say why. The resumed writer records it, and the gates run once more.
  - **(c) Stop here.** Leave the branch parked. You can edit the plan by hand, or narrow the task prompt (for example, split the 33-task scope across branches), and then re-drop.
- **answered:** "Q1: (a) Revise once more — I grant 5 more plan-loop rounds. Revise against review_2.md's Must Fix and resolve it as the reviewer asks, then run every gate again on a fresh walk with a budget of 5 further revisions."
- **carries beyond this branch:** nothing beyond this branch
