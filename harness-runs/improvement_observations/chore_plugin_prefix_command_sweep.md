## The plan unit needed 5 plan-review rounds and parked on the 5-revision cap before converging
- **category:** optimization
- **evidence:** Plan-review roots for this branch: `harness-runs/task_plan_reviews/chore_plugin_prefix_command_sweep/` holds `review_0.md`–`review_3.md` (4 FAIL rounds), `harness-runs/architecture_reviews/chore_plugin_prefix_command_sweep/` holds `review_0.md` (1 FAIL round), and `business_parity_reviews/` has none (parity off). That makes 5 rounds with findings. The loop hit `iteration >= 5` and parked as `harness-runs/clarifications/chore_plugin_prefix_command_sweep/question_1.md`. The operator's `answer_1.md` allowed 3 more rounds, and the first extra round passed: the writer fixed `review_3.md`, the architecture reviewer passed with no file, and task-plan-reviewer `iteration: 4` passed with no file. Must Fix per task-plan-reviewer round was 4 → 2 → 2 → 1. Matching finding titles against the prior round in the same root, every Must Fix in rounds 1–3 was **net-new** (1: 2 net-new / 0 re-raised; 2: 2 / 0; 3: 1 / 0). The Should Fix items were mostly **re-raised** without being applied (1: 1 re-raised / 2 net-new; 2: 3 / 1; 3: 4 / 1). The reviewer marks each of those "carries over from iteration N" and the writer returns say only Must Fix items get applied.
- **cost this run:** one park-and-resume cycle, which ended the session and needed an operator answer, plus 4 writer revisions before implementation started.
- **hypothesis:** (guess) each round's Must Fix is a narrower consequence of the previous round's fix, not a finding the earlier round should already have raised.

## Gate 6a "no machine paths" failed `bash scripts/test.sh` for every implementer dispatch in this worktree
- **category:** tooling-gap
- **evidence:** All 19 `layer-implementer` returns this run report `bash scripts/test.sh` exit 1, with `6a no machine paths` as the only failing gate: Tasks 1–13, architecture Finding 1, code-review Findings 1–5 and skeptic Finding 1. Hits named across those returns: the worktree's untracked `.git` pointer file, whose `gitdir:` line is an absolute path; `harness-runs/improvement_observations/feat_readme_summary_compact_llms_txt.md`, which quotes a machine path; and from Task 4 on, `harness-runs/scratch/t3npm.log`, a gitignored npm log the Task 3 implementer wrote. Each task plan's "`bash scripts/test.sh` exits 0" check was recorded as a deviation.
- **cost this run:** no dispatch in this run had a green test gate, so a real 6a regression introduced by the branch would not have stood out from the standing failure.

## The row-A `commit_prefix` rule produced `chore:` on every task commit, which the project's commit-message policy forbids
- **category:** agent-contract
- **evidence:** `unit_loop_core.md` row `A` → `commit_prefix rule` maps a branch name that is not `feat_`/`fix_`/`refactor_` to `chore`, so all 13 task commits (434c5c0 … 4cd9dc4) got `chore:`. `.claude/context/conventions.md` → `## Commit-message policy` gives a story task's commit prefix `none`. The committer flagged this in its return for Task 2 (35d66f4): *"Under that policy, a caller-chosen `chore` isn't a legal value for a task commit."* The five fix rows, which take the prefix from the policy's per-class designation, committed with no prefix (906e342, 1e47300, 0276181, 250a0b3, ecc17f5, cdbcae4, ee96733).
- **cost this run:** 13 task commit subjects do not follow the repository's commit-message policy.

## Two task plans split one cross-check assertion, so the suite stayed red across two task commits
- **category:** agent-contract
- **evidence:** The Task 2 `layer-implementer` returned `blocker: bash scripts/test.sh fails one init.test.mjs case that can't pass until Task 4 qualifies doctor's own ANALYZE_COMMAND`, without the `prohibited — ` marker. The case is `init names the source directories no layer covers, as doctor does`. The orchestrator applied ask-vs-assume, treated it as low-stakes and reversible, and committed anyway. `npm test` was red at 35d66f4 (Task 2) and c7ddb75 (Task 3), as the Task 3 return confirms, and passed at 9b87d92 (Task 4).
- **cost this run:** two intermediate commits whose test suite fails.
