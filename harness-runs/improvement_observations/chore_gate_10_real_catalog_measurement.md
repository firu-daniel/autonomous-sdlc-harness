# Improvement observations — chore_gate_10_real_catalog_measurement

## Gate `6a no machine paths` is red for the whole run, and every hit is the harness's own machinery
- **category:** tooling-gap
- **evidence:** Every implementer and fix dispatch of this run reported `bash scripts/test.sh` (the configured `commands.test`) exiting 1 with `run-gates: 1 failed, 14 passed`, the sole failure being `6a no machine paths` — 9 dispatches in all (Tasks 1–4, architecture findings 1–2, code-review findings 1–3, skeptic findings 1–2). Each dispatch independently enumerated the hits and reported the same set: the worktree's own untracked `.git` pointer file (`gitdir: …/.git/worktrees/…`), `harness-runs/task_prompts/chore_gate_10_real_catalog_measurement_task_prompt.md`, `harness-runs/architecture_reviews/…/review_0.md`, and the `harness-runs/improvement_observations/*.md` files that quote the failure as evidence. No hit was ever in a `docs/` file or in any file this branch changed.
- **cost this run:** The configured test command returns a non-zero exit for every dispatch on this branch, so no dispatch could use its exit status as a pass/fail signal; each of the 9 had to enumerate and classify the gate's hits by hand to establish that its own diff was clean.

## The orchestrator's `commit_prefix` argument disagreed with the repository's own commit-message policy on the first review-fix commit
- **category:** agent-contract
- **evidence:** At `[A2 · Item 1 · commit] → committer (#12)` the dispatch carried `commit_prefix: fix`. The committer returned that `.claude/context/conventions.md` → `## Commit-message policy` designates `none` for the fix-to-existing-work class and that its prefix vocabulary holds only `chore`, so `fix` is not a legal token in this repository; it rendered the subject with no prefix (commit `340dad1`, subject `Drop the search_docs roster from docs/retrieval.md (Finding 1)`). The orchestrator's instruction for the five fix rows is to pass the token the committer's contract designates for that class, but the orchestrator reads neither that contract nor the conventions document, so the correct token is not derivable at dispatch time. The remaining five review-fix commits (`cd6830b`, `8f59c99`, `5042b78`, `ea939c6`, `5a96578`, `a484f00`) passed `none` and were rendered without a refusal.
- **cost this run:** One commit's prefix argument was rejected and corrected by the committer; no commit was lost and no dispatch was repeated.

## Convergence churn — the plan needed 3 review rounds across its convergence gates
- **category:** optimization
- **evidence:** Plan-convergence gate folders, listed at Phase D: `harness-runs/task_plan_reviews/chore_gate_10_real_catalog_measurement/` holds `review_0.md`, `review_1.md` (2 rounds); `harness-runs/architecture_reviews/chore_gate_10_real_catalog_measurement/` holds `review_0.md` (1 round); `harness-runs/business_parity_reviews/…` does not exist (`phases.parity` is `false`, so that gate never ran and is not a round source). Total across the folders' own series: 3. Per-round split, diffed within each root's own sorted list: architecture round 0 has no predecessor in its root (1 Must Fix, net-new by construction); task-plan round 1 vs round 0 — 1 **net-new** Must Fix ("Under-inclusive derivation — a real-model leg is still recorded as unperformed … and no row or task owns it") and 1 **re-raised** Should Fix ("Derivation entry C still excludes `*.sh`", which the round-1 file itself records as carried over from iteration 0 and re-raised once).
- **cost this run:** 3 writer revisions before planning converged; 11 of the planning half's 40-dispatch budget consumed.

## Repeated defect class — a fact stated away from the file that owns it hit 3 units
- **category:** optimization
- **evidence:** Three findings across two reviewers name the same class — a fact restated or grounded outside the document of record for it — on three distinct units: `architecture-reviewer` (implemented-branch, `harness-runs/architecture_branch_reviews/…_arch_review.md`) finding 1, "`docs/retrieval.md` restates the `search_docs` grant roster that `plugin/agents/README.txt` owns" (Task 2's unit), and finding 2, "`docs/development.md`'s gate 10 run stamp carries leg (i)'s measured figure, whose home is `docs/retrieval.md` item (d)" (Task 3's unit); `branch-reviewer` (`harness-runs/code_reviews/…_code_review.md`) finding 2, "Stop grounding the cancellation on cost in the file of record" (Task 1's unit). All three were fixed and committed (`340dad1`, `cd6830b`, `5042b78`).
- **cost this run:** 3 of the run's 7 branch-level findings, one per document, each requiring its own implement → commit cycle.

## Zero-yield reviewer — `review-plan-reviewer` produced no change on either unit it reviewed
- **category:** optimization
- **evidence:** `review-plan-reviewer` was dispatched twice this run — B.2 over the code-review index (`#16`) and C2.2 over the skeptic-review index (`#25`) — and returned `verdict: PASS` on iteration 0 both times, writing no findings file either time (`harness-runs/review_plan_reviews/chore_gate_10_real_catalog_measurement/` and `harness-runs/skeptic_review_plan_reviews/chore_gate_10_real_catalog_measurement/` were both absent at the B.3 / C2.3 commit points, which is why each commit omitted its `meta_findings_folder` key). Unit count passed: 2 of 2, no change produced on either. The per-unit `layer-reviewer` is not measurable here at all — this fork binds `<per_unit_review>` `off`, so none was dispatched.
- **cost this run:** 2 dispatches, no finding added or removed from either review index.
