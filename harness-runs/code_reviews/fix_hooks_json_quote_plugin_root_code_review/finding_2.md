### 2. The quoting fact sits inside a bullet about hook composition, so the "Two facts" and "Five measured behaviours" counts no longer match what they introduce

**Files:**
- `docs/development.md` → `## 3. Manifest facts a contributor must not rediscover` → the bullet opening "**Plugin hooks append to an adopter's own hooks; they do not override them.**", and its follow-on paragraphs from "**The token sits inside double quotes together with the path it prefixes:" through "so the six extra hooks and the `deny` came from the spaced-path copy."
- `plugin/hooks/README.md` → `## Registration, and why it lives here rather than in generated settings` → "Two facts about this manifest were measured rather than assumed", and the bullet opening "`${CLAUDE_PLUGIN_ROOT}` expands inside a hook's `command` string declared here".

The branch adds a new measured fact: an unquoted token fails `--strict`, and the quoted form still fires from a spaced path. Both copies fold it into an existing bullet so that the count sentences stay numerically true. In `docs/development.md` §3, about fifty lines on quoting now sit under the bold lead "Plugin hooks append to an adopter's own hooks; they do not override them", which does not describe them. The section's own opening, "Five measured behaviours and one shipped contract", now leaves one of its measured behaviours out. The README has the same problem. It opens "Two facts about this manifest were measured", then carries a third measured fact inside its second bullet. This is a structure fix only. No sentence of the fact itself changes.

**Fix:**

- [ ] `docs/development.md` §3: move the follow-on content out of the "Plugin hooks append…" bullet and make it a new top-level bullet placed immediately after that bullet. The moved content runs from the paragraph beginning "**The token sits inside double quotes together with the path it prefixes:" through "so the six extra hooks and the `deny` came from the spaced-path copy.". Start the new bullet with `- ` in place of the current two-space indent on its first line. Keep its fenced blocks and its nested `- **Invocation.**` / `- **Stdin, exit status and stdout.**` / `- **Versions.**` sub-bullets indented two spaces under it, as they are now. The "Plugin hooks append…" bullet then ends at its original last sentence, "which is the reason the guards live in `hooks.json` rather than in generated settings."
- [ ] `docs/development.md` §3 opening: change "Five measured behaviours and one shipped contract" to "Six measured behaviours and one shipped contract".
- [ ] `plugin/hooks/README.md`: in the bullet opening "`${CLAUDE_PLUGIN_ROOT}` expands inside a hook's `command` string declared here", cut everything from "**Every occurrence of the token" through the end of the bullet ("…`## 3. Manifest facts a contributor must not rediscover`."). Paste it as a new third bullet directly after that one, starting with `- `. If Finding 1 has landed, carry its wording over unchanged.
- [ ] `plugin/hooks/README.md`: change "Two facts about this manifest were measured rather than assumed" to "Three facts about this manifest were measured rather than assumed".
