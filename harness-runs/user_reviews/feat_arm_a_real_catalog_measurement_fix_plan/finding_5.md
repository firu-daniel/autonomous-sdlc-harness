### 5. Adopter-facing and contributor surfaces still say retrieval is withdrawn or about to be removed

**Files and site anchors** (every one grep-verified in the current tree):

| # | File | Anchor |
|---|---|---|
| a | `README.md` | the verbs paragraph — "it came out **withdrawn** — the verdict is in" … "and the withdrawal is not yet carried out." |
| b | `README.md` | the documentation-list bullet "[`docs/retrieval.md`](docs/retrieval.md) — RAG (docs retrieval): opt-in, local, and measured on a real catalog with the outcome withdrawn;" |
| c | `llms.txt` | the `cli/` entry — "came out withdrawn; the withdrawal is not yet carried out." |
| d | `llms.txt` | the `docs/retrieval.md` entry — "measured on a real catalog with the outcome withdrawn:" |
| e | `schemas/harness.config.schema.json` | `docs.retrieval` → `"description"` — "came out withdrawn (docs/retrieval-eval-results.md, 'The decision, applied to the real catalog'); the withdrawal is not yet carried out." |
| f | `docs/config.md` | the `docs.retrieval` table row — "it came out **withdrawn**" … "the withdrawal, which removes this key with the rest of the tool, is roadmap item 18" … "so this row describes the key as it ships." |
| g | `evals/README.md` | the `docs-retrieval/` bullet — "records its verdict against agent navigation: **withdrawn**." |
| h | `ROADMAP.md` | `## Index` priority 1 — "[Docs-catalog retrieval](#engines-environments-and-integrations) — withdraw it, as its eval decided" |
| i | `ROADMAP.md` | `## Engines, environments and integrations` → the *Docs-catalog retrieval* row — "and has now decided it:" … "and the outcome is **withdrawn**" … "What holds this row `Open` is the withdrawal itself," |
| j | `docs/cli.md` | §11 (`docs` verb) opening paragraph — "with the outcome **withdrawn**. The verdict of record is" … "is not yet carried out, so this section describes the verb as it ships." |
| k | `cli/README.md` | "**What the five subcommands do.**" paragraph — "was measured once against agent navigation on a real 1,960-chunk catalog with the outcome **withdrawn**" |
| l | `cli/src/retrieval/queryLog.ts` | module header JSDoc, "**Why the record shape lives here rather than in its one caller.**" — "measured once against agent navigation on a real 1,960-chunk catalog with the outcome *withdrawn*" |

Sites (j), (k) and the second `ROADMAP.md` site were not in the user's list; the search the user asked for ("find the rest by searching for the verdict and the removal") turned them up. `docs/retrieval.md`'s opening paragraph is also on the user's list, and is fixed in Finding 3 with the rest of that file; the `## Threshold calibration` passages are Finding 4.

**The problem.** Each site says, or implies, that retrieval is withdrawn or is to be removed: "not yet carried out", "removes this key with the rest of the tool", "withdraw it, as its eval decided", "What holds this row Open is the withdrawal itself". The maintainer has decided it is kept opt-in (Finding 1). Every site must say instead: **measured, verdict withdrawn, kept opt-in by maintainer decision.**

**Fix.** Change only the withdrawal wording at each site; keep every surrounding sentence, link and figure. Suggested wording, in substance:

- (a) "…it is off by default and opt-in: measured against agent navigation on a real catalog, its verdict was **withdrawn**, and it is kept opt-in by maintainer decision — the verdict and the decision are in [`docs/retrieval-eval-results.md`](docs/retrieval-eval-results.md)."
- (b) "RAG (docs retrieval): opt-in and local; measured on a real catalog, verdict withdrawn, kept opt-in by maintainer decision; its design, measured facts, when to turn it on, what it buys and what it costs." ("when to turn it on" names the section Finding 3 adds.)
- (c) "…plus `docs`, an opt-in local docs-retrieval search: measured against agent navigation on a real catalog, verdict withdrawn, kept opt-in by maintainer decision."
- (d) "docs retrieval, opt-in and local; measured on a real catalog, verdict withdrawn, kept opt-in by maintainer decision: its design, measured facts, when to turn it on, what it buys and what it costs." Keep (b) and (d) parallel. `llms.txt` links may not carry `#` anchors (`scripts/check-llms-txt.sh`, contract 4), so add none.
- (e) **Description text only — no key, type or default moves.** "…Off by default and opt-in: turning it on installs a local runtime and downloads two models at setup time. Measured against agent navigation on a real catalog, its verdict was withdrawn, and it is kept opt-in by maintainer decision (docs/retrieval-eval-results.md, 'The decision, applied to the real catalog'); when to turn it on is docs/retrieval.md, 'When to turn it on'."
- (f) Replace from "Measured against agent navigation on a real catalog, it came out **withdrawn**" through "so this row describes the key as it ships." with: "Measured against agent navigation on a real catalog, its verdict was **withdrawn**, and it is kept opt-in by maintainer decision — both are [`retrieval-eval-results.md`](retrieval-eval-results.md) → `## The decision, applied to the real catalog`. When turning it on is worth it, and that none of those situations has been measured, is [`retrieval.md`](retrieval.md) → `## When to turn it on`." Keep the rest of the row (the `--force` re-run sentence, the design pointer).
- (g) "…`## The decision, applied to the real catalog` records its verdict against agent navigation, **withdrawn**, and the maintainer's decision to keep the tool opt-in rather than withdraw it."
- (h) "[Docs-catalog retrieval](#engines-environments-and-integrations) — kept opt-in by maintainer decision after a withdrawn verdict; improving it and measuring where it pays is open". Keep the entry at priority 1. Re-ordering the index is the maintainer's call and this review does not ask for it.
- (i) Replace "and has now decided it: on a real 1,960-chunk catalog, agent navigation outscored retrieval and the outcome is **withdrawn** ([…] → *The decision, applied to the real catalog*)." with "and has measured it once: on a real 1,960-chunk catalog, agent navigation outscored retrieval and the verdict is **withdrawn**; the maintainer did not execute the withdrawal and keeps retrieval opt-in ([…] → *The decision, applied to the real catalog*)." Replace the closing "What holds this row `Open` is the withdrawal itself, … a numbering separate from this file's priorities." with: "What holds this row `Open` is the work that decision leaves open: improving the stack as built — a code-aware reranker, a different embedder, BM25 weighting, sections longer than the models' window — and measuring the situations where retrieval is expected to pay and no run has covered, a catalog far larger than one grep can cover and one whose vocabulary does not match the queries. Item 18 of [`docs/development.md`](docs/development.md) → *6. The roadmap this tree defers to*, a numbering separate from this file's priorities, records that the withdrawal was not executed." Status stays `Open`.
- (j) Replace "and its retrieval quality has been measured against agent navigation on a real catalog, with the outcome **withdrawn**. The verdict of record is `docs/retrieval-eval-results.md` → `## The decision, applied to the real catalog`; the withdrawal, roadmap item 18 in `docs/development.md` → `## 6. The roadmap this tree defers to`, is not yet carried out, so this section describes the verb as it ships." with "and its retrieval quality has been measured against agent navigation on a real catalog: the verdict was **withdrawn**, and the verb is kept opt-in by maintainer decision. Both are recorded in `docs/retrieval-eval-results.md` → `## The decision, applied to the real catalog`, and roadmap item 18 in `docs/development.md` → `## 6. The roadmap this tree defers to` records that the withdrawal was not executed."
- (k) "It is **off by default** and opt-in: measured once against agent navigation on a real 1,960-chunk catalog, its verdict was **withdrawn**, and it is kept opt-in by maintainer decision — [`docs/retrieval-eval-results.md`](…) → *The decision, applied to the real catalog*;" (keep the absolute `blob/main` URL form this file uses).
- (l) Comment text only, no code change: "…the feature ships opt-in — measured once against agent navigation on a real 1,960-chunk catalog, verdict *withdrawn*, kept opt-in by maintainer decision (`docs/retrieval-eval-results.md` → `## The decision, applied to the real catalog`) — and this file is what distinguishes…". Re-wrap the JSDoc lines to the width of the neighbouring header lines.

**Deliberately not edited** — none of these says the tree will remove retrieval:

- `docs/retrieval-eval.md` → `## The decision rule`, its "**Relevance does not clear** → retrieval is **withdrawn**" outcome bullet and the "the outcome is **withdrawn**" sentence in its `### Two arm A variants, and how they combine` subsection. The review says the rule is not amended.
- Every generated region, the measured figures, and the `withdraw`/`withdrawal` hits in `evals/docs-retrieval/queries/gate10-catalog.jsonl` and `evals/docs-retrieval/transcripts/gate10-catalog/*.jsonl`, which are the measured catalog's own product vocabulary.
- `withdraw` hits unrelated to retrieval: `.claude/context/conventions.md` (script-allowlist permit), `cli/src/config/check.ts`, `cli/src/config/model.ts`, `cli/src/generators/claudeContext.ts`, `cli/src/generators/repoRoot.ts`, `cli/test/init.test.mjs`, `docs/guard-verification.md`, `docs/watcher.md`, `plugin/hooks/README.md`, and the frozen capture under `examples/notes-app/` (`.claude/context/conventions.md` → `## Documents of record`: editing the capture falsifies it).

**Layers.** (l) sits under the `cli` layer path, and so does (k) (`cli/README.md`); the rest are `general`.

**Depends on:** Finding 1 (every pointer cites its record), and Finding 3 for the `## When to turn it on` name cited in (b), (d), (e) and (f).

**Verification.** `git grep -niE "not yet carried out|withdraw it|removes this key|withdrawal itself|came out \*{0,2}withdrawn" -- ':!harness-runs'` returns nothing. `bash scripts/check-llms-txt.sh`; `npm run validate:config` (the schema still validates); `bash scripts/typecheck.sh`; `bash scripts/test.sh`.

**Deviations from plan:** (g) is worded "rather than withdrawing it", not the suggested "rather than withdraw it": the suggested text matches this finding's own `withdraw it` verification probe. The general-layer dispatch edited (a)–(j) only; (k) and (l) were already edited by the `cli`-layer dispatch in the working tree.
