### Task 11 — Repoint the planning commands and the flow overview

**Goal:** Three plugin documents tell a reader the planning core owns *"the iteration caps"* and *"writer/reviewer dispatch with the `>= 5` iteration caps"*. Correct them to say the caps and the routing are walked by `<scripts_dir>/flow-walker.sh` over the planning graph, and that the core owns everything else. The documents are `plugin/commands/branch-start-plan-autonomous.md`, `plugin/commands/branch-start-plan-semi-autonomous.md` and `plugin/docs/AUTONOMOUS_FLOW.md`.

**Depends on:** Task 9 and Task 5. Task 5 adds `flow-walker.sh` to `OUTER_LOOP_SCRIPTS` with `agentInvocable: true`. After Task 9, the core keeps `## Setup`, `## Safety contract`, `## Loop`, `## UI-test-plan write loop` and `## Convergence` by those headings. It adds `## The walker — routing is its, judgement is yours`, and it names `<scripts_dir>/flow-walker.sh` with `<scripts_dir>/flows/task_plan_writing.graph.json` as the owners of the next node, the counters, the cap and what escalates.

**Where this task stops.** The supervised `/autonomous-sdlc-harness:branch-start-plan` is **not** a target. `plugin/commands/branch-start-plan.md` names no instruction file (`grep -c "instructions/"` answers `0`), and the semi-autonomous fork says it *"is entirely self-contained … so that flow consumes neither this fork nor the core"*. The six planning agents are not targets either. Each cites a core heading Task 9 keeps. The routing sentences they carry, such as task-plan-reviewer's *"`verdict: FAIL`, which increments `iteration` and re-dispatches `task-plan-writer`"*, describe the calling side's behaviour, which is unchanged. And every wire string they return (`verdict: PASS`, `no_ui: true`, `## Questions`) is untouched. **Task 16** records that finding. `plugin/instructions/mode_contract.md`'s `<ask>` row (*"`## Loop` holds that present-and-stop handling today, at its writer-dispatch step"*) stays true, because the `## Questions` verbatim rule stays in `### 1.`, so it is not a target.

### Targets

- `plugin/commands/branch-start-plan-autonomous.md`: step `5. **Planning loop.**`, the parenthetical *"`## Loop` (writer/reviewer dispatch with the `>= 5` iteration caps)"*, the `**Planning**` bullet's core-section list, and the `<scripts_dir>` row of `## Resolved values` (its cell *"This file names two of them — `autonomous-watcher.sh`, … and `push-branch.sh`, … — and invokes neither."*).
- `plugin/commands/branch-start-plan-semi-autonomous.md`: step 3's *"The canonical loop — Setup, the safety contract, writer/reviewer dispatch, the iteration caps, clarification handling and the convergence facts — is `task_plan_writing_instructions_core.md`."*, and `## Resolved values`, which today declares only `<state_dir>` and `<parity_vocabulary>` / `<reference_impl>` and gains a `<scripts_dir>` row.
- `plugin/docs/AUTONOMOUS_FLOW.md`: the `Planning **core** (…)` table row, and the `| Git wrappers — the outer-loop scripts an agent may invoke |` row, whose cell calls the three git wrappers *"the rows the shipped table marks agent-invocable"*. Its `## Resolved values` already declares `<scripts_dir>`, so its table is not a target.

**Why the two command tables are targets.** `.claude/context/plugin.md` → `## The sections an asset carries` requires every command file to declare each configuration-derived token in its `## Resolved values` table ahead of the body that uses it, and `## The placeholder vocabulary` requires the cell to state how the token resolves in that file. The Work below writes `<scripts_dir>` into both command files' bodies, so each table must declare it for that use, in the same edit.

**Work:**

- [ ] `branch-start-plan-autonomous.md`: in step 5 and the `**Planning**` bullet, say that the loop's routing and its `>= 5` caps are walked by `<scripts_dir>/flow-walker.sh` over the planning graph, as the core's `## The walker` section instructs, and that the dispatch blocks, the safety contract and the convergence facts stay the core's. Keep every slash-command spelling in its prefixed `/autonomous-sdlc-harness:…` form (gate 6d). In the same edit, amend the `## Resolved values` `<scripts_dir>` cell so it also names `flow-walker.sh` and `flows/task_plan_writing.graph.json` as the owners of the planning loop's routing and caps, and says this file invokes **none** of the four it names (the core's `## Setup` step 7 and `## The walker` section are where the walker is invoked).
- [ ] `branch-start-plan-semi-autonomous.md`: split the canonical-loop sentence into *routing and caps: `<scripts_dir>/flow-walker.sh` over `<scripts_dir>/flows/task_plan_writing.graph.json`* and *Setup, safety contract, dispatch blocks, clarification handling, convergence facts: the core*. Do not duplicate either. In the same edit, add a `<scripts_dir>` row to `## Resolved values` after the `<state_dir>` row: class `config value`, cell *"`scriptsDir` — the directory the outer-loop scripts live in. This file names `flow-walker.sh` and `flows/task_plan_writing.graph.json` as the owners of the planning loop's routing and caps, and invokes neither."* The table's lead sentence (*"The tokens below … resolve from the adopting repository's `harness.config.json`"*) stays true unchanged.
- [ ] `AUTONOMOUS_FLOW.md`: add *the walker* to the planning-core row's parenthetical list. Add one row for `<scripts_dir>/flow-walker.sh` and `<scripts_dir>/flows/task_plan_writing.graph.json` (*"routing of the planning loop: next node, counter, cap, escalation payload"*), citing them by `<scripts_dir>` path. They ship from the CLI, not from the plugin root, so they are not cited by `${CLAUDE_PLUGIN_ROOT}` (`.claude/context/plugin.md` → `## Citation`).
- [ ] `AUTONOMOUS_FLOW.md`, the git-wrappers row. **Task 5** marks `flow-walker.sh` `agentInvocable: true` in `OUTER_LOOP_SCRIPTS` (`cli/src/generators/outerLoopScripts.ts`), so after this branch the three git wrappers are no longer *"the rows the shipped table marks agent-invocable"*: the walker row this task adds is another (and `scratch-run.sh` already was). Reword the cell so it names the three git wrappers as **the git wrappers among** the rows the shipped table marks agent-invocable, and does not state or imply that they are all of them. Keep the `refresh-branch.sh` sentence as written. Give the walker row added in the bullet above the same note: it is an agent-invocable row, run by the orchestrating session.

**Verification:**

- `bash scripts/check-command-spelling.sh` exits 0.
- `grep -n "iteration caps" plugin/commands/branch-start-plan-autonomous.md plugin/commands/branch-start-plan-semi-autonomous.md plugin/docs/AUTONOMOUS_FLOW.md`: no hit attributes the caps to the core.
- Every file this task writes `<scripts_dir>` into declares it: for each of `plugin/commands/branch-start-plan-autonomous.md`, `plugin/commands/branch-start-plan-semi-autonomous.md` and `plugin/docs/AUTONOMOUS_FLOW.md`, `grep -c '<scripts_dir>' <file>` is non-zero only if `grep -n '^| `<scripts_dir>`' <file>` finds a row in that file's `## Resolved values` table. Read each such row: it names every outer-loop script or data file the body names, and says which it invokes (none, in the two commands).
- `claude plugin validate --strict plugin` exits 0 (gate 1a): the command frontmatter is untouched.
- `grep -n "agent-invocable" plugin/docs/AUTONOMOUS_FLOW.md`: no hit says or implies that the git wrappers are the only rows marked agent-invocable, and the walker's row says it is one.

**Deviations from plan:**

- The walker row names `flow-walker.sh` as the agent-invocable row and the graph as the data it reads, because `OUTER_LOOP_SCRIPTS` marks `task_plan_writing.graph.json` `agentInvocable: false`.
- Evidence downgrade, gate 1a: a direct `claude plugin validate --strict plugin` call was refused by the permission layer. The gate ran inside `bash scripts/test.sh` and failed there. The only findings were six `hooks.PreToolUse` warnings about an unquoted `${CLAUDE_PLUGIN_ROOT}` in `plugin/hooks/hooks.json`, a file this task does not touch. No command frontmatter was changed, so the claim that frontmatter is untouched is backed by `git diff --stat`, not by a clean gate. Gate 6a also failed, only on files outside this diff: the worktree's `.git` pointer file and older `harness-runs/` artifacts.
