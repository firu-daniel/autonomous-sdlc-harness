### Task 2 — Add the repo-relative path rule, quoted command output included, to `## The entry format`

**Goal:** Give `plugin/instructions/improvement_observations_instructions.md` a content rule: every path inside an intake entry is repo-relative, a path in another checkout of the same repository carries a label naming that checkout, no absolute location on the machine ever appears, and quoted command output is rewritten by the same rule without that counting as a change to the evidence.

**Where this task stops.** This task writes the **rule** only. The mechanical **check** that enforces it before the commit — which strings it matches, the command, what happens on a hit — is **Task 3's**, in `## Commit mechanics` of the same file; Task 3 `**Depends on:**` this task and cites this rule by its bold lead, so spell that lead exactly **`Every path in an entry is repo-relative.`** Do not add anything to `## Commit mechanics`, `## Where it runs & the non-blocking rule`, `## Surfacing it in the Done summary` or the **A later round of the same branch appends** paragraph — those are Task 3's sites. The forks and `AUTONOMOUS_FLOW.md` are Task 4's.

### Targets

- `plugin/instructions/improvement_observations_instructions.md` — `## The entry format` only.

**Not a target:** `## Where it is written` → **Write mechanics — absolute write, repo-relative commit.** That paragraph is imported by pointer from `plugin/instructions/clarification_digest_instructions.md` → `## Where it is written` (*"the split … `## Where it is written` states and this file does not restate"*), so any sentence added to it would reach the digest through that import. It is left byte-identical; the argument-versus-content distinction lives inside the intake-only **Every path in an entry is repo-relative.** paragraph instead (Work bullet 4).

**Work:**

- [ ] In the fenced entry block of `## The entry format`, extend the `evidence:` line's gloss with *"— every path repo-relative (below)"*, keeping the block's four lines and their order.
- [ ] After the paragraph that opens *"Keep `evidence` and `hypothesis` **strictly separate.**"*, add a paragraph under the bold lead **`Every path in an entry is repo-relative.`** stating, in this order: (1) it binds every field and every heading, `evidence:` above all; (2) a path under the run's own checkout is written from that checkout's root (`<state_dir>/improvement_observations/<branch>.md`, `.git`); (3) a path in **another checkout of the same repository** — the main checkout, or a sibling worktree — is written from that checkout's root with its machine prefix dropped, followed by a parenthesised label naming the checkout, `(main checkout)` or `(worktree of <other branch>)`; (4) a location outside every checkout is named by what it is (*"the session transcript"*, *"the user-level settings file"*), never by its path; (5) the home directory, any checkout's root and any other absolute location on the machine never appear.
- [ ] In the same paragraph, the quoted-output clause: evidence that quotes command output carrying an absolute path is rewritten by the same rule, and the rewrite **does not count as changing the evidence** — it replaces the machine prefix and adds the label, the path still names the same file, so rule 2 (*"Encountered, not imagined."*) and the falsifiability `evidence` rests on both still hold. Give one generalized worked example, as a class with no project or branch name (the file's own **Worked examples are kept and generalized** rule): a linked worktree's `.git` pointer quoted as `gitdir: <main checkout root>/.git/worktrees/<name>` is written `gitdir: .git/worktrees/<name> (main checkout)`. Close with the reason in one sentence: the file is committed and merged into `<default_branch>`, where a path from one machine means nothing to any other reader and discloses that machine's layout — and name `## Commit mechanics` → **Machine-path check — before every wrapper call.** as where the rule is checked.
- [ ] Close the same **Every path in an entry is repo-relative.** paragraph with one sentence separating argument from content: the absolute `$REPO_ROOT`-anchored path `## Where it is written` → **Write mechanics — absolute write, repo-relative commit.** gives is the file tool's **argument**, never the file's **content** — what this intake file says is bound by this rule. Write it here and **not** in the **Write mechanics** paragraph, which `clarification_digest_instructions.md` imports by pointer and which this task leaves untouched.

**Verification:**

- The file carries no machine path and no adopter literal: `grep -nE '/(Users|home)/' plugin/instructions/improvement_observations_instructions.md` prints nothing, and every placeholder the new text uses (`<state_dir>`, `<branch>`, `<default_branch>`) is already declared in the file's `## Resolved values` or resolved by its **Placeholder resolution.** note — `<other branch>`, `<name>` and `<main checkout root>` are illustrative text inside the example and are spelled so; state that inline if a reviewer could read them as tokens.
- The four fields and seven categories of the entry block are byte-identical except the `evidence:` gloss: `git diff -- plugin/instructions/improvement_observations_instructions.md` shows no other change inside the fenced block.
- The digest's import is untouched: `git diff --exit-code -- plugin/instructions/clarification_digest_instructions.md` exits 0, and `git diff -- plugin/instructions/improvement_observations_instructions.md` shows no line changed inside the **Write mechanics — absolute write, repo-relative commit.** paragraph — so the pointer in `clarification_digest_instructions.md` → `## Where it is written` still resolves to a paragraph that states only the absolute-write / relative-commit split.
- `grep -rn 'Every path in an entry is repo-relative' plugin/` has no hit outside this file, and inside it the lead is written as a bold lead once and cited, not repeated, anywhere else.
- `bash scripts/run-gates.sh` fails no gate beyond the baseline the story index `## Context` records; `1a plugin manifest`'s failure text is still only the six unquoted-`${CLAUDE_PLUGIN_ROOT}` warnings.
