### Task 2 — Qualify the analyze command in `init`, the stub footers and the `flat` fallback warning, re-measure the headless leg, and update `init.test.mjs`

**Goal:** Every analyze-command spelling that `init` prints, or writes into an adopter's `.claude/` files, becomes `/autonomous-sdlc-harness:harness-analyze`, taken from `PLUGIN_NAME`. The suite that pins those strings moves with them. The headless `claude -p` leg that `init.ts`'s comments rest on is re-measured under the current plugin slug and recorded there.

**Where this task stops.** This task owns every `init.test.mjs` assertion, including the one that pins `signals.ts`'s `flat` fallback warning. That is why `signals.ts` belongs to this task and not to Task 3. `presets.ts` and `stack-presets.test.mjs` are Task 3's. `doctor`'s own `ANALYZE_COMMAND` and `doctor.test.mjs` are Task 4's. The comment-only modules and the `cli/templates/claude/` text files are Task 5's. This task supplies the `pluginName` render value that Task 5's `CLAUDE.md` template consumes. Task 5 also appends one test case to `init.test.mjs` after this task lands; it does not touch any assertion this task edits. Task 11 copies the headless-leg record written here into `docs/development.md` §6. This task changes no output except the spelling, with **one intended wording change**: `init`'s paste-line introducing sentence stops explaining why the paste line and step 2 are spelled differently, because they no longer are (the `pasteReason` bullet below). No step is added, dropped or reordered. Task 10 rewrites the `docs/cli.md` sentence that describes that introducing sentence, so it matches what this task leaves in `init.ts`.

### Targets

- `cli/src/commands/init.ts`
- `cli/src/generators/claudeContext.ts`
- `cli/src/detect/signals.ts`
- `cli/test/init.test.mjs`

**Work:**

- [ ] **`cli/src/commands/init.ts`.**
  - Define `ANALYZE_COMMAND` as `` `/${PLUGIN_NAME}:${ANALYZE_COMMAND_NAME}` ``. Remove `ANALYZE_COMMAND_QUALIFIED`: it would now be a second constant holding the same value. Have `ANALYZE_INVOCATION` use `ANALYZE_COMMAND`, and repoint every `{@link ANALYZE_COMMAND_QUALIFIED}`, including the doc comments on the closing pointer and the inline comment beside `pasteable`.
  - Rewrite the doc comment on `ANALYZE_COMMAND` so it no longer argues that picker text stays bare and pasted text is qualified. It now says one spelling serves both uses, and cites `docs/development.md` → `## 5. Verifying a change` → gate 6, where Task 11 records the spelling rule.
  - Qualify the module header's `/harness-analyze` mention.
  - **The paste intro in `reportNextSteps`.** Remove the `pasteReason` constant (*" It is spelled with the plugin prefix because a first message is matched exactly, where a session's command picker matches the name above."*) and drop `${pasteReason}` from both arms of `pasteIntro`. Once step 2 prints the same qualified name, the sentence explains a difference that no longer exists. Keep `pasteUnconfirmed`'s text unchanged: it says the first-message form is not confirmed and the typed route is, and both statements still hold. Rewrite the inline comment above `pasteUnconfirmed` so it no longer links `ANALYZE_COMMAND_QUALIFIED`. It says the name typed into an open session is the measured route, and cites `{@link ANALYZE_COMMAND}` and `docs/analyze.md` §9.
  - **The `reportNextSteps` doc comment.** Rewrite the passage *"It names {@link ANALYZE_COMMAND_QUALIFIED} where step 2 above it names {@link ANALYZE_COMMAND}, and the difference is the two paths rather than an inconsistency: … would read as a typo."* It now says the paste line and step 2 name the same `{@link ANALYZE_COMMAND}` spelling, so the introducing sentence gives no spelling reason. Also repoint *"succeeds is not established ({@link ANALYZE_COMMAND_QUALIFIED})"* to `{@link ANALYZE_COMMAND}`. Keep every other paragraph of that comment as it is.
- [ ] **Headless leg, re-measured.** Write `harness-runs/scratch/headless_analyze_probe.sh`. From the checkout root it prints `claude --version`. It then runs `claude -p "/autonomous-sdlc-harness:harness-analyze" --permission-mode plan --max-turns 2 --output-format stream-json --verbose` and the same with `"/harness-analyze"`, saving each stream to a file. For each run it prints the exit code, the final `result` event's `is_error` and verbatim first line, and whether the `system`/`init` event lists the name.

  Run it with `bash scripts/scratch-run.sh harness-runs/scratch/headless_analyze_probe.sh`. Add a paragraph to the `ANALYZE_COMMAND` doc comment that opens exactly `Headless first-message leg, re-measured:`. It states the version, both command lines, each exit code and each verbatim message. If the call is refused or `claude` is missing, it says `not re-run` and quotes the refusal verbatim. Quote the bare spelling literally: Task 13 exempts that quoted line. Delete the probe and its streams afterwards.
- [ ] **`cli/src/generators/claudeContext.ts`.**
  - Define `ANALYZE_COMMAND` as `` `/${PLUGIN_NAME}:harness-analyze` `` (`PLUGIN_NAME` is already imported from `./projectSettings.js`). The stub footers, the setup-pending banner (`PLUGIN_ORIGIN_CLAUSE`, `accepted`, `declined`), the reserved-target collision message and the `.bak` note all take it from there. Qualify the `/harness-analyze` in the doc comment near the layer-path paragraph (*"A path a `/harness-analyze` layer-profile revision newly names"*).
  - **The `{{pluginName}}` value Task 5 consumes.** Add `pluginName: PLUGIN_NAME` to the variables of the `render('CLAUDE.md', { projectName, setupBanner, routingRows, stateDir })` call, so it reads `render('CLAUDE.md', { projectName, setupBanner, routingRows, stateDir, pluginName: PLUGIN_NAME })`. This is the same token name and the same value the sibling `render('harness-task-offer.md', { pluginName: PLUGIN_NAME })` call already passes. It is safe before Task 5 lands: `renderTemplate` in `cli/src/core/templating.ts` throws only for a token the template uses and the caller does not supply, never for a supplied value the template does not use. Task 5 then spells `/{{pluginName}}:…` in `cli/templates/claude/CLAUDE.md`. This task does not edit that template.
- [ ] **`cli/src/detect/signals.ts`.** Build `FLAT_FALLBACK_WARNING` as a template literal from `PLUGIN_NAME`: `` `unrecognised layout — using the \`flat\` preset; run \`/${PLUGIN_NAME}:harness-analyze\` to refine the layer profile` ``. Add `import { PLUGIN_NAME } from '../generators/projectSettings.js';` to the module's import block, exactly as Task 3 does for `detect/presets.ts`, so `projectSettings.ts` stays the single owner of the plugin name. Do not type the plugin name into the string. `projectSettings.ts` imports nothing from `detect/`, so the import creates no cycle. Qualify the four doc and inline comments that name the command bare. Comments may carry the literal `autonomous-sdlc-harness`.
- [ ] **`cli/test/init.test.mjs`.**
  - Set `ANALYZE_COMMAND` to `'/autonomous-sdlc-harness:harness-analyze'` and drop `QUALIFIED_ANALYZE_COMMAND`, pointing `PASTEABLE_INVOCATION` at `ANALYZE_COMMAND`.
  - Respell `assert.match(stderr, /\/harness-analyze/)` in the `flat` case to match the qualified form.
  - Respell `ANALYZE_FIRST_REMEDY`, the `note.indexOf('/harness-analyze', …)` literal, and the comments that name the command (the `detection.review` record comment, the reserved-target test's header).
  - Keep each test's rule and header. Only the spelling it quotes changes.
  - The paste-intro assertions pin `PASTE_PRECONDITION` (`'Once the plugin resolves'`) and `PASTE_UNCONFIRMED` (`'not confirmed on the version measured here'`), and neither is affected by removing `pasteReason`. If any case turns out to pin `pasteReason`'s text, update it in this task.

**Verification:**

- `bash scripts/test.sh` exits 0, and the `flat`, reserved-target, closing-pointer and layer-gap cases all pass against the qualified strings.
- `grep -n '/harness-analyze' cli/src/commands/init.ts cli/src/generators/claudeContext.ts cli/src/detect/signals.ts cli/test/init.test.mjs` prints only lines inside the `Headless first-message leg, re-measured:` paragraph of `init.ts`. The qualified form does not contain that substring, so any other line is a site this task missed.
- In a throwaway fixture a test builds, a generated conventions stub's footer reads `` `/autonomous-sdlc-harness:harness-analyze <target>` ``. The existing footer assertion, now on the qualified constant, is that check.
- `grep -n ANALYZE_COMMAND_QUALIFIED cli/src/commands/init.ts` returns nothing.
- `grep -n "matches the name above" cli/src/commands/init.ts` returns nothing, and neither does `grep -n "two paths rather than an inconsistency" cli/src/commands/init.ts`.
- `grep -n "not confirmed on the version measured here" cli/src/commands/init.ts` still prints the `pasteUnconfirmed` line.
- `grep -n "autonomous-sdlc-harness" cli/src/detect/signals.ts` prints only comment lines. A hit on the `FLAT_FALLBACK_WARNING` line means the name was retyped instead of imported.
- `grep -n "pluginName: PLUGIN_NAME" cli/src/generators/claudeContext.ts` prints the `render('CLAUDE.md', …)` call as well as the existing `harness-task-offer.md` call.
