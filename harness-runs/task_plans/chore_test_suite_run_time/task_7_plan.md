### Task 7 — Measure the before and after, prove nothing was lost, and record both in `docs/development.md` gate 4

**Goal:** Produce the task prompt's acceptance evidence — `npm test` and `bash scripts/run-gates.sh` timed before and after, on this machine and on the core count chosen to model the remote runners, both **measured**; the same tests passing before and after; three consecutive green runs — and write the figures where this repository keeps a measured fact: `docs/development.md` → `## 5. Verifying a change` → **Gate 4**; and keep `docs/cli.md` → `## 10. How this is tested`, which restates the fixture helper's numbered choices, complete.

**Depends on:** Tasks 1–6, all landed. From them this task uses: `scripts/measure-suite.sh` (Tasks 5–6) — `bash scripts/measure-suite.sh [--ref <commit>] [--runs <k>] [--cpus <n>]`, printing one line per timed run, `measure-suite: <host|container> cpus=<n> ref=<12-char sha> <npm-test|run-gates> run <i>/<k>: <seconds> s, exit <status>`, plus each `run-gates` run's closing summary, and exiting `0` all runs reported · `1` setup failed · `2` bad usage · `3` no container runtime; `cli/test/helpers/concurrency.mjs` (Task 2) — the variable `HARNESS_TEST_CONCURRENCY`, whose value `1` runs every case in series; the template-copied fixtures (Task 1); and the three concurrent suites (Tasks 2–4) with one `init` case running after its suite.

**The modelled core count: 4, with 2 measured beside it.** GitHub's standard hosted Linux runner has **4 vCPUs for a public repository** — this one is public — and **2 for a private one**. This task measures at `--cpus 4` as the acceptance figure and at `--cpus 2` as the second row, so a reader who moves the repository private, or an adopter on a private runner, has a measured figure rather than an estimate. Record, next to the figures, that the container runs arm64 Linux on this Apple-silicon host where the hosted runners are x86-64: the figures model the **core count**, not the runner's per-core speed.

**This task writes no new mechanism.** If a measurement shows a flake or a regression, stop and report it as the task's blocker naming the run and the failing case; do not tune a bound or skip a case to make the record look green.

### Targets

- `docs/development.md` — `## 5. Verifying a change` → the **Gate 4** block (the paragraph opening *"Every test builds its **own** fixture repository under the system temp directory"*, and a new paragraph after it).
- `docs/cli.md` — `## 10. How this is tested`: the list introduced by *"Four choices in that harness are worth knowing before they cost a debugging round"*, which restates `cli/test/helpers/fixture.mjs`'s numbered header choices and so must gain the fifth one Task 1 added there.

**Work:**

- [ ] Find the **before** commit: `git merge-base HEAD dev` gives the branch point `M`; then `git log --reverse --format=%H M..HEAD -- cli/test/helpers/fixture.mjs` — its first line is Task 1's commit, and the before commit is that SHA followed by `^`. Measure it: `bash scripts/measure-suite.sh --ref <before> --runs 1`, then the same with `--cpus 4` and with `--cpus 2`. If `--cpus` exits `3`, stop and report the story index's `Manual setup required` step as the blocker — never substitute a host-only figure for a restricted one.
- [ ] Measure the **after** at `HEAD`: `bash scripts/measure-suite.sh --runs 3` and `bash scripts/measure-suite.sh --cpus 4 --runs 3`, then `--cpus 2 --runs 1`. The three `npm-test` lines of each `--runs 3` run must all show `exit 0` — those six runs are the three-consecutive-green evidence at both core counts — and every `run-gates` summary must name no failure the before run's summary did not name.
- [ ] Prove nothing was lost: from `cli/`, run `node --test --test-reporter=spec test/` at the before commit (in the worktree `measure-suite.sh` would use, or a `git worktree add --detach` of your own, removed with `git worktree remove --force`) and at `HEAD`; compare `ℹ tests` / `ℹ pass`, and diff the sorted case names with durations stripped — the only additions are `fixture-template.test.mjs`'s cases and the three `describe` suite lines, and nothing is removed. Then compare `git grep -c "assert\." <before> -- cli/test` with `git grep -c "assert\." HEAD -- cli/test`: every file present at the before commit reports the same count.
- [ ] `docs/development.md`, Gate 4: amend the *"Every test builds its **own** fixture repository…"* sentence so it stays true — each test's repository is its own **copy** of a seeded template its test process builds once — and add one paragraph, **Run time, measured.**, stating: the date, the host facts from the preamble, and the commands, each in its own fenced block, one per line (the ledger's `## Adopter-facing documentation` rule); a table of the before and after seconds for `npm test` and `run-gates.sh` at host / `--cpus 4` / `--cpus 2`, labelled by the commit each was measured at; the modelled core count and why, as stated above; what the time is spent on (every case is a serial chain of subprocesses — the CLI's own start and git probes, and before Task 1 about fourteen git processes per fixture — and runs CPU-bound, so wall time at `n` cores is bounded below by the suite's CPU time over `n`); that three files run their cases concurrently under a bound; and the switch that turns it off, in a fenced block: `HARNESS_TEST_CONCURRENCY=1 npm test`. Write every figure as the command's own output rounded to whole seconds, with its commit, and **no path from the measurement output** — the worktree and archive paths sit under the system temp directory and gate 6a greps for the home directory.
- [ ] `docs/cli.md` → `## 10. How this is tested`: change *"Four choices in that harness"* to five and add the bullet for Task 1's header choice — the seeded repository is copied from a template each test process builds once, never handed to a test, with the origin's one stored path rewritten per copy — in the list's existing register (a bold lead clause, then why). Leave the paragraph *"Every test therefore **builds its own throwaway fixture**…"* as it is: each test still gets a repository of its own and nothing is shared, which a copy keeps true.

**Verification:**

- `docs/development.md` Gate 4 carries a before and an after for both commands at host and at `--cpus 4`, each figure traceable to a `measure-suite:` line of this task's runs (quote those lines in the implementer's return summary).
- Re-run this branch's scope-register derivation command (story index → `## Scope register`) and confirm every site it reaches is a register row and the only `change` rows are this task's.
- `bash scripts/run-gates.sh` prints no failure its run at the before commit did not print; `bash scripts/check-llms-txt.sh` exits 0 (no link was added, so none can break).
- `grep -n "$HOME" docs/development.md` prints nothing.
