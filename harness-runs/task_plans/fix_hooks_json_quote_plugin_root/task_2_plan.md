### Task 2 — Record the quoting requirement as a measured manifest fact in `docs/development.md` and run the acceptance checks

**Goal:** Record, as a measured fact in `docs/development.md`, the rule that a hook `command` string must double-quote `${CLAUDE_PLUGIN_ROOT}` with its path: what the validator said, on which Claude Code version, which form was chosen and why, and what a plugin loaded from a path containing a space does after the change. Then run the task prompt's three acceptance checks against the tree Task 1 left.

**Depends on:** Task 1. It rewrites each of the six commands in `plugin/hooks/hooks.json` to the JSON string `"bash \"${CLAUDE_PLUGIN_ROOT}/hooks/<guard>.sh\""`, which the shell sees as `bash "<plugin root>/hooks/<guard>.sh"`. It extends `plugin/hooks/README.md` → `## Registration, and why it lives here rather than in generated settings` with the same rule and only a short rule-level reason for shell form, pointing at this file's §3 for the exec-form findings and the measured message. This task records that form under that spelling, and it is the **source** of the exec-form findings and the decision of record: `docs/` is their only home (`.claude/context/conventions.md` → `### Where a new responsibility goes`, the row "a measured fact or a decision of record"), and the README bullet is the copy that points here. It does not edit `plugin/`: if the form recorded here and the one in `hooks.json` disagree, that is a finding against Task 1, not something to fix here.

**How this task's implementer reads the conventions.** This is the catch-all layer, so read `.claude/context/conventions.md` and also `.claude/context/plugin.md`, because the fact recorded here describes a plugin manifest. Three rules bind this edit. First, `## Documents of record`: *"A measured fact states what was measured, the command and the exact message"*. Second, `## The testing bar`: the self-containment gate means nothing in this tree may name a location on the machine that wrote it. Third, `harness-runs/lessons.md` → `## Adopter-facing documentation`: every command a reader is meant to run sits in its own fenced block, one per line.

### Targets

- `docs/development.md` → `## 3. Manifest facts a contributor must not rediscover` → the bullet opening "**Plugin hooks append to an adopter's own hooks; they do not override them.**"
- `docs/development.md` → `## 5. Verifying a change` → the **Gate 1 — manifests.** paragraph.

**Work:** run the two probe bullets (validator measurement and in-session check) and the exec-form establishment bullet first. The §3 and §5 bullets record what those established and printed, and nothing else.

- [ ] **Validator measurement probe, before and after the change.** This task measures both validator messages itself; it does not take them from the task prompt or from Task 1's return, which is not an input to this task. Write `harness-runs/scratch/hooks_validate_probe.mjs`, which is gitignored and never committed, and run it with `bash scripts/scratch-run.sh harness-runs/scratch/hooks_validate_probe.mjs`. Every external call is a `child_process.spawnSync` / `execFileSync` with a fixed argument vector (use `spawnSync` for the validator so a non-zero exit still yields its stdout and stderr), and temp trees are created with `fs.mkdtempSync` / `fs.cpSync` and removed with `fs.rmSync`, in-process. The probe:
  - runs `claude --version` and records it;
  - **pre-change run:** resolves the commit this branch forked from with `git merge-base HEAD main`, copies `plugin/` to a temp directory, overwrites the copy's `hooks/hooks.json` with the output of `git show <base>:plugin/hooks/hooks.json`, runs `claude plugin validate --strict <copy>`, and records its exit status, its full combined stdout and stderr verbatim (the copy's path replaced with the placeholder `<tmp>/plugin`), and the count of warnings it reports;
  - **post-change run:** runs `claude plugin validate --strict plugin` against the working tree Task 1 left, and records its exit status and full combined stdout and stderr verbatim;
  - removes the temp copy, and prints that it did.
- [ ] **Establish the exec-form question.** The task prompt asks what exec form would change, and this task is where that is answered. Read the installed CLI's own answer: `claude --version` (from the validator probe), `claude plugin validate --help` (add it to the validator probe as a fixed-argv call), the validator's warning text from the pre-change run, and the hooks documentation the installed CLI ships or links to. Check three things: how stdin reaches the script under exec form; whether the exit-status and stdout contract each guard relies on (`plugin/hooks/README.md` → the outcome table: at most one JSON object, `deny` / `ask` / `allow` or silence) is unchanged; and which Claude Code versions accept exec form. Note what you settle, with its source, and what you cannot. **This does not reopen the decision.** The quoted shell form stays because it changes nothing but word boundaries, is right whether the runtime substitutes the token or the shell expands it, and needs no manifest field beyond the three the README allows. Where exec form cannot be settled, the prompt itself says to quote.
- [ ] §3, the "Plugin hooks append…" bullet: **extend this bullet, do not add a new one.** Its closing sentence already states that `${CLAUDE_PLUGIN_ROOT}` expands inside a hook `command` string, and the section opens "Five measured behaviours and one shipped contract", which a new bullet would make wrong. Add a follow-on paragraph to that bullet, in the style of the neighbouring measured bullets, recording:
  - the Claude Code version measured, from the validator probe's `claude --version`;
  - the command `claude plugin validate --strict plugin` and the warning it gave on the unquoted form: **the measured output of the probe's pre-change run**, quoted verbatim with the placeholder path, with the number of warnings it reported and that `--strict` made it fail gate `1a`. The task prompt's `## The defect` quote ("Shell command uses ${CLAUDE_PLUGIN_ROOT} without quotes … Wrap the placeholder in double quotes, or use exec form") is elided and serves only as a cross-check: if the measured message differs from it, record the measured one and say in your return that they differ;
  - the chosen form `bash "${CLAUDE_PLUGIN_ROOT}/hooks/<guard>.sh"`, why shell form was kept over exec form, and what this task's exec-form bullet above established (stdin, the exit and output contract, version acceptance, each with its source) and could not settle. This task is the source of all of it; state no exec-form finding that bullet did not establish;
  - the validator's output after the change: **the measured output of the probe's post-change run**, verbatim;
  - the spaced-path result from the in-session check below, with every path written as a placeholder (`<tmp>/harness plugin copy/plugin`, `<fixture>`) and never as a real absolute path.
- [ ] §5, the Gate 1 paragraph: add one sentence saying which of the two commands grades `hooks/hooks.json`'s command strings. `claude plugin validate --strict plugin` does. `claude plugin validate --strict .` passed on the unquoted file, as the `fix_gate_6a_worktree_git_file` run recorded with `1a` its only failure. Re-confirm that claim with this task's own `1b` run and state it as measured. Leave the two fenced command lines and "Both must report `Validation passed` with zero warnings" unchanged.
- [ ] **In-session acceptance check (task prompt `## Acceptance` item 3).** Write `harness-runs/scratch/hooks_session_probe.mjs`, which is gitignored and never committed, and run it with `bash scripts/scratch-run.sh harness-runs/scratch/hooks_session_probe.mjs`. It uses fixed-argv `child_process` calls and in-process `fs` removal only. The probe does four things:
  - Copy `plugin/` to a temp directory whose path contains a space.
  - Build a throwaway fixture repository outside this checkout: a `harness.config.json` inside the guards' jurisdiction (`version: 1`, `defaultBranch: "main"`, `stateDir`, `layers` with a `.` entry, `commands`), on branch `main`, one commit, plus a branch `feat_x`.
  - From that fixture, start a non-interactive `claude -p` session with the copy loaded via `--plugin-dir <copy>`. Instruct it to run exactly one Bash command, `git merge feat_x`, while HEAD is on `main`.
  - Capture the result, then remove the copy and the fixture.

  The guard to observe is `autonomous-protected-branch-guard.sh`, which denies a merge issued while HEAD is on a protected branch (`plugin/hooks/README.md` → the outcome table). The prompt's own example, "refusing a commit", describes the commit guard, which answers `ask`, not a refusal.

  **Attribute the refusal to the copy.** This plugin is also enabled at user level on this machine, so an unisolated session would show the *installed* copy denying, which proves nothing about the spaced path. Disable the installed plugin for that one session (for example a `--settings` value setting `enabledPlugins` for `autonomous-sdlc-harness@autonomous-sdlc-harness` to `false`, or whatever the installed CLI's `--help` offers), and show attribution. If you cannot isolate the session, record in the §3 paragraph that the in-session leg was not attributed and why. Do not present the shell-level emulation from Task 1 as the acceptance evidence (`harness-runs/lessons.md` → `## Evidence and measurement`), and report the gap in your return.

**Verification:**

- The validator probe's output shows a non-zero pre-change exit with the unquoted-placeholder warning, a zero post-change exit whose combined output contains no line mentioning a warning or quotes, and that its temp copy was removed. The §3 paragraph's two quoted messages match that output verbatim, apart from the `<tmp>/plugin` placeholder.
- Run `bash scripts/run-gates.sh` without a pipe from this worktree. It prints `ok    1a plugin manifest` and `ok    1b marketplace manifest`. Any other failing gate is one the pre-change tree also failed, not a new one. Name each remaining failure in your return with the reason it is not this branch's. `6a no machine paths` must pass, so the recorded measurement names no machine location.
- The in-session probe's output shows the `deny` from `autonomous-protected-branch-guard.sh` coming from the spaced-path copy, and shows that `git merge feat_x` did not run. Put the verbatim output (paths replaced with the placeholders above) in your return, and confirm that both temp trees were removed.
- The quoted **form** `bash "${CLAUDE_PLUGIN_ROOT}/hooks/<guard>.sh"` is byte-identical across all three files: the §3 paragraph, `plugin/hooks/hooks.json` (less JSON escaping) and `plugin/hooks/README.md`'s bullet. Check that by reading all three. The exec-form findings live only in §3; the README bullet's short reason must not contradict them, and if it does, that is a finding against Task 1, not something to fix here.
- Re-run the story index's `## Scope register` derivation entries 1–3 verbatim. Every hit falls on a row of that register, and the new `without quotes` / `exec form` hits fall only on the register's `change` rows (2, 3, 4 and 5).
