### Task 25 — Restate `ARCHITECTURE.md`'s MCP position in §6, §7 and §10 on the evidence after this branch

**Goal:** `ARCHITECTURE.md` states, under **[shipped]** markers, that this repository authors no MCP server, declares its servers in one template, grants an `mcp__` name to one agent, and is an MCP consumer only. This branch makes each of those false. Restate them on the evidence in the tree once Tasks 8–10 and 15–16 have landed: this repository now authors **one** opt-in, local, read-only MCP server for its own agents, the browser servers stay the consumed surface, and neither of §10's candidate capabilities is built.

**Depends on:**

- **Task 8:** `cli/src/retrieval/server.ts` exports `DOCS_SERVER_NAME = 'harness-docs'`, `SEARCH_TOOL_NAME = 'search_docs'` and `SEARCH_TOOL_PERMISSION = 'mcp__harness-docs__search_docs'`, and `serveDocs(...)`, a stdio server built on the SDK's low-level `Server`, loaded through `loadRetrievalModule`. It exposes one read-only tool, `search_docs(query, k)`, and writes nothing to stdout but the transport. `docs serve` is the sub-verb.
- **Task 9:** `cli/templates/scripts/docs-search-server.sh`, the outer-loop launcher the server entry names. It `exec`s the machine-shared runtime's `docs serve`, and the runtime is installed at the CLI's own version (Task 12).
- **Task 10:** the second server template `cli/templates/repo/mcp.retrieval.json` (`harness-docs`, `{"type": "stdio", "command": "bash", "args": ["{{docsSearchServerPath}}"]}`) and the second profile fragment `cli/templates/claude/settings.autonomous.retrieval.json` (`enabledMcpjsonServers: ["harness-docs"]`, allow `mcp__harness-docs__search_docs`), exported from `cli/src/generators/permissionProfile.ts` as `RETRIEVAL_TEMPLATE_PATH`. Both are written only when `retrievalApplies(config)` holds (Task 1: `phases.docs` and `docs.retrieval` both `true`). `cli/src/generators/repoRoot.ts` gains `retrievalWiring` and `assertRetrievalServersMatchProfile`, a second reader of the `mcpServers` key.
- **Tasks 15–16:** ten agent definitions (`architecture-reviewer`, `branch-reviewer`, `business-parity-reviewer`, `review-plan-reviewer`, `skeptic-reviewer`, `task-plan-writer`, `task-plan-reviewer`, `ui-tests-plan-reviewer`, `ui-tests-plan-writer`, `user-review-fix-plan-writer`) carry `mcp__harness-docs__search_docs` in their `tools:` front matter, always, whatever the configuration.
- **Task 21:** `docs/retrieval.md` is the design document and says the feature is off by default, opt-in and not yet measured.
- **Task 23:** it changes `ARCHITECTURE.md`'s *"the four subcommands"* to *"the five subcommands"* and nothing else in the file. This task edits the file after it.

**Where this task stops.** It restates what the tree now does. It does not argue that the docs-search server fulfils §10's *The argument for MCP as the open surface*: that server was built to serve this harness's own agents, and neither candidate capability §10 names (the interactive-test account and fixture handling, the two ledgers) is built. It schedules nothing and cites the roadmap row by title (*Docs-catalog retrieval*) only, never by item number.

### Targets

- `ARCHITECTURE.md` §6 — the **The permission model** inventory row (story index `## Scope register` row 59).
- `ARCHITECTURE.md` §7 → `### What this release does not build` — the **No MCP server.** bullet (row 60).
- `ARCHITECTURE.md` §10 → `### What MCP is here today` — the opening paragraph, the re-derive greps and the sentence interpreting them, the **Conditional** and **Reachable by one agent** bullets, and the closing sentence (rows 61–65).
- `ARCHITECTURE.md` §10 → `### What the claim would require` — its opening and closing sentences (row 66).

**Work:**

- [ ] **§6, the permission-model row.** Its first cell names `settings.autonomous.json` and `settings.autonomous.qa.json` and the constants `TEMPLATE_PATH`, `QA_TEMPLATE_PATH`, `PROFILE_PATH`. Add `settings.autonomous.retrieval.json` and `RETRIEVAL_TEMPLATE_PATH`. Change no other cell.
- [ ] **§7, the *No MCP server* bullet.** Keep the section's count of **six** boundaries and the bullet's pointer to §10. Restate it as the boundary that still holds, for example **"No MCP server for another runtime."**: **[shipped]** the tree authors one server, the opt-in docs-retrieval search that `docs serve` runs for this harness's own agents, and publishes no capability as a server for a runtime other than the one these prompts run in. Every claim sentence opens with a status marker (§2).
- [ ] **§10, `### What MCP is here today`, opening and greps.** Replace *"This repository **authors no MCP server**"* with a **[shipped]** statement that it authors one: `harness-docs`, one read-only tool `search_docs`, over `stdio`, written only when `phases.docs` and `docs.retrieval` are both `true`, launched by the repository's own `<scriptsDir>/docs-search-server.sh`, and versioned with the CLI package, with a link to [`docs/retrieval.md`](docs/retrieval.md). Keep the consumed browser servers' sentence, and replace *"declared in one template"* with the two templates, `cli/templates/repo/mcp.json` and `cli/templates/repo/mcp.retrieval.json`, which `init` merges into one `.mcp.json`. In the fenced block, make the first two greps name both templates and both fragments (`grep -n '"type": "stdio"' cli/templates/repo/mcp.json cli/templates/repo/mcp.retrieval.json` and `grep -n '"mcp__' cli/templates/claude/settings.autonomous.qa.json cli/templates/claude/settings.autonomous.retrieval.json`), one command per line. Keep the third. **Run all the greps** after the edit, and rewrite the interpreting sentence so that every line they print is covered by a sentence (§10's own rule: *"a hit no sentence below covers means this section takes another sentence"*). The third grep now also prints `repoRoot.ts`'s retrieval reader, and its *"no server this project implements"* clause has to say that the server is found in `cli/src/retrieval/server.ts`, not by that grep. Keep *"No expected hit count is written here"*.
- [ ] **§10, the four properties and the closing.** **Pinned deliberately**: say it describes the consumed browser servers. The docs server is not pinned by an adopter, because it installs at the CLI's own version. **Conditional**: an adopter who drives no browser still gets MCP wiring when retrieval is on, so restate the bullet with both conditions, each gating its template and its profile half in the same run. **Started by one key**: add that the retrieval half follows the same grammar, with no browser-specific claim changed. **Reachable by one agent**: restate from `grep -ln "^tools:.*mcp__" plugin/agents/*.md`, run after Tasks 15–16. The browser tools stay in `qa-tester.md` alone, `mcp__harness-docs__search_docs` is in the ten granted definitions, and each agent's contract text says to ignore the tool when retrieval is off. Keep the per-agent-allowlist and no-namespace-`deny` reasoning, and keep the checker sentence (`doctor` starts no server). Restate the closing sentence as a **[shipped]** position that is true of the tree, for example: *"MCP is a consumption surface for one optional phase, plus one opt-in, local, read-only search server this project authors for its own agents, and not yet a capability published for another runtime."*
- [ ] **§10, `### What the claim would require`.** Keep both candidates and their **[designed]** status. In the opening, make *"One thing this release does not do"* exclude the docs-search server explicitly: it serves this harness's own agents and was not reshaped from one of the capabilities below. In the closing paragraph, keep *"Neither is built"*, and point *"the sentence closing `### What MCP is here today`"* at the restated sentence so the two still agree word for word. Keep §7's cross-reference. Change nothing in `### The argument for MCP as the open surface`.

**Verification:**

- Re-run story index derivation entry 7 verbatim: `git grep -n -i -E "no MCP server|authors? no MCP|publishes none|consumption surface|carries an .mcp__. name|declared in one template" -- . ":!harness-runs" ":!examples/notes-app" ":!cli/src" ":!cli/test"`. Every hit is either a `no-change` row of the `## Scope register` (rows 67 and 68) or a sentence this task restated whose claim the tree now bears out. No hit repeats *"authors no MCP server"*, *"publishes none"* or *"declared in one template"*.
- Run the §10 fenced greps and the `tools:` grep exactly as the section now writes them. Match every printed line to the sentence that covers it, and every file those sentences name to a printed line. Compare by name, not by count.
- `grep -n "Six boundaries" ARCHITECTURE.md` still matches, and the bullets under `### What this release does not build` still match that number word.
- Every sentence this task added or changed that makes a claim about the system opens with `**[shipped]**`, `**[designed]**` or `**[external]**` (`ARCHITECTURE.md` §2). Check with `git diff -U0 ARCHITECTURE.md`.
- `git diff ARCHITECTURE.md` touches only §6's permission-model row, §7's one bullet and §10's two sub-sections named above, plus Task 23's *"five subcommands"* line. `git grep -n -E "items? [0-9]+" -- ARCHITECTURE.md` reports no line this task added.
- `bash scripts/run-gates.sh` passes gate 6a (no machine paths): the new text names `<scriptsDir>/docs-search-server.sh` and `docs serve`, never a cache path.
