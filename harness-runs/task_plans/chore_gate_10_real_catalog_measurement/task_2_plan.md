### Task 2 — Rewrite `docs/retrieval.md`: strike the rule, make the decision standing, restate the cost as the refresh, record gate 10 as run

**Goal:** Bring `docs/retrieval.md` — the retrieval design document and the home of its measured facts — into agreement with what gate 10 measured and with what the timeout research settled. Four of its sections currently state things that are false: the decision paragraph rests on a 60-second rule that does not exist, item (d) describes gate 10 as *partly satisfied* with three legs unrun, `## Still open` carries two questions leg (iv) and leg (v) have now answered, and `## What it costs` publishes the 244 kB-per-chunk extrapolation. This task owns every edit to that file; no other task touches it.

**Depends on:** **Task 1**, which makes `docs/retrieval-eval-results.md` → `## Cold build and index size` the file of record for every real-catalog number — the four leg (iii) wall times and their thermal cause, the per-chunk refresh cost, the two-route cross-check, the phase breakdown and the index-size fit. **This task cites that section and restates no figure from it.** Task 1 also owns `docs/retrieval-eval-results.md` → `## Threshold calibration` → `### The limit on this calibration`, where it records leg (iv)'s confirmation of the abstain threshold on the real catalog — so when this task retires `## Still open`'s *"The abstain threshold on a real catalog"* entry and points the reader at `## Threshold calibration` as the record, that record says the confirming run was made. It stays another file's to write: do not edit it here. The prompt's item 1 is explicit: the results file *"is the file of record and the only place the numbers are written; every other site cites it rather than restating it."* The two numbers this task may name are the ones it needs to make a sentence true — the corpus size (156 files, 1,960 chunks) and leg (i)'s 128 s setup time, which is a figure the results file does not own.

**Four prohibitions.** **No machine-local path enters the document.** Record the corpus by its **commit, size and character** — `010c50e`, 156 files / 1,960 chunks, a private real documentation catalog held outside this checkout — and **never by its filesystem path**: no absolute path under `/Users/…` or `/home/…` may enter `docs/retrieval.md`, and the `strings -a` citation is written in its tilde form `~/.local/share/claude/versions/2.1.278` and **never expanded**. The gate is `scripts/run-gates.sh` `6a no machine paths` (`.claude/context/conventions.md` → `## The testing bar`; `docs/development.md` → **Gate 6 — self-containment**), which is **already red in this checkout by design**, so a hit added here does not read as a *new* failure and the independent `grep` in **Verification** is the real check. Then: this task changes **no script** — `scripts/setup-worktree.sh` and `cli/templates/scripts/setup-worktree.sh` must stay byte-identical to branch point `d31e6c8`, because warming the index at worktree setup is **cancelled work, not deferred work**, and no later branch makes the move either. It changes **no agent's `tools:` allowlist** under `plugin/agents/` — establishing who holds the `search_docs` grant is this task's work; changing who holds it is not. And it introduces **no single bandwidth number** and no wall-clock figure derived from one.

### Targets

- `docs/retrieval.md` → `## How it fits together` → the two paragraphs and the figure list under **Why `setup-worktree.sh` does not warm the index.**
- `docs/retrieval.md` → `## Measured, and how` → item **(d) The real models end to end.**, including the *"Three legs remain unrun"* list beneath it.
- `docs/retrieval.md` → `## Still open`.
- `docs/retrieval.md` → `## What it costs`, the index bullet.

**Source:** `harness-runs/task_prompts/chore_gate_10_real_catalog_measurement_task_prompt.md` — `## The figures block — gate 10 RUN IN FULL, legs (i)-(vi)` for the leg outputs this task records, and `## The timeout question, already settled` for the research. **Nothing is re-run and no figure is re-derived.**

**The leg outputs this task records, restated so nothing is looked up twice.** Run date **2026-09-22**, target **a private real documentation catalog held outside this checkout**, at commit `010c50e`, **156 files / 1,960 chunks** (the ~1,500 floor cleared by 31%). **Record the corpus by those facts — commit, size, character — and never by its filesystem path** (see the fourth prohibition below). Host: MacBook Air, Apple M4, 10 cores (4P + 6E), 16 GB, macOS 15.7.4 (24G517) / `Darwin 24.6.0` / arm64, Node v22.23.2, npm 10.9.8, Claude Code 2.1.278, package `autonomous-sdlc-harness@0.2.0`.

- **(i)** `time npx --yes autonomous-sdlc-harness@0.2.0 init --docs --docs-retrieval --non-interactive` → `13.17s user 7.33s system 16% cpu 2:08.08 total` = **128.08 s**, cache **COLD** (no `_cacache`, no `_npx`, no harness retrieval cache). `du -sh` runtime **551M**, models **57M**. A re-install with npm **warm** cost **17 s** — a warm-cache elapsed time, not a setup time. Wiring confirmed: `phases.docs: true`, `docs.retrieval: true`, `stateDir: "sdlc-harness/"`, `.mcp.json` declaring the `harness-docs` stdio server.
- **(ii)** Normal run: `retrieval-dependencies`, `retrieval-model-cache` and `retrieval-index` **all PASS** (`retrieval-index` reporting `docs index: 156 files, 1960 chunks; embedded 1960, unchanged 0, deleted 0`), with `Summary: 30 pass, 5 warn, 1 fail`, the one FAIL being `remote` in a scratch repository — gate 5's subject, exactly as gate 10 says to expect. Models-moved-aside run: `retrieval-dependencies` unchanged PASS, `retrieval-model-cache` and `retrieval-index` **FAIL** naming the same 8 missing model files, `Summary: 28 pass, 5 warn, 3 fail`. The shared cache was restored **in the same command** that moved it and verified back at **57M**.
- **(iii)** Cited to `docs/retrieval-eval-results.md` → `## Cold build and index size`. Name only the corpus stamp and that the `du` figure and wall times live there.
- **(iv)** Default `fused-rerank`, so the reranker ran. Positive, *"How do I configure a proxy for the Vite dev server?"* → `docs/vite/config/server-options.md#serverproxy` at score **1.000**, naming the known section exactly. Negative, *"What is the recommended marinade time for lamb souvlaki?"* → **`no confident match`**. The calibration recorded in `docs/retrieval-eval-results.md` → `## Threshold calibration` **holds on a real catalog at both ends**.
- **(v)** `claude -p … --settings .claude/settings.autonomous.json --permission-mode acceptEdits --output-format stream-json --verbose`, **no permission-bypass flag**. `system/init` → `mcp_servers: [{"name":"harness-docs","status":"connected","source":"project"}, …]`; `mcp__harness-docs__search_docs` present in the session's tool list; called once; **no permission denial anywhere in the stream**; returned the same ranked list the CLI printed, `#serverproxy` at 1.000 first; `result: subtype=success, is_error=false, num_turns=3, duration_ms=9235`, exit 0. **The relative launcher path in `.mcp.json` resolved from the session's working directory** — the returned results are what prove it. One observation worth recording: the session reached the tool through a `ToolSearch` call first (`select:mcp__harness-docs__search_docs`), because on this runner version the MCP tool arrives **deferred** rather than pre-loaded; it cost one extra turn of the three and required no additional grant.
- **(vi)** `uname -sr` → `Darwin 24.6.0`; `node --version` → `v22.23.2`; `claude --version` → `2.1.278 (Claude Code)`; `npx autonomous-sdlc-harness --version` → `0.1.0`, a **shadowed global link** rather than the registry package. That link has since been removed and the bare name now answers `0.2.0`, so the recorded `0.1.0` line does not reproduce today; what every leg actually measured was `node …/runtime/…/dist/cli.js --version` → **`0.2.0`**.

**Work:**

- [ ] **Rewrite the two cold-build figure bullets under the section lead-in, and add the host-dependence statement.** Keep the stub's figure as it stands. Replace the real-models bullet's *"measures 12.2 s total at 177 chunks, of which the refresh is 62.51 ms per chunk"* with a citation of the file of record that names **both** measured corpora — this repository's own 177 chunks and the real 1,960-chunk catalog — and restates neither's numbers. Then state, in this document, what item 5 of the prompt requires as a standing sentence: **every published cost figure is the one measured on the recording host, and a slower host or a slower route pays more.** Give the second, independent demonstration this branch holds: download throughput on the recording host is **route-dependent, not a single figure** — ~1.1–1.4 MB/s over Cloudflare and npm against ~17.5–19.1 MB/s from HuggingFace **on the same connection** — so a single-number reading predicted ~six minutes for leg (i) where the measured cold setup was **128 s** (npm ships compressed tarballs in parallel, and installed size is not transfer size). **Do not reintroduce a single bandwidth number, do not derive any wall-clock figure from one, and do not invent a scaling factor for other hardware** — one host is one host, and saying so is the honest record. Whether the harness should detect host speed at run time is explicitly not this document's question.
- [ ] **Rewrite the decision paragraph under **Why `setup-worktree.sh` does not warm the index.** so it reads as the standing decision rather than a deferral.** The current lead-in sentence — *"the decision below is that it should change, and this branch does not change it"* — and the whole *"The standing rule is that a cold build costing more than 60 seconds…"* paragraph go, along with the ~940-chunk crossover, the 93.8 s / 94.8 s extrapolation, the *"the rule trips"* conclusion, the deferral to roadmap item 17, and the closing paragraph whose revisit condition is *"a measurement there that comes back under 60 s"*. What replaces them: **the cold build stays inside the first `search_docs` call, because that is the only mechanism serving every entry point.** A session that never ran `setup-worktree.sh` — a plain interactive session in the main checkout — still gets a built index, which a warm performed by a script on the autonomous path would not give it. Say that **roadmap item 17 is cancelled** on that reason and that no later branch makes the move. Then state the timeout correction with its citation, in this document's own terms and without restating the results file's tables: there is **no 60 s tool-call timeout and nothing near one** — `MCP_TOOL_TIMEOUT` defaults to `1e8` ms (~27.8 h) and governs MCP tool-call **execution**; `MCP_TIMEOUT`'s 30,000 ms is MCP **server startup**, a different setting whose conflation is the likeliest origin of the error; what can end a long call is **silence, not duration** (`CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`, 1,800,000 ms for stdio servers, and the `harness-docs` server is stdio); `CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS` backgrounds a long call rather than failing it; source `strings -a` over the installed binary at `~/.local/share/claude/versions/2.1.278` — **in exactly that tilde form, never expanded to a home directory** — Claude Code 2.1.278, macOS arm64, 2026-09-22. **State no replacement constant** — not 1,800 s, not `${MCP_TOOL_TIMEOUT}`, not a crossover chunk count: the premise that wall time causes a failure is false, and a rule restated against any number carries the same defect in a new coat. Finally, **replace the dead revisit condition with one that can actually fire** — the struck condition (a cold build long enough to risk the tool-call timeout) can never fire, so leaving it in place leaves a dead trigger; state instead what would genuinely reopen the question, a cost argument rather than a clock.
- [ ] **Restate, in that same paragraph, the cost the move would have accepted — and derive the grant list rather than asserting it.** Both halves of the current framing are wrong and both are checkable. First: **the index is built once per worktree and shared by every dispatch in it**, so the question is never *which agents query* but only *whether the worktree queries at all*, and the build is paid either way the moment one agent does. Derive the list of agents holding `mcp__harness-docs__search_docs` from the `tools:` allowlists under `plugin/agents/` — read the frontmatter, do not work from memory or from a mention in an agent's body — and establish from that list whether a worktree reaching planning can fail to query. Name the states that genuinely never query and say how rare they are, rather than implying a class. Two candidates are known and **both are checked rather than taken on trust**: a run that stalls before its first planning dispatch, and — the structural one — the docs-catalog engine `/autonomous-sdlc-harness:branch-start-docs-autonomous`, which dispatches only `docs-writer` and `docs-reviewer`; confirm from those two agents' own frontmatter that neither carries the grant, and record that that engine's runs never query the index at all. Second: **the model load is not the cost.** The measured breakdown (cited to the file of record, not restated) puts the model load at a fraction of a percent of the total and the **refresh** at almost all of it, and the refresh is paid at first query if it is not paid at setup. Restate the cost in terms of what is actually spent and actually wasted — and **if the conclusion is that the move is close to free rather than a trade, say so**: a row that overstates a cost is as wrong as one that understates it, and saying it strengthens the cancellation independently of the timeout question.
- [ ] **Rewrite item (d) under `## Measured, and how` so it records gate 10 as run.** Replace *"is **partly** satisfied … so its own six legs stay owed"* with the run record: date, host stamp in the shape `## The pre-step` requires (chip and model, cores, memory, OS, Node, Claude version, package under test, what else was running), the target corpus — **named by its commit `010c50e`, its size and its character (a private real documentation catalog held outside this checkout), never by its filesystem path; no absolute path under `/Users/…` or `/home/…` may enter this file** — and its **156 files / 1,960 chunks**, and that the ~1,500-chunk floor was cleared by 31%. Record each leg's command and exact output per `docs/development.md` §5 gate 10's *"Where the results go"* — legs (i), (ii), (iv), (v) and (vi) in full from the figures above, each stating what it establishes; leg (iii)'s wall times, chunk count and `du` figure **cited** to `docs/retrieval-eval-results.md` → `## Cold build and index size` rather than restated, with leg (iii)'s own summary line quoted so the corpus floor is visibly met. Attach leg (i)'s **bandwidth caveat** — cold cache, 128 s, route-dependent throughput, no single number. Record the two environment faults the run hit, because they are facts about the shipped product a later reader needs: `npx autonomous-sdlc-harness` resolved a **shadowed global link** at `0.1.0` rather than the registry package, so every leg pinned `@0.2.0` (the link has since been removed and the bare name now answers `0.2.0`, but any adopter with a linked or globally installed copy hits the same thing and the gate gives no way to notice); and `npx … docs index` **cannot load the optional peers** — it answered `autonomous-sdlc-harness: docs retrieval needs the optional package @huggingface/transformers, which this installation cannot load.` — so legs (iii) and (iv) ran through the route `scripts/docs-search-server.sh` itself execs, `node "$cache/retrieval/runtime/node_modules/autonomous-sdlc-harness/dist/cli.js"`. Then rewrite the *"Three legs remain unrun"* list: legs (i) and (v) have been run — the published `0.2.0` carrying `dist/retrieval` is what made leg (i) runnable, and leg (v) started the server the way the agent runner does — so **only the Linux item survives**, and it is not a leg but a platform gap. Record the leg (v) `ToolSearch` observation, and record that the machine-wide model cache is in the state gate 10 left it: moved aside and restored in the same leg (ii) command, verified back at 57M, and untouched by any of this branch's own work, which runs no index build.
- [ ] **Retire the two settled entries in `## Still open`, and correct the index bullet in `## What it costs`.** Leg (v) settles *"Whether the agent runner starts the `.mcp.json` server with the checkout root as its working directory"* — it does, and the returned results are the evidence; remove the entry and reconcile the `AUTONOMOUS_SDLC_HARNESS_RETRIEVAL_LOG` note above that says *"whether the agent runner starts this server with the checkout root as its working directory is still open (`## Still open`)"*, which now points at nothing — keep the absolute-path advice if it still holds for another reason, but do not leave a citation to a retired entry. Leg (iv) settles *"The abstain threshold on a real catalog"* — `ABSTAIN_SCORE_THRESHOLD = 0.32` held at both ends on a 1,960-chunk real catalog, 1.000 on the positive and `no confident match` on the negative; retire the entry and cite the leg, **without** re-running the eval, re-calibrating anything or touching `floor.json`. Keep the Linux entry and the lexical-arm entry as they stand. In `## What it costs`, replace *"43.2 MB for this repository's own 177 chunks, extrapolating to a few hundred megabytes at a mature catalog's ~1,500, with a 12.2 s cold build … whose size row is an upper bound"* with a citation of the file of record and the corrected shape — the index is **fixed-cost dominated**, measured at two corpus sizes — publishing **no projection** of its own.

**Verification:**

- `git diff --stat d31e6c8 -- scripts/setup-worktree.sh cli/templates/scripts/setup-worktree.sh` is empty, and `git diff --stat d31e6c8 -- plugin/agents/` is empty. Acceptances 9's two halves; a proposal to warm the index, or to gate warming on `docs.retrieval`, is work that has been decided against.
- `git grep -nE '60 s|60 seconds|940 chunks|tool-call timeout' -- docs/retrieval.md` returns only text that is **naming the real settings and their defaults with their citation**, never a surviving statement of a cold-build rule against a wall-clock threshold. The invariant is *no hit that states a rule*, not a count.
- `git grep -nE 'extrapolat|244 kB|366 MB|62\.51|93\.8|94\.8|12\.2 s|43\.2' -- docs/retrieval.md` returns nothing that presents a figure as current or as extrapolated; every cold-build and index-size number in the file is either absent or explicitly a citation of `docs/retrieval-eval-results.md` → `## Cold build and index size`.
- `git grep -nE 'MB/s|megabytes per second|six minutes' -- docs/retrieval.md` returns only the **route-dependent pair** (~1.1–1.4 and ~17.5–19.1) as the demonstration that a single number is wrong — never a single bandwidth figure standing for the host, and no wall-clock prediction derived from one.
- The grant list in the rewritten cost paragraph matches what `grep -l 'mcp__harness-docs__search_docs' plugin/agents/*.md` reports **once each hit is confirmed to sit in that file's `tools:` frontmatter rather than in its body** — a mention in prose is not a grant. Confirm by reading the frontmatter of every hit, and confirm from their own frontmatter that `plugin/agents/docs-writer.md` and `plugin/agents/docs-reviewer.md` carry no grant.
- Every retired `## Still open` entry has no surviving citation anywhere in the file: `git grep -n 'Still open' -- docs/retrieval.md` and read each hit's surrounding sentence.
- **No machine-local path entered the document:** `grep -rnE '/(Users|home)/[a-z]' docs/retrieval.md` returns nothing. Read this check **on its own, independently of `scripts/run-gates.sh`** — gate `6a no machine paths` is already red in this checkout by design, so a hit added here would not show as a *new* failure of that gate. Confirm by eye that the corpus is named by commit, size and character, and that the `strings -a` citation still reads `~/.local/share/claude/versions/2.1.278` unexpanded.
- `bash scripts/run-gates.sh` prints no new failure. Run it **without a pipe**. Its `6a` result is **not** the machine-path check for this task; the `grep` above is.
- `git status --porcelain` shows `docs/retrieval.md` as the only modified path.
