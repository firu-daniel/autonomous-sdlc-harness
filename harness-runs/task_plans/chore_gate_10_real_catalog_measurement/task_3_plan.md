### Task 3 — Update `docs/development.md`: gate 10's own text records the run and its two command defects, and roadmap item 17 is cancelled

**Goal:** Bring the two places `docs/development.md` speaks about this subject into agreement with what happened. Gate 10's paragraph in `## 5. Verifying a change` must record that it has now been run — with the date, the host and the target corpus's chunk count — must fix the two defects in its own leg commands that the run exposed, and must lose the sentence that judges leg (iii)'s wall time against a tool-call timeout. Row 17 of `## 6. The roadmap this tree defers to` must record the item as **cancelled**, on a reason that is not a figure. This task owns every edit to that file; no other task touches it.

**Depends on:** **Task 1**, which makes `docs/retrieval-eval-results.md` → `## Cold build and index size` the file of record for the real-catalog figures. **This task restates none of them** — item 1 of the prompt is explicit that every other site cites that section rather than restating it. The one figure this task names is the corpus's chunk count (1,960 over 156 files), because gate 10's own text is where the floor's satisfaction is shown.

**Three prohibitions.** **No machine-local path enters the document.** Record the corpus by its **commit, size and character** — `010c50e`, 156 files / 1,960 chunks, a private real documentation catalog held outside this checkout — and **never by its filesystem path**: no absolute path under `/Users/…` or `/home/…` may enter `docs/development.md`, and the `strings -a` citation is written in its tilde form `~/.local/share/claude/versions/2.1.278` and **never expanded**. (The gate-command blocks this task edits keep their `${XDG_CACHE_HOME:-$HOME/.cache}` form, which is a shell expansion, not a machine location, and is already what the file carries.) The gate is `scripts/run-gates.sh` `6a no machine paths` (`.claude/context/conventions.md` → `## The testing bar`; `docs/development.md` → **Gate 6 — self-containment**), which is **already red in this checkout by design**, so a hit added here does not read as a *new* failure and the independent `grep` in **Verification** is the real check. Then: this task changes **no script** — `scripts/setup-worktree.sh` and `cli/templates/scripts/setup-worktree.sh` stay byte-identical to branch point `d31e6c8`; warming the index at worktree setup is **cancelled work, not deferred work**, and a change to either script here is a failed change. And it changes **no agent's `tools:` allowlist** under `plugin/agents/`.

**One convention binds the fenced blocks this task edits.** `harness-runs/lessons.md` → `## Adopter-facing documentation`: *"Every command an adopter is meant to run sits in a fenced block, one command per line; never inline it, join two with prose, or cut its block when compacting a document."* Gate 10's leg commands are exactly that; keep every one of them in its fenced block, one per line.

### Targets

- `docs/development.md` → `## 5. Verifying a change` → the **Gate 10 — docs retrieval with the real models.** paragraph, its six leg blocks, and its closing **Where the results go** paragraph.
- `docs/development.md` → `## 6. The roadmap this tree defers to` → the table row for item **17**.

**Source:** `harness-runs/task_prompts/chore_gate_10_real_catalog_measurement_task_prompt.md` — `## The figures block` for the run record and the two environment faults, and `## The timeout question, already settled` for the research. **Nothing is re-run.**

**The facts this task needs, restated so nothing is looked up twice.**

- **Run stamp.** 2026-09-22, hand run in a supervised session, all six legs. Target **a private real documentation catalog held outside this checkout**, at commit `010c50e`, indexed to **156 files / 1,960 chunks** — the ~1,500-chunk floor cleared by **31%**. Host: MacBook Air, Apple M4, 10 cores (4P + 6E), 16 GB, macOS 15.7.4 (24G517) / `Darwin 24.6.0` / arm64, Node **v22.23.2**, npm 10.9.8, Claude Code **2.1.278**, package under test `autonomous-sdlc-harness@0.2.0` from the registry, nothing else of consequence running. Leg (i) cold setup **128 s**; leg (v) exit 0 with no permission denial anywhere in the stream.
- **Defect 1 — the unpinned name.** `npx autonomous-sdlc-harness` resolved a **shadowed global link** at `0.1.0` on the run host, which carries no `docs` command at all; taken literally, gate 10's leg commands would have measured a stale dev build. Every leg pinned `@0.2.0`. The particular link has since been removed, so the recorded `0.1.0` line does not reproduce today — **but the gate-text defect stands regardless**: any adopter with a linked or globally installed copy hits the same thing, and the gate gives no way to notice, since `docs index` would simply fail or, worse, a leg would silently measure the wrong build.
- **Defect 2 — the peers the npx copy cannot load, and the more serious one.** `npx --yes autonomous-sdlc-harness@0.2.0 docs index` answered `autonomous-sdlc-harness: docs retrieval needs the optional package @huggingface/transformers, which this installation cannot load.` The working route is the one `scripts/docs-search-server.sh` itself execs: `node "$cache/retrieval/runtime/node_modules/autonomous-sdlc-harness/dist/cli.js"`. Legs (iii) and (iv) used it. **Gate 10's leg (iii) and (iv) commands as written do not run.**
- **Two deviations the run recorded against its own procedure**, worth keeping in the gate's text as things to avoid: leg (i)'s cold run was **piped** to `tail -20`, which gate 10 forbids (`time` then measures the pipeline; `--non-interactive` was explicit on both runs, so TTY detection could not change what `init` did); and the two leg (i) runs' cache sizes differ by ~17 MB on the same pinned version, cause not established.
- **The timeout research.** No 60 s tool-call timeout and nothing near one: `MCP_TOOL_TIMEOUT` defaults to `1e8` ms (~27.8 h) and governs tool-call **execution**; `MCP_TIMEOUT`'s 30,000 ms is MCP **server startup**; `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` is 1,800,000 ms for stdio servers and trips on **silence, not duration**; `CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS` backgrounds rather than fails. Source: `strings -a` over the installed binary at `~/.local/share/claude/versions/2.1.278` — **in exactly that tilde form, never expanded to a home directory** — Claude Code 2.1.278, macOS arm64, 2026-09-22.

**Work:**

- [ ] **Record in gate 10's opening paragraph that the gate has been run.** Add the run stamp — date, host (chip, cores, memory, OS, Node, Claude version, package under test), the target corpus **named by its commit `010c50e`, its size and its character (a private real documentation catalog held outside this checkout), never by its filesystem path; no absolute path under `/Users/…` or `/home/…` may enter this file** — and its **156 files / 1,960 chunks**, and the floor cleared by 31% — so the next reader knows the legs have been exercised and what they cost, and points at where the full record lives (`docs/retrieval.md` → `## Measured, and how` item (d), and `docs/retrieval-eval-results.md` → `## Cold build and index size` for leg (iii)'s figures). Keep everything in the paragraph that still holds: why the gate is hand-run, the throwaway-repository-outside-this-checkout requirement, the ~1,500-chunk floor and why it exists, the invalid-under-floor rule, and the run-without-a-pipe rule. Do **not** turn the paragraph into a results record — the figures live in the two documents it cites.
- [ ] **Fix defect 1 in the gate's own text: pin the version, or verify what the bare name resolves to before the first leg.** Every leg's fenced block currently invokes the bare `npx autonomous-sdlc-harness`. Make the gate state the hazard — a linked or globally installed copy shadows the registry package silently, and a leg then measures the wrong build — and give the reader the check or the pin, in fenced blocks, one command per line. Record the measured instance with its output lines (`npm ls -g --depth=0` showing the link, the bare name answering `0.1.0`, the pinned name answering `0.2.0`) as the evidence that this is a real failure mode rather than a caution.
- [ ] **Fix defect 2: legs (iii) and (iv) get commands that run.** Replace or supplement their `npx …` invocations with the runtime-entry route `scripts/docs-search-server.sh` execs — `node "$cache/retrieval/runtime/node_modules/autonomous-sdlc-harness/dist/cli.js"` with `$cache` resolved as the gate resolves it elsewhere (`${XDG_CACHE_HOME:-$HOME/.cache}/autonomous-sdlc-harness`) — and state why: the npx copy is a standalone install without `@huggingface/transformers` and refuses by name, so the leg as written does not run at all. Quote the refusal message verbatim, per the `## Documents of record` rule that a measured fact states what was measured, the command and the exact message. While in leg (i), note the two recorded deviations — never pipe the timed command, and the ~17 MB size difference between two runs of the same pinned version, cause not established — and attach leg (i)'s **bandwidth caveat**: cold-cache elapsed time is **128 s**, throughput on the recording host is route-dependent, and **no single bandwidth number** may be recorded or used to predict a wall time. **And in leg (ii), make the machine-wide model cache recoverable mid-leg.** Leg (ii) moves `${XDG_CACHE_HOME:-$HOME/.cache}/autonomous-sdlc-harness/retrieval/models` aside to `…/models.aside`, and the cache is shared by every checkout on the machine, so an operator whose session ends, whose `doctor` run aborts, or who is interrupted between the `mv` aside and the `mv` back leaves every other checkout without a model cache and with no note of why. The gate's leg (ii) text currently says only *"move it back before anything else uses it"*. Make it state the recovery explicitly: the single `mv` that restores `…/models.aside` to `…/models` — in its own fenced block, one command per line — is the whole repair, it is safe to run at any later time and from any checkout, and it is what the operator runs first if a leg fails after the move. Add the check that confirms the restore landed: the directory is back at its own path and `npx autonomous-sdlc-harness@<pinned> doctor` reports `retrieval-model-cache` `PASS` again. Say that nothing else in the gate, and nothing in this repository's own tooling, moves or re-points that cache, so a stranded `…/models.aside` is always this leg and always fixed this way.
- [ ] **Strike the timeout judgement from leg (iii)'s text.** The sentences *"That comparison decides one thing — `docs/retrieval.md` → **Why `setup-worktree.sh` does not warm the index.** states that warming is not worth a model load per worktree, and its stated revisit condition is a real-model cold build long enough that a first `search_docs` call risks the agent runner's tool-call timeout. This wall time is the number that condition is judged against; if it is met, warming in `setup-worktree.sh` is the move."* are false in every clause and go. **Put no replacement threshold in their place** — not 1,800 s, not `${MCP_TOOL_TIMEOUT}`, not a crossover chunk count. State instead what leg (iii) is for: it is the real-catalog **cost** figure, recorded in the file of record, and it arbitrates nothing about where the build belongs — that decision is taken and is `docs/retrieval.md` → **Why `setup-worktree.sh` does not warm the index.** Keep the leg's recording obligations as they stand: the `docs index: <files> files, <chunks> chunks; embedded <e>, unchanged <u>, deleted <d>` line, the wall time, and the `du -sh <stateDir>/docs_index` figure, the last being reported nowhere else. Also update the **Where the results go** paragraph: item (d)'s placeholder is filled, leg (iii)'s figures live in `docs/retrieval-eval-results.md` → `## Cold build and index size` rather than in item (d), and the standing repetition requirement is that leg (iii) is run **more than once into fresh index directories with every wall time recorded and the spread reported with its cause** — this run took four, and a single build is an anecdote.
- [ ] **Rewrite row 17 as cancelled.** Everything the row currently rests on goes: the *"whose runner timeout defaults to 60 s"* clause, the *"The rule's trip is an extrapolation"* limit with its 62.51 ms / 93.8 s / 94.8 s / 940-chunk arithmetic, the *"The measurement that settles it is owed by a different gate"* limit with its *"a figure that comes back under 60 s … cancels this item"* condition, and the *"the cost the move accepts is a model load per worktree, spent on agents that do not query"* limit. Record instead: **CANCELLED**, and the reason, which is **not a measurement** — the item's stated condition rested on a threshold that does not exist, so the condition can never be evaluated and the item is cancelled without it; the cold build stays inside the first `search_docs` call because that is the only mechanism serving **every** entry point, a plain interactive session in the main checkout included, and a warm performed by a script on the autonomous path would not serve it. Say that `docs/retrieval.md` → **Why `setup-worktree.sh` does not warm the index.** is now the **standing decision** rather than a deferral, and that **this branch does not make the move and no later branch does either**. Restate what the move would have cost, in the corrected terms: the index is built once per worktree and shared by every dispatch in it, so the question is *whether the worktree queries at all* rather than which agents do; the model load is a fraction of a percent of the build and the **refresh** is almost all of it, paid at first query if not paid at setup — figures cited to the file of record, not restated here. If the honest conclusion is that the move would have been close to free rather than a trade, **say so**: a row that overstates a cost is as wrong as one that understates it. Keep the row's numbering and its place in the table — a citation written before the item closed still has to resolve to something — and check whether the closing paragraphs of `## 6.` that enumerate shipped and owed items need item 17 named among the resolved ones so that *"everything not named in this paragraph or marked in its own row is still owed"* stays true.

**Verification:**

- `git grep -nE '60 s|60 seconds|940 chunks|tool-call timeout|runner timeout' -- docs/development.md` returns only text **naming the real settings and their defaults with their citation** where it explains what changed — no surviving statement of a cold-build rule against a wall-clock threshold, and no crossover chunk count. The invariant is *no hit that states a rule*, not a count.
- `git grep -nE 'extrapolat|62\.51|93\.8|94\.8|244 kB|366 MB|agents that do not query' -- docs/development.md` returns nothing: row 17's arithmetic and its cost framing are gone, and no figure is restated from the file of record.
- Run the roadmap's own standing check from the tree root and confirm every number it reports other than 1 and 2 still has a row: `grep -rn -E "items? [0-9]+" . --exclude-dir=node_modules --exclude-dir=dist`. Item 17's row must still exist and still resolve — cancelling an item does not delete its row.
- Leg (ii) states the mid-leg recovery for the machine-wide model cache: the restoring `mv` from `…/retrieval/models.aside` back to `…/retrieval/models` appears in a **fenced block, one command per line**, is named as the first thing an operator runs if a leg fails between the move aside and the move back, and is followed by the `doctor` check that confirms `retrieval-model-cache` reports `PASS` again. Read the leg by eye: the recovery is stated as a procedure the reader can follow without inferring it from the forward commands, and the restore command is not inlined into prose.
- Every leg command in the gate is **runnable as written** on a machine in the state the gate describes. Walk each fenced block by eye against the recorded faults: no bare-name invocation is left standing without the pin or the resolution check beside it, and legs (iii) and (iv) name the runtime entry rather than a route that refuses. Each block keeps one command per line and none has been inlined into prose.
- `git diff --stat d31e6c8 -- scripts/setup-worktree.sh cli/templates/scripts/setup-worktree.sh plugin/agents/` is empty (acceptance 9).
- **No machine-local path entered the document:** `grep -rnE '/(Users|home)/[a-z]' docs/development.md`. The invariant is **not** *returns nothing* for this file: it has exactly **one** pre-existing hit at branch point `d31e6c8` — the `## 5. Verifying a change` → **Gate 6 — self-containment** paragraph, which quotes this very sweep and its fictional `/Users/me` / `/home/ada` examples as prose. Read every hit by eye: the only permitted one is that paragraph, and any hit elsewhere — in gate 10's text or row 17 — is this task's and fails. Cross-check the count against branch point with `git grep -cE '/(Users|home)/[a-z]' d31e6c8 -- docs/development.md`, which reports `1`. Read this check **on its own, independently of `scripts/run-gates.sh`** — gate `6a no machine paths` is already red in this checkout by design, so a hit added here would not show as a *new* failure of that gate. Confirm by eye that the corpus is named by commit, size and character, that the `strings -a` citation still reads `~/.local/share/claude/versions/2.1.278` unexpanded, and that the leg blocks still use `${XDG_CACHE_HOME:-$HOME/.cache}` rather than any expanded home directory.
- `bash scripts/run-gates.sh` prints no new failure. Run it **without a pipe**. Its `6a` result is **not** the machine-path check for this task; the `grep` above is.
- `git status --porcelain` shows `docs/development.md` as the only modified path.
