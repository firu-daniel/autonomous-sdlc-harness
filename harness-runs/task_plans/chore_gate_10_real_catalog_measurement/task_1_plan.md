### Task 1 — Record the real-catalog cold build and index size in `docs/retrieval-eval-results.md`, the file of record

**Goal:** Make `docs/retrieval-eval-results.md` → `## Cold build and index size` the one place the real-catalog numbers are written — the four leg (iii) wall times with their thermal cause, the eval-route cross-check, the phase breakdown, the index size and its two-anchor fit — recorded **beside** the 177-chunk figures with its own host and corpus stamp, and strike from that section the 60-second rule, the ~940-chunk crossover and the extrapolations they rested on. Tasks 2, 3 and 4 cite this section; none of them restates a number from it, so this file is where every figure lives.

**Three prohibitions before any edit.** (0) **No machine-local path enters the document.** Record the corpus by its **commit, its size and its character** — commit `010c50e`, 156 files / 1,960 chunks, a private real documentation catalog held outside this checkout — and **never by its filesystem path**: no absolute path under `/Users/…` or `/home/…` may enter a `docs/` file. The same rule binds the `strings -a` citation this task writes: the installed-binary path is written in its tilde form, `~/.local/share/claude/versions/2.1.278`, and is **never expanded** to a home directory (Tasks 2 and 3 carry the same citation and are held to the same form). The gate behind this is `scripts/run-gates.sh` gate `6a no machine paths` (`.claude/context/conventions.md` → `## The testing bar`; `docs/development.md` → **Gate 6 — self-containment**), which is **already red in this checkout by design** — so a hit added here is invisible to the *"no new failure"* reading of `run-gates.sh` below, and the independent `grep` in **Verification** is the check that actually catches it. (1) **Nothing may be typed inside the generated region.** The markers are `<!-- eval:generated:start -->` and `<!-- eval:generated:end -->`; `evals/docs-retrieval/run.mjs` is their only writer and a hand edit inside them is destroyed by the next run. `## Cold build and index size` sits **below** the end marker and is hand-written territory. Acceptance 10 requires the generated region byte-identical to branch point `d31e6c8`. (2) This task changes no script, no source file and no agent allowlist.

### Targets

- `docs/retrieval-eval-results.md` → `## Cold build and index size` — the whole section, below the generated-region end marker.
- `docs/retrieval-eval-results.md` → `## The shipped default against fusion alone`, its closing **What this does not settle** paragraph — one sentence there says the gate-10 hand run is a step *"which this branch does not perform"*.
- `docs/retrieval-eval-results.md` → `## Threshold calibration` → `### The limit on this calibration` — the same false-after-the-run claim in its own words: *"its confirmation is a hand run against a private real catalog, which this branch does not perform."* That confirmation has now been taken, by leg (iv). The section sits **below** the `<!-- eval:generated:end -->` marker and is hand-written territory.
- `docs/retrieval-eval-results.md` → `## The query-log pass`, its closing *"Repeatability, and why the gap above is a ceiling…"* paragraph — it cites the sentence above (*"the same real-catalog hand run `## The limit on this calibration` above already names as out of scope here"*) and must stay true once that sentence is rewritten.

**Source:** `harness-runs/task_prompts/chore_gate_10_real_catalog_measurement_task_prompt.md` → `## The figures block — gate 10 RUN IN FULL, legs (i)-(vi)`. **Do not re-run a leg and do not re-derive a figure the block carries** — the measurements are all taken. Every number this task writes comes from that block or from the section's existing 177-chunk tables, which stay.

**The figures this task records, restated here so nothing is looked up twice.**

*Host stamp of the real-catalog run:* MacBook Air, Apple M4, 10 cores (4P + 6E), 16 GB, macOS 15.7.4 (24G517), `uname -sr` → `Darwin 24.6.0`, arm64, Node v22.23.2, npm 10.9.8, Claude Code 2.1.278, package `autonomous-sdlc-harness@0.2.0` from the registry, load `1.92 1.76 1.76`, nothing else of consequence running. Node differs from the 177-chunk figures' v20.19.5: **recorded, not reconciled.**

*Corpus stamp:* **a private real documentation catalog held outside this checkout**, at commit `010c50e`, **156 files, 1,960 chunks**, summary line identical on all four runs: `docs index: 156 files, 1960 chunks; embedded 1960, unchanged 0, deleted 0`. The ~1,500-chunk floor cleared by 31%.

*Leg (iii), CLI route, four runs into fresh index directories:* run 1 `1:51.25` = 111.25 s (machine rested); run 2 `1:59.30` = 119.30 s (immediately after run 1); run 3 `2:07.58` = 127.58 s (immediately after run 2); run 4 `1:54.47` = 114.47 s (after ~10 min of light load). CPU 452–459% throughout. `du -sh sdlc-harness/docs_index` → **72M**, identical on all four. Median of runs 1–3 = 119.30 s; spread 16.33 s = **13.7% of the median**.

*Per-chunk refresh, CLI route:* **60.87 ms** median, 56.76 ms min, 65.09 ms max, against the **62.51 ms** the extrapolation used — within 2.6% of the median.

*Acceptance 2a, eval ad-hoc route, same corpus snapshot (156 files, 1,960 chunks), `cold: { index: true, modelCache: false }`:* `modelLoadMs` 334.38, `storeOpenMs` 941.54, `refreshMs` 107,984.15, `totalMs` **109,260.07**; `size` `apparentBytes` 74,195,245, `allocatedBytes` 75,808,768, `files` 986. Refresh per chunk on this route **55.1 ms**.

*Index on disk:* measured **37.6 kB per chunk** at 1,960 chunks against the **244 kB** the extrapolation used; the two-point fit over 43.2 MB @ 177 chunks and 72 MB @ 1,960 chunks is **~40.3 MB fixed overhead plus ~16.5 kB per chunk**; the ~366 MB projection at 1,500 chunks overshoots by **5.6×** (the fit gives ~65 MB there).

*Leg (iv), search, on the same 1,960-chunk real catalog, default `fused-rerank` mode so the reranker ran:* the positive query *"How do I configure a proxy for the Vite dev server?"* returned the known section **first and exactly** — `docs/vite/config/server-options.md#serverproxy` at score **1.000**; the negative query *"What is the recommended marinade time for lamb souvlaki?"* returned **`no confident match`** — it **abstained**. The task prompt states the consequence: *"The calibration in `docs/retrieval-eval-results.md` → `## Threshold calibration` holds on a real catalog at both ends."* This is the confirming run, not a re-calibration.

*State left behind:* the machine-wide model cache was moved aside and restored **in the same command** during leg (ii) and verified back at 57M; the teardown copies and the extra index directories were deleted; `_cacache` was cleaned and has repopulated normally.

**Work:**

- [ ] **Replace the section's opening decision paragraph.** The current lead-in — *"The standing rule is that a cold build costing more than **60 seconds** moves out of the agent's path and into `scripts/setup-worktree.sh`, because the agent runner's MCP tool-call timeout defaults to 60 s and an adopter may set their Bash timeouts lower"*, its 93.8 s / 94.8 s extrapolation, its **940-chunk** crossover and its *"the rule trips"* conclusion — goes entirely. **Restate no constant in its place**, not 1,800 s, not `${MCP_TOOL_TIMEOUT}`, not a new crossover chunk count: the premise that wall time causes a failure is false on every path the harness uses. What replaces it is (a) the measured facts with their citation — `MCP_TOOL_TIMEOUT` defaults to `1e8` ms (~27.8 h), floor 1,000 ms, overridable per server in `.mcp.json`; `MCP_TIMEOUT`'s 30,000 ms is MCP **server startup**, a different setting, and conflating the two is the likeliest origin of the error; `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` is 1,800,000 ms for **stdio** servers (the `harness-docs` server is stdio) and trips on *silence*, not duration; `CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS` (120,000 ms) moves a long call to a background task without failing it; a stdio MCP tool sleeping 75 s returned `DONE after 75.0s` under `claude -p`; `BASH_DEFAULT_TIMEOUT_MS` 120,000 / `BASH_MAX_TIMEOUT_MS` 600,000, and a 25 s command given a 5 s timeout was *"moved to the background"* and completed normally — so this section's own *"an adopter may set their Bash timeouts lower"* is true and harmless; the only `60000` near MCP is `var pr=60000` in the HTTP transport, used as `Math.max(n,pr)`, a request **floor** that means the opposite of what the rule claimed. Cite the source: `strings -a` over the installed binary at `~/.local/share/claude/versions/2.1.278` — **written in exactly that tilde form and never expanded to a home directory**, per prohibition (0) — Claude Code **2.1.278**, macOS arm64, 2026-09-22 — bundled JS, so source rather than inference. And (b) the property that replaces the constant: **the build must not go silent for longer than the idle timeout**, and where it belongs is a cost question, not a timeout question. Note that these are host-side settings in the Claude Code runner and do not change with the model the harness runs. Delete the sentence pointing at *"Task 12"* and the one promising that gate 10 *"replaces the extrapolation with a measurement"* — it has.
- [ ] **Add the real-catalog measurement beside the 177-chunk one, with its own host and corpus stamp — never as a before/after series.** Keep every existing 177-chunk table and its *"What was measured, and in which sense it was cold"* paragraph intact, and add the real-catalog block under its own sub-heading with the host stamp and corpus stamp above — **recording the corpus by its commit, size and character (`010c50e`, 156 files / 1,960 chunks, a private real documentation catalog held outside this checkout) and never by its filesystem path; no absolute path under `/Users/…` or `/home/…` may enter this file** — stating explicitly that the two are **not a before/after pair**: different host (Node v22.23.2 against v20.19.5), different corpus (1,960 chunks against 177, an 11× difference), different date. State the sense in which the real-catalog build was cold — each of the four runs into a **fresh** index directory; the eval-route run's `dataDir` removed immediately before the call, the module asserting the directory absent and `embedded === chunks` so an incremental refresh cannot masquerade as a cold one; and the **model cache warm** on both routes, leg (i) being the download leg and out of this section's scope.
- [ ] **Record the four wall times with the spread and its cause, and do not average them.** Table the four runs with their context column, state the three-run spread as **16.33 s = 13.7% of the median (119.30 s)**, and give the cause rather than the number alone: runs 1–3 rise **monotonically** back-to-back and run 4, after a pause, **returns to baseline** — thermal behaviour of a fanless M4 Air under 8+ minutes of sustained ~4.5-core load, not variance in the software. State the honest figure as **~111–114 s cold on a rested machine, degrading to ~128 s when builds run back-to-back**. State that the existing 177-chunk section's sub-1% spread is **not comparable**, because that corpus is 11× smaller and its runs are too short to heat the machine — do not present the two spreads as a series.
- [ ] **State what the measurement did to the extrapolation, in both directions.** Time **held**: 60.87 ms per chunk measured against 62.51 ms predicted, within 2.6% of the median — and say it is **linear-and-lucky rather than linear-and-right**, because the thermal spread straddles the predicted value, so the prediction lands inside the noise band of the host it was tested on. Size **missed**: 37.6 kB per chunk against 244 kB, the ~366 MB projection overshooting by **5.6×**; publish the **fit and its two anchors** (~40.3 MB fixed plus ~16.5 kB per chunk, over 43.2 MB @ 177 chunks and 72 MB @ 1,960 chunks) and say why the original number was wrong — a fixed cost divided by a small chunk count, the same error shape as the 60 s rule and the six-minute download prediction. **Do not publish a new projection to a larger size on the strength of one more data point**; the fit and its anchors are the whole of what may be written. Rewrite the existing *"Read the size row as an upper bound; separating the fixed term from the per-chunk one needs a second measurement at a different chunk count, which this pass does not take"* sentence — that second measurement has now been taken, and it is the one that produced the fit.
- [ ] **Record acceptance 2a's cross-check and the phase breakdown, each labelled by route.** Give both routes' figures side by side — CLI total 111.25 s rested / 114.47 s run 4 against the eval route's **109.26 s**; `du -sh`'s 72M against 75,808,768 allocated bytes = **72.3 MiB** (74,195,245 apparent); identical snapshot on both. State that there is **no disagreement to investigate**: 1.8% apart, inside the thermal band the four CLI runs established, and `du -sh`'s figure is the allocated one to the megabyte — the shipped CLI path and the library the eval drives are measuring the same thing. **Record both, labelled; do not reconcile by choosing one.** Then record the three things only the phase-separating route could show: the **store-open phase is still per-index-directory, not per-chunk** (875.8 ms at 177 chunks, 941.5 ms at 1,960 — an 11× corpus for a 7.5% rise, so the assumption held at the first size that could have broken it and the recorded section's arithmetic stands); the **model load is flat and is not the cost** (192.5 ms at 177 chunks, 334.4 ms here, falling from ~1.6% of the total to **0.31%**); and **refresh is 107,984 ms of a 109,260 ms total, 98.8% of it** — which is the measured confirmation that the cost of warming is the refresh. Note how the cross-check was driven, since it is not a shipped route: `cold-build.mjs` exports `measureColdBuild` but has **no CLI entry** — no `--cold` flag on `run.mjs`, no npm script — so a throwaway module imported it directly with `repoRoot` and `docsRoot` at the corpus, `conventions: ['.claude/context/conventions.md']`. Close the section by recording the **state left behind on the machine**: the model cache moved aside and restored in the same leg (ii) command and verified back at 57M, the teardown copies and extra index directories deleted, `_cacache` cleaned and repopulated — and that **nothing in this branch's work touches that cache**, because no task here runs an index build. **Then correct the three sentences elsewhere in this file that the run has made false, all of them below the `<!-- eval:generated:end -->` marker and none of them a re-calibration.** (i) `## The shipped default against fusion alone` → **What this does not settle**: its *"the hand run of `docs/development.md` §5 → gate 10 against a private real catalog, which this branch does not perform"* — that run has been performed; say so, cite where its figures live, and keep the surviving half intact (the two fixture-sized corpora and the ledger rule still bind what the *fusion-versus-default* comparison settles). (ii) `## Threshold calibration` → `### The limit on this calibration`: its *"its confirmation is a hand run against a private real catalog, which this branch does not perform"* — record that the confirmation **has now been taken**, dated 2026-09-22, with the corpus by **commit `010c50e`, 156 files / 1,960 chunks, a private real documentation catalog held outside this checkout** and **never by path**, citing leg (iv)'s two queries and their outcomes (positive → `docs/vite/config/server-options.md#serverproxy` first at **1.000**; negative → **`no confident match`**, abstained) and stating that the calibration **held at both ends**. Keep the section's standing reproduction sentence (gate 11 of `scripts/run-gates.sh`) and its re-opening condition as they are. **Do not re-calibrate**: `ABSTAIN_SCORE_THRESHOLD` stays at `0.32` in `cli/src/retrieval/search.ts`, `evals/docs-retrieval/floor.json` is not touched, and one confirming run at each end is not a measured negative distribution — say so, so the *"a run that observes the negative distribution instead of bounding it"* condition survives the rewrite. (iii) `## The query-log pass`, closing *"Repeatability, and why the gap above is a ceiling…"* paragraph: it points at (ii) as the record of an out-of-scope run. **Read it after the rewrite and keep it true** — the repetitions-on-an-idle-host gap it describes is still out of scope here, so if (ii) no longer supports that cross-reference, restate the paragraph's own limit directly rather than deleting the limit.

**Verification:**

- `git diff d31e6c8 -- docs/retrieval-eval-results.md` shows **no change between** `<!-- eval:generated:start -->` and `<!-- eval:generated:end -->`. Extract the region from both revisions and compare them byte for byte; acceptance 10 fails on a single altered byte, and a diff that merely *looks* confined to the tail is not the check.
- `git grep -nE '60 s|60 seconds|940 chunks|tool-call timeout' -- docs/retrieval-eval-results.md` returns only occurrences that are **quoting the corrected record** — the `MCP_TIMEOUT` / idle-timeout / HTTP-floor facts and the account of what the struck rule used to say. It returns no surviving statement of a cold-build rule against a wall-clock threshold. The invariant is *no hit that states a rule*, not a count.
- `git grep -nE 'this branch does not perform|does not perform|real-catalog hand run' -- docs/retrieval-eval-results.md` returns **no sentence that still records the gate-10 real-catalog hand run as unperformed** — read each hit's sentence; the invariant is *no surviving claim of non-performance*, not a count. Both `## The shipped default against fusion alone` → **What this does not settle** and `## Threshold calibration` → `### The limit on this calibration` now record the run as taken, and `## The query-log pass`'s closing paragraph reads correctly against the rewritten section it cites.
- The calibration was **confirmed, not moved**: `git diff --stat d31e6c8 -- cli/src/retrieval/search.ts evals/docs-retrieval/floor.json` is empty, and `### The limit on this calibration` still states the condition under which the value would be re-derived (an observed rather than censored negative distribution).
- `git grep -nE '366 MB|244 kB|93\.8|94\.8' -- docs/retrieval-eval-results.md` returns only hits inside the *"what the extrapolation predicted"* comparison, never a figure presented as current.
- Read the finished section end to end and confirm the two corpora's figures are never joined by a word implying a series (*"improved"*, *"before"*, *"after"*, *"now"*), that each carries its own host and corpus stamp, and that no projection to a size beyond the two measured anchors appears anywhere.
- **No machine-local path entered the document:** `grep -rnE '/(Users|home)/[a-z]' docs/retrieval-eval-results.md` returns nothing. This check is **independent of `scripts/run-gates.sh` and must be read on its own** — gate `6a no machine paths` is already red in this checkout by design (`.claude/context/conventions.md` → `## The testing bar`; `docs/development.md` → **Gate 6 — self-containment**), so a hit added here would not show as a *new* failure of that gate. Also confirm by eye that the `strings -a` citation reads `~/.local/share/claude/versions/2.1.278` in its tilde form and has not been expanded, and that the corpus appears by commit, size and character rather than by path.
- `bash scripts/run-gates.sh` prints no new failure. Run it **without a pipe** — piping into a pager returns the pager's status, and a failing gate then reads as a passing one. Its `6a` result is **not** the machine-path check for this task; the `grep` above is.
- `git status --porcelain` shows `docs/retrieval-eval-results.md` as the only modified path: no script, no source file and no agent definition changed.
