### Task 4 — Correct `docs/retrieval-eval.md`'s two timeout-dependent claims and sweep the tree for surviving sites

**Goal:** Close the branch. Two claims in `docs/retrieval-eval.md` rest on things the earlier tasks have removed — its cost bar cites *"the agent tool-call budget the cold-build section names"*, which after Task 1 that section names no more, and two of its paragraphs describe gate 10 as a step still to run. Fix both, then re-run the story index's own derivation commands across the whole tree and prove that no site anywhere still describes a cold-build or index-size figure as extrapolated, states a cold-build rule against a wall-clock threshold, or restates a figure the file of record owns.

**Depends on:** **Tasks 1, 2 and 3**, which own `docs/retrieval-eval-results.md`, `docs/retrieval.md` and `docs/development.md` respectively. This task **edits none of those three files** — if the sweep finds a surviving site in one of them, that is a defect in the owning task, and the finding is raised rather than patched here, so the file keeps one owner. This task's only edit target is `docs/retrieval-eval.md`.

**Three prohibitions, and one boundary.** **No machine-local path enters any changed document.** Where this task names the corpus, it names it by its **commit `010c50e`, its size (156 files / 1,960 chunks) and its character (a private real documentation catalog held outside this checkout)** and **never by its filesystem path** — no absolute path under `/Users/…` or `/home/…` may enter `docs/retrieval-eval.md`, and any `strings -a` citation stays in its tilde form `~/.local/share/claude/versions/2.1.278`. This task also carries the **branch-wide** version of that check across all four changed files, because `scripts/run-gates.sh` gate `6a no machine paths` is **already red in this checkout by design** (`.claude/context/conventions.md` → `## The testing bar`; `docs/development.md` → **Gate 6 — self-containment**) and a newly added hit is therefore invisible to a *"no new failure"* reading of it. No script changes — `scripts/setup-worktree.sh` and `cli/templates/scripts/setup-worktree.sh` stay byte-identical to branch point `d31e6c8`. No agent's `tools:` allowlist under `plugin/agents/` changes. And **nothing here re-runs the relevance eval, re-calibrates the abstention threshold or touches `evals/docs-retrieval/floor.json`** — the regression floor is graded on `fixture-catalog` alone by design, and a real-catalog corpus is not a second floor. Arm A is out of scope entirely: the index-first-navigation baseline needs a labelled query set and an `INDEX.md` that exist for no catalog outside `fixture-catalog`, and nothing here produces or plans one.

### Targets

- `docs/retrieval-eval.md` → `## The decision rule`, bar 2 (**Cost**) and the following *"The decision is taken on a real catalog, not on either committed corpus."* paragraph.
- `docs/retrieval-eval.md` → `## Running arm A by hand`, its closing *"This step does not close the roadmap row, and neither did this branch."* paragraph. **It is that section's closing paragraph, not `## The regression floor`'s** — `## The regression floor` ends earlier, on the `BLOCKED` / shortfall paragraph about the empty model cache, which is the floor-policy record this branch must **not** touch.

**Source:** `harness-runs/task_prompts/chore_gate_10_real_catalog_measurement_task_prompt.md` — `## Acceptance` items 4, 5, 9, 10 and 12, and `## Out of scope`. The real-catalog figures are cited, never restated: `docs/retrieval-eval-results.md` → `## Cold build and index size` is their one home.

**What changed under this file's feet, restated so nothing is looked up twice.** There is **no agent tool-call budget** of the kind bar 2 assumes: `MCP_TOOL_TIMEOUT` defaults to `1e8` ms (~27.8 h), `MCP_TIMEOUT`'s 30,000 ms is server startup rather than tool execution, `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` (1,800,000 ms for stdio) trips on silence rather than duration, and `CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS` backgrounds a long call rather than failing it. And **gate 10 has been run** — 2026-09-22, against a real catalog at 156 files / 1,960 chunks, all six legs plus the eval-route cross-check.

**Work:**

- [ ] **Rewrite bar 2 of `## The decision rule` so it does not depend on a budget that does not exist.** The clause *"the bar is that arm E's p95 stays inside the agent tool-call budget the cold-build section names while remaining a fraction of arm A's"* loses its referent once Task 1 strikes the 60-second rule. Replace the budget half with a bar that is real — arm E's p95 measured against arm A's agent wall time per query, which is the comparison that actually decides cost — and **state no wall-clock constant of your own**. Leave the rest of bar 2 as it stands: arm E bills no tokens against arm A's one agent call per query, and the charge against retrieval is the one-time cold build plus the index on disk. Check the sibling reference at `## What each metric means` (the **cost** bullet, *"The one-time index build those local arms depend on is charged where `docs/retrieval-eval-results.md` → `## Cold build and index size` puts it"*) and the **latency** bullet's host-stamp sentence: both still resolve after Task 1, so they are read and confirmed rather than edited — but if either has become false, fix it here, since this file is this task's.
- [ ] **Correct both paragraphs that describe gate 10 as a step still to run.** *"The step that produces the real-shape figure is the hand run in `docs/development.md` §5 → gate 10, against a private real catalog; the corpora here fix the *procedure* and the regression floor, not the verdict."* — the hand run has happened; say so with its date and its corpus size, cite where its figures live, and keep the surviving half of the point intact: the committed corpora fix the procedure and the floor, not the verdict, and the lessons-ledger rule that a fixture-sized figure never justifies a design decision on its own still binds. Then the closing paragraph of `## Running arm A by hand` — *"This step does not close the roadmap row, and neither did this branch."*, whose last sentence is *"This hand run plus the private-catalog numbers from `docs/development.md` §5 → gate 10 are together what closes it"* — must say precisely what gate 10 did and did **not** settle: it produced the real-catalog cold-build, index-size and calibration evidence, and it is **not** the arm A hand run, which remains awaited and needs a labelled query set and an `INDEX.md` that exist for no catalog outside `fixture-catalog`. Do not rename or fill `docs/retrieval-eval-results.md` → `## Arm A — awaiting a hand run`: that heading is still accurate, and it is another file's anyway. And leave `## The regression floor` **unedited**: its own closing paragraph is the `BLOCKED` / shortfall record about the empty model cache and the floor's failure policy, which this branch does not move.
- [ ] **Sweep the tree and prove closure.** Re-run every command derivation entry in the story index's `## Scope register` verbatim, and re-walk its procedure entry, then check every site each one reaches against that register's rows, confirming each is either an owning task's changed file or a recorded `no-change` with its reason still true. Then run the **machine-path sweep** the register's own derivations do not reach — `grep -rnE '/(Users|home)/[a-z]' docs/retrieval-eval-results.md docs/retrieval.md docs/development.md docs/retrieval-eval.md` — over all four changed files, and confirm it returns no hit this branch added — the single permitted hit is `docs/development.md`'s pre-existing **Gate 6 — self-containment** paragraph, which quotes the sweep itself; a hit in one of the other three tasks' files is raised against that owning task rather than patched here. A path the commands now return that has **no row** is a missed site: raise it, and — if it belongs to `docs/retrieval-eval-results.md`, `docs/retrieval.md` or `docs/development.md` — raise it against the owning task rather than editing that file here.

**Verification:**

- Derivation A, re-run verbatim: `git grep -lE '62\.51|93\.8|94\.8|244 kB|366 MB|940 chunks|extrapolat' -- '*.md' '*.mjs' '*.ts' ':!harness-runs' ':!examples'`. Every path it returns is a register row, and for each `.md` hit the matching lines are read: none presents a cold-build or index-size figure as extrapolated or as current outside the file of record's own *"what the extrapolation predicted"* comparison. `evals/docs-retrieval/cold-build.mjs` stays a `no-change` row — its header explains the instrument, not a figure.
- Derivation B, re-run verbatim: `git grep -lE '60 s|60 seconds|tool-call timeout|tool-call budget|agents that do not query' -- '*.md' ':!harness-runs' ':!examples'`. Every path is a register row; every `plugin/instructions/*.md` hit is the API-overload retry backoff and stays untouched; no hit in the four `docs/` files states a cold-build rule against a wall-clock threshold, and none states the move's cost as a model load spent on agents that do not query. The invariant is *no hit that states a rule*, not a count.
- Derivation C, re-run verbatim in its widened form: `git grep -nE 'ate 10|private real catalog|real-catalog hand run|this branch does not perform|hand run .*(not|never) (made|performed|run)' -- '*.md' '*.mjs' '*.ts' '*.sh' ':!harness-runs' ':!examples'`. **Read every matched line** — this entry is `-n` rather than `-l` precisely because the disposition turns on what the sentence asserts, not on which file it sits in. No line still describes gate 10, any of its legs, or the real-catalog hand run that confirms the abstain threshold, as unperformed. Register rows 23 and 24 (`### The limit on this calibration` and `## The query-log pass`'s closing cross-reference, both in `docs/retrieval-eval-results.md`) are **Task 1's** — a surviving *"does not perform"* there is raised against Task 1, never patched here. Rows 9, 11, 25, 26, 27 and 28 (`docs/cli.md`, `cli/test/init.test.mjs`, `scripts/run-gates.sh`, this file's own `## Arm A — awaiting a hand run` citation, `docs/development.md`'s gate 11 paragraph and `evals/docs-retrieval/README.md`) stay `no-change`: each states a coverage boundary, a provisioning fact or an invocation rule that the run does not alter. A line the widened entry returns with **no row** is a missed site: raise it.
- Derivation D, re-run verbatim: `git ls-files '*setup-worktree.sh'` returns the mirrored pair, and `git diff --stat d31e6c8 -- scripts/setup-worktree.sh cli/templates/scripts/setup-worktree.sh` is empty (acceptance 9, first half).
- Derivation E, re-walked: run its first step, `grep -nE '^## |^- \*\*' harness-runs/lessons.md`, then walk the ledger's topic headings in file order and apply the register's own decision rule to each rule under them — a rule is reached when it constrains a measurement, a design decision taken on a figure, or adopter-facing documentation. Every rule so reached is a register row and its recorded disposition still holds against the four changed files; a rule the walk reaches with no row is a missed site and is raised. **Nothing in this branch appends to `harness-runs/lessons.md`** — the ledger is the user-review fix-plan writer's to write.
- `git diff --stat d31e6c8 -- plugin/agents/` is empty (acceptance 9, second half).
- The generated region of `docs/retrieval-eval-results.md` is byte-identical to branch point: extract the span between `<!-- eval:generated:start -->` and `<!-- eval:generated:end -->` from `git show d31e6c8:docs/retrieval-eval-results.md` and from the working copy and compare them byte for byte (acceptance 10).
- `git diff --stat d31e6c8 -- evals/docs-retrieval/floor.json` is empty — the floor is not this branch's to move.
- **Machine-path sweep over every file this branch changed**, run verbatim: `grep -rnE '/(Users|home)/[a-z]' docs/retrieval-eval-results.md docs/retrieval.md docs/development.md docs/retrieval-eval.md` returns **no hit this branch added**. It is not a print-nothing check: `docs/retrieval-eval-results.md`, `docs/retrieval.md` and `docs/retrieval-eval.md` must each return nothing, while `docs/development.md` carries exactly **one** pre-existing hit at branch point `d31e6c8` — its `## 5. Verifying a change` → **Gate 6 — self-containment** paragraph, which quotes this sweep and its fictional `/Users/me` / `/home/ada` examples as prose. Read every hit by eye against that; confirm the count is unchanged with `git grep -cE '/(Users|home)/[a-z]' d31e6c8 -- docs/retrieval-eval-results.md docs/retrieval.md docs/development.md docs/retrieval-eval.md`, which reports `docs/development.md:1` and nothing else. This is the branch's real self-containment check and must be read **independently of `scripts/run-gates.sh`**: gate `6a no machine paths` is already red in this checkout by design, so a hit added by any of the four tasks would not surface as a *new* gate failure. Also confirm by eye that every `strings -a` citation across the four files reads `~/.local/share/claude/versions/2.1.278` in tilde form and none has been expanded, and that the corpus is named everywhere by commit, size and character rather than by a path. Any hit inside `docs/retrieval-eval-results.md`, `docs/retrieval.md` or `docs/development.md` is **raised against the owning task**, never patched here.
- `bash scripts/run-gates.sh` prints no new failure (acceptance 12). Run it **without a pipe** — piping into a pager returns the pager's status, and a failing gate then reads as a passing one.
- The machine-wide model cache is untouched by this branch: no task in this plan runs an index build, a `docs fetch-models`, an `init` or a `doctor` against any repository, so the cache is in the state gate 10 left it — restored and verified at 57M — and the branch says so in `docs/retrieval.md` item (d), which Task 2 wrote. Confirm the sentence is there and that nothing in the four changed files points the cache elsewhere.
