### Task 10 — Rewrite `## Running arm A by hand` for two variants, establish the ad-hoc route's comparability, and record the rejected subagent route

**Goal:** Make `docs/retrieval-eval.md` → `## Running arm A by hand` the procedure the operator follows at Task 16's park — both variants, five repetitions each, against a catalog named only by an environment variable — and, before anyone spends a token, **establish** the property the whole comparison rests on: that a `ref` arm A writes is string-comparable to a label and to a B–E hit when the catalog is read through the ad-hoc route. Record the result as a measured fact, and write the one paragraph the task prompt asks for rejecting a dispatched-subagent route.

**Depends on:** Task 9, which ships the runner contract this procedure documents:

```
run-arm-a.sh [--variant index|search] [--index <path>] [--model <name>] <corpus-root> <query-set.jsonl> <out.jsonl>
```

— defaults `--variant index --index docs/INDEX.md`; a pre-flight refusing a task file whose token counts are wrong (exit 65) or a missing index (exit 66); `--output-format stream-json --verbose` and `--strict-mcp-config`; records `{ id, query, refs, durationMs, usage, variant, toolCalls }`. **Also depends on Task 7** (`--corpus-id`, and the query set of a named ad-hoc corpus defaulting to `evals/docs-retrieval/queries/<id>.jsonl` in this checkout) and **Task 8** (repeatable `--transcript [index=|search=]<path>`, one generated row per variant).

### Targets

- `docs/retrieval-eval.md` → `## Running arm A by hand` (its opening, `**Before spending tokens.**`, `### The command`, `### Reading the token cost out of the run`, `### Five repetitions, and what is recorded`, `### What to do with the result`). The section's citation of `docs/retrieval-eval-results.md` → `## Arm A — awaiting a hand run` and its closing *"What gate 10 settled, and why this hand run is still owed."* paragraph are **not** this task's: Task 16 re-points the citation when it renames that heading, and Task 25 rewrites the closing paragraph once the run is recorded.

**Work:**

- [ ] **The comparability check, run first.** Through `bash scripts/scratch-run.sh`, copy `evals/docs-retrieval/corpora/fixture-catalog/docs/` in process into a throwaway catalog under `harness-runs/scratch/throwaway-catalog/docs/` (a public, invented catalog read as a repository of its own), cut `fixture-catalog.jsonl` to the three ids `evals/docs-retrieval/arm-a/sample-transcript.json` answers, and run `runEval` twice with `--transcript <sample>`: once as `--corpus fixture-catalog`, once as `--repo harness-runs/scratch/throwaway-catalog --docs-root docs --corpus-id throwaway-catalog --queries <the cut set, absolute>`. Assert that every label resolves on both, that arm A's per-query `hits[].ref` and its recall@5 / MRR are identical across the two, and that each B–E arm's first-query `hits[].ref` list is identical across the two. Remove the throwaway catalog in process. If any assertion fails, stop and return a blocker naming it — the operator must not spend tokens on a route that cannot score.
- [ ] **Record the check** in `**Before spending tokens.**` as a measured fact, in this tree's form (`.claude/context/conventions.md` → `## Documents of record`): what was compared, the launcher's body, the command in a fenced block, and the result, with every path shown repo-relative. Then the conventions comparability, **settled rather than hidden**: the B–E corpus is `docs.root` plus the conventions documents the catalog's own `harness.config.json` names, neither arm A variant is pointed at them though both can read them from the corpus root, and A-index's index may not link every half of a mixed catalog — both are reported per half as limits of the comparison.
- [ ] **The procedure for two variants.** The pre-checks (both files' token counts per `run-arm-a.sh`'s `REPRO` block; the query set's ids parse; for A-index, the index file exists under the corpus root; the flag spellings `-p`, `--output-format stream-json`, `--verbose`, `--allowed-tools`, `--disallowed-tools`, `--strict-mcp-config`, `--model` confirmed against the agent CLI's own `--help`), and the first-spent-query check (the first record carries bare `path#anchor` strings in `refs` and no code-fence entry). The fixture command, unchanged; then the real-catalog form, **one command per fenced line**, for each variant, with `"$HARNESS_EVAL_CORPUS_ROOT"` as the corpus root, `evals/docs-retrieval/queries/<corpus-id>.jsonl` as the set, an output under `harness-runs/scratch/arm-a/<corpus-id>/<variant>-rep<N>.jsonl`, and `--model <name>`. Say that the session starts with no MCP server (`--strict-mcp-config`), that a harness-initialised catalog's own `CLAUDE.md` is still loaded — the realistic adopter case — and that `toolCalls` is how a reader later tells whether A-index grepped.
- [ ] **What is recorded, per variant** (the task prompt's `## The arm A runs`): every repetition kept; median and p95 across the five for latency and token cost; per-repetition recall@5 and MRR, pooled and per half; the per-query `usage` blocks, which are the committed transcripts themselves; the host stamp in this tree's shape plus the agent CLI version and the model; the 5-hour and 7-day window utilisation before and after each pass. Point at `docs/retrieval-eval.md` → `## The decision rule` → `### Two arm A variants, and how they combine` for how the figures are graded, restating none of it. `### What to do with the result`: publication through `--out … --transcript index=<rep 1> --transcript search=<rep 1>`, and the spread hand-written below the end marker.
- [ ] **The rejected dispatched-subagent route** — one paragraph in the section's opening, beside the two routes already refused (`run-arm-a.sh`'s own header): a dispatched subagent returns no `usage` block for the cost column, inherits the parent's working directory and context rather than starting clean in the corpus, and is fenced by a static agent allowlist rather than per-invocation flags; so it is rejected, and nothing is built for it (task prompt `## Out of scope`; `## Acceptance` 12).

**Verification:**

- The comparability check's assertions all held, and the recorded paragraph quotes its result with no absolute path; `bash scripts/check-eval-artifacts.sh` prints nothing.
- Every command in the section sits in a fenced block, one command per line (`harness-runs/lessons.md` → the fenced-block rule), and no command carries a literal path to the catalog — only `"$HARNESS_EVAL_CORPUS_ROOT"`.
- The fixture command in `### The command` is byte-identical to the one before this task.
- `grep -n 'Arm A — awaiting a hand run' docs/retrieval-eval.md` still returns the section's citation (Task 16 re-points it), and the closing *"still owed"* paragraph is unchanged (Task 25's).
