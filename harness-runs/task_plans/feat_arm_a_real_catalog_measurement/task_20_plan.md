### Task 20 — Apply the decision rule bar by bar, record the outcome, and open the change it names

**Goal:** Take the verdict `docs/retrieval-eval.md` → `## The decision rule` was written to receive: answer each of its three bars in writing against each arm A variant's measured figures with the arithmetic shown, combine them into one verdict by the rule Task 3 committed before any figure existed, record the outcome as one of the rule's three — or as a stated inability to apply it that names the missing measurement — and open the change the outcome names as a numbered roadmap item.

**Depends on:** Task 3, whose `docs/retrieval-eval.md` → `## The decision rule` → `### Two arm A variants, and how they combine` fixes every reading this task applies and none it may choose: per-metric "stronger"; a variant's figure is the median of its repetitions; pooled figures decide, splits shown; relevance margins `1 / positives` of recall@5 and `0.5 / positives` of MRR, which the lead must **exceed**; cost ratio = arm E's p95 over the stronger variant's median per-repetition p50 per-query wall time, *a fraction* meaning any ratio below 1; token cost per query as defined there; failure = arm E's `abstainedOnNegative / negatives` against the stronger variant's median share of negatives answered `none`, arm E at least as high; relevance and cost clearing with failure not → **stays opt-in**, arm E's negative-abstention rate recorded as the figure to move; a variant stopped at the checkpoint graded on what it has, marked partial. **Also depends on** Task 19 (every arm A median, p95, per-half and far/near figure, in `## Arm A — the real-catalog hand run`), Task 18 (arm E's pooled recall@5, MRR, p95, `abstainedOnNegative` and negatives in the `gate10-catalog` block), and Task 11's `breakdown({ records, queries })`, used here for arm E's per-half and far/near figures from the block's `perQuery` entries.

### Targets

- `docs/retrieval-eval-results.md` — a new hand-written section **`## The decision, applied to the real catalog`**, after `## Arm A — the real-catalog hand run`, below the end marker.
- `docs/development.md` → `## 6. The roadmap this tree defers to` — **roadmap item 18**, only when the outcome names a change: *on by default* (the four-place configuration contract `.claude/context/conventions.md` → `### The order files are created…` describes) or *withdrawn* (the tool, its optional dependencies, and the constant with it).

**Work:**

- [ ] **Relevance**, pooled, with the per-half figures shown beside: `positives` and `1 / positives` **as numbers**, `0.5 / positives`; arm E's recall@5 and MRR; each variant's median recall@5 and MRR (every repetition's values cited from Task 19's section); the stronger figure per metric; each lead and whether it exceeds its margin. Then the same bar against each variant alone.
- [ ] **Cost**: arm E's p95 over each variant's median per-query p50 as a ratio, and over the stronger one; arm E's zero billed tokens against each variant's median billed tokens per query; the statement that both clear **by construction** (local milliseconds and zero tokens against an agent session per lookup), so this bar cannot discriminate here and the verdict turns on relevance and failure. Cite `## Cold build and index size` → `### The real-catalog build — 1,960 chunks, 2026-09-22` for the one-time charge — do not re-measure — and say whether it is the same snapshot, from the `gate10-catalog` block's own provenance stamp and the catalog commit against gate 10's `010c50e` (whose `docs/` the operator verified identical to `57a6c25`). **Failure**: arm E's negative-abstention rate against each variant's median `none` rate, pooled for the bar, `far` and `near` shown.
- [ ] **The verdict**: each bar graded against the stronger variant on that bar, the combined verdict, the verdict against A-index alone and against A-search alone, and the outcome — **on by default**, **stays opt-in** (with the figure a later change must move), or **withdrawn** — or *the rule cannot be applied here*, naming the missing measurement (never the corpora's size). Where a bar turned out unmeasurable or ambiguous at these values, record it as **a finding about the rule**, and do not rewrite the rule.
- [ ] **The limits, stated with the verdict**: the set is model-authored and arm A's navigator is a model; the conventions documents and `docs/vite/` against each variant's reach (from `## Running arm A by hand`'s comparability note); and the one this corpus cannot measure — **whether agents would search the docs at all**: the operator's observation that agents with a catalog and no retrieval almost never navigate from `INDEX.md` and go to the code instead, so the realistic alternative is often *not consulting the docs*; what would measure it — the query log switched on in a repository with both code and a catalog, over real branches, counting `search_docs` calls per planning and review dispatch — and that this branch does not take it.
- [ ] **The change it names.** *Stays opt-in* or *cannot be applied*: no roadmap row; the section says what holds the verdict. *On by default* or *withdrawn*: add row **18** to `docs/development.md` §6 in the table's existing shape — what the change is, the section of record this verdict is in, and that this branch took the decision and deliberately did not execute it (a flip is the four-place contract plus every adopter's behaviour; a withdrawal removes a shipped verb, its optional peers and the plugin's grants — neither belongs in the review of a measurement). The section cites the row by number.

**Verification:**

- Every figure in the new section traces to the `gate10-catalog` block or to `## Arm A — the real-catalog hand run`, and the arithmetic is shown for each bar, not only its result.
- The outcome line names exactly one of the rule's three outcomes, or *cannot be applied* with the missing measurement named.
- `git diff docs/retrieval-eval.md` for this task is empty — the rule is applied, not edited; `git diff` on the results file touches nothing between the generated markers.
- If row 18 was added, `docs/development.md` §6's table still parses as one table and the number is cited from the new section.
