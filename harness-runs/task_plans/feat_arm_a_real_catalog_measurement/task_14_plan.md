### Task 14 — Author the negatives, pass the label pre-flight, and record the set in `## The real-catalog query set`

**Goal:** Complete `evals/docs-retrieval/queries/gate10-catalog.jsonl` with **at least 24 negative queries, at least 14 of them `near`** — plausible queries an agent would issue from a real situation that this catalog happens not to answer, each confirmed uncovered by reading — pass the label pre-flight over the finished set, and write the one record of what the set is and how it was made, ready for the operator's review at Task 15.

**Depends on:** Tasks 12 and 13, which leave the file holding at least 32 positives (at least 18 under `docs/expause-web/`, ids `q-g10-ew-<slug>`; at least 14 under `docs/vite/`, ids `q-g10-vite-<slug>`), each `{ id, query, labels, situation, intent, origin }`. And Task 4's contract for a negative: `labels: []`, plus `negativeKind` one of `far` / `near`, which the loader refuses on a positive. The task prompt asks for at least 20 negatives with at least half `near`; the higher targets here are margin, because the calibration needs a distribution to observe, not to confirm.

### Targets

- `evals/docs-retrieval/queries/gate10-catalog.jsonl` — appended negative records only.
- `docs/retrieval-eval-results.md` — a new hand-written section **`## The real-catalog query set`**, placed after `## The query-log pass` and before `## Arm A — awaiting a hand run`, below the end marker.

**Work:**

- [ ] **Negatives, situation-first, `near` above all.** Each carries `situation`, `intent`, `origin`, `labels: []` and `negativeKind`. `near`: a feature Expause plausibly has but does not document, a Vite option or behaviour that does not exist, a topic adjacent to a documented one — close to covered material, which is what decides a threshold. `far`: plausible for an agent in a software project but off this catalog's subject. **`near` is a judgement about meaning, made now**, before any score exists. Ids `q-g10-neg-<slug>`.
- [ ] **Confirm each negative uncovered by reading**, not by a search coming back empty: read the documents its subject would live in (under `printenv HARNESS_EVAL_CORPUS_ROOT`, with `Read` / `Grep` / `Glob`) and satisfy yourself no section answers it; a `Grep` that finds nothing is where the reading starts, never the proof. **No arm, no `docs search`, no MCP tool** (story index `## Context`, prohibition 1).
- [ ] **The label pre-flight over the complete set**, through the launcher Tasks 12 and 13 used (`corpusConfig` with `docsRoot: 'docs'`, the catalog's own conventions documents and `corpusId: 'gate10-catalog'` → in-memory `buildIndex` → `loadQueries` → `assertLabelsResolve`; nothing else). It must print `labels resolve`; record the command, the snapshot it printed, and whether that equals gate 10's `{ files: 156, chunks: 1960 }` — if not, which files the composition adds or leaves out against it, established by listing the catalog's `docs/` and conventions documents.
- [ ] **`## The real-catalog query set`**: who reads it and what it owns; the catalog named by commit (`57a6c25`, `docs/` byte-identical to gate 10's `010c50e` per the operator's check) and size, never by path; the counts **re-derived from the file** — positives in total and per half, negatives in total and per `far` / `near`, per intent, per `origin` (with the harvest stated as used or not, per the task prompt's row 8) — and `1 / positives` for this set **as a number**; the method (situation first, query from the situation alone, labels by reading, grades assigned deliberately, negatives confirmed uncovered by reading, no search or eval arm run while authoring); the **limits**, disclosed rather than harmonised: the author is a model and so is arm A's navigator, so the set is not neutral; this set is agent-shaped while `fixture-catalog` and `self-docs` carry natural-language questions, so a figure over it beside a committed-corpus figure is not over like-phrased sets; and the label pre-flight's command and result. End with a line that the set **awaits the operator's approval** (Task 15 replaces it).

**Verification:**

- `loadQueries` accepts the file; the pre-flight prints `labels resolve`, and the section records its snapshot against gate 10's.
- Counting the file's own fields: at least 32 positives (at least 18 and 14 per half), at least 24 negatives, at least 14 `near`, every record with `situation`, `intent` and `origin`, every negative with `negativeKind`, and no positive with it.
- `git diff` on the `.jsonl` shows appended lines only; `git diff` on `docs/retrieval-eval-results.md` touches nothing between the generated markers.
- `bash scripts/check-eval-artifacts.sh` prints nothing; the new section names no path outside this tree.

**Deviations from plan:**

- The implementer's toolset carries `Read` but no `Grep` / `Glob`. Term probes over the catalog ran through a read-only scratch script, `harness-runs/scratch/task14_probe.py` via `bash scripts/scratch-run.sh`, which reads `HARNESS_EVAL_CORPUS_ROOT` itself and lists matching lines, file headings and per-half file counts; every candidate negative was then confirmed by `Read` of the documents its subject would live in (e.g. `features/chat.md`, `features/login-session.md`, `features/content-comments.md`, `features/wallet-coins.md`, `features/share.md`, `features/community-wall.md`, `features/video-playback.md`, `features/gifting.md`, `concepts/cloud-functions-client-surface.md`, `architecture/ARCHITECTURE.md`, and the Vite `config/*-options.md` / `guide/{assets,build,static-deploy,performance}.md` heading sets). One early directory listing and `git log` of the catalog ran as bare shell commands before the probe existed; both were read-only and nothing was written. No `docs search`, MCP tool or eval arm ran.
- Candidates dropped on reading because a section covers them: global uncaught-error capture (`concepts/analytics-logging.md`), content drafts (`features/content-management.md`), dark mode (`concepts/responsive-theme.md`), subtitles and picture-in-picture (listed in `QUICK_SETUP_VIDEO.md`), referral mechanics and age verification (product docs), a public dev-server tunnel (adjacent to `server.allowedHosts`), per-locale i18n builds (risk of `concepts/localization-l10n.md` reading as an answer).
