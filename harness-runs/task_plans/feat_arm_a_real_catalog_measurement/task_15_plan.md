### Task 15 — Park for the operator's approval of the query set, and record it before anything is scored

**Goal:** Put the finished `gate10-catalog` query set in front of the operator (task prompt `## Operator checklist` row 6: *"Nothing is scored against a set the operator has not approved"*), apply whatever corrections come back, and commit the approval into `## The real-catalog query set` **before any arm scores the set** — the commit order is acceptance 1's evidence that approval preceded scoring.

**Depends on:** Task 14, which completes `evals/docs-retrieval/queries/gate10-catalog.jsonl` (at least 32 positives split `docs/expause-web/` / `docs/vite/`, at least 24 negatives with at least 14 `near`, every record carrying `situation`, `intent`, `origin`, negatives carrying `negativeKind`), passes the label pre-flight (recording its snapshot against gate 10's `{ files: 156, chunks: 1960 }`), and writes `docs/retrieval-eval-results.md` → `## The real-catalog query set` ending with a line that the set awaits approval. **Also depends on Task 10**, whose `docs/retrieval-eval.md` → `## Running arm A by hand` → `**Before spending tokens.**` records the ad-hoc route's comparability check as passed — the task prompt requires both before this park, so the operator is never asked to approve a set or a route that cannot score.

### Targets

- `docs/retrieval-eval-results.md` → `## The real-catalog query set` — its closing line replaced by the approval record.
- `evals/docs-retrieval/queries/gate10-catalog.jsonl` — **only** the corrections the operator returns, if any.

### Operator answer

*(Filled in by the task-plan writer when the run resumes from this task's park, from the operator's `answer_<n>.md`, with no machine path.)*

**Recorded on resume from park 2** (`question_2.md` / `answer_2.md` in this branch's clarifications directory). **Q1: option (b), approved with corrections.** This is the **approval path**: apply every correction below, re-run the label pre-flight, update the counts, write the `**Operator approval.**` record, and **do not park again**. The operator's answer, verbatim:

> Q1: (b) Approved with corrections. Positives: q-g10-ew-mark-notification-read: add docs/expause-web/features/inbox.md#data grade 3. q-g10-ew-unlock-payload: relabel docs/expause-web/features/unlock-premium-content.md#cloud-functions to grade 3. q-g10-vite-mock-updated-event: add docs/vite/guide/api-plugin.md#handlehotupdate grade 3. q-g10-ew-watch-later-feed-signal: add docs/expause-web/features/saved-content.md#domain grade 2. q-g10-ew-new-notification-tap: add docs/expause-web/features/inbox.md#presentation grade 2. q-g10-ew-group-room-agora-token: add docs/expause-web/features/community-live-streaming.md#cloud-functions grade 2. q-g10-vite-wsl-file-save: relabel docs/vite/guide/troubleshooting.md#vite-does-not-detect-a-file-change to grade 2. q-g10-vite-dev-api-forward: drop docs/vite/config/server-options.md#servercors. Negatives: q-g10-neg-vite-server-mock: convert to positive with docs/vite/guide/api-plugin.md#configureserver grade 3 and docs/vite/config/server-options.md#serverproxy grade 1 (remove negativeKind). q-g10-neg-coin-promo-code: convert to positive with docs/expause-web/product/product-decision-rate-card.md#34-accompanying-mechanics-relabeled-honestly grade 3 and docs/expause-web/features/wallet-coins.md#business-behaviour grade 1 (remove negativeKind). Drop q-g10-neg-sendgift-rate-limit and q-g10-neg-vite-dev-basic-auth. q-g10-neg-storybook-stories: reword situation to "Plan step: add Storybook stories for the new shared `PayoutBadge` component's variants." q-g10-neg-group-chat: reword situation to "Task: let users start an ad-hoc group DM with three or more other users and add participants later." Update the counts in docs/retrieval-eval-results.md → ## The real-catalog query set accordingly.

**The corrections as edits to `evals/docs-retrieval/queries/gate10-catalog.jsonl`** — each touches only the named record; every other record stays byte-identical. The writer checked on resume that all fourteen ids exist in the file and that every heading the new refs point at exists in the catalog (`### Data`, `### Presentation` in `features/inbox.md`; `### Domain` in `features/saved-content.md`; `### Cloud Functions` in `features/community-live-streaming.md`; `` ### `handleHotUpdate` `` and `` ### `configureServer` `` in `guide/api-plugin.md`; `### 3.4 Accompanying mechanics (relabeled honestly)` in `product/product-decision-rate-card.md`; `## Business behaviour` in `features/wallet-coins.md`); the pre-flight's `labels resolve` is what proves the slugs.

| # | Query id | Edit |
|---|---|---|
| 1 | `q-g10-ew-mark-notification-read` | add label `docs/expause-web/features/inbox.md#data` grade 3 |
| 2 | `q-g10-ew-unlock-payload` | change the grade of `docs/expause-web/features/unlock-premium-content.md#cloud-functions` from 2 to 3 |
| 3 | `q-g10-vite-mock-updated-event` | add label `docs/vite/guide/api-plugin.md#handlehotupdate` grade 3 |
| 4 | `q-g10-ew-watch-later-feed-signal` | add label `docs/expause-web/features/saved-content.md#domain` grade 2 |
| 5 | `q-g10-ew-new-notification-tap` | add label `docs/expause-web/features/inbox.md#presentation` grade 2 |
| 6 | `q-g10-ew-group-room-agora-token` | add label `docs/expause-web/features/community-live-streaming.md#cloud-functions` grade 2 |
| 7 | `q-g10-vite-wsl-file-save` | change the grade of `docs/vite/guide/troubleshooting.md#vite-does-not-detect-a-file-change` from 3 to 2 (`server-options.md#serverwatch` stays the grade-3 label) |
| 8 | `q-g10-vite-dev-api-forward` | remove label `docs/vite/config/server-options.md#servercors` (`#serverproxy` grade 3 stays) |
| 9 | `q-g10-neg-vite-server-mock` | becomes a positive: `labels` = `docs/vite/guide/api-plugin.md#configureserver` grade 3, `docs/vite/config/server-options.md#serverproxy` grade 1; remove `negativeKind` |
| 10 | `q-g10-neg-coin-promo-code` | becomes a positive: `labels` = `docs/expause-web/product/product-decision-rate-card.md#34-accompanying-mechanics-relabeled-honestly` grade 3, `docs/expause-web/features/wallet-coins.md#business-behaviour` grade 1; remove `negativeKind` |
| 11 | `q-g10-neg-sendgift-rate-limit` | delete the record |
| 12 | `q-g10-neg-vite-dev-basic-auth` | delete the record |
| 13 | `q-g10-neg-storybook-stories` | replace `situation` with the operator's quoted sentence for this id in the answer above, character for character — it keeps the backticks around `PayoutBadge` and drops the old *"next to the other component stories"* clause |
| 14 | `q-g10-neg-group-chat` | replace `situation` with the operator's quoted sentence for this id in the answer above, character for character |

**How to apply them, where the answer is silent.** A new label is inserted so the record's `labels` stay ordered by grade, highest first, after any existing label of the same grade. **Only the fields named change**: no `query`, `intent`, `origin` or `id` is edited — the two converted records keep their `q-g10-neg-` ids (an id is a stable key and the operator renamed neither), and rows 13–14 change `situation` alone. Deleting a record removes its whole line and leaves no blank line.

**What the corrections imply for the counts**, as a cross-check only — the published figures are the counting script's output, never this arithmetic: 71 records → 69 (two deleted); positives 42 → 44 (two converted; `q-g10-neg-coin-promo-code`'s labels sit under `docs/expause-web/`, `q-g10-neg-vite-server-mock`'s under `docs/vite/`); negatives 29 → 25, all four removed from `near` (two Expause-shaped, two Vite-shaped), `far` unchanged; `1 / positives` moves with the positive count. If the script disagrees with this arithmetic, the script's file read is right and the edit is wrong — re-check the edits before publishing anything.

**What the approval record must disclose beyond the list of corrections:** that the operator found **two negatives answered by the catalog** (`q-g10-neg-vite-server-mock`, `q-g10-neg-coin-promo-code`) and converted them, and dropped two more — a finding about the reading-only negative check that `**Limits, disclosed rather than harmonised.**` → *the set is not neutral* bears on, stated in the approval record, not by rewriting the method bullets; and that the two converted records keep ids reading `neg`.

**Work:**

- [ ] **Find the answer.** Read `### Operator answer` above; if it is empty, look for an `answer_<n>.md` beside a `question_<n>.md` that names **Task 15** at the top level of `harness-runs/clarifications/feat_arm_a_real_catalog_measurement/`, then under its `answered/` archive. No answer found → the next bullet; an answer found → the bullet after it.
- [ ] **No answer: park.** Confirm the two preconditions are committed (the `## The real-catalog query set` section with its pre-flight result, and the comparability check in `**Before spending tokens.**`), then change nothing and return exactly one line: `blocker: operator approval of the gate10-catalog query set owed — task prompt ## Operator checklist row 6 is a mandated mid-run park, not an assumption; review evals/docs-retrieval/queries/gate10-catalog.jsonl (counts and method in docs/retrieval-eval-results.md → ## The real-catalog query set): spot-check that every grade-3 label answers its query and that every negative is uncovered, then answer "approved", or list corrections by query id`. This is an **operator-only, high-stakes** step (it gates token spend and the measurement's integrity), so the orchestrator parks rather than assuming.
- [ ] **Corrections without approval:** apply each correction to the named record — never to any other — re-run the label pre-flight exactly as Task 14 did (no arm, no search), update the counts in `## The real-catalog query set`, and park again with the same blocker, naming the corrections applied. **Approval, with or without corrections:** apply any corrections as above — re-running the pre-flight with `bash scripts/scratch-run.sh harness-runs/scratch/task12_preflight.mjs` and re-deriving the counts with `bash scripts/scratch-run.sh harness-runs/scratch/task14_counts.mjs`, then rewriting from that output every count the section states (the `**The counts**` table rows, the `**`1 / positives`**` line with its percentage-point gloss, and the quoted pre-flight output's `loaded … queries` line) — then replace the section's closing *awaits approval* line with **`**Operator approval.**`** — the date, that the operator spot-checked the grade-3 labels and the negatives, each correction applied by id (or *none*), and the statement that **no arm had scored this set when it was approved**.

**Verification:**

- On the approval path: the commit landing this task's record contains no file under `evals/docs-retrieval/transcripts/` and no `gate10-catalog` block in the generated region — `git log --oneline` puts it before Task 17's and Task 18's commits.
- After any correction, the pre-flight prints `labels resolve` and the same snapshot Task 14 recorded, and the recorded counts still meet at least 30 positives split per half and at least 20 negatives with at least half `near`.
- Every count in `## The real-catalog query set` equals the `task14_counts.mjs` output taken after the corrections; `git diff` on the query set touches only the fourteen ids in `### Operator answer`, and neither converted record's line carries `negativeKind`.
- `bash scripts/check-eval-artifacts.sh` prints nothing; the approval record carries no path outside this tree and none of the answer file's own location.
