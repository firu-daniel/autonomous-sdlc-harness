### Task 22 — Move `ABSTAIN_SCORE_THRESHOLD` to the value the fixed method gives, or record why it stays, and correct the package's own "not yet measured" statements

**Goal:** Apply the re-calibration method — computed by the committed `calibrate.mjs`, not by hand — to the pooled observed distributions, move the constant in `cli/src/retrieval/search.ts` when the method gives a new value, and bring the `cli` package's two statements that retrieval is "not yet measured" in line with the recorded decision.

**Depends on:** Task 6, whose `evals/docs-retrieval/calibrate.mjs` exports:

```js
readPerQuery(resultsText, corpusId, mode)   // mode required, a member of SEARCH_MODES, unknown refused by name
                                            // -> that arm's per-query entries of one generated block (read via results.mjs → readCorpusMachineHalf)
calibrateThreshold(pool)                                     // pool: [{ corpus, queries, perQuery }]
//   -> { method: 'separable' | 'overlapping' | 'cannot-separate', value: number | null, highestNegative,
//        lowestPositive, lowestGrade3Positive, nearBelowL, price, excluded, points }
```

**Also depends on** Task 21 (the `fixture-catalog` and `self-docs` blocks regenerated with `bestRerankScore`), Task 18 (the `gate10-catalog` block), and Task 20, whose `docs/retrieval-eval-results.md` → `## The decision, applied to the real catalog` records the outcome — on a **withdrawn** outcome the constant goes with the tool rather than moving here. Task 1 already made `SearchResult.bestRerankScore` the value abstention compares; this task does not touch the field.

### Targets

- `cli/src/retrieval/search.ts` → `ABSTAIN_SCORE_THRESHOLD` and its doc comment — **only** when the method gives a value other than `0.32` and the outcome is not *withdrawn*.
- `cli/src/retrieval/queryLog.ts` — the header sentence *"the feature ships opt-in and not yet measured against the index-first navigation agents use today"*.
- `cli/README.md` → **What the five subcommands do.** — *"It is **off by default, and not yet measured** against index-first navigation"*.

**Work:**

- [ ] **Compute.** A launcher under `harness-runs/scratch/`, run with `bash scripts/scratch-run.sh`, reads `docs/retrieval-eval-results.md`, calls `readPerQuery(text, id, mode)` for `gate10-catalog`, `fixture-catalog` and `self-docs` with `mode` the arm E mode — taken from `evals/docs-retrieval/arms.mjs` → `armForLetter('E').mode` (the arm table, the one owner of the letter-to-mode mapping) rather than typed, loads each corpus's set with `loadQueries` (`evals/docs-retrieval/queries/<id>.jsonl`), and prints `calibrateThreshold` over the three as JSON. Take its `method` and `value` as they come; do not adjust the pool, a mark or a rounding.
- [ ] **Predict gate 11 at the new value — before the constant is touched.** Only when the previous bullet's `value` would be applied (not *withdrawn*, not `cannot-separate`, not `0.32`): extend the same launcher to read `readPerQuery(text, 'fixture-catalog', <arm E mode>)` — the regenerated block Task 21 wrote — and compute, for the **positive** entries only, the arm E recall@5 and MRR `fixture-catalog` would have at `value`, with the definitions `evals/docs-retrieval/metrics.mjs` uses (a query's rank counts when `rank > 0 && rank <= 5`; MRR is the reciprocal-rank sum over the positives). A positive whose `bestRerankScore < value` is predicted to abstain (rank `0`); any other keeps its recorded `rank`. `bestRerankScore` does not depend on the threshold, so no eval re-run is needed. **When `value < 0.32`** a positive that abstained at `0.32` would stop abstaining with a rank the block does not carry; both figures can then only rise, so record the prediction as *"cannot fall — lowered threshold"* and skip the arithmetic. Compare the two predicted figures with `evals/docs-retrieval/floor.json`'s arm E floors (`recallAt5` `0.61`, `mrr` `0.5` as of planning — read the file, do not trust these numbers) and report in the return: both predicted figures, both floors, and whether either falls **below** its floor. This bullet edits no file.
- [ ] **Move, or record why not.** *Withdrawn* outcome → no constant edit. `method: 'cannot-separate'`, or `value === 0.32` → no constant edit. Otherwise set `ABSTAIN_SCORE_THRESHOLD` to `value` and change the doc comment's *"calibrated at `0.32`"* to the new value — the comment keeps citing `docs/retrieval-eval-results.md` → `## Threshold calibration` as the only record of how it was chosen and restates no figure of the evidence; its stub-bound paragraph stays true for any value strictly inside `(0.000, 1.000)`. Whichever branch applies, report the launcher's JSON in the return — Task 23 re-runs the same computation to write the record.
- [ ] **The two package statements.** Rewrite each to state what the decision recorded — measured once against agent navigation on a real 1,960-chunk catalog, the outcome in one word, and a pointer to `docs/retrieval-eval-results.md` → `## The decision, applied to the real catalog` — keeping each sentence's own purpose (`queryLog.ts` explains why the record shape has an owner file; `cli/README.md` introduces the verb). Rename prose only: no configuration key, tool name or verb changes (`harness-runs/lessons.md` → the adopter-vocabulary rule).

**Verification:**

- `bash scripts/typecheck.sh` exits 0, without a pipe.
- `bash scripts/test.sh`, without a pipe — including `cli/test/docs-retrieval.test.mjs`'s abstention cases under `stub-overlap` and Task 1's `cli/test/docs-retrieval-rerank-score.test.mjs`, whose below-threshold case derives its score from the constant and so holds at any value. The known pre-existing failure, in both branches below, is gate `6a no machine paths`, red in this self-adopted checkout by design (`docs/development.md` §5 → **"The `$HOME` half is a different matter, and self-adoption breaches it."**); it is not this task's to fix and does not count against either branch:
  - **When the prediction bullet did not run, or predicted no figure below its floor** → prints no failure that was not already failing before this task (gate 11 may still report `BLOCKED` on an empty model cache, as before).
  - **When the prediction bullet predicted an arm E figure below its floor** (Route 1 of the same-commit conflict — story index `## Context` → **The threshold move and the floor re-record**) → the **only** permitted new failure is gate 11: `run-gates.sh` prints `  FAIL  11 docs-retrieval relevance floor (exit 1)`, its indented log detail is the one line `check-floor: fixture-catalog arm E <recall@5 | MRR> floor <floor> (recorded <recordedAt>), measured <figure> — below the floor. …` for the first floored arm E metric the prediction put below its floor (`check-floor.mjs` stops at the first shortfall, and `recallAt5` is listed before `mrr`), `<figure>` equals the predicted figure, and the closing summary names `11 docs-retrieval relevance floor` and no gate that was not already failing before this task. Any other gate failing that was not already failing before this task, a shortfall on another arm or corpus, or a measured figure that differs from the prediction is a real failure — return a `blocker:` naming it rather than committing. The return then states, in one sentence the committer can carry into the commit message, that gate 11 is red by design at this commit and that **Task 23 — the immediately following readiness entry — re-records the arm E floors**; this task does not touch `evals/docs-retrieval/floor.json` (a `general`-layer file, outside this task's layer).
- `git diff cli/src/retrieval/search.ts` is either empty (and the return says which of the three no-move reasons applied) or changes exactly the constant's value and the value in its doc comment.
- `git grep -n 'not yet measured' -- cli` returns nothing.
- The return carries the prediction bullet's figures (or *"not run"* with the no-move reason), so the committed record and Task 23's disclosure can cite them.
