### Task 23 — Rewrite `## Threshold calibration` on the observed distributions and re-run the abstention tests and the floor

**Goal:** Make `docs/retrieval-eval-results.md` → `## Threshold calibration` the one record of the value on its new evidence — the observed, uncensored arm E distributions over three corpora, the method's case and arithmetic, the value (moved or not), what it costs, and the limit on it — and, when the value moved, re-run the fixture abstention tests and gate 11 against it and re-record the floor only if a floored figure changed.

**Depends on:** Task 22, which ran `calibrateThreshold` over the pooled `gate10-catalog`, `fixture-catalog` and `self-docs` blocks and either moved `ABSTAIN_SCORE_THRESHOLD` in `cli/src/retrieval/search.ts` to the returned `value` or left it at `0.32` for one of three stated reasons (*withdrawn* outcome, `cannot-separate`, or the method returning `0.32`). Read the constant's current value from `search.ts` and the reason from that task's commit. The computation's interface, from Task 6:

```js
readPerQuery(resultsText, corpusId, mode)   // mode required, a member of SEARCH_MODES, unknown refused by name;
                                            // the launcher passes arms.mjs → armForLetter('E').mode, never a typed string
calibrateThreshold(pool)   // pool: [{ corpus, queries, perQuery }]
//   -> { method, value, highestNegative, lowestPositive, lowestGrade3Positive, nearBelowL, price, excluded, points }
```

**Also depends on** Task 3's `### The re-calibration method, fixed before the real-catalog run` (the method as written, and Task 4's appended record of how the committed negatives were classed), which this task keeps and does not reword.

### Targets

- `docs/retrieval-eval-results.md` → `## Threshold calibration` — everything except `### The re-calibration method, fixed before the real-catalog run`, which stays as Tasks 3 and 4 wrote it.
- `docs/retrieval.md` → **Abstention.** — the clause restating `ABSTAIN_SCORE_THRESHOLD = 0.32`.
- `docs/retrieval.md` → `## Measured, and how` → **(iv) Search.** — the closing sentence *"That is a confirmation and not a re-calibration: `ABSTAIN_SCORE_THRESHOLD` stays at `0.32`."* only; the rest of the (iv) paragraph stays as it is.
- `evals/docs-retrieval/floor.json` and the `fixture-catalog` generated block — **only** when the value moved, per the last bullet.

**Work:**

- [ ] **Re-run the computation** with the same launcher body Task 22 used (via `bash scripts/scratch-run.sh`), and write from its JSON.
- [ ] **Rewrite the section's figures**: `**The value.**` (the constant as it now stands, applied as `best < ABSTAIN_SCORE_THRESHOLD` to `fused-rerank` alone); `**What it was calibrated on.**` (the three corpora by id and snapshot stamp — `gate10-catalog` by commit and size, never path — the date, host, Node, models, and that every point is an observed `bestRerankScore`, with any `excluded` id named); **the method's case and arithmetic** (`separable` with the gap and midpoint; or `overlapping` with `L`, the `near` negatives below it, the chosen value and the **price** — every positive it abstains on, by corpus and id; or `cannot-separate` with why, and *the constant is unchanged rather than trading recall silently*); and **the observed distributions, quoted** — one row per point (corpus, id, kind `positive` / `far` / `near` / `unclassed`, grade-3 or not, `bestRerankScore`), replacing the censored table. Keep a short **`### The value this replaces`** recording `0.32`, the censored interval it was the midpoint of, and why it could not be re-derived then.
- [ ] **`### What the move cost`** rewritten from the scores themselves: per corpus, the positives that abstain at the new value and did not at `0.32`, and the negatives that newly abstain — computed from `bestRerankScore`, which does not depend on the threshold, so no re-run is needed to count it. **`### The limit on this calibration`** rewritten: what the three corpora are and are not (one real catalog, one host, one query-set author, agent-shaped real-catalog queries beside natural-language committed ones), and what would move the value next. On a *withdrawn* outcome, record the distributions and say the constant goes with the tool per roadmap item 18.
- [ ] **`docs/retrieval.md`, two sentences.** **Abstention.**: drop the restated `= 0.32`, keeping the sentence's citation of `## Threshold calibration` as the one record. **(iv) Search.**, its closing sentence: rewrite it as the record of what gate 10 confirmed — that leg was a confirmation of the value then in force and not a re-calibration, and the value now in force and its evidence are `docs/retrieval-eval-results.md` → `## Threshold calibration` — **without** restating any numeric value. So a moved value never leaves a second copy behind in either place.
- [ ] **Only if the value moved:** run `bash scripts/test.sh` (the fixture abstention cases), then regenerate the `fixture-catalog` block with `bash scripts/scratch-run.sh harness-runs/scratch/eval.mjs --corpus fixture-catalog --out docs/retrieval-eval-results.md`, and compare its arm E recall@5 and MRR with the values `evals/docs-retrieval/floor.json`'s arm E floors were recorded from. Unchanged → `floor.json` stays byte-identical. Changed → re-record those two floors per `docs/retrieval-eval.md` → `## The regression floor` (0.05 below the new measurement, truncated to two decimals, today's `recordedAt`) in **this task's commit — the one immediately after Task 22's**, never in a commit of its own. Task 22 cannot carry it: `floor.json` is a `general`-layer file and Task 22 is a `cli` task, and the single-layer rule makes one commit holding both `search.ts` and `floor.json` impossible. When Task 22 predicted a shortfall, its commit carries a gate 11 `FAIL` that is red by design (story index `## Context` → **The threshold move and the floor re-record**), and this commit is the one that turns it green. In `### What the move cost`, state the change, its cause — the threshold move — and **disclose, as a finding about `## The regression floor` → the same-commit re-record rule rather than a quiet workaround**, that the re-record lands one commit behind the move that caused it because a cross-layer change cannot be one commit under this tree's single-layer tasks, naming whether Task 22's commit left gate 11 red. The `self-docs` and `gate10-catalog` blocks are not regenerated: each keeps the provenance of the threshold it was taken under, and the section says so.

**Verification:**

- Every figure in the rewritten section appears in the launcher's JSON or in a generated block; `## Threshold calibration` is the only place the evidence is written — `git grep -n 'ABSTAIN_SCORE_THRESHOLD = ' -- docs/retrieval.md docs/cli.md` returns nothing, and reading each hit of `git grep -n 'ABSTAIN_SCORE_THRESHOLD' -- docs/retrieval.md docs/cli.md` — including the **(iv) Search.** closing sentence — finds a citation, never a value.
- `bash scripts/run-gates.sh`, without a pipe, prints no new failure; gate 11 passes (or reports `BLOCKED` on an empty model cache, as before).
- `git diff evals/docs-retrieval/floor.json` is empty unless a floored arm E figure changed, in which case the section names the change, its cause, and the one-commit-behind disclosure; and any gate 11 `FAIL` Task 22's commit carried by design is gone at this commit.
- `### The re-calibration method, fixed before the real-catalog run` is byte-identical to its state after Task 4.
