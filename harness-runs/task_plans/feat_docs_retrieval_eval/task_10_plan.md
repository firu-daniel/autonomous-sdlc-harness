### Task 10 — `docs/retrieval-eval.md`: how to run it anywhere, what each metric means, and the decision rule

**Goal:** Write the operator-facing document for the eval: how to run it against **any** adopter's checkout, what every metric in the results file means, the query-set format, how the regression floor works — and the **decision rule**, written before any real-catalog numbers exist, for when retrieval counts as better than index-first navigation.

**Depends on:** Task 3 (the argument surface: `--repo`, `--corpus`, `--docs-root`, `--conventions`, `--queries`, `--arms`, `--k`, `--repeat`, `--out`, `--data-dir`, `--floor`, and `--transcript <path>`, whose reader is Task 4's `run.mjs` — it scores an arm A hand-run transcript through `scoreTranscript` and is the sole writer of arm A's row; and the label-hygiene refusal `eval: label <ref> (query <id>, <query-file>) resolves to no chunk in corpus <corpus-id>`). Task 4 (the metrics and the results file's two halves, separated by `<!-- eval:generated:start -->` and `<!-- eval:generated:end -->`). Task 9 (the floor, `evals/docs-retrieval/floor.json`, gate 11 in `scripts/run-gates.sh`, and the recorded output of the planted drop). Task 9 also left **two pointers aimed at this file** — `floor.json`'s `see` field and §5's gate 11 paragraph, both naming `docs/retrieval-eval.md`'s regression-floor section — and deliberately stated none of the floor policy itself, so this task is where that policy is written down for the first and only time. Take the two pointer strings from Task 9's commit body and make the heading match them.

**Where this task stops.** It writes the **procedure and the definitions**, not the numbers: every measured figure lives in `docs/retrieval-eval-results.md`, which this document points at and never restates. The **arm A hand-run procedure** is deliberately not here either — it is Task 11's section of this same file, and this task leaves a `## Running arm A by hand` heading with one line saying so, so the two tasks cannot collide over the same paragraph.

### Targets

- `docs/retrieval-eval.md` (new).

**Work:**

- [ ] Open with the *who reads this and what it owns* paragraph `.claude/context/conventions.md` → `## What accompanies a new unit of each kind` requires of a document under `docs/`: it is read by anyone re-running the eval — on this repository or on their own — and it owns the procedure, the metric definitions and the decision rule, while `docs/retrieval.md` owns the retrieval design and `docs/retrieval-eval-results.md` owns the numbers. State up front **what the runner owns and what it does not**: it supplies `docs` and `phases` itself and so does not read `phases.docs` or `docs.retrieval` — which is why this repository's `harness.config.json`, where both are off, is deliberately unchanged — but it **reads `layers[]` and `stateDir` out of the `harness.config.json` at `--repo`**, because `cli/src/retrieval/corpus.ts`'s header forbids any second copy of the conventions list. The operational consequence is the line an adopter needs: point `--repo` at your checkout and the conventions documents your own `layers[]` names are in the corpus automatically, with nothing to configure here; a checkout with no `harness.config.json` is served by `--docs-root` and `--conventions` instead.
- [ ] **How to run it**, in fenced blocks, one command per line, with every command an adopter is meant to run in a block of its own rather than inline — the ledger rule from `harness-runs/lessons.md` → `## Adopter-facing documentation`. Cover: the two built-in corpora by id; an **arbitrary checkout** via `--repo <path> --docs-root <path> --conventions <path>` with a query set of their own; the two permitted execution routes and why a bare interpreter invocation stalls an unattended run; and the preconditions — the real models present (`modelFilesPresent`), the stub variable unset, and `npm run build` having run, because the runner imports the compiled retrieval modules. Document **`--transcript <path>` by its purpose rather than as a bare flag in a list**: it names an arm A transcript produced by a hand run, which `run.mjs` scores through `scoreTranscript` and renders into arm A's row in the same walk as every other arm — so arm A's row is **generated, never hand-edited**, and a hand run is published by re-running the eval with `--out … --transcript …` rather than by typing numbers into the marked region. Give that invocation in a block of its own and point at `## Running arm A by hand` as the procedure that produces the transcript it takes. **Warn, in one sentence beside the `--out` block, that a results file written into a path under `docs.root` joins the corpus the next run measures** — the corpus is every `*.md` under that root, so an adopter either keeps the results file outside it or reads each run's corpus `snapshot` (file and chunk counts) out of the provenance before comparing two runs. Say that this repository takes the second route: `docs/retrieval-eval-results.md` is itself in `self-docs`, which is why every figure recorded over that corpus is stamped and why two figures carrying different stamps are not a before/after pair.
- [ ] **The query-set format and label hygiene**, written as **the** one home of the format — the single home a responsibility gets under `.claude/context/conventions.md` → `### Where a new responsibility goes`. This section owns the field list *and* its interpretation together, because splitting the two is what produces a second copy: `evals/docs-retrieval/queries/README.md` (Task 2) carries the directory contract and a pointer to this heading and states none of the format itself, and Task 2 handed the exact heading text it cited over in its commit body — **take that heading text and make this section's heading match it**, so the pointer Task 2 wrote one commit earlier resolves. Say in one clause that this section is the only file that states the format and that the queries README cites it. The content: the record shape (`id`, `query`, `labels[]` of `{ ref, grade }`), that `ref` is `path#anchor` exactly as a search hit cites it, the `1`–`3` grade scale and what each grade means, that an **empty `labels` array is a negative query** and what it is for, and the refusal a stale label produces — quoted verbatim, as the measured fact `.claude/context/conventions.md` → `## Documents of record` asks for: what was measured, the command, and the exact message.
- [ ] **What each metric means**: recall@k and MRR over `grade >= 1` and the strict variants over `grade == 3`; p50/p95 latency by nearest rank and what is inside the timed span; the cost column and why arms B–E read `local — no billed tokens` while arm A's is an agent's token cost; the abstention columns over negative queries; and the one caveat that must not be lost — arms B, C and D score in rank-derived reciprocal-rank-fusion units in the `0.004`–`0.033` range while arm E's are calibrated `[0, 1]` reranker scores, so **the score columns are not comparable across arms**.
- [ ] **The decision rule**, written now, before any real-catalog numbers exist, so it cannot be chosen to fit them: state the comparison as arm E against arm A on the same queries and the same corpus, and give the rule in three parts — the **relevance** bar (by how much arm E's recall@5 and MRR must exceed arm A's before retrieval is worth its cost), the **cost** bar (arm E's p95 latency against arm A's, and arm A's token cost against arm E's zero billed tokens plus its one-time build), and the **failure** bar (arm E abstaining on negative queries at least as reliably as arm A answers `none`). Say what happens in each outcome — retrieval on by default, retrieval kept opt-in, or retrieval withdrawn — and that the decision is taken on a **real catalog**, since both committed corpora are far below a mature catalog's size and the lessons ledger's standing rule is that a fixture-sized figure never justifies a design decision on its own. Add the **regression floor** section beside it, and write it as **the** record of the floor policy — the one home for a decision of record, which `.claude/context/conventions.md` → `### Where a new responsibility goes` puts in `docs/` *"and nowhere else"*. It carries, and is the only file that carries: what `floor.json` holds; the **margin** each recorded floor sits below its measured value by, **and the reason for that margin**; that gate 11 enforces the floor on `fixture-catalog` only **and why** — that corpus is committed and moves only when the eval does, while `self-docs` moves on every documentation branch and a floor over it would be disabled within a month; the **same-commit re-record rule** — a change that legitimately moves a floor re-records it in the same commit as the change that moved it, with the reason, and the changes that legitimately move one are a change to the fixture, the chunker, the models or the search modes, never the addition of a document under `docs/`; and the exact failure line from Task 9's planted drop. Say in one clause that `evals/docs-retrieval/floor.json`'s `see` field and `docs/development.md` §5's gate 11 paragraph both cite this section and state none of it themselves.

**Verification:**

- Every command in the document is run once, as written, through the route the document itself names, and its output matches what the document says it produces. A command that is described but not exercised is not documented.
- The stale-label refusal quoted in the document is byte-identical to the one Task 3 recorded; re-produce it once against a scratch copy of a query set rather than trusting the quote.
- No number appears in this file that is not also in `docs/retrieval-eval-results.md`, **with two stated exceptions**: the numbers inside the quoted failure output, and the **floor margin**, which is a decision this section now owns and which no other file records. Every other figure is a pointer, because two copies of a measurement drift and a pointer does not.
- **The query-set format has exactly one home.** After writing that section, read `evals/docs-retrieval/queries/README.md`: it states none of the format — no field list, no grade scale, no statement of what an empty `labels` array means — and cites this section by the heading it now carries. Confirm the citation resolves to a heading that exists, the same read-back this task does for `floor.json` and §5. A mismatch is closed by **renaming this section to the heading Task 2 cited**, never by editing that README, which this task does not target; Task 2 wrote that pointer against a file this task creates, and this is where that hand-off is closed.
- **The floor policy has exactly one home.** After writing this section, read `evals/docs-retrieval/floor.json` and `docs/development.md` §5's gate 11 paragraph: each cites this section by heading and states neither the margin, nor its reason, nor the reason for the single corpus, nor the re-record rule. Both pointers resolve to a heading that now exists — Task 9 wrote them one commit earlier against a file this task creates, and this is where that hand-off is closed.
- `bash scripts/run-gates.sh` passes, with gate 6a green over the new file and gate 11 unaffected — this task adds a document under `docs/`, which grows the `self-docs` corpus and not the gate's `fixture-catalog` one.
