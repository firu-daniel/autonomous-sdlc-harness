### Task 6 — Arm A's harness, its agent-task text and its transcript scorer — built, never run

**Goal:** Build everything arm A needs — the task text a headless index-first agent is given, the script that would invoke it under a read-only tool set, and the scorer that turns what it returns into the same metrics arms B–E report — and run **none** of it. No `claude -p` call is made on this branch.

**Depends on:** Task 2, which committed the `fixture-catalog` corpus and its own `INDEX.md` under `evals/docs-retrieval/corpora/fixture-catalog/docs/` — arm A navigates from that index, and it is the only corpus arm A is defined over, because this repository's `docs/` has no `INDEX.md`. Task 4, whose `evals/docs-retrieval/metrics.mjs` exports `scoreArm(records, queries, { kValues })` over records of `{ id, arm, hits: [{ ref, score }], abstained, durationMs }` and whose `evals/docs-retrieval/results.mjs` renders arm A's row; and whose `docs/retrieval-eval-results.md` carries an empty `## Arm A — awaiting a hand run` section below its `<!-- eval:generated:end -->` marker, which this task fills.

**Why nothing here runs.** Arm A needs a nested `claude -p` subprocess, and `.claude/settings.autonomous.json` carries no `Bash(claude:*)` entry; per that profile's own `_README` a tool call matching neither `allow` nor `deny` **stalls in print mode rather than prompting**, so an unattended attempt hangs the run instead of reporting a refusal. The `scripts/scratch-run.sh` route to it is technically open and is **declined on purpose**: it would put an unsupervised nested agent session, with its own auth and no token cap, inside an unattended run to take a measurement. Do not add a `claude` grant, do not route arm A through `scratch-run.sh`, and do not drop the arm. One structural property makes the fence self-enforcing and is worth keeping: `evals/docs-retrieval/arm-a/run-arm-a.sh` sits **outside** the configured `scriptsDir`, and the script-allowlist guard requires every `.sh` token in a command to resolve under that directory — so no agent can invoke this file, by construction.

### Targets

- `evals/docs-retrieval/arm-a/agent-task.md` (new) — what the headless agent is told. **Not** named `prompt.md`: see the first Work bullet.
- `evals/docs-retrieval/arm-a/run-arm-a.sh` (new) — the invocation an operator runs by hand.
- `evals/docs-retrieval/arm-a/sample-transcript.json` (new) — a hand-written sample the scorer is exercised against.
- `evals/docs-retrieval/arm-a/score-transcript.mjs` (new) — transcript → arm A records.
- `docs/retrieval-eval-results.md` → `## Arm A — awaiting a hand run` (the section Task 4 created empty, below the generated region).

**Work:**

- [x] `agent-task.md` — **named that, and not `prompt.md`, on purpose, with the reason stated in the file's own first lines**: `evals/README.md` states that the native eval runner discovers its cases at `evals/**/case.yaml` or `evals/**/prompt.md` alongside `graders/*.md`, so any `prompt.md` under `evals/` is by that pattern a runner case. Arm A is not one — it is agent-task text for a hand-run measurement that answers to no runner — so the file stays out of that discovery glob, and the constraint is recorded where the next person tempted to rename it will read it. Do **not** resolve the collision the other way by restating or widening the discovery pattern in `evals/README.md`: that statement has one home and this branch does not touch the `plan-shape/` paragraphs. The content: the task given to the agent, written so the same text works on any catalog — read `INDEX.md` first, follow its links, and answer **only** with the `path#heading` references of the sections you would use, one per line, most useful first, at most five; answer with the single word `none` when the catalog holds no section that answers the question. State that the path is repo-relative and the anchor is the GitHub slug of the heading, with one worked example, because that spelling is what the scorer matches against the query set's `ref` values. Leave a single `{{query}}` token for the runner script to substitute, and say in the file that it is the only one.
- [x] `run-arm-a.sh`: `#!/usr/bin/env bash` and `set -euo pipefail`, a header comment stating what it is, that it is **hand-run only** and why, and a `REPRO` block. It takes the corpus root, the query-set file and an output path; for each query it substitutes `{{query}}` into `agent-task.md`'s text and invokes the agent CLI **once**, with the working directory set to the corpus root, a read-only tool set limited to reading files there (grant `Read`, `Grep` and `Glob`; grant no `Bash`, no `Write`, no `Edit` and no web tool, so the run cannot reach the network and cannot leave the corpus), print mode, and a JSON output format so the usage figures are machine-readable. Append one record per query to the output file: `id`, `query`, the returned refs in order, the wall time, and the usage block verbatim. Take the agent binary from `${HARNESS_AGENT_CLI:-claude}`, which is the indirection `ARCHITECTURE.md` records as the one place the engine binary is chosen. **Do not run this file.**
- [x] `sample-transcript.json`: three hand-written records in exactly that output shape — one where the agent returns the labelled section first, one where it returns it third, one `none` against a negative query — with an invented usage block. It exists so the scorer has something to be exercised on without an agent, and its header comment says so.
- [x] `score-transcript.mjs`: export `scoreTranscript({ transcript, queries })` — parse the transcript, normalise each returned ref (trim, strip a leading `./`, drop a trailing slash, leave everything else alone: a ref the agent spelled wrong is a miss, not something to repair), map `none` to zero hits, build `{ id, arm: 'A', hits, abstained, durationMs }` records, and hand them to `scoreArm` unchanged so arm A is scored by the same code as every other arm. Sum the usage figures into a token-cost total per run. Refuse, naming the id, on a transcript record whose `id` is not in the query set. **Its output is the runner's input, not a stand-alone report:** the consumer is Task 4's `evals/docs-retrieval/run.mjs`, which calls `scoreTranscript` when it is given `--transcript <path>` and passes the records into the same rendering path as arms B–E, so arm A's row in `docs/retrieval-eval-results.md` is generated like every other row and never hand-written. Say that in this module's header, and say with it that Task 3 declared the flag and Task 4 landed its reader one task earlier, so the contract exists before this module fills it — the same contract-before-consumer hand-off Task 9 and Task 10 use for the floor pointer. Export it and write no results file from here.
- [x] Fill `docs/retrieval-eval-results.md` → `## Arm A — awaiting a hand run`: what arm A measures, that **no arm A number is recorded on this branch and the run made no `claude -p` call**, why (the permission-profile reason above, in two sentences), that its harness and fixture are committed at the four paths above, and that the procedure for running it is `docs/retrieval-eval.md`'s — Task 11 writes that section, so name it as the companion without quoting it.

**Verification:**

- The scorer works without an agent: run `bash scripts/scratch-run.sh harness-runs/scratch/score-arm-a.mjs`, a launcher that calls `scoreTranscript` on the committed sample transcript against the `fixture-catalog` query set, and confirm it produces an arm A metric row with recall@1 and MRR consistent with the three hand-written records (first-place hit, third-place hit, correct `none`). This is the "arm A's harness runs against the fixture and its `INDEX.md`" acceptance: the harness is exercised, the agent is not invoked.
- `run-arm-a.sh` is not executed, in any form, by any step of this task. Confirm afterwards that no `claude` process was started: the branch's own record of that is this verification line plus the absence of any transcript file other than the committed sample.
- `agent-task.md` substitutes cleanly: print its substituted text for one query through the same scratch launcher and confirm exactly one substitution happened and no `{{` remains.
- `git ls-files 'evals/**/prompt.md'` returns only what already existed before this branch — no file this task adds lands on the native eval runner's case-discovery glob.
- `bash scripts/run-gates.sh` passes, with gate 6a green over the new files and `run-arm-a.sh` carrying an LF-terminated `#!/usr/bin/env bash` first line.

**Deviations from plan:**

- The `score-transcript.mjs` bullet says the module should "hand them to `scoreArm`". `evals/docs-retrieval/run.mjs` — landed by Task 4 and the declared consumer of this module — already calls `scoreArm(records, queries)` on whatever `scoreTranscript` returns, and its `transcriptRecords` helper accepts an array or `{ records, cost }` and nothing else. Scoring inside this module would score the set twice and hand back a shape that reader does not take, so `scoreTranscript` returns `{ records, cost }` and `run.mjs` makes the single `scoreArm` call — which is the property the bullet asks for (arm A scored by the same code as every other arm) through the contract that already shipped. Stated in the module's own header.
- The plan asks the sample transcript for a header comment; JSON has no comment syntax. The sample is therefore a JSON object carrying its records under `records`, with three `_`-prefixed string fields saying what it is, what its three cases are and that no published figure comes from it. `score-transcript.mjs` reads three transcript forms — one JSON object per line (what `run-arm-a.sh` appends, so an interrupted run keeps its records), a bare JSON array, and that `records` object — and says so in its header.
- Arm A returns an ordered list and no scores, while a record's `hits` carry `{ ref, score }`. The scorer synthesises `1 / position`, and its header states that this is a rank-derived value of the same class as arms B–D's RRF values, not comparable to arm E's calibrated score, and that no arm A score enters the abstention calibration. A `null` score would have reached `metrics.mjs`'s `Math.max` as `0` — a number that looks measured.
- The `agent-task.md` header comment names its substitution token in words ("`query` inside doubled braces") rather than spelling it. Spelling it twice made the file carry two tokens, which fails the plan's own verification that exactly one substitution happens and no `{{` remains.
- **Evidence downgrade.** `run-arm-a.sh` was not syntax-checked by execution: `bash -n evals/docs-retrieval/arm-a/run-arm-a.sh` was refused, because a `.sh` token outside the configured `scriptsDir` obtains no automatic permit from the script-allowlist guard and falls through to the permission system — the same structural property the plan's `## Why nothing here runs` relies on. Its syntax rests on reading, not on running. Everything else in `**Verification:**` above was executed.
- `run-arm-a.sh` lands at mode `644`, while `evals/plan-shape/scaffold.sh` and every script under `scripts/` is `755`: `chmod` was refused in this run. The mode is not load-bearing — the file's own usage line and its REPRO block invoke it as `bash <path>`, and the plan forbids running it here at all — but a maintainer who wants the tree consistent sets the bit with one `chmod 755` on that path.
- The scorer verification ran as `bash scripts/scratch-run.sh harness-runs/scratch/score-arm-a.mjs`. The sample covers 3 of the fixture set's 12 queries by design, so the launcher scores the metric row over the 3 covered queries and separately shows `scoreArm`'s refusal of the short set against the full 12. Measured: 2 positives and 1 negative; recall@1 `0.5`, recall@3 `1`, recall@5 `1`, MRR `0.667`, strict MRR `0.667`, strict recall@5 `1`, ranks `q-fc-verify-callback=1` and `q-fc-billable-weight=3`, `abstainedOnNegative` 1 of 1, cost `agent hand run over 3 queries — cache_read_input_tokens 0, input_tokens 28160, output_tokens 124`. Every figure is consistent with the three hand-written records: one first-place hit, one third-place hit, one correct `none`.
- `bash scripts/run-gates.sh` reports `1 failed, 13 passed`, the failure being the branch's standing gate 6a hit — the worktree's untracked `.git` pointer file and the `harness-runs/improvement_observations/` entries quoting it. No hit is in a file this task touched.
- No `claude` process was started by this task. The only one on the machine is the session executing it; the arm-a directory holds no transcript other than the committed sample, and `git ls-files 'evals/**/prompt.md'` still returns `evals/plan-shape/prompt.md` alone.
