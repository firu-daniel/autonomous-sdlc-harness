### Task 12 — `docs/retrieval.md`: the real-model record, the retired and reworded `## Still open` entries, and the `setup-worktree.sh` argument

**Goal:** Bring the retrieval design document in line with what this branch measured: item (d) stops saying the real models have never been run, the `## Still open` entries this branch settled are retired or reworded with their numbers, the threshold stops being called provisional here and in `docs/cli.md`, and the `setup-worktree.sh` argument is rewritten on both its premise and its conclusion.

**Depends on:** Task 1, whose new suite prints `lexical-arm index-scan crossover: <N> rows` and whose cases record what `lexicalSearch` returns at both committed corpora's sizes. Task 5, which set the calibrated `ABSTAIN_SCORE_THRESHOLD` in `cli/src/retrieval/search.ts`, and Task 14, which wrote the measured record of how it was chosen into `docs/retrieval-eval-results.md` → `## Threshold calibration` — **that section is the record this task cites and never restates — not a condensation of it and not a second derivation of the value**, per `.claude/context/conventions.md` → `### Where a new responsibility goes`, which puts a measured fact in `docs/` and nowhere else. Task 8, whose `docs/retrieval-eval-results.md` → `## Cold build and index size` carries the measured cold build, the size on disk, the extrapolation to ~1,500 chunks and the 60-second rule's conclusion. Task 4, whose generated region in that same file holds the arm tables and provenance.

**What must not change.** The `## Still open` entry about **Linux** stays as written: settling it needs manual host setup this branch does not own. The entry about **whether the agent runner starts the `.mcp.json` server with the checkout root as its working directory** also stays: Task 7's pass started the server with an explicit `--cwd` against a throwaway fixture, which is not the `.mcp.json` launch path and settles nothing about it. And `ROADMAP.md`'s *Docs-catalog retrieval* row keeps status **`Open`** — the comparison this branch enables is closed by arm A's hand run and the private-catalog numbers, not here.

### Targets

- `docs/retrieval.md` — items (b) and (d), four `## Still open` entries, the opening paragraph, the *Who reads this* paragraph, and two paragraphs inside `## How it fits together`: the **Abstention.** paragraph and the **Why `setup-worktree.sh` does not warm the index.** paragraph. **Both are bold lead-in paragraphs, not `##` headings** — the file's only `##` headings are `## How it fits together`, `## Measured, and how`, `## Still open`, `## What this buys you`, `## Where it goes next` and `## What it costs`. Find each by its quoted lead-in text; do **not** promote either to a heading, which is not part of this task's work.
- `docs/cli.md` §11 — the two sentences calling the abstention threshold provisional.

This task writes **no** measured figure of its own into `docs/retrieval-eval-results.md`: that file is Tasks 4–8's and Task 14's, and `## Threshold calibration` in particular is Task 14's record, which this task cites.

**Work:**

- [ ] `## Measured, and how` item (d): replace *"Gate 10 (`docs/development.md` §5) has not been run"* with the partial result. State what has now run outside the stub and what has not: both models loaded and ran on this host, with the date, the host, the Node version and the two model ids; the reranker's real-model load and run, which is what retires a `## Still open` entry below; the arm numbers, the cold build and the index size, each cited to `docs/retrieval-eval-results.md` rather than copied; and the legs of gate 10 that remain unrun — the adopter-facing provisioning path (leg (i)), the `.mcp.json` unattended-session leg, and any Linux host. Say plainly why the provisioning leg could not run here: `setUpRetrieval` installs `autonomous-sdlc-harness@<own manifest version>` **from the registry**, and the published `0.1.0` carries no `dist/retrieval`, so the path cannot be exercised until a version carrying retrieval is published — and that publishing is its own decision on its own branch, not something this branch works around or version-bumps.
- [ ] `## Still open`, four entries. **Retire** the reranker entry — `Xenova/ms-marco-MiniLM-L-6-v2` has now been loaded and run outside the stub — moving what it said into item (d) rather than deleting the fact. **Reword** the lexical-arm entry with Task 1's measurement: what `lexicalSearch` returns at roughly 40 and roughly 166 chunks, and the crossover row count the synthetic bisect found, with the caveat that it is a planner decision that a version bump can move and that the suite pins it. Drop the entry entirely only if the property holds at both committed corpora's sizes, which the prior says it will not. **Replace** the abstain-threshold entry with the value and a pointer alone — **not** a condensation of the record's figures, because `docs/retrieval-eval-results.md` → `## Threshold calibration` is **the** record of how the threshold was chosen and on what, and Task 14 wrote that sentence into it one task earlier. The entry states: that the threshold is now calibrated; the **value**, which is the constant's own literal and not a figure derived from the calibration; `cli/src/retrieval/search.ts` as where the constant lives; `docs/retrieval-eval-results.md` → `## Threshold calibration` as the record of how it was chosen and on what; and what remains open — confirmation on a real catalog. **No corpus name, no chunk count, no score-distribution figure, no host, date or model id goes into `docs/retrieval.md`**: each of those is part of the record and copying it here is the second home the conventions rule forbids. **Leave** the Linux and working-directory entries untouched.
- [ ] The **Abstention.** paragraph inside `## How it fits together` (the paragraph opening *"**Abstention.** Only `fused-rerank` abstains"*, whose *"That constant is **provisional**"* sentence is the site — a paragraph, not a section), the opening paragraph, and `docs/cli.md` §11's two sites. In `docs/retrieval.md`: the threshold is no longer *provisional* but calibrated, with the value and a pointer to where the evidence is; the opening *"not yet measured"* clause keeps its subject — the comparison against index-first navigation is still unmeasured, because arm A has not been run — and gains one sentence naming what **was** measured and where, plus the pointers to `docs/retrieval-eval.md` and `docs/retrieval-eval-results.md`. Update the *Who reads this* paragraph, which names this branch as the one that will measure it. In `docs/cli.md` §11: the opening sentence's *"so its abstention threshold is provisional"* and the `docs search` paragraph's *"below the provisional `ABSTAIN_SCORE_THRESHOLD`"* both take the calibrated framing, with no number restated — §11 is a surface reference, the constant lives in `cli/src/retrieval/search.ts`, and the **record of how the value was chosen and on what is `docs/retrieval-eval-results.md` → `## Threshold calibration`**, which §11 cites by heading. §11 restates neither the value's derivation nor any figure behind it.
- [ ] Rewrite the **Why `setup-worktree.sh` does not warm the index.** paragraph — the bold lead-in paragraph inside `## How it fits together`, not a `##` section — on **both** its premise and its conclusion. The premise first: it rests on *"branches that never query"*, which is close to an empty category — every branch runs planning and every plan agent holds the grant — so the honest statement is *"agents that do not query"*. Then the conclusion, from Task 8's measured figure and the standing rule **if the cold build exceeds 60 seconds, the build moves into `setup-worktree.sh`**: apply the rule to the extrapolation to ~1,500 chunks, state the answer it gives, and say which measurement would overturn it. Keep the stub's own figure where it is, labelled as the stub's, and set the real-model figure beside it. If the rule trips, this paragraph says the build **moves** into `setup-worktree.sh` and names gate 10's hand run against a real catalog as what settles it on a measured rather than an extrapolated number; this branch does not make that move, and the paragraph says that too.

**Verification:**

- `grep -rn "provisional\|PROVISIONAL" docs/retrieval.md docs/cli.md cli/src/retrieval/search.ts` returns nothing about the abstention threshold.
- **The calibration record is not copied here.** Read `docs/retrieval.md` and `docs/cli.md` §11 back after writing: no score-distribution figure, no corpus name or chunk count from the calibration, no host, no date and no model id appears in either — the only threshold number either file may carry is the constant's own literal — and both cite `docs/retrieval-eval-results.md` → `## Threshold calibration` by that heading, which must resolve to a heading Task 14 left in that file.
- Every figure written into either document is traceable: for each one, name the file and section it was read out of — `docs/retrieval-eval-results.md`'s generated region, its `## Threshold calibration` or its `## Cold build and index size`, or Task 1's printed crossover line — and confirm it matches byte for byte. No figure is recalled from a run rather than read from a record.
- `git diff --stat` shows `docs/retrieval-eval-results.md` **unchanged** by this task: the results file is read here, never written.
- `git grep -n "Gate 10" docs/retrieval.md` no longer returns the "has not been run" sentence, and the legs that genuinely remain unrun are still named somewhere in the file.
- `ROADMAP.md` is unchanged and its *Docs-catalog retrieval* row still reads `Open`: `git diff --stat` shows no ROADMAP change from this task.
- `bash scripts/run-gates.sh` passes, including gate 6c (`llms.txt` links resolve) — this task changes no link — and gate 11.
