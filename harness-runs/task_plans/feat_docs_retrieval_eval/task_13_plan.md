### Task 13 — The two directory contracts: `evals/README.md`'s second tenant and `evals/docs-retrieval/README.md`

**Goal:** Make what this branch added findable and governed: say in `evals/README.md` that the directory now has two tenants with different jobs, give `evals/docs-retrieval/` its own contract README, and list the two new documents under `docs/` in the two index surfaces that carry the tree's reference lists.

**Depends on:** every earlier task, because this one describes what they built. The paths it names are: `evals/docs-retrieval/` holding `corpora.mjs`, `queries.mjs`, `index-build.mjs`, `args.mjs`, `arms.mjs`, `metrics.mjs`, `results.mjs`, `run.mjs`, `check-floor.mjs`, `cold-build.mjs`, `query-log-pass.mjs`, `floor.json`, `arm-a/`, `corpora/fixture-catalog/` and `queries/`; and `docs/retrieval-eval.md` with `docs/retrieval-eval-results.md`. **This is the catch-all task and it ships last for that reason** — it wires up and documents what the other tasks built, so each of them must already have landed for its description to be true rather than anticipated.

**The boundary this task must not cross.** `evals/README.md` currently describes one tenant, the `plan-shape/` case for the native eval runner, and calls the whole directory *"The harness's evaluation cases, which are roadmap item 8"*. That statement is now too narrow, not wrong: the retrieval eval is a measurement of shipped behaviour rather than a graded agent case, and it answers to no runner. Widen the opening to name both tenants and what separates them; **change nothing else about the `plan-shape/` paragraphs**, whose PROVISIONAL framing and envelope caveats are untouched by this branch.

### Targets

- `evals/README.md` — the opening statement of what the directory holds.
- `evals/docs-retrieval/README.md` (new) — the directory's own contract.
- `llms.txt` — the reference list of documents under `docs/`.
- `README.md` — the reference-documents list.

**Work:**

- [ ] `evals/README.md`: rewrite the opening so the directory states its two tenants — `plan-shape/`, a case for the native eval runner under roadmap item 8, and `docs-retrieval/`, the relevance eval for the shipped docs-retrieval tool under the *Docs-catalog retrieval* row — and what separates them: one grades an agent's output against a rubric and waits on a gated runner; the other measures shipped code against labelled queries and runs today as gate 11. Keep the closing *"This directory is a measurement, not a format exemplar"* paragraph true of both.
- [ ] `evals/docs-retrieval/README.md`: what the directory holds, in the shape a directory contract takes here — the module map (one line per file above, saying what it owns), the two corpora and their ids, the two **single sources the directory reads rather than copies** — `arms.mjs`'s exported arm table, built by pairing letters onto `SEARCH_MODES` from `cli/dist/retrieval/search.js`, which every other module's default arm set, arm column, floor key and refusal message reads, so a new `SearchMode` is added in one place and fails loudly here rather than going missing; and the `layers[]` list, which `corpora.mjs`'s `self-docs` reads out of the resolved checkout's `harness.config.json` and never composes, because `cli/src/retrieval/corpus.ts`'s header forbids a copy of it, where the numbers go (`docs/retrieval-eval-results.md`) and where the procedure is (`docs/retrieval-eval.md`), **a pointer — and nothing more — to the section of `docs/retrieval-eval.md` that states how the eval is run**, cited by the heading that document carries as Task 10 committed it (`## How to run it` unless Task 10's committed heading differs, in which case take the committed one — it is readable by the time this task runs, so read it rather than assuming it); this README **names no execution route and gives no reason**: not the two permitted routes, not the wrapper, not the interpreter invocation, not why a bare one stalls an unattended run. That rule is a consequence of what the permission profile grants and changes when the profile does, so it has one home and this README only points at it — the same boundary this bullet already draws for the numbers and the procedure. And the one thing that is **never run from here**: `arm-a/run-arm-a.sh`, which sits outside the configured scripts directory precisely so that no agent can invoke it. The module map's `arm-a/` lines name the agent-task text as **`arm-a/agent-task.md`** and say in one clause why it is not `prompt.md` — `evals/**/prompt.md` is the native eval runner's case-discovery glob, and arm A is not a runner case — so the reason is findable from the directory contract as well as from the file itself. Do not restate the discovery pattern itself; `evals/README.md` owns it. Do not restate the metric definitions or the decision rule — they have a home and this README points at it.
- [ ] `llms.txt`: add one entry each for `docs/retrieval-eval.md` and `docs/retrieval-eval-results.md`, in the file's existing form — a `blob/main/<path>` target with a one-line description, in the same list and the same order convention as the neighbouring `docs/` entries. Both files are tracked by then, which is what gate 6c grades; no target may carry a `#` or a `?`.
- [ ] `README.md`: add the same two documents to the reference-documents list, each with the one-line description that distinguishes them — the procedure and the definitions in one, the numbers in the other — and leave the surrounding bullets, including the retrieval bullet's *not yet measured* clause, untouched: that clause is about the index-first comparison, which arm A's unrun hand step still owns.

**Verification:**

- Every path named in either README exists, and every file under `evals/docs-retrieval/` is named in one of them: walk the directory and read the two lists against it, in both directions. A module with no line is undocumented; a line with no module is a stale pointer.
- **The execution-route rule has exactly one home.** Read `evals/docs-retrieval/README.md` back after writing it: it names no execution route and no reason one is required, and the only wrapper script it names anywhere is `arm-a/run-arm-a.sh`, named solely as the one thing never run from there. Its citation of `docs/retrieval-eval.md` resolves to a heading that exists in that file as Task 10 committed it — grep the heading out of `docs/retrieval-eval.md` rather than trusting the citation. A mismatch is closed by correcting this README's citation, never by editing `docs/retrieval-eval.md`, which this task does not target.
- `bash scripts/run-gates.sh` passes, with gate 6c (`llms.txt` links resolve on `main`) green — the two new targets are tracked files under `docs/`, which `scripts/publish-main.sh`'s `removed_paths` does not remove — and gate 11 green.
- The `plan-shape/` paragraphs of `evals/README.md` are byte-identical to what they were: `git diff evals/README.md` shows only the opening.
- The Done summary for this branch names arm A as awaiting a hand run, which is where this task's description of `arm-a/` is read back against the results file's own `## Arm A — awaiting a hand run` section.
