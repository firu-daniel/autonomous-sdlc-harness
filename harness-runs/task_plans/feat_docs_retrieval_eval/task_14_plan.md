### Task 14 — The threshold calibration record in `docs/retrieval-eval-results.md`

**Goal:** Write the single measured record of how `ABSTAIN_SCORE_THRESHOLD` was chosen — the value, the evidence it was chosen from, what it cost, and the limit on it — into `docs/retrieval-eval-results.md` → `## Threshold calibration`, so that the fact has one owner and every other document cites it rather than restating it.

**Where this task sits in the order.** It is entry **6** in the story index's readiness list, shipping immediately after Task 5 and before Task 6 (entry 7). Its number is 14 because it was split out of Task 5 during plan revision and renumbering thirteen files would have broken every cross-reference in them; the readiness list's ordinal is the ship order, and the task number is only the file's name.

**Why this is its own task rather than a bullet in Task 5.** The record is a measured fact, and `.claude/context/conventions.md` → `### Where a new responsibility goes` puts a measured fact in `docs/`, **and nowhere else**. `docs/` is the `general` layer; Task 5 edits `cli/src/retrieval/search.ts` and is a `cli` task. A single task carries exactly one layer, so the calibration's code half and its record half are two tasks with an ordering between them, not one task with two layers.

**Depends on:** Task 4, whose `docs/retrieval-eval-results.md` carries the arm tables and provenance inside its `<!-- eval:generated:start -->` / `<!-- eval:generated:end -->` region, a fenced `json` block holding arm E's per-query `bestScoreOnPositive` and `bestScoreOnNegative` for both corpora, and — below the end marker — an **empty** `## Threshold calibration` section, which this task fills. Task 5, which chose the value, committed it in `cli/src/retrieval/search.ts` and put in its commit body exactly the figures this task writes: the chosen value; the highest best-score on a negative query and the lowest on a positive one, per corpus and pooled; the positives the choice costs, with the count; and arm E's recall@5 before and after the move. Read those from Task 5's commit body and check the **pre-move** ones against the committed `json` fence — **derive none of them a second time**, and re-run no arm.

**The fence this task reads is not the fence that ships, which is why the evidence is quoted rather than cited.** Task 4 generated that region **before** Task 5 moved the threshold, so it is the pre-move run; Task 9 (entry 10) **regenerates it after the move** so the file of record describes the shipped threshold. Regeneration is lossy in one direction: on an abstaining query `searchDocs` returns `{ abstained: true, hits: [] }`, so the shipped fence will have no `bestScoreOnPositive` for exactly the queries the new threshold abstains on — the queries whose scores are the whole evidence for the choice. So this section **quotes the pre-move per-query distributions into itself** rather than pointing at a fence a later task overwrites, and it cites Task 5's commit body — not the fence — for the **post-move** figures. Below the end marker is hand-written territory the runner never rewrites, so the quoted copy is permanent. Task 9's own verification reads this quotation back before it regenerates.

**Where this task stops.** It writes **one section of one file**: `docs/retrieval-eval-results.md` → `## Threshold calibration`. It does not touch that file's generated region, which is the runner's and is Task 9's to regenerate; it does not touch the file's other hand-written sections (`## Arm A — awaiting a hand run` is Task 6's, the query-log pass's is Task 7's, `## Cold build and index size` is Task 8's); and it does not touch `cli/src/retrieval/search.ts`, `docs/retrieval.md`, `docs/cli.md` or `docs/development.md`.

### Targets

- `docs/retrieval-eval-results.md` → `## Threshold calibration` (the section Task 4 created empty, below the `<!-- eval:generated:end -->` marker).

**Work:**

- [ ] Write the section as the record: the chosen value; the date and the host; the two corpora **by name and by chunk count** (`fixture-catalog` and `self-docs`); the two models by id; the highest best-score on a negative query and the lowest on a positive one, **per corpus and pooled**; and the positives the choice costs, with the count, or plainly that it costs none. **Quote the pre-move per-query distributions into this section**, as a small table or a short fenced block — each positive query's `bestScoreOnPositive` and each negative query's `bestScoreOnNegative`, per corpus, copied verbatim out of the generated region's `json` fence as it stands now — with one line saying they are the pre-calibration run and are quoted here because the region is regenerated post-move (Task 9) and a regenerated fence cannot hold them.
- [ ] State the two bounds that fixed the choice and keep them distinguishable, because they come from different places: the **measured distribution** bounds it from inside and is the evidence above; the **stub-fixture bound** bounds it from outside — `cli/test/docs-retrieval.test.mjs`'s abstention cases run under the `stub-overlap` reranker, where any value strictly inside `(0.000, 1.000)` keeps them passing — and is a property of the code, stated here because the record is incomplete without it and restated in the constant's own doc comment because that is where it binds.
- [ ] Record the effect: arm E's recall@5 on both corpora before and after the move, and that the arm abstains on every negative query after it. **Both halves of that pair come from Task 5's commit body and from nowhere else** — Task 5 takes them in one pass over one tree, before and after editing the constant, so the threshold is the only difference between them. Write the corpus `snapshot` stamp Task 5 recorded beside the pair, and say in one clause that the pair is a before/after only because both readings carry that same stamp. **Do not cite the generated region's `json` fence for either figure**, even for the *before* one: that fence was generated at entry 4 over a `self-docs` corpus that did not yet contain `docs/retrieval-eval-results.md`, so a recall number read from it differs from Task 5's by corpus as well as by threshold, and the two are not a pair (the story index's `self-docs` snapshot rule). The fence's job in this section is the **per-query distributions** above and nothing else. Say where the after figures become re-checkable in this file: **Task 9's post-move regeneration of the generated region**, whose provenance states its own, later snapshot.
- [ ] Close with the limit, in the same paragraph as the conclusion: both corpora are far below a mature catalog's ~1,500 chunks, so this is a calibration on committed corpora rather than on a real catalog; its standing reproduction is the eval gate Task 9 adds to `scripts/run-gates.sh` (gate 11) rather than a remembered run; and its confirmation is the hand run against a private real catalog that this branch does not perform. Say which measurement would move the value.
- [ ] Say once, in the section's opening line, that this section is **the** record of the calibration and that the constant's doc comment, `docs/retrieval.md` and `docs/cli.md` §11 cite it rather than restating its figures — so the next person to add a number knows which file it belongs in.

**Verification:**

- Every figure in the section is traceable, and the two sources are not interchangeable: every **quoted per-query distribution value** matches the `json` fence in this file's generated region byte for byte; every **recall figure, before and after the move alike**, matches **Task 5's commit body** byte for byte and carries the stamp Task 5 recorded, and **no** recall figure is cited to the fence — which was generated over an earlier corpus snapshot as well as under the earlier threshold. Name the source for each figure as you check it. No figure is recalled from a run rather than read from a record, and this task runs no arm.
- The quoted pre-move distributions are complete for what the calibration rests on, **and censored where the fence is censored**: every positive query and every negative query of both corpora that appears in the fence appears in the quotation, so nothing the choice was made from is left behind when Task 9 regenerates the region. Compare the quotation against the fence's per-query records and confirm the quotation's query-id set is not a subset of it. The fence was generated with the threshold at `0.3` and `bestScore` is the top **returned** hit's score, so every abstaining query carries `null` and **no negative score is observable at all** (Task 5's own `**Deviations from plan:**` note): grade every negative as stated `< 0.30` — the bound the evidence asserts — and reject any negative written as a measured value, and grade each censored positive the same way rather than as a number. A quotation that reports a figure the fence does not carry fails this criterion.
- The value written here is byte-identical to the literal `ABSTAIN_SCORE_THRESHOLD` carries in `cli/src/retrieval/search.ts`: `grep -n 'ABSTAIN_SCORE_THRESHOLD' cli/src/retrieval/search.ts` and read the two against each other.
- `git diff --stat` shows one file changed, and `git diff docs/retrieval-eval-results.md` shows no hunk between the `<!-- eval:generated:start -->` and `<!-- eval:generated:end -->` markers and none in another hand-written section.
- `bash scripts/run-gates.sh` passes, including gate 6a over the changed document.
