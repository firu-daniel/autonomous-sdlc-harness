### Task 7 — Planning fork: redefine the clarification channel as one exchange per park

**Goal:** Restate the canonical clarification channel — owned by `plugin/instructions/task_plan_writing_instructions_autonomous.md` on behalf of every autonomous flow — as one question file per park and one answer file per park, record why that design was chosen, and make this fork's own writing site and its resume case (a) write and consume a park as a unit.

**Depends on:** Task 1 for the watcher behaviour this section is the contract for, restated so this file stands alone: the watcher resumes a parked run only when every top-level `question_<n>.md` has its `answer_<n>.md`; it records every such index and launches the engine with a prompt beginning `This is a RESUME: every question file of this park has been answered — consume ALL of these answer files, each paired by index with its question_<i>.md, and resume from the park point rather than restarting:` followed by the answer paths; on exit it archives exactly that set into `answered/`, and a pair that appeared during the resumed session stays at the top level for the next resume. The launch prompt's park instruction now reads `write every question of this park into ONE <state_dir>/clarifications/<branch>/question_<n>.md and END the session to park`. The watcher's loop guard (status `park_loop`, cleared by `<state_dir>/clarifications/<branch>/PARK_LOOP_CLEAR`) is Task 2's and is described in `docs/watcher.md` §4 by Task 12; this section only points at it.

**Where this task stops.** The other three autonomous forks that cite this section are Task 8's; the digest module and the ledger are Task 9's; the commands are Task 10's. **Do not rename or relocate** `## Clarification channel — file format (canonical, single source of truth)`, `## Override 2 — resumability` or `## Ask-vs-assume policy` — this file's own `**Anchors resolved from outside this file.**` paragraph forbids it, and the citers resolve them by heading text and by the `Override 2(a)` label.

### Targets

- `plugin/instructions/task_plan_writing_instructions_autonomous.md`

**Work:**

- [ ] `## Clarification channel — file format`: rewrite the **Question file** bullet — `question_<n>.md` is **one file per park**, holding every question that park raises; the file opens with the raising agent/phase and the latest findings path where one applies, then one `## Q<k> — <decision needed>` section per question (`k` from 1), each self-contained with why it is ask-worthy under the policy and the options. The index rule (1-based, unique across the top level **and** `answered/`, never reused, never overwritten, and why) is kept as it stands. Rewrite the **Answer file** bullet — one `answer_<n>.md` answers the whole park; rulings address questions by `Q<k>`; a park of one question needs no label; text addressed to no label answers every question in the park; a question the answer does not address stays open, and the resumed run parks it again in a **new** question file (next index) rather than assuming.
- [ ] Same section: add a **Why one file per park** bullet — every site that writes a question does so at the moment it parks and then ends the session (this fork's headless reroute and its three siblings'), so no question is ever written while its run keeps going, and a park's questions are known together; one file per question lets a watcher consume part of a park. The bullet states that contract reason and nothing else: **no branch name, no date and no account of which change fixed what** — `plugin/` names no adopter literal (`.claude/context/conventions.md` → `## Configuration is the source of truth, and it is read at run time`), and that history is a decision of record which Task 12 writes into `docs/watcher.md` §4. If the bullet points there, cite it in prose as the Consume-then-archive bullet below does (the harness repository's `docs/watcher.md` §4). Add a **Parks written before this layout** bullet — several one-question files form one park; every rule here applies to them unchanged.
- [ ] Same section: rewrite the **Consume-then-archive** bullet to the set semantics restated under `**Depends on:**` above (resume only when every top-level question file is answered; the resume prompt names every answered pair; the engine consumes all of them; the watcher archives exactly that set on exit; a pair written during the session stays), keeping its closing sentence that the watcher comment and `plan_orchestration_instructions_autonomous.md` reference this contract. Add one sentence: a run whose resumes make no progress is held by the watcher under the status `park_loop` until an operator clears it (the harness repository's `docs/watcher.md` §4).
- [ ] The reroute and case (a): in ``### `<escalate>` / `<ask>` — the headless clarification reroute`` change "for each blocking question or blocker, write a self-contained … `question_<n>.md`" to writing **every** blocking question or blocker of this park into **one** self-contained `question_<n>.md` in the format below; in the `<ask>` table row change "writes the question file(s)" to "writes the park's question file"; in the `<existing_artifact_decision>` row and in `## Override 2 — resumability` case (a) replace the single "sibling answer file" with **every** top-level answered pair (the ones the resume prompt names): resume the writer with every such question file and its answer appended, and let questions the answers leave unaddressed be re-parked per the channel section.

**Verification:**

- `grep -n "one file per blocking question\|for each blocking question\|a sibling answer file\|question file(s)" plugin/instructions/task_plan_writing_instructions_autonomous.md` prints nothing.
- The three anchor headings are byte-identical to before: `git diff -U0 plugin/instructions/task_plan_writing_instructions_autonomous.md` shows no removed line beginning `## Clarification channel`, `## Override 2` or `## Ask-vs-assume`.
- `plugin/instructions/mode_contract.md` → class (i-b) *Reason* is still true of this file and of the watcher (no heading renamed, the `question_<n>.md` ↔ `answer_<n>.md` names and pairing kept) — read it against the edit.
- `bash scripts/check-command-spelling.sh` exits 0, and the section still names no adopter literal (`<state_dir>`, never `harness-runs`).
- `grep -n "fix_watcher_multi_question_resume_loop\|feat_docs_catalog_retrieval\|2026-" plugin/instructions/task_plan_writing_instructions_autonomous.md` prints nothing.
