### Task 4 — Tests: the observed multi-question sequence, mid-session answers and late questions

**Goal:** Replay the `feat_docs_catalog_retrieval` sequence against the watcher `init` writes, and pin the consumed-set contract: exactly one resume per fully answered park, every consumed pair archived, an answer written during a resumed session left for the next resume, a question that appears after the park holding the resume back, and a park written under the old layout resuming once.

**Depends on:** Task 1, whose contract these cases assert, restated so this file stands alone:

- `resume_parked_run` resumes a `parked` record only when **every** top-level numbered `question_<n>.md` has its `answer_<n>.md`; it records every such index, ascending and space-separated, as `resumed_for_index` (e.g. `"1 2 3"`), sends one `resumed` notification, and launches the engine with a prompt carrying `This is a RESUME: every question file of this park has been answered — consume ALL of these answer files` followed by each `<state_dir>/clarifications/<branch>/answer_<i>.md`.
- `classify_run_exit` moves every pair in `resumed_for_index` into `clarifications/<branch>/answered/`, clears the field, and records `parked` when a top-level question lacks its answer **or** a top-level pair remains.

**Where this task stops.** It asserts no loop-guard behaviour — `park_loop`, the cycle counter and the clear sentinel are Task 5's, which reuses the helper module this task creates. It does not edit the watcher: a case that fails is a finding against Task 1, not a reason to change the script here.

### Targets

- `cli/test/helpers/watcher.mjs` (new) — the driver both watcher suites share.
- `cli/test/watcher-park-resume.test.mjs` (new) — the consumed-set suite.

**Work:**

- [ ] `cli/test/helpers/watcher.mjs`, with a header stating what it drives and why (the written watcher, sourced so a pass can `wait` for its background session). Export `async function createWatcherFixture(t, { branch = 'feat_x' } = {})`, which builds a fixture with `createFixture` from `./fixture.mjs` (a `package.json` whose four scripts are `echo`s, as `cli/test/outer-loop-scripts.test.mjs` → `nodeProjectFiles` seeds), runs `init` through `runCli` and fails on a non-zero exit, registers `t.after(cleanup)`, and returns: `dir`; `branch`; `clarDir` (absolute `sdlc-harness/clarifications/<branch>`); `writeQuestion(n, body)` and `writeAnswer(n, body)` (top level, creating the directory); `topLevel()` and `archived()` (sorted file names at the top level, excluding `answered`, and under `answered/`); `setStub(shellBody)` (the body the agent stub runs with its working directory in the run's working copy and `CLAR` set to `clarDir`, then exit 0); `seedRecord(fields)` (writes `sdlc-harness/autonomous_logs/registry.json` as `{"runs":{"<branch>":{status:'parked', worktree: dir, log_path, engine:'task', ...fields}}}`); `record()` (that record, parsed); `tick(env = {})`; `prompts()` (every `-p` argument the stub received, in order); `notifications()` (`{ event, branch, detail }` in order); `watcherLog()`.
- [ ] Inside that helper: the stub is an executable file in the fixture that appends its `$2` (the argument after `-p`) to a prompts file with a delimiter line and then runs the `setStub` body; the fixture's written `scripts/autonomous-notify.sh` is replaced by a recorder appending `"$1" "$2" "$4"` as one line, so no desktop or push delivery can happen; `tick` runs `bash -c '. "$1" status >/dev/null; tick; wait' _ <dir>/scripts/autonomous-watcher.sh` (the watcher header's own REPRO form, so the pass waits for the session it launched and its exit classification) with `HARNESS_AGENT_CLI` pointing at the stub, `AUTO_TAIL_TERMINAL=0`, `USAGE_CHECK_ENABLED=0`, `STALL_CHECK_ENABLED=0`, `USAGE_LANE_STATE_ENABLED=0`, `USAGE_LANE_LOCK_ENABLED=0`, and `HOME`, `XDG_CONFIG_HOME` and `XDG_STATE_HOME` inside the fixture, merged with the caller's `env`. Skip, in `cli/test/outer-loop-scripts.test.mjs`'s idiom, when `bash` or `jq` does not resolve.
- [ ] `cli/test/watcher-park-resume.test.mjs`, header first: the rule it enforces (one resume per answered park; the exit archives exactly the set the resume consumed) and the defect it replays — described in its own words, since Task 13's retired-form sweep over `cli/test` expects none of the phrases `lowest answered index`, `one question per file` or `THE LOWEST INDEX WINS`. Case **the observed sequence, old layout**: `question_1..3.md` at the top level, a `parked` record, only `answer_1.md` and `answer_2.md` → a tick launches nothing and the record stays `parked`; add `answer_3.md` → one tick gives exactly one prompt naming `answer_1.md`, `answer_2.md` and `answer_3.md`, with a stub that writes `question_4.md`; afterwards pairs 1–3 are archived, `question_4.md` is the only top-level file, `resumed_for_index` is empty, the record is `parked`, and the notifications are one `resumed` then one `parked`; a second tick launches nothing; add `answer_4.md` → exactly one more prompt, naming `answer_4.md` and no other answer file.
- [ ] Case **one file per park**: a single `question_1.md` whose body carries `## Q1`, `## Q2` and `## Q3` sections, and one `answer_1.md` → one resume, one prompt naming `answer_1.md`, the pair archived after exit. Case **an answer written during the resumed session**: park `question_1.md` answered; the stub writes `question_2.md` **and** `answer_2.md` before it exits → after exit only pair 1 is archived, pair 2 is still at the top level, the record is `parked`, and the next tick resumes with a prompt naming `answer_2.md` alone. Case **a question that appears after the park**: a `parked` record with `question_1.md`; then `question_2.md` and `answer_1.md` are added → a tick launches nothing; add `answer_2.md` → one resume naming both.
- [ ] Case **a run parked under the old layout, partly archived**: pair 1 already under `answered/`, pairs 2 and 3 at the top level (used by an earlier session but never archived), `question_4.md` unanswered, a `parked` record with an empty `resumed_for_index` → a tick launches nothing; add `answer_4.md` → exactly one resume, whose prompt names answers 2, 3 and 4; after exit every pair is archived and, with a stub that completes without asking anything, the record is `completed`.

**Verification:**

- `bash scripts/test.sh` exits 0 with both new files in the run; every case above passes against the watcher Task 1 delivered.
- Perturbation, by hand and not committed: restore the lowest-index selection in the fixture's written watcher (`resume_parked_run` picking one index) and the observed-sequence case fails on the second tick with a spurious resume — the suite catches the defect it was written for.
- No fixture is created inside this checkout: every path the helper writes sits under the directory `createFixture` returned.
