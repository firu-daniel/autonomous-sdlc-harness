# Review plan meta-review — iteration 0

## Must Fix

1. **Finding 1 rests on a plan-gap premise the governing task plan contradicts** — refers to review finding #1. Offending files: `sdlc-harness/code_reviews/feat_note_updated_at_code_review/finding_1.md` (whole `**The problem.**` section) and `sdlc-harness/code_reviews/feat_note_updated_at_code_review.md` (the Context sentence "one test-coverage commitment the story plan made and did not deliver").

   The finding's stated problem is that the plan committed to a `deleteNote` case and the suite delivered two of three: it quotes the story index's `Top risks` sentence ("Task 5 asserts all three non-stamping actions leave the value alone"), then says "The suite delivers two of the three" and "A case here … closes the third leg the plan already said it was closing."

   The plan file that actually governs Task 5 says the opposite, explicitly and with a reason. `sdlc-harness/task_plans/feat_note_updated_at/task_5_plan.md`, fourth `Work:` bullet, ends:

   > `deleteNote` removes the note, so its claim is the one it already has — nothing to stamp — and **needs no new case here.**

   The implementer therefore did not miss a commitment; they implemented the bullet as written, and the deviation note on that same task file records a different deviation entirely (the `seedEditedLongAgo` seeding shape), not this one. The finding never mentions the task plan at all. Its supporting citation of the task prompt is also mis-used: `sdlc-harness/task_prompts/feat_note_updated_at_task_prompt.md` line 16 names the three actions as a statement about **behaviour** ("`editNote` does; `setArchived`, `setPinned` and `deleteNote` do not"), not as a coverage commitment. As written, the fix-loop implementer reads finding_1.md alone and applies it believing it is closing a plan gap that the plan deliberately closed the other way.

   The suggested case itself is technically sound — I re-checked `src/domain/notesService.ts`: `update` maps only the matched record through `toNote`/`toRecord` and leaves siblings as raw records, and `deleteNote` splices raw records and calls `writeAll`, so the fixture reasoning in the finding (`seedEditedLongAgo` → `[legacy-2, doomed]` → `stored()[0]` is `legacy-2`) is correct, `deleteNote` is imported in the test prologue, and the insertion point named is the right one. Only the justification is wrong.

   **Fix:** in `finding_1.md`, either (a) withdraw the finding — remove `finding_1.md`, its `### 1.` pointer under `## Should Fix` in the index, and readiness entry 1, renumbering nothing else since `**Finding K**` identities are stable — or (b) keep it and rewrite `**The problem.**` so it rests only on the defence-in-depth argument the finding already makes in its later paragraphs: quote the Task 5 bullet that says `deleteNote` needs no case, say why that judgement is worth revisiting (the plan's own second `Top risks` entry is a stamp leaking into the shared `update` helper, and the obvious refactor routes `deleteNote` through it), and drop every sentence claiming the plan committed to a case that was not delivered. If (b), also correct the index Context sentence, which currently characterises the finding as "one test-coverage commitment the story plan made and did not deliver".

2. **No presentation-layer check and no stylesheet check appears anywhere in the review, though the diff touched both** — "Missed checks". Offending file: `sdlc-harness/code_reviews/feat_note_updated_at_code_review.md` (Context).

   The reviewed diff touches all five configured layers. Four of them get something traceable to their own conventions document: `data` (the widened `isNoteRecord` clause, `.claude/context/data-layer.md` `## The persisted shape`), `domain` (the mapper's two directions and the stamp's placement, `.claude/context/domain.md`), `tests` (Finding 1), `general` (Finding 2, from `.claude/context/conventions.md` `## Commit-message policy`).

   `presentation` gets one clause — "the presentation branches on `=== null` too" — which is a restatement of the branch's zero-versus-absent argument, not a check drawn from `.claude/context/presentation.md`. That document states checkable rules the diff engages directly and the review never mentions: `qa()` wrapping the creation expression rather than a hand-written `setAttribute('data-qa-…')`; a kebab-case id carrying its surface prefix (`note-`); a status token written in **every** state including the neutral one; a state that is both visual and testable expressed once as the token and styled through an attribute selector rather than a second modifier class; `className` and nothing else about appearance; and the accompanying set for a new control inside an existing region ("an id, a status token if a test waits on it, the CSS rule for any state it shows, and the UI-test-plan scenario"). `styles.css` — the `general` layer's own file, and one of the eight files the Context lists as reviewed — draws no comment at all, though `.claude/context/conventions.md` `## The general layer` fixes BEM class naming, the `:root` custom-property rule (`var(--muted)` versus a re-declared literal) and the one-site-literal rule, all of which the new `.note__edited` rules engage.

   I expect both scans to come back clean — `src/presentation/noteListView.ts` publishes through `qa()` with `id: 'note-edited'`, writes `never-edited`/`edited` in both states, sets `className` only, and `styles.css` hides the state through `.note__edited[data-qa-status='never-edited']` while reading `--muted` back through `var(…)`; the accompanying UI-test scenarios exist in `sdlc-harness/ui_test_plans/feat_note_updated_at/` (ui_test_1, 2, 4, 5, 6, 7 all read `note-edited`). That is the point: a clean pass has to be **shown**, not left implicit, or nothing distinguishes it from a layer that was never opened.

   **Fix:** in the index Context, add an explicit clean-pass rationale naming what was checked in `src/presentation/noteListView.ts` against `.claude/context/presentation.md` (at minimum: the `qa()` publication route, the surface-prefixed id, the status token in both states, the attribute-selector styling of a visual-and-testable state, and the presence of the UI-test-plan scenarios that stand in for the unit test this layer does not get) and what was checked in `styles.css` against `.claude/context/conventions.md` `## The general layer` (BEM naming, `var(--muted)`, the literal-at-one-site rule for `0.875rem` / `0.5rem`). If either scan turns up something, file it as a new `finding_<N>.md` with its own index pointer and readiness entry instead.

## Should Fix

1. **Finding 2 asks the fix loop to rewrite eight commits of branch history mid-flow** — refers to review finding #2. Offending file: `sdlc-harness/code_reviews/feat_note_updated_at_code_review/finding_2.md` (`**The fix.**` and its sub-steps).

   The observation is correct and correctly graded: `25be2c2`'s subject `Task 1 — Add optional updatedAt to NoteRecord and widen its guard` is not a bare imperative sentence, `.claude/context/conventions.md` `## Commit-message policy` requires one, and the other five code subjects are in the right form. The finding's safety claim also checks out — `git remote -v` is empty and the branch has no upstream.

   What is out of proportion is the remediation. `25be2c2` is eight commits behind `HEAD`, and the `git rebase --onto` the finding prescribes rewrites all eight, including the harness's own `chore: Flow progress …` artifact commits, at a point in the flow where `Bg`, `Bm`, `C`, `C2*`, `E` and `D` are still open in `sdlc-harness/flow_progress/feat_note_updated_at_progress.md` and the code review itself is not yet committed. A Nice to Have that costs a history rewrite of every commit on the branch is a bad trade for a fix loop to make unattended.

   **Fix:** in `finding_2.md`, either state plainly that the item is recorded for the record and is **not** to be actioned by the fix loop (moving it to the index's `## Intentional divergences to confirm` call-out section and dropping its readiness entry, which is what a call-out is for), or keep it actionable but say it must be the **last** thing done on the branch, after `D`, and never between two flow phases that will append commits.

2. **Finding 2 mis-locates the target commit** — refers to review finding #2. Offending file: `sdlc-harness/code_reviews/feat_note_updated_at_code_review/finding_2.md` (last line of `**The fix.**` prose).

   "`25be2c2` is the sixth commit back from `HEAD`" is wrong: `git rev-list --count 25be2c2..HEAD` is 8, so it is `HEAD~8`. Six is the count of *code* commits back, which is not what the sentence says. The hash is given correctly everywhere else, so this misleads only an implementer who counts rather than reads the hash — but the sentence exists precisely to be counted from.

   **Fix:** replace with "`25be2c2` is `HEAD~8`; it is the sixth **code** commit back, the two above it being `chore:` artifact commits" — or re-derive the number at fix time.

## Nice to Have

1. **The call-out section's heading text diverges from the fixture** — "Structure". Offending file: `sdlc-harness/code_reviews/feat_note_updated_at_code_review.md` line 51. The index uses `## Intentional divergences to confirm`; `plugin/samples/sample_code_review.md` uses `## Out of scope / verified-OK (intentional divergences / call-outs)`. Nothing downstream keys off this heading — only `## Phase 2 Readiness — Ordered Fix List` is load-bearing, and that one matches exactly — so this costs nothing today. **Fix:** adopt the fixture's heading so the family stays byte-compatible.

2. **Finding 1's verification sub-step pins a case count the task plan explicitly declines to pin** — refers to review finding #1. Offending file: `sdlc-harness/code_reviews/feat_note_updated_at_code_review/finding_1.md`, second sub-step ("confirm it passes with 43 cases and 0 failures"). The arithmetic is right (42 today), but `sdlc-harness/task_plans/feat_note_updated_at/task_5_plan.md`'s first `Verification:` bullet says "the run's own case list is the invariant, not a number written here". **Fix:** if the finding survives, drop the count and assert the run is green with the new case present. Moot if Must Fix 1 is resolved by withdrawing the finding.
