# examples/

`harness.config.json` is a worked, copyable configuration for a fabricated project — every value in it is invented, so it is safe to copy as a starting point and then replace key by key. It is validated against the sibling schema by `npm run validate:config`, which is what keeps it from drifting: a key that stops existing, or one whose shape changes, fails here rather than in an adopter's repository. (A key *added* to the schema is not caught this way — the example simply omits it, as it already does for the whole `parity` object — so a new key is added to this file by hand.) One mechanical note when copying: the `$schema` line is relative to this directory, so drop it or repoint it after the copy lands at a repository root.

`notes-app/` is the **minimal example project** — a dull TypeScript notes application the harness was adopted into — and its own `sdlc-harness/` directory carries the artifacts of one **real end-to-end run** over it, the branch `feat_note_updated_at`, beside the one of the two ledgers `init` seeded there that the fixture carries — `lessons.md`, untouched by the run, kept because `.claude/CLAUDE.md` links it; `improvement_suggestions.md` is omitted, and `notes-app/README.md` states by what rule. The tree around those artifacts is not that one run: it was produced by four legs in order — `init`, an opening analyze pass, the run, and a closing analyze pass — and the two analyze passes are what wrote the `.claude/` conventions corpus beside `sdlc-harness/` and the `detection.review` verdict in the configuration. That `harness.config.json` is its own, generated by `init` from that project's real layout, and is never the fabricated `acme-shop` file beside it: `npm run validate:config` guards the `acme-shop` configuration, `npm run validate:config:example` the fixture's. `notes-app/README.md` records what the run kept, what it omitted and by what rule, and is where the capture's shape and its one disclosed edit are stated in full. The `sdlc-harness/` artifacts are **one coherent run over one real project**, written for a person to read, and they are not a format reference: the format canon is `plugin/samples/`, reached as `${CLAUDE_PLUGIN_ROOT}/samples/`, and those fixtures are the only ones an agent body dereferences — so a reader who finds a difference between a run artifact here and a sample resolves it in the sample's favour.
